import path from "path";

export const WODS_JSON_PATH = path.join(process.cwd(), "data/wods.json");
export const PROGRAMS_JSON_PATH = path.join(process.cwd(), "data/programs.json");
export const BENCHMARKS_JSON_PATH = path.join(process.cwd(), "data/benchmarks.json");
export const PUBLISH_STATUS_PATH = path.join(process.cwd(), "publish-status.json");
