# v1.5.1 Triage (Approved)

**Date:** 2026-04-19
**Status:** Approved by user
**Source:** `docs/superpowers/specs/2026-04-19-v1.5.1-audit-findings.md`

## must-fix (4)

1. **R-M3** [reliability] Guest → signed-in data migration — `AuthViewModel.swift:48–101` — migrate LocalDataClient contents to CloudKit on Apple sign-in; clear local after; prevents silent data loss.
2. **U-H2** [ux] Raw error strings in alerts — `SettingsView.swift:175` + Insights, Workouts, Substitution picker — wrap `error.localizedDescription` with user-friendly copy.
3. **U-H1** [ux] Bare `ProgressView()` in Challenges — `ChallengesView.swift:16` — add "Loading challenges…" label.
4. **C-M1** [code health] Variable shadowing — `ProgramsListView.swift:557` — rename local `setsCount` to `setCount`.

## should-fix (17)

### Perf
5. **P-H1** [`BenchmarksViewModel.swift:217–236`] Parallelize readiness + results fetch with `async let`.
6. **P-H2** [`ProgramsListView.swift:543–551`] Parallelize cycle multiplier + maxes fetch.
7. **P-H3** [`BenchmarksViewModel.swift:38–52`] Parallelize user results + cycle phase + category update.
8. **P-M1** [`DashboardView.swift:685`] Cache workouts from `loadStats` and reuse in `loadActiveChallenge`.
9. **P-M2** [`MaxesListView.swift:373`] Session-cache `UserSettings` to avoid per-refresh fetch.
10. **P-L2** [`ShareCardRenderer.swift:15–23`] Offload `ImageRenderer` rendering off main thread.

### Reliability
11. **R-M1** [`SyncQueue.swift:176–187`] Surface stuck mutations via `@Published` + Settings diagnostic row.
12. **R-M4** [`CloudKitClient.swift:561`, `LocalDataClient.swift:49`] Surface decode-failure count via `DiagnosticsService` + Settings row (combine with R-M1 as a single diagnostic section).

### UX polish
13. **U-M1** [`BenchmarksListView.swift`] Empty state for empty category.
14. **U-M2** [`PainTrackingView.swift`] Empty state for no injuries/logs.
15. **U-M3** [Pain, Analytics, Cycle Settings] Contextual loading labels (replace "Loading…").
16. **U-M4** [`InsightsView.swift:17–19`] Add loading label.
17. **U-M5** [`AnalyticsView.swift:24–25`] Add loading label.
18. **U-M6** [ActiveWorkoutSessionViewModel] Haptic on set complete.
19. **U-M7** [`ActiveWorkoutView.swift:480–490`] Haptic on PR achievement.
20. **U-M8** [ChallengesView milestone path] Haptic on challenge tier/completion.
21. **U-M9** [ShareCardSheet] Haptic on share saved/copied.
22. **U-M10** [Dashboard, Insights, Settings, Workouts] Migrate hardcoded `.font(.system(size: N))` on icons to Dynamic Type–friendly sizing.
23. **U-M11** [`SettingsView.swift:109–141`] Icon-only link rows need `.accessibilityHidden(true)` or paired labels.
24. **U-M12** [`SharkWeekBanner.swift:77`, `CycleCalendarView.swift:112`, `PainTrackingView.swift:116,121`] Hardcoded `Color.red/.orange/.green` → AppTheme tokens.
25. **U-M13** [`DashboardView.swift:336–384`] Explicit `.accessibilityLabel` on quick-action grid.

### Code health
26. **C-L2** [`MaxesListView.swift:206–239`] Delete unused `MaxRow` struct.

## skip (deferred to 1.6+)

- **R-M2** SyncQueue exponential backoff — architectural; risk of regression in a .1.
- **R-L1** Challenge schema migration — backcompat decode already works.
- **R-L2** Revoked Apple ID handling — edge case.
- **U-M14** RecoveryScoreCard loading — feature hidden anyway.
- **P-L1** Intensity circles ForEach — trivial cost.
- **C-L1** Weight formatter extraction — wait for third use (rule of three).
- **C-L3** Heat bar magic numbers — document, don't refactor.
- **C-L4/L5** Clean / "leave alone" findings.
- Remaining 13+ low-severity UX items (U-L range) — batch for 1.6.

## Fix ordering (execution)

Ordering heuristic: safest + fewest files first; tests alongside domain changes; UI polish grouped by file for coherent commits.

**Batch 1 — Code health quick wins (low risk, clears the deck)**
1. C-M1 Rename shadow variable
2. C-L2 Delete MaxRow

**Batch 2 — Perf parallelization (concentrated in 2 files)**
3. P-H1 BenchmarkDetailViewModel
4. P-H3 BenchmarksListViewModel (same file as P-H1)
5. P-H2 ProgramDetailViewModel.startSession

**Batch 3 — Perf caching**
6. P-M2 Maxes UserSettings cache
7. P-M1 Dashboard workout cache

**Batch 4 — Share card jank**
8. P-L2 Offload ShareCardRenderer

**Batch 5 — UX correctness (must-fix H)**
9. U-H1 Challenges loading label
10. U-H2 Error string wrapping (sweep across 4 files)

**Batch 6 — UX polish (empty states + labels)**
11. U-M1 Benchmarks empty state
12. U-M2 Pain empty state
13. U-M3/M4/M5 Contextual loading labels

**Batch 7 — Haptics (one helper + 4 call sites)**
14. U-M6/M7/M8/M9 Haptic feedback

**Batch 8 — Accessibility + theme sweep**
15. U-M10 Icon Dynamic Type
16. U-M11 Settings link accessibility
17. U-M13 Dashboard quick-action accessibility
18. U-M12 Hardcoded colors → AppTheme

**Batch 9 — Reliability diagnostics (most complex — test-first)**
19. R-M1 SyncQueue stuck mutations
20. R-M4 DiagnosticsService decode-failure count
21. Settings diagnostic row (combines 19+20)

**Batch 10 — Guest migration (highest user-impact; test-first)**
22. R-M3 Guest → signed-in migration

**Batch 11 — Regression + release**
23. Clean build, `swift test`, `swiftlint`, smoke test golden paths
24. Version bump to 1.5.1, changelog, tag v1.5.1
