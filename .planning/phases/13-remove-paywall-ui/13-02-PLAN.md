---
phase: 13-remove-paywall-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - SundeeFundee/Sources/SundeeFundeeKit/Subscription/StoreKitClient.swift (DELETE)
  - SundeeFundee/Sources/SundeeFundeeKit/Subscription/MockSubscriptionClient.swift (DELETE)
  - SundeeFundee/Sources/SundeeFundeeKit/Subscription/SubscriptionClientFactory.swift (DELETE)
  - SundeeFundee/Sources/SundeeFundeeKit/Subscription/SubscriptionError.swift (DELETE)
  - SundeeFundee/Sources/SundeeFundeeKit/UI/ViewModels/AuthViewModel.swift
  - SundeeFundee/Sources/SundeeFundeeKit/Exports.swift
autonomous: true
requirements:
  - SUB-02

must_haves:
  truths:
    - "The Subscription/ directory contains only FreeSubscriptionClient.swift, SubscriptionTier.swift, and SubscriptionClientProtocol.swift"
    - "AuthViewModel contains zero StoreKit references"
    - "Exports.swift contains zero references to deleted types"
    - "The project builds without errors"
  artifacts:
    - path: "SundeeFundee/Sources/SundeeFundeeKit/Subscription/StoreKitClient.swift"
      provides: "File deleted"
      must_not_exist: true
    - path: "SundeeFundee/Sources/SundeeFundeeKit/Subscription/MockSubscriptionClient.swift"
      provides: "File deleted"
      must_not_exist: true
    - path: "SundeeFundee/Sources/SundeeFundeeKit/Subscription/SubscriptionClientFactory.swift"
      provides: "File deleted"
      must_not_exist: true
    - path: "SundeeFundee/Sources/SundeeFundeeKit/Subscription/SubscriptionError.swift"
      provides: "File deleted"
      must_not_exist: true
    - path: "SundeeFundee/Sources/SundeeFundeeKit/Subscription/FreeSubscriptionClient.swift"
      provides: "Free subscription client (kept)"
      contains: "FreeSubscriptionClient"
    - path: "SundeeFundee/Sources/SundeeFundeeKit/Subscription/SubscriptionTier.swift"
      provides: "Tier definitions with all-true capability flags (kept)"
      contains: "hasAIBuilder: Bool { true }"
    - path: "SundeeFundee/Sources/SundeeFundeeKit/Subscription/SubscriptionClientProtocol.swift"
      provides: "Protocol kept for FreeSubscriptionClient conformance"
      contains: "SubscriptionClientProtocol"
  key_links:
    - from: "AuthViewModel.swift"
      to: "SubscriptionClientFactory"
      via: "direct reference"
      pattern: "SubscriptionClientFactory\\.shared\\.client as\\? StoreKitClient"
---

<objective>
Delete StoreKit and mock subscription files, clean AuthViewModel StoreKit references, update Exports.swift, and verify the project builds cleanly.

Purpose: Per SUB-02, all StoreKit 2 code must be removed from the codebase. Plan 01 already removed all subscription UI and view model subscription-checking. This plan deletes the implementation files and cleans up remaining references.

Output: Subscription/ directory trimmed to 3 files. Zero StoreKit references. Clean build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-remove-paywall-ui/13-CONTEXT.md
@.planning/phases/13-remove-paywall-ui/13-01-SUMMARY.md

<interfaces>
<!-- Files being KEPT in Subscription/ directory -->

From FreeSubscriptionClient.swift:
```swift
public struct FreeSubscriptionClient: SubscriptionClientProtocol {
    public init() {}
    public var currentSubscription: SubscriptionInfo? { premiumInfo }
    public func getSubscriptionInfo() async throws -> SubscriptionInfo { premiumInfo }
    public func purchase(tier:) async throws -> SubscriptionInfo { premiumInfo }
    public func restorePurchases() async throws -> SubscriptionInfo { premiumInfo }
    public func presentManageSubscriptions() async throws { }
    public func isTierAvailable(_ tier:) async -> Bool { true }
    public func getPrice(for tier:) async -> String? { nil }
}
```

From App.swift (Phase 12 set this up):
```swift
SubscriptionClientFactory.shared.client = FreeSubscriptionClient()
```

Note: SubscriptionClientFactory is being DELETED in this plan. The App.swift init line references it -- but App.swift is in SundeeFundeeApp/ (not in this plan's scope). That reference will need to be updated. However, since FreeSubscriptionClient is the default and App.swift already sets it, the factory deletion means App.swift must be updated too. App.swift is listed as a file to modify.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Subscription/ files and clean AuthViewModel, App.swift, and Exports.swift</name>
  <files>
    SundeeFundee/Sources/SundeeFundeeKit/Subscription/StoreKitClient.swift,
    SundeeFundee/Sources/SundeeFundeeKit/Subscription/MockSubscriptionClient.swift,
    SundeeFundee/Sources/SundeeFundeeKit/Subscription/SubscriptionClientFactory.swift,
    SundeeFundee/Sources/SundeeFundeeKit/Subscription/SubscriptionError.swift,
    SundeeFundee/Sources/SundeeFundeeKit/UI/ViewModels/AuthViewModel.swift,
    SundeeFundee/Sources/SundeeFundeeKit/Exports.swift,
    SundeeFundeeApp/SundeeFundee/App.swift
  </files>
  <action>
**DELETE these files** (use `rm`, not editing):
  - `SundeeFundee/Sources/SundeeFundeeKit/Subscription/StoreKitClient.swift`
  - `SundeeFundee/Sources/SundeeFundeeKit/Subscription/MockSubscriptionClient.swift`
  - `SundeeFundee/Sources/SundeeFundeeKit/Subscription/SubscriptionClientFactory.swift`
  - `SundeeFundee/Sources/SundeeFundeeKit/Subscription/SubscriptionError.swift`

**KEEP these files** (do NOT delete):
  - `SundeeFundee/Sources/SundeeFundeeKit/Subscription/FreeSubscriptionClient.swift`
  - `SundeeFundee/Sources/SundeeFundeeKit/Subscription/SubscriptionTier.swift`
  - `SundeeFundee/Sources/SundeeFundeeKit/Subscription/SubscriptionClientProtocol.swift`

**AuthViewModel.swift** -- Remove StoreKit references:
  - In `signInWithApple()`: Remove the entire `if let subscriptionClient = SubscriptionClientFactory.shared.client as? StoreKitClient { await subscriptionClient.identify(...) }` block (lines 88-90)
  - In `resetState()`: Remove the entire `if let subscriptionClient = SubscriptionClientFactory.shared.client as? StoreKitClient { await subscriptionClient.logout() }` block (lines 164-166)
  - In `checkExistingSession()`: Remove the `if let subscriptionClient = SubscriptionClientFactory.shared.client as? StoreKitClient { await subscriptionClient.identify(...) }` block (lines 200-202)

**App.swift** (SundeeFundeeApp/SundeeFundee/App.swift) -- Remove SubscriptionClientFactory reference:
  - Remove the line `SubscriptionClientFactory.shared.client = FreeSubscriptionClient()` from init()
  - Remove the now-empty init() if it only contained that line (keep it if it has other content)
  - The import `import SundeeFundeeKit` must remain since it uses MainTabView, AuthView, etc.

**Exports.swift** -- Remove documentation references to deleted types:
  - Remove the comment blocks for: SubscriptionError (lines ~519-524), SubscriptionClientFactory (lines ~536-538), StoreKitClient (lines ~540-547), MockSubscriptionClient (lines ~549-558)
  - Keep the comment blocks for: SubscriptionTier, SubscriptionStatus, SubscriptionInfo, SubscriptionClientProtocol, FreeSubscriptionClient (add if missing)

After all edits, commit each file deletion and modification separately per project convention.

**Then verify the project builds:**
  ```
  cd SundeeFundeeApp && xcodebuild -project SundeeFundee.xcodeproj -scheme SundeeFundee -destination 'platform=iOS Simulator,name=iPhone 17' build 2>&1 | tail -5
  ```
  </action>
  <verify>
    <automated>cd /Users/dustinober/Projects/sundee-fundee && (test ! -f SundeeFundee/Sources/SundeeFundeeKit/Subscription/StoreKitClient.swift && test ! -f SundeeFundee/Sources/SundeeFundeeKit/Subscription/MockSubscriptionClient.swift && test ! -f SundeeFundee/Sources/SundeeFundeeKit/Subscription/SubscriptionClientFactory.swift && test ! -f SundeeFundee/Sources/SundeeFundeeKit/Subscription/SubscriptionError.swift && echo "PASS: All 4 files deleted") || echo "FAIL: Some files still exist"</automated>
  </verify>
  <done>StoreKitClient.swift, MockSubscriptionClient.swift, SubscriptionClientFactory.swift, and SubscriptionError.swift are deleted. AuthViewModel has zero StoreKit references. App.swift has zero SubscriptionClientFactory references. Exports.swift has zero references to deleted types. Project builds without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This phase deletes code, does not introduce new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | D (Denial of Service) | App.swift init | accept | Removing FreeSubscriptionClient init is safe -- views no longer check subscription status |
</threat_model>

<verification>
1. Four Subscription/ files deleted (StoreKitClient, MockSubscriptionClient, SubscriptionClientFactory, SubscriptionError)
2. Three Subscription/ files remain (FreeSubscriptionClient, SubscriptionTier, SubscriptionClientProtocol)
3. grep for "StoreKitClient" across all Swift source files returns zero hits
4. grep for "SubscriptionClientFactory" across all Swift source files returns zero hits
5. grep for "SubscriptionError" across all Swift source files returns zero hits (except Exports.swift comment cleanup)
6. Project builds without errors via xcodebuild
</verification>

<success_criteria>
- Subscription/ directory contains exactly 3 files: FreeSubscriptionClient.swift, SubscriptionTier.swift, SubscriptionClientProtocol.swift
- Zero StoreKit or SubscriptionClientFactory references in any Swift source file
- Zero SubscriptionError references in any Swift source file
- Project builds cleanly with xcodebuild
</success_criteria>

<output>
After completion, create `.planning/phases/13-remove-paywall-ui/13-02-SUMMARY.md`
</output>
