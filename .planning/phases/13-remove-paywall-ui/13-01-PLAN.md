---
phase: 13-remove-paywall-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Dashboard/DashboardView.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Analytics/AnalyticsView.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Analytics/CycleCorrelationChart.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Settings/SettingsView.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Export/ExportView.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/ViewModels/AnalyticsViewModel.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/ViewModels/ExportViewModel.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/ViewModels/PainTrackingViewModel.swift
autonomous: true
requirements:
  - SUB-03
  - SUB-04

must_haves:
  truths:
    - "No upgrade prompts, lock icons, tier badges, or subscription sheets appear on any screen"
    - "Dashboard, Analytics, Export, and Settings views contain zero subscription UI elements"
    - "Dashboard, Analytics, Export, PainTracking, and Settings view models contain zero subscription-checking code"
    - "All features are always enabled regardless of any tier concept"
  artifacts:
    - path: "SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Dashboard/DashboardView.swift"
      provides: "Dashboard view without upgrade prompts or subscription sheet"
      contains: "coachingInsightsCard shows unconditionally"
    - path: "SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Analytics/AnalyticsView.swift"
      provides: "Analytics view without subscription gating"
      contains: "CycleCorrelationChart"
    - path: "SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Analytics/CycleCorrelationChart.swift"
      provides: "Cycle correlation chart always showing data"
      contains: "chartContent"
    - path: "SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Settings/SettingsView.swift"
      provides: "Settings without Manage Subscription or SubscriptionView"
      contains: "Sign Out"
    - path: "SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Export/ExportView.swift"
      provides: "Export always accessible"
      contains: "dataCategoriesSection"
  key_links:
    - from: "AnalyticsView.swift"
      to: "CycleCorrelationChart"
      via: "direct view reference"
      pattern: "CycleCorrelationChart\\("
    - from: "ExportView.swift"
      to: "ExportViewModel"
      via: "viewModel property access"
      pattern: "viewModel\\."
---

<objective>
Remove all subscription gating UI and subscription-checking code from views and view models.

Purpose: Per SUB-03 and SUB-04, users must see no subscription-related UI anywhere and view models must contain zero subscription-checking code. All features are always unlocked (guaranteed by FreeSubscriptionClient from Phase 12).

Output: Views and view models stripped of subscription prompts, lock icons, tier badges, subscription sheets, and subscription-checking logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-remove-paywall-ui/13-CONTEXT.md

<interfaces>
<!-- Key types referenced by subscription-removal targets -->

From SubscriptionTier.swift (KEEPING - do not delete):
```swift
public enum SubscriptionTier: String, Sendable, Equatable, Codable, CaseIterable {
    case free, plus, premium
    public var hasAIBuilder: Bool { true }        // All return true
    public var hasCoachMemory: Bool { true }
    public var hasAdvancedInsights: Bool { true }
    public var hasExportShare: Bool { true }
    public var hasSmartSubstitutions: Bool { true }
    public var displayName: String { ... }
    public var tagline: String { ... }
}
```

From SubscriptionClientProtocol.swift (KEEPING):
```swift
public protocol SubscriptionClientProtocol: Sendable {
    var currentSubscription: SubscriptionInfo? { get async }
    func getSubscriptionInfo() async throws -> SubscriptionInfo
    func purchase(tier:) async throws -> SubscriptionInfo
    func restorePurchases() async throws -> SubscriptionInfo
    ...
}
```

From SubscriptionInfo (in SubscriptionTier.swift):
```swift
public struct SubscriptionInfo: Sendable, Equatable, Codable {
    public let tier: SubscriptionTier
    public let status: SubscriptionStatus
    ...
}
```

From DataClientProtocol:
```swift
func fetchAll<T: Codable & Sendable>(recordType: String) async throws -> [T]
func save<T: Codable & Sendable>(_ items: [T], recordType: String) async throws
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip subscription UI from Dashboard, Analytics, CycleCorrelation, Settings, and Export views</name>
  <files>
    SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Dashboard/DashboardView.swift,
    SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Analytics/AnalyticsView.swift,
    SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Analytics/CycleCorrelationChart.swift,
    SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Settings/SettingsView.swift,
    SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Export/ExportView.swift
  </files>
  <action>
**DashboardView.swift** (contains both View and DashboardViewModel):
  - In `DashboardView` struct: Remove `@State private var showingSubscription = false`
  - Remove `.sheet(isPresented: $showingSubscription) { SubscriptionView() }`
  - Remove the entire `upgradePrompts` ViewBuilder property and the `lockedFeatureCard` helper function
  - Remove the call to `upgradePrompts` in the body
  - In `coachingInsightsCard`: Remove the `viewModel.subscriptionTier == .premium` guard -- show coaching insights unconditionally. Keep the `viewModel.insightsSummary != nil` check since it's a data availability check.
  - In `DashboardViewModel`:
    - Remove `private let subscriptionClient: SubscriptionClientProtocol` property
    - Remove `subscriptionClient` from init parameter list and init body
    - Remove `@Published var subscriptionTier: SubscriptionTier = .free`
    - Remove `@Published var canGenerateAIWorkout: Bool = false`
    - In `loadData()`: Remove `await loadSubscriptionInfo()` call
    - In `loadSubscriptionInfo()`: Remove the subscription client call. Always set `canGenerateAIWorkout = true`. Always call `await loadCoachingInsights()`.
    - In `loadCoachingInsights()`: Remove `subscriptionClient` from `CoachContextBuilder` init -- it has a default value so just remove the explicit parameter.

**AnalyticsView.swift**:
  - Remove `@State private var showingSubscription = false`
  - In the body, change `CycleCorrelationChart(data: viewModel.cycleData, hasAccess: viewModel.hasCycleAccess)` to `CycleCorrelationChart(data: viewModel.cycleData)` (remove `hasAccess` parameter).

**CycleCorrelationChart.swift**:
  - Remove `let hasAccess: Bool` parameter from the struct
  - Remove `@State private var showingSubscription = false`
  - Remove the `if !hasAccess` check in the header (lock icon)
  - Remove the entire `upgradeCard` ViewBuilder and its `.sheet(isPresented: $showingSubscription)` modifier
  - In the body, remove the `if !hasAccess { upgradeCard }` branch -- always show either `chartContent` or `emptyState`
  - The body should be: header (without lock icon) + chartContent or emptyState

**SettingsView.swift** (contains SettingsView, SubscriptionView, SubscriptionViewModel, TierBadge, SettingsViewModel):
  - In `SettingsView`:
    - Remove the "Manage Subscription" button section entirely
    - Remove `TierBadge(tier: viewModel.currentTier)` from the profile HStack
    - Remove `.sheet(isPresented: $viewModel.showingSubscription) { SubscriptionView() }`
    - In the "Data & Privacy" section, remove the "Pro" label from the Export NavigationLink HStack -- just show `Label("Export My Data", systemImage: "square.and.arrow.up")` without the "Pro" text
  - Delete the entire `SubscriptionView` struct (lines ~312-494)
  - Delete the entire `SubscriptionViewModel` class (lines ~498-542)
  - Delete the entire `TierBadge` struct (lines ~647-668)
  - In `SettingsViewModel`:
    - Remove `@Published var showingSubscription: Bool = false`
    - Remove `@Published var currentTier: SubscriptionTier = .free`
    - Remove `private let subscriptionClient: SubscriptionClientProtocol`
    - Remove `subscriptionClient` from init parameters and init body
    - Remove `await loadSubscriptionTier()` call from init
    - Delete the entire `loadSubscriptionTier()` function

**ExportView.swift**:
  - Remove `@StateObject private var viewModel = ExportViewModel()` -- wait, this is needed. Keep it.
  - Remove the `if !viewModel.canExport { upgradeSection } else { ... }` conditional. Always show the else branch content: `dataCategoriesSection`, `exportSection`, share and error sections.
  - Remove `upgradeSection` computed property entirely
  - Remove `.sheet(isPresented: $viewModel.showingSubscription) { SubscriptionView() }`
  - Remove `.task { await viewModel.checkSubscription() }`
  - Update the comment at top of file to remove "Gated behind SubscriptionTier.hasExportShare (Pro only)."
  </action>
  <verify>
    <automated>cd /Users/dustinober/Projects/sundee-fundee && grep -rn "showingSubscription\|SubscriptionView()\|upgradeCard\|upgradeSection\|TierBadge\|SubscriptionViewModel\|lockedFeatureCard\|upgradePrompts\|Manage Subscription\|\"Pro\".*Accent.gold" SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Dashboard/DashboardView.swift SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Analytics/AnalyticsView.swift SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Analytics/CycleCorrelationChart.swift SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Settings/SettingsView.swift SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Export/ExportView.swift | grep -v "^Binary" || echo "PASS: No subscription UI references found in views"</automated>
  </verify>
  <done>Zero subscription UI elements remain in DashboardView, AnalyticsView, CycleCorrelationChart, SettingsView, or ExportView. No upgrade prompts, lock icons, subscription sheets, tier badges, or "Manage Subscription" buttons.</done>
</task>

<task type="auto">
  <name>Task 2: Remove subscription-checking code from view models</name>
  <files>
    SundeeFundee/Sources/SundeeFundeeKit/UI/ViewModels/AnalyticsViewModel.swift,
    SundeeFundee/Sources/SundeeFundeeKit/UI/ViewModels/ExportViewModel.swift,
    SundeeFundee/Sources/SundeeFundeeKit/UI/ViewModels/PainTrackingViewModel.swift
  </files>
  <action>
**AnalyticsViewModel.swift**:
  - Remove `@Published public var subscriptionTier: SubscriptionTier = .free`
  - Remove `@Published public var hasCycleAccess: Bool = false`
  - Remove `private let subscriptionClient: SubscriptionClientProtocol` property
  - Remove `subscriptionClient` from init parameters and init body (keep only `dataClient`)
  - In `loadAnalytics()`: Remove the subscription check block (`let info = try await subscriptionClient.getSubscriptionInfo()` and the two lines that set `subscriptionTier` and `hasCycleAccess`). Always compute cycle data.
  - In `reaggregate()`: Remove the `if hasCycleAccess` guard around `cycleData` -- always compute cycle correlation data.

**ExportViewModel.swift**:
  - Remove `@Published public var currentTier: SubscriptionTier = .free`
  - Remove `@Published public var showingSubscription: Bool = false`
  - Remove `private let subscriptionClient: SubscriptionClientProtocol` property
  - Remove `subscriptionClient` from both init parameter lists (public and internal)
  - Change `canExport` computed property to always return `true` (or remove it and update ExportView accordingly -- but ExportView was updated in Task 1 to not use it, so just remove it)
  - Delete the entire `checkSubscription()` function

**PainTrackingViewModel.swift**:
  - Remove `private let subscriptionClient: SubscriptionClientProtocol` property
  - Remove `subscriptionClient` from init parameters and init body (keep only `dataClient`)
  - In `loadSubstitutionSuggestions()`: Remove the subscription tier check block (the `do/catch` that calls `subscriptionClient.getSubscriptionInfo()` and checks `info.tier.hasSmartSubstitutions`). Substitution suggestions should always load when there are active injuries. Replace the guard block with just `guard !activeInjuries.isEmpty else { substitutionSuggestions = []; return }`
  </action>
  <verify>
    <automated>cd /Users/dustinober/Projects/sundee-fundee && grep -rn "subscriptionClient\|hasCycleAccess\|canExport\|checkSubscription\|showingSubscription\|currentTier" SundeeFundee/Sources/SundeeFundeeKit/UI/ViewModels/AnalyticsViewModel.swift SundeeFundee/Sources/SundeeFundeeKit/UI/ViewModels/ExportViewModel.swift SundeeFundee/Sources/SundeeFundeeKit/UI/ViewModels/PainTrackingViewModel.swift | grep -v "^Binary" || echo "PASS: No subscription-checking code in view models"</automated>
  </verify>
  <done>AnalyticsViewModel, ExportViewModel, and PainTrackingViewModel contain zero subscription-checking code. All features are always enabled without tier checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This phase removes code, does not introduce new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | E (Elevation of Privilege) | All views | accept | Removing subscription gates is the explicit intent -- all features are free per v1.1 decision |
| T-13-02 | I (Information Disclosure) | ExportView | accept | Export was previously gated; now always available. No PII risk change since user exports their own data |
</threat_model>

<verification>
1. No subscription UI elements remain in any view file
2. No subscription-checking code remains in any view model
3. grep confirms zero hits for `showingSubscription`, `SubscriptionView()`, `upgradeCard`, `lockedFeatureCard`, `subscriptionClient` across all view and view model files
</verification>

<success_criteria>
- grep for subscription UI terms across all view files returns zero results
- grep for subscription-checking terms across all view model files returns zero results
- All features (coaching insights, cycle correlation, export, substitutions) are always enabled
</success_criteria>

<output>
After completion, create `.planning/phases/13-remove-paywall-ui/13-01-SUMMARY.md`
</output>
