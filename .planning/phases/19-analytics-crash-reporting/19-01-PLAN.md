---
phase: 19-analytics-crash-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - __mocks__/@react-native-firebase/analytics.js
  - __mocks__/@react-native-firebase/crashlytics.js
  - src/firebase/analytics.ts
  - src/firebase/crashlytics.ts
  - src/hooks/useScreenTracking.ts
  - src/hooks/__tests__/useScreenTracking.test.ts
  - src/firebase/__tests__/analytics.test.ts
  - src/firebase/__tests__/crashlytics.test.ts
autonomous: true
requirements:
  - ANLYT-01
  - ANLYT-02
  - ANLYT-03
  - ANLYT-04
  - ANLYT-05

must_haves:
  truths:
    - "logEvent helper calls analytics().logEvent with correct event name and params on native platforms"
    - "logEvent is a no-op on web platform"
    - "setUserProperties helper calls analytics().setUserProperties with stringified values"
    - "useScreenTracking hook calls logScreenView on pathname change"
    - "useScreenTracking hook sets Crashlytics current_screen attribute on pathname change"
    - "recordError wrapper calls crashlytics().recordError with the error object"
    - "setCrashlyticsKeys wrapper calls crashlytics().setAttributes with provided keys"
  artifacts:
    - path: "src/firebase/analytics.ts"
      provides: "logEvent and setUserProperties helper functions"
      exports: ["initAnalytics", "logEvent", "setUserProperties"]
    - path: "src/firebase/crashlytics.ts"
      provides: "recordError and setCrashlyticsKeys helper functions"
      exports: ["initCrashlytics", "recordError", "setCrashlyticsKeys"]
    - path: "src/hooks/useScreenTracking.ts"
      provides: "Screen tracking hook for root layout"
      exports: ["useScreenTracking"]
    - path: "__mocks__/@react-native-firebase/analytics.js"
      provides: "Jest mock for analytics module"
    - path: "__mocks__/@react-native-firebase/crashlytics.js"
      provides: "Jest mock for crashlytics module"
  key_links:
    - from: "src/hooks/useScreenTracking.ts"
      to: "@react-native-firebase/analytics"
      via: "require() call inside try/catch"
      pattern: "require.*analytics.*logScreenView"
    - from: "src/hooks/useScreenTracking.ts"
      to: "@react-native-firebase/crashlytics"
      via: "require() call inside try/catch"
      pattern: "require.*crashlytics.*setAttribute"
    - from: "src/firebase/analytics.ts"
      to: "@react-native-firebase/analytics"
      via: "require() call inside try/catch"
      pattern: "require.*analytics.*logEvent"
---

<objective>
Create all analytics and crashlytics helper functions, the screen tracking hook, Jest mocks, and unit tests. This plan builds the entire instrumentation layer without wiring it into app screens.

Purpose: Establish tested, platform-safe wrappers that Plan 02 will wire into the live app. Mocks must exist before any test can import these modules.
Output: 8 files -- 2 Jest mocks, 3 source modules (extended analytics.ts, extended crashlytics.ts, new useScreenTracking.ts), 3 test files.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-analytics-crash-reporting/19-RESEARCH.md

<interfaces>
<!-- Existing Firebase module patterns to follow -->

From src/firebase/analytics.ts:
```typescript
// Existing — will be extended with new exports
import { Platform } from 'react-native';
let initialized = false;
export function initAnalytics(): void { ... }
// Phase 19 adds: logEvent(), setUserProperties()
```

From src/firebase/crashlytics.ts:
```typescript
// Existing — will be extended with new exports
import { Platform } from 'react-native';
let initialized = false;
export function initCrashlytics(): void { ... }
// Phase 19 adds: recordError(), setCrashlyticsKeys()
```

From __mocks__/@react-native-firebase/app.ts (mock pattern to follow):
```typescript
// Existing mocks: app.ts, auth.ts, firestore.ts
// Phase 19 adds: analytics.js, crashlytics.js
```

From expo-router (usePathname):
```typescript
import { usePathname } from 'expo-router';
// Returns current route path string, updates on navigation
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Jest mocks and helper functions</name>
  <files>
    __mocks__/@react-native-firebase/analytics.js
    __mocks__/@react-native-firebase/crashlytics.js
    src/firebase/analytics.ts
    src/firebase/crashlytics.ts
    src/hooks/useScreenTracking.ts
  </files>
  <action>
1. Create `__mocks__/@react-native-firebase/analytics.js`:
   - Export a jest.fn() that returns an object with mock methods: `logScreenView`, `logEvent`, `setUserProperties`, `setUserProperty`, `setAnalyticsCollectionEnabled` — all returning `jest.fn().mockResolvedValue(undefined)`.
   - Follow the pattern in existing `__mocks__/@react-native-firebase/app.ts`.
   - Export as both `module.exports` and `module.exports.default`.

2. Create `__mocks__/@react-native-firebase/crashlytics.js`:
   - Export a jest.fn() that returns an object with mock methods: `setCrashlyticsCollectionEnabled`, `recordError`, `setAttribute`, `setAttributes`, `log`, `setUserId`, `crash` — all returning appropriate jest.fn() (recordError/log/crash are sync, others async).
   - Export as both `module.exports` and `module.exports.default`.

3. Extend `src/firebase/analytics.ts` — ADD below existing `initAnalytics` function (do NOT modify initAnalytics):
   - `export async function logEvent(eventName: string, params?: Record<string, string | number | boolean>): Promise<void>` — guards with `Platform.OS === 'web'` return, wraps `require('@react-native-firebase/analytics').default` in try/catch, calls `analytics().logEvent(eventName, params ?? {})`.
   - `export async function setUserProperties(props: { subscriptionTier: 'free' | 'premium'; cycleTrackingEnabled: boolean }): Promise<void>` — guards web, calls `analytics().setUserProperties({ subscription_tier: props.subscriptionTier, cycle_tracking_enabled: String(props.cycleTrackingEnabled) })`. Note: Firebase user property values MUST be strings.

4. Extend `src/firebase/crashlytics.ts` — ADD below existing `initCrashlytics` function (do NOT modify initCrashlytics):
   - `export function recordError(error: Error, context?: string): void` — guards web, wraps in try/catch, calls `crashlytics().log(context)` if context provided, then `crashlytics().recordError(error)`.
   - `export async function setCrashlyticsKeys(keys: { subscriptionTier?: 'free' | 'premium'; cyclePhase?: string }): Promise<void>` — guards web, builds `attrs` Record from non-undefined keys (subscription_tier, cycle_phase), calls `crashlytics().setAttributes(attrs)` if any keys present.

5. Create `src/hooks/useScreenTracking.ts`:
   - Import `useEffect` from react, `Platform` from react-native, `usePathname` from expo-router.
   - Export function `useScreenTracking(): void`.
   - Inside: get `pathname` from `usePathname()`. In `useEffect` keyed on `[pathname]`:
     - If `Platform.OS === 'web'`, return early.
     - Set `screenName = pathname ?? 'unknown'`.
     - Try: `require('@react-native-firebase/analytics').default` then `void analytics().logScreenView({ screen_name: screenName, screen_class: screenName })`.
     - Try: `require('@react-native-firebase/crashlytics').default` then `void crashlytics().setAttribute('current_screen', screenName)`.
     - Both try blocks have empty catch (non-fatal).
   - Add eslint-disable comments for `@typescript-eslint/no-require-imports` on require lines.
  </action>
  <verify>
    <automated>npx jest --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>All 5 files created. Existing test suite still passes (mocks don't break anything). The new helpers and hook are importable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write unit tests for all helpers and hook</name>
  <files>
    src/hooks/__tests__/useScreenTracking.test.ts
    src/firebase/__tests__/analytics.test.ts
    src/firebase/__tests__/crashlytics.test.ts
  </files>
  <behavior>
    - useScreenTracking: logScreenView called with { screen_name, screen_class } when pathname changes; setAttribute called with 'current_screen'; no calls on web platform
    - logEvent: calls analytics().logEvent(name, params) on native; no-op on web
    - logEvent: handles missing analytics module gracefully (no throw)
    - setUserProperties: calls analytics().setUserProperties with string values; cycleTrackingEnabled boolean is stringified
    - recordError: calls crashlytics().recordError(error); logs context string if provided
    - recordError: no-op on web
    - setCrashlyticsKeys: calls crashlytics().setAttributes with mapped keys; skips undefined keys
  </behavior>
  <action>
1. Create `src/firebase/__tests__/analytics.test.ts`:
   - Mock Platform.OS to 'ios' by default.
   - Test `logEvent('workout_started', { source: 'manual' })` calls `analytics().logEvent('workout_started', { source: 'manual' })`.
   - Test `logEvent('test')` with no params passes `{}` as second arg.
   - Test `logEvent` on web (`Platform.OS = 'web'`) does NOT call analytics().logEvent.
   - Test `setUserProperties({ subscriptionTier: 'premium', cycleTrackingEnabled: true })` calls `analytics().setUserProperties({ subscription_tier: 'premium', cycle_tracking_enabled: 'true' })`.
   - Test `setUserProperties({ subscriptionTier: 'free', cycleTrackingEnabled: false })` passes `'false'` string.

2. Create `src/firebase/__tests__/crashlytics.test.ts`:
   - Test `recordError(new Error('test'))` calls `crashlytics().recordError` with the error.
   - Test `recordError(new Error('test'), 'context info')` calls `crashlytics().log('context info')` then `crashlytics().recordError`.
   - Test `recordError` on web does NOT call crashlytics().
   - Test `setCrashlyticsKeys({ subscriptionTier: 'premium', cyclePhase: 'follicular' })` calls `crashlytics().setAttributes({ subscription_tier: 'premium', cycle_phase: 'follicular' })`.
   - Test `setCrashlyticsKeys({ subscriptionTier: 'free' })` only includes subscription_tier (not cycle_phase).
   - Test `setCrashlyticsKeys({})` does NOT call setAttributes (empty attrs).

3. Create `src/hooks/__tests__/useScreenTracking.test.ts`:
   - Mock `expo-router` to provide a controllable `usePathname` that returns a test value.
   - Mock `react-native` Platform.OS as 'ios'.
   - Use `renderHook` from `@testing-library/react-hooks` or `@testing-library/react-native` (check what's available in package.json).
   - Test: initial render with pathname '/dashboard' calls `logScreenView({ screen_name: '/dashboard', screen_class: '/dashboard' })` and `setAttribute('current_screen', '/dashboard')`.
   - Test: pathname change triggers new calls.
   - Test: web platform skips all calls.
  </action>
  <verify>
    <automated>npx jest --testPathPattern="(useScreenTracking|analytics\.test|crashlytics\.test)" --passWithNoTests -x 2>&1 | tail -10</automated>
  </verify>
  <done>All unit tests pass. Coverage: logEvent (3 tests), setUserProperties (2 tests), recordError (3 tests), setCrashlyticsKeys (3 tests), useScreenTracking (3 tests). Total ~14 test cases green.</done>
</task>

</tasks>

<verification>
npx jest --passWithNoTests
All existing tests continue to pass. New analytics/crashlytics/useScreenTracking tests are green.
</verification>

<success_criteria>
- `logEvent`, `setUserProperties` exported from `src/firebase/analytics.ts` alongside existing `initAnalytics`
- `recordError`, `setCrashlyticsKeys` exported from `src/firebase/crashlytics.ts` alongside existing `initCrashlytics`
- `useScreenTracking` exported from `src/hooks/useScreenTracking.ts`
- Jest mocks exist for both `@react-native-firebase/analytics` and `@react-native-firebase/crashlytics`
- All new unit tests pass
- All existing tests still pass (no regressions from new mocks)
</success_criteria>

<output>
After completion, create `.planning/phases/19-analytics-crash-reporting/19-01-SUMMARY.md`
</output>
