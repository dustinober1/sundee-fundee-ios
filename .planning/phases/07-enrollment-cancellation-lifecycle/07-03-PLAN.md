---
phase: 07-enrollment-cancellation-lifecycle
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - flutter_app/lib/features/programs/data/program_repository.dart
  - flutter_app/lib/features/programs/presentation/programs_screen.dart
  - flutter_app/lib/domain/models/completed_workout_model.dart
  - flutter_app/lib/features/workouts/presentation/workout_execution_providers.dart
  - flutter_app/lib/features/dashboard/presentation/dashboard_screen.dart
  - flutter_app/lib/features/workouts/presentation/workout_summary_screen.dart
  - flutter_app/test/features/programs/data/program_repository_test.dart
  - flutter_app/test/features/repositories/data/firestore_enrollment_repository_test.dart
  - flutter_app/test/features/repositories/data/firestore_workout_repository_test.dart
  - flutter_app/test/features/repositories/data/firestore_phase1_smoke_test.dart
  - flutter_app/test/migration/legacy_migration_orchestrator_test.dart
autonomous: true
must_haves:
  truths:
    - "Re-enrollment always prompts the user to choose restore prior canceled enrollment vs start new enrollment when relevant."
    - "Both re-enrollment paths clear prior progress position (start at week 1/day 1) while preserving profile and injury context."
    - "If stale active-state artifacts exist during re-enrollment, the app auto-heals conflicts before commit or surfaces a clear fallback error when healing fails."
    - "Completed workouts remain intact after cancellation and are visibly marked as belonging to canceled enrollments in history surfaces."
  artifacts:
    - path: flutter_app/lib/features/programs/presentation/programs_screen.dart
      provides: "Restore-vs-new re-enrollment prompt UI and fallback error handling for heal failures"
      contains: "Restore prior enrollment"
    - path: flutter_app/lib/features/programs/data/program_repository.dart
      provides: "Re-enrollment orchestration that invokes stale-state healing and applies reset progress semantics"
      contains: "reEnroll"
    - path: flutter_app/lib/domain/models/completed_workout_model.dart
      provides: "Optional enrollment linkage field for canceled-history markers without breaking legacy decode"
      contains: "enrollmentId"
    - path: flutter_app/lib/features/dashboard/presentation/dashboard_screen.dart
      provides: "History marker rendering for workouts associated with canceled enrollments"
      contains: "Canceled plan"
  key_links:
    - from: flutter_app/lib/features/programs/presentation/programs_screen.dart
      to: flutter_app/lib/features/programs/data/program_repository.dart
      via: "Enroll action routes through restore/new decision and repository guardrails"
      pattern: "showDialog|reEnroll|enrollUser"
    - from: flutter_app/lib/features/workouts/presentation/workout_execution_providers.dart
      to: flutter_app/lib/domain/models/completed_workout_model.dart
      via: "Workout completion writes enrollment linkage needed for canceled-history marker joins"
      pattern: "enrollmentId"
    - from: flutter_app/lib/features/dashboard/presentation/dashboard_screen.dart
      to: flutter_app/lib/features/repositories/data/firestore_repositories.dart
      via: "History view joins workout enrollment linkage to lifecycle status/event data for marker chip"
      pattern: "watchWorkouts|watchLatestEnrollmentEvent"
---

<objective>
Complete Phase 7 safety behavior by adding guarded re-enrollment flow and post-cancel history integrity markers.

Purpose: Cancellation lifecycle is incomplete without safe re-entry paths and visible historical continuity.
Output: Restore/new re-enrollment decision flow + stale-state guardrails + history marker plumbing + regression coverage.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-enrollment-cancellation-lifecycle/07-CONTEXT.md
@.planning/phases/07-enrollment-cancellation-lifecycle/07-RESEARCH.md
@.planning/phases/07-enrollment-cancellation-lifecycle/07-01-PLAN.md
@.planning/phases/07-enrollment-cancellation-lifecycle/07-02-PLAN.md
@flutter_app/lib/features/programs/data/program_repository.dart
@flutter_app/lib/features/programs/presentation/programs_screen.dart
@flutter_app/lib/domain/models/completed_workout_model.dart
@flutter_app/lib/features/workouts/presentation/workout_execution_providers.dart
@flutter_app/lib/features/dashboard/presentation/dashboard_screen.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement restore-vs-new re-enrollment flow with stale-state auto-heal guardrails</name>
  <files>
    flutter_app/lib/features/programs/data/program_repository.dart
    flutter_app/lib/features/programs/presentation/programs_screen.dart
    flutter_app/test/features/programs/data/program_repository_test.dart
    flutter_app/test/features/repositories/data/firestore_enrollment_repository_test.dart
  </files>
  <action>
    Add explicit re-enrollment decision behavior per locked context:
    - When user enrolls into a program with prior canceled enrollment history, always prompt:
      1) `Restore prior canceled enrollment`
      2) `Start new enrollment`
    - Before either path commits, invoke stale-active auto-heal.
    - If healing succeeds, continue; if healing fails, show clear fallback error and abort commit.
    - Ensure both restore/new paths reset progress to week 1/day 1 and clear completed-week position.
    - Keep profile and injury context unchanged (already user-scoped data, not enrollment-scoped).
    - Do not add cooldown.

    Add tests for both re-enrollment choices, progress-reset semantics, and heal-failure fallback behavior.
  </action>
  <verify>
    cd flutter_app && flutter test test/features/programs/data/program_repository_test.dart test/features/repositories/data/firestore_enrollment_repository_test.dart -r expanded
  </verify>
  <done>
    Re-enrollment always asks restore-vs-new when applicable, heals stale conflicts before commit, and enforces fresh progress start.
  </done>
</task>

<task type="auto">
  <name>Task 2: Persist workout-to-enrollment linkage and render canceled-history markers</name>
  <files>
    flutter_app/lib/domain/models/completed_workout_model.dart
    flutter_app/lib/features/workouts/presentation/workout_execution_providers.dart
    flutter_app/lib/features/dashboard/presentation/dashboard_screen.dart
    flutter_app/lib/features/workouts/presentation/workout_summary_screen.dart
    flutter_app/test/features/repositories/data/firestore_workout_repository_test.dart
  </files>
  <action>
    Preserve and expose cancellation history context without deleting workouts:
    - Add optional `enrollmentId` (and optional marker-friendly derived helpers if needed) to `CompletedWorkoutModel` with backward-compatible JSON handling.
    - During workout completion save, write the current enrollment ID onto the workout record.
    - In dashboard/workout summary history surfaces, join workout enrollment linkage against lifecycle status to render a `Canceled plan` marker when applicable.
    - Keep aggregate metrics and PR calculations unchanged; marker is informational only.

    Ensure canceled enrollment details are not resurrected as active-plan UI; history marker should be distinct from active enrollment views.
  </action>
  <verify>
    cd flutter_app && flutter test test/features/repositories/data/firestore_workout_repository_test.dart -r expanded
  </verify>
  <done>
    Completed workouts remain intact and visibly tagged when associated with canceled enrollments, with no metrics/PR regression.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run lifecycle regression and migration compatibility hardening</name>
  <files>
    flutter_app/test/features/repositories/data/firestore_phase1_smoke_test.dart
    flutter_app/test/migration/legacy_migration_orchestrator_test.dart
    flutter_app/test/features/repositories/data/firestore_enrollment_repository_test.dart
  </files>
  <action>
    Expand end-to-end regression to lock in Phase 7 guarantees:
    - Smoke: enroll -> cancel -> re-enroll (restore/new) without conflicting active-state artifacts.
    - Validate cancellation leaves historical workouts readable and marker-capable.
    - Confirm migration/legacy decode stays stable when new optional enrollment/workout fields are absent.

    Keep tests deterministic and focused on lifecycle safety behaviors introduced by this phase.
  </action>
  <verify>
    cd flutter_app && flutter test test/features/repositories/data/firestore_phase1_smoke_test.dart test/migration/legacy_migration_orchestrator_test.dart -r expanded
  </verify>
  <done>
    Regression and migration suites prove Phase 7 behavior is stable for legacy and new enrollment/workout records.
  </done>
</task>

</tasks>

<verification>
Run Phase 7 re-enrollment/history regression suites:
- `cd flutter_app && flutter test test/features/programs/data/program_repository_test.dart test/features/repositories/data/firestore_enrollment_repository_test.dart test/features/repositories/data/firestore_workout_repository_test.dart test/features/repositories/data/firestore_phase1_smoke_test.dart test/migration/legacy_migration_orchestrator_test.dart -r expanded`
</verification>

<success_criteria>
- Re-enrollment flow enforces restore-vs-new prompt and stale-state healing guardrails.
- Re-enrollment never carries prior progress position, but keeps profile/injury context intact.
- Cancellation never deletes completed workouts; history surfaces display canceled-plan markers.
- Regression and migration tests cover lifecycle behavior and compatibility.
</success_criteria>

<output>
After completion, create `.planning/phases/07-enrollment-cancellation-lifecycle/07-03-SUMMARY.md`.
</output>
