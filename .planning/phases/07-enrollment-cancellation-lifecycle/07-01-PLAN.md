---
phase: 07-enrollment-cancellation-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flutter_app/lib/domain/models/program_models.dart
  - flutter_app/lib/features/repositories/domain/repository_interfaces.dart
  - flutter_app/lib/features/repositories/data/firestore_repositories.dart
  - flutter_app/lib/features/programs/data/program_repository.dart
  - firestore.rules
  - firestore.indexes.json
  - flutter_app/test/domain/program_models_test.dart
  - flutter_app/test/features/repositories/data/firestore_enrollment_repository_test.dart
  - flutter_app/test/features/repositories/data/firestore_phase1_smoke_test.dart
autonomous: true
must_haves:
  truths:
    - "Enrollment records use explicit lifecycle states (active, canceled, completed) with cancellation metadata while remaining backward-compatible with legacy isActive-only documents."
    - "Canceling an enrollment applies immediately and writes both lifecycle state transition and cancellation event atomically."
    - "Active enrollment reads are deterministic, and stale duplicate-active artifacts can be auto-healed before re-enrollment flows proceed."
    - "Firestore rules enforce lifecycle shape and owner-safe writes for enrollments and enrollment events."
  artifacts:
    - path: flutter_app/lib/domain/models/program_models.dart
      provides: "Enrollment lifecycle enum/fields and event model parsing compatible with legacy enrollment docs"
      contains: "EnrollmentStatus"
    - path: flutter_app/lib/features/repositories/data/firestore_repositories.dart
      provides: "Lifecycle-aware enrollment repository methods for cancel, active lookup, event logging, and stale-state healing"
      contains: "cancelEnrollment"
    - path: firestore.rules
      provides: "Owner-only lifecycle validation for /enrollments and /enrollmentEvents writes"
      contains: "match /enrollments/{enrollmentId}"
    - path: flutter_app/test/features/repositories/data/firestore_enrollment_repository_test.dart
      provides: "Regression coverage for lifecycle transitions, deterministic reads, and duplicate-active healing"
      min_lines: 80
  key_links:
    - from: flutter_app/lib/features/programs/data/program_repository.dart
      to: flutter_app/lib/features/repositories/domain/repository_interfaces.dart
      via: "ProgramRepository calls new lifecycle repository APIs for cancel/re-enroll foundations"
      pattern: "cancelEnrollment|watchLatestEnrollmentEvent|healDuplicateActiveEnrollments"
    - from: flutter_app/lib/features/repositories/data/firestore_repositories.dart
      to: firestore.rules
      via: "Repository write payloads align with rule-enforced enrollment/enrollmentEvents contracts"
      pattern: "status|eventType|lastSyncedAt|canceledAt"
    - from: flutter_app/lib/features/repositories/data/firestore_repositories.dart
      to: firestore.indexes.json
      via: "Deterministic active enrollment query relies on indexed status + lastSyncedAt ordering"
      pattern: "orderBy\\('lastSyncedAt'"
---

<objective>
Establish the lifecycle data contract and repository foundation for cancellation-safe enrollment management.

Purpose: Phase 7 needs cancellation, post-cancel state clarity, and re-enrollment safety; all of that depends on a deterministic lifecycle model and write/query primitives first.
Output: Lifecycle-capable enrollment model + repository interfaces/implementation + rules/indexes + regression tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-enrollment-cancellation-lifecycle/07-CONTEXT.md
@.planning/phases/07-enrollment-cancellation-lifecycle/07-RESEARCH.md
@flutter_app/lib/domain/models/program_models.dart
@flutter_app/lib/features/repositories/domain/repository_interfaces.dart
@flutter_app/lib/features/repositories/data/firestore_repositories.dart
@flutter_app/lib/features/programs/data/program_repository.dart
@firestore.rules
@firestore.indexes.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit enrollment lifecycle and event domain models</name>
  <files>
    flutter_app/lib/domain/models/program_models.dart
    flutter_app/test/domain/program_models_test.dart
  </files>
  <action>
    Replace implicit inactive semantics with explicit lifecycle modeling:
    - Add `EnrollmentStatus` enum (`active`, `canceled`, `completed`) and persistable parser/serializer helpers.
    - Extend `EnrolledProgramModel` with `status`, `canceledAt`, and resilient legacy derivation when `status` is missing.
    - Keep `isActive` as a derived compatibility getter so existing code paths can migrate incrementally.
    - Add `EnrollmentEventModel` for timeline/event stream records (`enrolled`, `canceled`, `completed`, `restored`, `auto_healed`) with `occurredAt` parsing.
    - Ensure parsing accepts Firestore `Timestamp` and ISO strings.

    Preserve backward compatibility: existing docs with only `isActive` must still parse safely and map deterministically into lifecycle status.
  </action>
  <verify>
    cd flutter_app && flutter test test/domain/program_models_test.dart -r expanded
  </verify>
  <done>
    Enrollment lifecycle state is explicit in domain models, event records are modeled, and legacy enrollment documents remain parse-compatible.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement lifecycle-aware enrollment repository APIs with deterministic active query and auto-heal</name>
  <files>
    flutter_app/lib/features/repositories/domain/repository_interfaces.dart
    flutter_app/lib/features/repositories/data/firestore_repositories.dart
    flutter_app/lib/features/programs/data/program_repository.dart
  </files>
  <action>
    Expand repository contracts and Firestore implementation to support Phase 7 lifecycle behavior:
    - Add methods for `cancelEnrollment`, `watchLatestEnrollmentEvent`, canceled enrollment lookup by program, and `healDuplicateActiveEnrollments`.
    - Update `watchActiveEnrollment` to query active status deterministically (ordered newest first), not unordered `limit(1)` on a boolean.
    - Make cancel immediate and atomic with a `WriteBatch`: update enrollment lifecycle fields + append cancellation event in the same commit.
    - Add completion and restore event writes so timeline/event surfaces have source data.
    - Implement stale-state auto-heal by canceling extra active enrollments and writing `auto_healed` events before re-enrollment flows continue.
    - Wire `ProgramRepository` through the new lifecycle APIs while preserving existing call sites where possible.
  </action>
  <verify>
    cd flutter_app && flutter test test/features/repositories/data/firestore_enrollment_repository_test.dart -r expanded
  </verify>
  <done>
    Enrollment repository supports cancellation/event logging/deterministic active queries and can auto-heal conflicting active-state artifacts.
  </done>
</task>

<task type="auto">
  <name>Task 3: Harden Firestore rules/indexes and expand enrollment lifecycle smoke coverage</name>
  <files>
    firestore.rules
    firestore.indexes.json
    flutter_app/test/features/repositories/data/firestore_enrollment_repository_test.dart
    flutter_app/test/features/repositories/data/firestore_phase1_smoke_test.dart
  </files>
  <action>
    Enforce lifecycle safety at the storage boundary:
    - Add enrollment field-shape and allowed-status validation in rules.
    - Add `/users/{uid}/enrollmentEvents/{eventId}` rules with owner-only access and event payload validation.
    - Add/update indexes required for active enrollment lifecycle query ordering.
    - Expand smoke tests to cover enroll -> cancel -> active null behavior and duplicate-active healing.

    Keep rule hardening aligned with current project style: strict owner-only scope with type validation, no broad permission changes.
  </action>
  <verify>
    cd flutter_app && flutter test test/features/repositories/data/firestore_enrollment_repository_test.dart test/features/repositories/data/firestore_phase1_smoke_test.dart -r expanded
  </verify>
  <done>
    Lifecycle repository writes are rule-compliant, indexed for deterministic queries, and regression-covered by repository/smoke tests.
  </done>
</task>

</tasks>

<verification>
Run domain + repository + smoke coverage for lifecycle foundation:
- `cd flutter_app && flutter test test/domain/program_models_test.dart test/features/repositories/data/firestore_enrollment_repository_test.dart test/features/repositories/data/firestore_phase1_smoke_test.dart -r expanded`
</verification>

<success_criteria>
- Enrollment lifecycle status and cancellation metadata are explicit and backward-compatible.
- Cancel operation writes lifecycle transition + event atomically and immediately.
- Active enrollment lookup is deterministic and stale duplicates can be auto-healed.
- Firestore rules/indexes and tests enforce lifecycle-safe repository behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/07-enrollment-cancellation-lifecycle/07-01-SUMMARY.md`.
</output>
