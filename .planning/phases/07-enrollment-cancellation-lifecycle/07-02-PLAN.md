---
phase: 07-enrollment-cancellation-lifecycle
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - flutter_app/lib/features/programs/providers/enrollment_lifecycle_provider.dart
  - flutter_app/lib/features/programs/data/program_repository.dart
  - flutter_app/lib/features/programs/presentation/programs_screen.dart
  - flutter_app/lib/features/workouts/presentation/workout_landing_screen.dart
  - flutter_app/lib/features/dashboard/presentation/dashboard_screen.dart
  - flutter_app/test/features/programs/presentation/programs_screen_test.dart
  - flutter_app/test/features/workouts/presentation/workout_landing_screen_test.dart
autonomous: true
must_haves:
  truths:
    - "Users can cancel an active enrollment from plan management surfaces using a two-step confirmation flow with immediate effect and no cancellation-reason prompt."
    - "After cancellation, plan surfaces render an unambiguous replacement state (`No active plan`) instead of continuing to show active-program details."
    - "Post-cancel replacement state includes both CTAs (`Browse plans` and `Enroll in new plan`) and shows cancellation date/timeline context."
    - "Workout-start actions are hidden or disabled once cancellation is effective, preventing accidental session start from stale active UI."
  artifacts:
    - path: flutter_app/lib/features/programs/providers/enrollment_lifecycle_provider.dart
      provides: "Derived lifecycle UI state (active/canceled/none) from enrollment + latest event streams"
      contains: "EnrollmentLifecycleState"
    - path: flutter_app/lib/features/programs/presentation/programs_screen.dart
      provides: "Two-step cancel flow, replacement post-cancel card, and cancellation timeline/event entry rendering"
      contains: "No active plan"
    - path: flutter_app/lib/features/workouts/presentation/workout_landing_screen.dart
      provides: "Canceled-state handling that removes workout session start affordances"
      contains: "START SESSION"
    - path: flutter_app/test/features/programs/presentation/programs_screen_test.dart
      provides: "Widget coverage for cancel confirmation and post-cancel replacement state behavior"
      min_lines: 60
  key_links:
    - from: flutter_app/lib/features/programs/presentation/programs_screen.dart
      to: flutter_app/lib/features/programs/data/program_repository.dart
      via: "Cancel confirmation action invokes lifecycle cancel API and refreshes derived lifecycle state"
      pattern: "cancelEnrollment|cancelActiveEnrollment"
    - from: flutter_app/lib/features/programs/providers/enrollment_lifecycle_provider.dart
      to: flutter_app/lib/features/repositories/data/firestore_repositories.dart
      via: "Lifecycle provider combines active enrollment and latest event streams from repository layer"
      pattern: "watchActiveEnrollment|watchLatestEnrollmentEvent"
    - from: flutter_app/lib/features/workouts/presentation/workout_landing_screen.dart
      to: flutter_app/lib/features/programs/providers/enrollment_lifecycle_provider.dart
      via: "Workout landing hides start actions based on canceled lifecycle state"
      pattern: "EnrollmentLifecycleState"
---

<objective>
Implement the user-facing cancellation flow and clear post-cancel UX semantics across plan/workout surfaces.

Purpose: Phase 7 requires explicit, safe cancellation with no ambiguous UI state and immediate visible effect.
Output: Lifecycle-derived UI state provider + cancel confirmation UX + post-cancel replacement cards + widget coverage.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-enrollment-cancellation-lifecycle/07-CONTEXT.md
@.planning/phases/07-enrollment-cancellation-lifecycle/07-RESEARCH.md
@.planning/phases/07-enrollment-cancellation-lifecycle/07-01-PLAN.md
@flutter_app/lib/features/programs/data/program_repository.dart
@flutter_app/lib/features/programs/presentation/programs_screen.dart
@flutter_app/lib/features/workouts/presentation/workout_landing_screen.dart
@flutter_app/lib/features/dashboard/presentation/dashboard_screen.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create enrollment lifecycle UI state provider</name>
  <files>
    flutter_app/lib/features/programs/providers/enrollment_lifecycle_provider.dart
    flutter_app/lib/features/programs/data/program_repository.dart
  </files>
  <action>
    Add a dedicated provider layer that turns repository streams into presentation-ready states:
    - `active` with active enrollment payload
    - `canceled` with cancellation date/event metadata
    - `none` for users with no enrollment history

    Source of truth must combine active enrollment stream plus latest enrollment event stream so canceled state remains visible after active enrollment becomes null.
    Keep this provider isolated from screen widgets to avoid repeating lifecycle branching logic in multiple UIs.
  </action>
  <verify>
    cd flutter_app && flutter analyze --no-pub
  </verify>
  <done>
    Screens can read one lifecycle provider and render active/canceled/none states consistently without duplicating state derivation logic.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add two-step cancel flow and post-cancel replacement state in Programs screen</name>
  <files>
    flutter_app/lib/features/programs/presentation/programs_screen.dart
    flutter_app/lib/features/programs/data/program_repository.dart
    flutter_app/test/features/programs/presentation/programs_screen_test.dart
  </files>
  <action>
    Implement cancellation UX aligned to locked phase decisions:
    - Add cancel entry on active plan management surface.
    - Use two-step confirmation dialogs:
      1) confirm intent to cancel
      2) irreversible confirmation before commit
    - Do not collect cancellation reason.
    - Execute cancellation immediately via repository.

    Replace active-plan week card surface after cancel with a full replacement card:
    - Title/state: `No active plan`
    - CTA buttons: `Browse plans` and `Enroll in new plan`
    - Timeline row showing cancellation date from lifecycle event data.

    Ensure active program details and workout controls are not shown once canceled state is effective.
  </action>
  <verify>
    cd flutter_app && flutter test test/features/programs/presentation/programs_screen_test.dart -r expanded
  </verify>
  <done>
    Programs screen supports safe immediate cancellation and switches to explicit post-cancel state with both required CTAs and timeline context.
  </done>
</task>

<task type="auto">
  <name>Task 3: Propagate canceled-state UI to workout and dashboard surfaces</name>
  <files>
    flutter_app/lib/features/workouts/presentation/workout_landing_screen.dart
    flutter_app/lib/features/dashboard/presentation/dashboard_screen.dart
    flutter_app/test/features/workouts/presentation/workout_landing_screen_test.dart
  </files>
  <action>
    Apply lifecycle provider state to workout/dashboard entry points:
    - When lifecycle state is canceled, hide or disable workout session start/resume actions.
    - Render the same explicit inactive wording (`No active plan`) to avoid ambiguity.
    - Keep active behavior unchanged for active enrollments.
    - Keep replacement-state CTA affordances visible where appropriate.

    Add widget tests for canceled-state rendering and action gating so future UI changes cannot regress cancellation semantics.
  </action>
  <verify>
    cd flutter_app && flutter test test/features/workouts/presentation/workout_landing_screen_test.dart test/features/programs/presentation/programs_screen_test.dart -r expanded
  </verify>
  <done>
    Workout/dashboard surfaces no longer expose active workout affordances after cancel and clearly communicate inactive state.
  </done>
</task>

</tasks>

<verification>
Run cancellation UX/widget regression checks:
- `cd flutter_app && flutter analyze --no-pub`
- `cd flutter_app && flutter test test/features/programs/presentation/programs_screen_test.dart test/features/workouts/presentation/workout_landing_screen_test.dart -r expanded`
</verification>

<success_criteria>
- Two-step immediate cancellation flow is implemented with no cancellation-reason prompt.
- Post-cancel state is explicit (`No active plan`) and uses replacement-card UX, not residual active details.
- Required post-cancel CTAs and cancellation timeline entry are visible.
- Workout start actions are blocked/hidden after cancellation across key entry surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/07-enrollment-cancellation-lifecycle/07-02-SUMMARY.md`.
</output>
