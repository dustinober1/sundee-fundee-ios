---
phase: 01-foundation-mens-program-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestore.rules
  - flutter_app/lib/app/router.dart
  - flutter_app/lib/features/auth/data/auth_repository.dart
  - flutter_app/lib/features/auth/presentation/sign_in_screen.dart
  - flutter_app/test/features/auth/data/auth_repository_test.dart
  - flutter_app/test/features/auth/presentation/router_redirect_test.dart
  - flutter_app/test/features/auth/presentation/sign_in_screen_test.dart
  - flutter_app/test/features/repositories/data/firestore_cycle_repository_test.dart
  - flutter_app/test/features/repositories/data/firestore_enrollment_repository_test.dart
autonomous: true
must_haves:
  truths:
    - "Authenticated users can access baseline Phase 1 user-scoped Firestore paths without security rule conflicts."
    - "A valid persisted auth session routes to the main shell after restart."
    - "Invalid or missing session states route to auth entry points without crashes."
  artifacts:
    - path: firestore.rules
      provides: "Rules aligned to repository collection paths used in Phase 1"
    - path: flutter_app/test/features/auth/data/auth_repository_test.dart
      provides: "Auth session and guest-mode regression coverage"
    - path: flutter_app/test/features/repositories/data/firestore_cycle_repository_test.dart
      provides: "Cycle repository read/write contract coverage"
    - path: flutter_app/test/features/repositories/data/firestore_enrollment_repository_test.dart
      provides: "Enrollment repository read/write contract coverage"
  key_links:
    - from: firestore.rules
      to: flutter_app/lib/features/repositories/data/firestore_repositories.dart
      via: "Collection path parity for users/* and program documents"
      pattern: "match /users/{userId} and match /programs/{programId}"
    - from: flutter_app/lib/app/router.dart
      to: flutter_app/lib/features/auth/providers.dart
      via: "authSessionStreamProvider redirect branching"
      pattern: "switch (authStatus)"
---

<objective>
Harden the auth and Firestore baseline so Phase 1 feature work runs on trustworthy contracts.

Purpose: Remove rule/path ambiguity and lock in auth routing behavior before UI-heavy changes.
Output: Aligned rules plus focused regression tests for auth and baseline Firestore repositories.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-mens-program-layout/01-CONTEXT.md
@.planning/phases/01-foundation-mens-program-layout/01-RESEARCH.md
@flutter_app/lib/app/router.dart
@flutter_app/lib/features/auth/data/auth_repository.dart
@flutter_app/lib/features/repositories/data/firestore_repositories.dart
@firestore.rules
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align Firestore rules with repository paths used by Phase 1 flows</name>
  <files>
    firestore.rules
  </files>
  <action>
    Update Firestore rules so the collection paths used by repositories are explicitly represented and readable/writable under the intended ownership model. Include top-level program document access that matches `ProgramRepository` behavior, and preserve user-scoped protections for `users/{userId}` subcollections.

    Keep the ruleset minimal and phase-focused. Do not broaden permissions beyond what baseline auth and Phase 1 read/write flows require.
  </action>
  <verify>
    rg -n "match /users/|match /programs/|isOwner\(" firestore.rules
  </verify>
  <done>
    Rules contain explicit matches for repository-backed paths and no missing collection used by current Phase 1 repositories.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add auth session routing regression tests for restart and invalid-session behavior</name>
  <files>
    flutter_app/lib/features/auth/data/auth_repository.dart
    flutter_app/lib/features/auth/presentation/sign_in_screen.dart
    flutter_app/test/features/auth/data/auth_repository_test.dart
    flutter_app/test/features/auth/presentation/router_redirect_test.dart
    flutter_app/test/features/auth/presentation/sign_in_screen_test.dart
  </files>
  <action>
    Add tests that prove:
    - persisted/valid session leads to authenticated routing behavior,
    - unauthenticated or invalid session routes to auth,
    - guest mode and onboarding state transitions stay deterministic.

    Implement startup session validation that attempts a silent token/session refresh before treating a restored session as invalid, and only route to login when refresh fails.

    Keep auth error UI copy minimal and action-focused in sign-in surfaces while preserving existing flow behavior.
  </action>
  <verify>
    cd flutter_app && flutter test test/features/auth/data/auth_repository_test.dart test/features/auth/presentation/router_redirect_test.dart test/features/auth/presentation/sign_in_screen_test.dart
  </verify>
  <done>
    Auth routing expectations for valid session, invalid session, and guest/unauth states are codified and passing, with startup refresh behavior and concise auth error messaging enforced by tests.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add baseline Firestore repository contract tests for cycle and enrollment read/write</name>
  <files>
    flutter_app/test/features/repositories/data/firestore_cycle_repository_test.dart
    flutter_app/test/features/repositories/data/firestore_enrollment_repository_test.dart
  </files>
  <action>
    Add fake-firestore backed tests covering core Phase 1 operations:
    - save/watch period logs,
    - save/watch cycle settings,
    - enroll/watch active enrollment,
    - update enrollment progress and stop/completion toggles.

    Include one edge case per repository where data is partial/legacy and behavior remains non-crashing.
  </action>
  <verify>
    cd flutter_app && flutter test test/features/repositories/data/firestore_cycle_repository_test.dart test/features/repositories/data/firestore_enrollment_repository_test.dart
  </verify>
  <done>
    Cycle and enrollment repositories have passing tests that confirm baseline read/write behavior and edge-case tolerance.
  </done>
</task>

</tasks>

<verification>
Run the focused test suites added in this plan and ensure there are no failing assertions.
</verification>

<success_criteria>
- Firestore rules include every repository path needed for Phase 1 baseline data flows.
- Auth routing behavior is regression-tested for restart-valid, invalid, and unauthenticated cases.
- Cycle and enrollment repository contracts are covered by deterministic tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-mens-program-layout/01-01-SUMMARY.md`.
</output>
