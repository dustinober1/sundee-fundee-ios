---
phase: 08-firestore-access-contract-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestore.rules
  - firestore.indexes.json
  - deploy.sh
  - flutter_app/lib/features/repositories/data/firestore_repositories.dart
  - flutter_app/lib/features/migration/data/legacy_migration_orchestrator.dart
  - flutter_app/test/features/repositories/data/firestore_enrollment_repository_test.dart
  - flutter_app/test/features/repositories/data/firestore_phase1_smoke_test.dart
autonomous: true
must_haves:
  truths:
    - "Authenticated users can read active enrollment/session data on Home, Programs, and Workout paths without permission-denied caused by repository-rules contract drift."
    - "Legacy enrollment documents missing modern contract fields are auto-normalized before mutation writes so progress/start/cancel flows do not fail with permission-denied."
    - "Dashboard migration-state writes do not trigger permission-denied under authenticated owner scope."
    - "Release flow updates Firestore rules/indexes alongside app changes to avoid environment drift."
  artifacts:
    - path: firestore.rules
      provides: "Owner-safe coverage for migration state writes and enrollment write validation aligned with repository payload behavior"
      contains: "match /migrations/{migrationId}"
    - path: flutter_app/lib/features/repositories/data/firestore_repositories.dart
      provides: "Enrollment contract normalization and deterministic active-enrollment query behavior across legacy and canonical rows"
      contains: "watchActiveEnrollment"
    - path: deploy.sh
      provides: "Deploy guardrail that includes firestore rules/indexes, not hosting-only deploy"
      contains: "firebase deploy --only firestore,firestore:indexes,hosting"
    - path: flutter_app/test/features/repositories/data/firestore_phase1_smoke_test.dart
      provides: "Regression coverage for legacy enrollment shape recovery and authenticated read/write contract stability"
      min_lines: 40
  key_links:
    - from: flutter_app/lib/features/repositories/data/firestore_repositories.dart
      to: firestore.rules
      via: "Repository enrollment mutation payloads align with rule-required fields for both canonical and legacy enrollment rows"
      pattern: "updateEnrollmentProgress|markWeekComplete|jumpToWeek|validEnrollmentWrite"
    - from: flutter_app/lib/features/migration/data/legacy_migration_orchestrator.dart
      to: firestore.rules
      via: "Migration status writes target an explicit owner-safe rule path"
      pattern: "collection\\('migrations'\\)|match /migrations"
    - from: deploy.sh
      to: firestore.rules
      via: "Deploy command ships app and firestore contract changes together"
      pattern: "firebase deploy --only firestore,firestore:indexes,hosting"
---

<objective>
Recover the core Firestore access contract by aligning repository behavior, rules coverage, and deploy workflow for authenticated training surfaces.

Purpose: Phase 08 cannot stabilize UAT access failures unless runtime queries/writes, server rules, and deployment semantics are consistent.
Output: Legacy-safe enrollment repository contract + rules/index updates + deploy guardrail + regression evidence.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-firestore-access-contract-recovery/08-CONTEXT.md
@.planning/phases/08-firestore-access-contract-recovery/08-RESEARCH.md
@firestore.rules
@firestore.indexes.json
@deploy.sh
@flutter_app/lib/features/repositories/data/firestore_repositories.dart
@flutter_app/lib/features/migration/data/legacy_migration_orchestrator.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make enrollment repository reads/writes legacy-safe without weakening owner access boundaries</name>
  <files>
    flutter_app/lib/features/repositories/data/firestore_repositories.dart
    flutter_app/test/features/repositories/data/firestore_enrollment_repository_test.dart
    flutter_app/test/features/repositories/data/firestore_phase1_smoke_test.dart
  </files>
  <action>
    Harden the enrollment repository for mixed-generation docs used in UAT:
    - Add a normalization path that can repair missing legacy enrollment fields (`id`, `status`, `isActive`, `lastSyncedAt`) before mutating writes.
    - Ensure `watchActiveEnrollment` remains deterministic while supporting both canonical status-based rows and legacy rows that still rely on `isActive`.
    - Ensure mutation methods (`updateEnrollmentProgress`, `markWeekComplete`, `jumpToWeek`, cancel/complete flows) call normalization when needed before update.
    - Keep owner scoping and existing lifecycle semantics intact; do not broaden read/write scope beyond authenticated owner paths.

    Add regression tests that seed legacy enrollment shapes and verify progress/cancel/update flows complete without permission-denied style contract failures.
  </action>
  <verify>
    cd flutter_app && flutter test test/features/repositories/data/firestore_enrollment_repository_test.dart test/features/repositories/data/firestore_phase1_smoke_test.dart -r expanded
  </verify>
  <done>
    Enrollment repository read/write flows are deterministic and legacy-safe, with explicit tests proving mutation paths no longer depend on already-perfect document shape.
  </done>
</task>

<task type="auto">
  <name>Task 2: Align Firestore rules/indexes with migration and enrollment contract behavior</name>
  <files>
    firestore.rules
    firestore.indexes.json
    flutter_app/lib/features/migration/data/legacy_migration_orchestrator.dart
  </files>
  <action>
    Update the Firestore contract to match real authenticated app behavior:
    - Add explicit owner-scoped rule coverage for `/users/{uid}/migrations/{migrationId}` state writes used by legacy migration orchestration.
    - Keep enrollment rule validation strict enough for data integrity while ensuring repository-normalized payloads satisfy validation paths.
    - Verify index coverage for any adjusted active/canceled enrollment query patterns introduced by Task 1.
    - Keep all new rules owner-only and avoid any public write expansion.

    If migration-state schema validation is introduced, keep it minimal and compatible with existing migration writer payload.
  </action>
  <verify>
    rg -n "match /migrations/|validEnrollmentWrite|match /enrollments/" firestore.rules && rg -n "\"enrollments\"|\"enrollmentEvents\"" firestore.indexes.json
  </verify>
  <done>
    Rules/indexes explicitly cover migration and enrollment paths used by the app, with no owner-scope regression.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close deploy drift by shipping firestore contract artifacts with app deploy</name>
  <files>
    deploy.sh
    .planning/phases/08-firestore-access-contract-recovery/08-RESEARCH.md
  </files>
  <action>
    Update deploy automation so UAT cannot run new app code against stale firestore contracts:
    - Replace hosting-only deploy with a deploy command that includes `firestore` rules and indexes.
    - Keep current build behavior unchanged.
    - Ensure script remains idempotent and shell-valid.

    Record this guardrail in phase docs so future phases keep contract parity expectations explicit.
  </action>
  <verify>
    bash -n deploy.sh && rg -n "firebase deploy --only firestore,firestore:indexes,hosting" deploy.sh
  </verify>
  <done>
    Deploy script ships hosting + firestore rules/indexes together, reducing UAT-only permission-denied regressions from contract drift.
  </done>
</task>

</tasks>

<verification>
Run contract hardening suites and script checks:
- `cd flutter_app && flutter test test/features/repositories/data/firestore_enrollment_repository_test.dart test/features/repositories/data/firestore_phase1_smoke_test.dart -r expanded`
- `bash -n deploy.sh`
- `rg -n "match /migrations/|validEnrollmentWrite" firestore.rules`
</verification>

<success_criteria>
- Enrollment repository mutation flows are legacy-safe and deterministic for authenticated users.
- Firestore rules explicitly cover migration-state writes and remain owner-only.
- Query/index contract remains aligned for active/canceled enrollment/event lookups.
- Deploy path includes firestore rules/indexes with app release.
</success_criteria>

<output>
After completion, create `.planning/phases/08-firestore-access-contract-recovery/08-01-SUMMARY.md`.
</output>
