---
phase: 08-firestore-access-contract-recovery
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - flutter_app/lib/features/programs/providers/enrollment_lifecycle_provider.dart
  - flutter_app/lib/features/dashboard/presentation/dashboard_screen.dart
  - flutter_app/lib/features/programs/presentation/programs_screen.dart
  - flutter_app/lib/features/workouts/presentation/workout_landing_screen.dart
  - flutter_app/lib/features/shared/presentation/recoverable_access_banner.dart
  - flutter_app/test/features/programs/presentation/programs_screen_test.dart
  - flutter_app/test/features/workouts/presentation/workout_landing_screen_test.dart
  - flutter_app/test/features/dashboard/presentation/dashboard_screen_test.dart
autonomous: true
must_haves:
  truths:
    - "Home, Programs, and Workout surfaces distinguish valid empty enrollment data from genuine backend access failures."
    - "Recoverable backend access failures show a calm top-banner with retry guidance while preserving the underlying screen skeleton/content."
    - "Access retries auto-run up to 3 attempts with short backoff, then allow manual retry and escalation when failures persist."
    - "On app resume, access paths silently revalidate once without forcing tab/navigation loss."
  artifacts:
    - path: flutter_app/lib/features/programs/providers/enrollment_lifecycle_provider.dart
      provides: "Lifecycle/access state model that separates `valid_empty` from `recoverable_failure`/`blocking_failure`"
      contains: "EnrollmentLifecycleState"
    - path: flutter_app/lib/features/shared/presentation/recoverable_access_banner.dart
      provides: "Shared top-banner UI for retry guidance and manual retry action"
      contains: "retry"
    - path: flutter_app/lib/features/dashboard/presentation/dashboard_screen.dart
      provides: "Home-surface access recovery UX replacing raw exception card behavior"
      contains: "_NextWorkoutCard"
    - path: flutter_app/test/features/programs/presentation/programs_screen_test.dart
      provides: "Widget coverage proving recoverable failures render guidance banner instead of terminal screen error"
      min_lines: 50
  key_links:
    - from: flutter_app/lib/features/programs/providers/enrollment_lifecycle_provider.dart
      to: flutter_app/lib/features/dashboard/presentation/dashboard_screen.dart
      via: "Dashboard consumes lifecycle access-state categories to render empty/retry/blocking states correctly"
      pattern: "EnrollmentLifecycleState|recoverable|retry"
    - from: flutter_app/lib/features/programs/providers/enrollment_lifecycle_provider.dart
      to: flutter_app/lib/features/programs/presentation/programs_screen.dart
      via: "Programs screen maps lifecycle failure state to top-banner recovery UX, not terminal error widget"
      pattern: "error:|retry|recoverable"
    - from: flutter_app/lib/features/shared/presentation/recoverable_access_banner.dart
      to: flutter_app/lib/features/workouts/presentation/workout_landing_screen.dart
      via: "Workout surface reuses shared banner contract for consistent retry guidance"
      pattern: "RecoverableAccessBanner|manual retry"
---

<objective>
Implement recoverable access UX for core training surfaces so Firestore failures no longer render broken tabs.

Purpose: Phase 08 requires resilient user-facing behavior for ACL-01/02/03/05, not just backend contract fixes.
Output: Shared lifecycle access-state model + retry orchestration + top-banner recovery UX across Home, Programs, and Workout.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-firestore-access-contract-recovery/08-CONTEXT.md
@.planning/phases/08-firestore-access-contract-recovery/08-RESEARCH.md
@.planning/phases/08-firestore-access-contract-recovery/08-01-PLAN.md
@flutter_app/lib/features/programs/providers/enrollment_lifecycle_provider.dart
@flutter_app/lib/features/dashboard/presentation/dashboard_screen.dart
@flutter_app/lib/features/programs/presentation/programs_screen.dart
@flutter_app/lib/features/workouts/presentation/workout_landing_screen.dart
@flutter_app/lib/features/cycle/providers.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce lifecycle access-state classifier with bounded retry strategy</name>
  <files>
    flutter_app/lib/features/programs/providers/enrollment_lifecycle_provider.dart
    flutter_app/lib/features/shared/presentation/recoverable_access_banner.dart
  </files>
  <action>
    Replace raw AsyncError propagation with explicit lifecycle access states:
    - Add recoverable-vs-blocking error classification for Firestore read failures (`permission-denied`, `unauthenticated`, `unavailable`, `deadline-exceeded`).
    - Encode state outcomes explicitly: active, canceled, valid_empty, recoverable_failure, blocking_failure.
    - Implement retry orchestration aligned with context decisions: auto-retry up to 3 attempts with short backoff, then manual retry.
    - Keep retries scoped and deterministic to avoid infinite retry loops.

    Add a shared recoverable banner widget with retry action and short checklist copy so all surfaces present consistent guidance.
  </action>
  <verify>
    cd flutter_app && flutter test test/features/programs/presentation/programs_screen_test.dart test/features/workouts/presentation/workout_landing_screen_test.dart -r expanded
  </verify>
  <done>
    Lifecycle provider exposes deterministic recoverable/blocking states with bounded retry and a reusable recovery banner contract.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply recoverable banner + empty-state distinction to Home, Programs, and Workout screens</name>
  <files>
    flutter_app/lib/features/dashboard/presentation/dashboard_screen.dart
    flutter_app/lib/features/programs/presentation/programs_screen.dart
    flutter_app/lib/features/workouts/presentation/workout_landing_screen.dart
    flutter_app/lib/features/shared/presentation/recoverable_access_banner.dart
  </files>
  <action>
    Integrate the new access-state contract into all core training surfaces:
    - Replace raw error text/cards with top-banner guidance and retry action for recoverable failures.
    - Preserve underlying content skeleton/state while banner is visible.
    - Render valid empty enrollment/missing-session states explicitly (for example "No upcoming session yet") rather than treating as backend failure.
    - Add escalation path to blocking recovery state after retry exhaustion.
    - Keep behavior tab-stable so users remain on the current surface after auth refresh/retry.
  </action>
  <verify>
    cd flutter_app && flutter test test/features/programs/presentation/programs_screen_test.dart test/features/workouts/presentation/workout_landing_screen_test.dart test/features/dashboard/presentation/dashboard_screen_test.dart -r expanded
  </verify>
  <done>
    Home, Programs, and Workout surfaces show consistent recoverable banner UX, preserve skeleton state, and correctly separate empty-data vs backend-failure outcomes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add widget-level regression coverage for retry, resume, and escalation behavior</name>
  <files>
    flutter_app/test/features/dashboard/presentation/dashboard_screen_test.dart
    flutter_app/test/features/programs/presentation/programs_screen_test.dart
    flutter_app/test/features/workouts/presentation/workout_landing_screen_test.dart
  </files>
  <action>
    Expand UI tests to lock Phase 08 UX behavior:
    - Recoverable error branch renders top banner and retry guidance.
    - Manual retry path is available after auto-retry attempts.
    - Empty-data state does not render as backend failure.
    - Blocking state appears only after persistent retry failure.

    Keep tests deterministic by injecting fake provider states rather than real network dependencies.
  </action>
  <verify>
    cd flutter_app && flutter test test/features/dashboard/presentation/dashboard_screen_test.dart test/features/programs/presentation/programs_screen_test.dart test/features/workouts/presentation/workout_landing_screen_test.dart -r expanded
  </verify>
  <done>
    Widget regression suites prove ACL-05 UX contract is consistently enforced across Home, Programs, and Workout screens.
  </done>
</task>

</tasks>

<verification>
Run UI recovery-state regression suites:
- `cd flutter_app && flutter test test/features/dashboard/presentation/dashboard_screen_test.dart test/features/programs/presentation/programs_screen_test.dart test/features/workouts/presentation/workout_landing_screen_test.dart -r expanded`
</verification>

<success_criteria>
- Core training surfaces no longer present raw terminal error text for recoverable Firestore failures.
- Retry behavior matches context contract (3 auto-retries + manual retry + blocking escalation).
- Empty-data and backend-failure outcomes are clearly distinct.
- Widget tests lock in recoverable access UX behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/08-firestore-access-contract-recovery/08-02-SUMMARY.md`.
</output>
