---
phase: 08-firestore-access-contract-recovery
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - flutter_app/lib/features/workouts/presentation/workout_execution_providers.dart
  - flutter_app/lib/features/workouts/presentation/workout_execution_screen.dart
  - flutter_app/lib/features/workouts/data/workout_sync_queue_store.dart
  - flutter_app/lib/features/workouts/providers/workout_sync_recovery_provider.dart
  - flutter_app/test/features/workouts/presentation/workout_execution_progress_test.dart
  - flutter_app/test/features/workouts/presentation/workout_execution_screen_phase_update_test.dart
  - flutter_app/test/features/workouts/presentation/workout_write_resilience_test.dart
autonomous: true
must_haves:
  truths:
    - "Workout start/progress flows continue locally when transient Firestore writes fail, without immediate hard stop."
    - "Pending workout writes are queued and retried automatically when access recovers."
    - "Workout completion cannot be finalized as fully synced until required backend writes succeed; persistent completion-sync failure shows blocking recovery UI with manual retry."
    - "Transient write failures use non-blocking feedback while preserving in-session continuity."
  artifacts:
    - path: flutter_app/lib/features/workouts/data/workout_sync_queue_store.dart
      provides: "Durable pending-write queue model for workout write intents and retry metadata"
      contains: "pending"
    - path: flutter_app/lib/features/workouts/presentation/workout_execution_providers.dart
      provides: "Write-resilient finish flow that stages local completion, enqueues failures, and replays writes"
      contains: "finishWorkout"
    - path: flutter_app/lib/features/workouts/presentation/workout_execution_screen.dart
      provides: "Toast/modal UX for transient vs blocking workout write failures and manual retry"
      contains: "Failed to save workout"
    - path: flutter_app/test/features/workouts/presentation/workout_write_resilience_test.dart
      provides: "Regression coverage for queue/replay and blocking finalization behavior"
      min_lines: 80
  key_links:
    - from: flutter_app/lib/features/workouts/presentation/workout_execution_providers.dart
      to: flutter_app/lib/features/workouts/data/workout_sync_queue_store.dart
      via: "Finish workflow persists pending write intents and replay metadata through queue store"
      pattern: "queue|pending|retry"
    - from: flutter_app/lib/features/workouts/providers/workout_sync_recovery_provider.dart
      to: flutter_app/lib/features/workouts/presentation/workout_execution_screen.dart
      via: "UI reacts to pending-sync/blocking states and exposes manual retry for finalization failures"
      pattern: "blocking|manual retry|toast|modal"
    - from: flutter_app/lib/features/workouts/presentation/workout_execution_providers.dart
      to: flutter_app/lib/features/repositories/data/firestore_repositories.dart
      via: "Enrollment/workout write replay uses canonical repository contract recovered in plan 08-01"
      pattern: "saveWorkout|saveCompletedSet|updateEnrollmentProgress"
---

<objective>
Add workout write resilience so transient Firestore failures no longer break in-session progress while preserving durable finalization guarantees.

Purpose: ACL-04 requires successful write behavior in expected authenticated flows plus explicit recovery behavior for transient backend failures.
Output: Local pending-write queue + replay orchestration + workout UX for transient and blocking write outcomes + regression tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-firestore-access-contract-recovery/08-CONTEXT.md
@.planning/phases/08-firestore-access-contract-recovery/08-RESEARCH.md
@.planning/phases/08-firestore-access-contract-recovery/08-01-PLAN.md
@flutter_app/lib/features/workouts/presentation/workout_execution_providers.dart
@flutter_app/lib/features/workouts/presentation/workout_execution_screen.dart
@flutter_app/lib/features/repositories/data/firestore_repositories.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce pending-write queue contract for workout start/progress/completion writes</name>
  <files>
    flutter_app/lib/features/workouts/data/workout_sync_queue_store.dart
    flutter_app/lib/features/workouts/providers/workout_sync_recovery_provider.dart
    flutter_app/lib/features/workouts/presentation/workout_execution_providers.dart
  </files>
  <action>
    Build a lightweight local write queue contract for workout persistence:
    - Model pending write intents for completed sets, workout summary, lift max updates, and enrollment progress updates.
    - Persist minimal metadata needed for deterministic replay (operation type, payload key, retry count, last attempt).
    - Add retry scheduling hooks for automatic background replay when access returns.
    - Keep queue logic bounded and deterministic; prevent unbounded duplicate replay.

    Reuse existing app stack and patterns; avoid introducing new external queue infrastructure for this phase.
  </action>
  <verify>
    cd flutter_app && flutter test test/features/workouts/presentation/workout_write_resilience_test.dart -r expanded
  </verify>
  <done>
    Workout write intents can be queued locally and replayed deterministically, enabling resilient persistence behavior under transient backend failures.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor workout finish flow to support local continuity plus sync-gated finalization</name>
  <files>
    flutter_app/lib/features/workouts/presentation/workout_execution_providers.dart
    flutter_app/lib/features/workouts/providers/workout_sync_recovery_provider.dart
    flutter_app/lib/features/repositories/data/firestore_repositories.dart
  </files>
  <action>
    Update `finishWorkout(...)` behavior to match phase context:
    - On transient write failure, keep the session continuity path and enqueue pending writes instead of immediate hard failure.
    - Distinguish transient vs blocking failures; transient failures should not discard local completion/progress state.
    - Require successful completion sync before final "fully finalized" state, with explicit pending marker when still unsynced.
    - Keep repository interactions compatible with the canonical contract from plan 08-01.

    Preserve existing workout data semantics (set numbering, lift max calculations, enrollment progression recommendations) while making write execution resilient.
  </action>
  <verify>
    cd flutter_app && flutter test test/features/workouts/presentation/workout_execution_progress_test.dart test/features/workouts/presentation/workout_write_resilience_test.dart -r expanded
  </verify>
  <done>
    Workout completion flow supports local continuity and queued replay while enforcing sync-gated finalization semantics.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add user-facing recovery UX for transient and persistent workout write failures</name>
  <files>
    flutter_app/lib/features/workouts/presentation/workout_execution_screen.dart
    flutter_app/lib/features/workouts/providers/workout_sync_recovery_provider.dart
    flutter_app/test/features/workouts/presentation/workout_execution_screen_phase_update_test.dart
    flutter_app/test/features/workouts/presentation/workout_write_resilience_test.dart
  </files>
  <action>
    Implement phase-context-compliant workout failure UX:
    - Show non-blocking toast/snackbar feedback for transient write failures while session continues.
    - Show blocking modal/recovery view when completion finalization remains unsynced after retry budget.
    - Provide manual retry action from blocking state.
    - Ensure successful retry clears blocking state and preserves user progress context.

    Keep copy calm and direct, aligned with phase context guidelines.
  </action>
  <verify>
    cd flutter_app && flutter test test/features/workouts/presentation/workout_execution_screen_phase_update_test.dart test/features/workouts/presentation/workout_write_resilience_test.dart -r expanded
  </verify>
  <done>
    Workout users get clear, non-disruptive transient error feedback and an explicit blocking recovery path with manual retry for persistent finalization failure.
  </done>
</task>

</tasks>

<verification>
Run workout resilience suites:
- `cd flutter_app && flutter test test/features/workouts/presentation/workout_execution_progress_test.dart test/features/workouts/presentation/workout_execution_screen_phase_update_test.dart test/features/workouts/presentation/workout_write_resilience_test.dart -r expanded`
</verification>

<success_criteria>
- Transient workout write failures no longer cause immediate workflow collapse.
- Pending writes are queued and replayed after access recovery.
- Completion finalization has explicit blocking recovery path when persistent failures remain.
- Widget/provider tests verify retry, replay, and escalation behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/08-firestore-access-contract-recovery/08-03-SUMMARY.md`.
</output>
