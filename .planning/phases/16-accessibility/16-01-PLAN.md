---
phase: 16-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SundeeFundee/Sources/SundeeFundeeKit/UI/App/SundeeFundeeApp.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Dashboard/DashboardView.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Settings/SettingsView.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Workouts/WorkoutsListView.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Workouts/WorkoutDetailView.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Workouts/AIWorkoutView.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Workouts/ExercisePickerView.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Analytics/AnalyticsView.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Analytics/CycleCorrelationChart.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Analytics/VolumeChart.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Analytics/StrengthProgressionChart.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Analytics/FrequencyChart.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Benchmarks/BenchmarksListView.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Cycle/CycleCalendarView.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Maxes/MaxesListView.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Onboarding/OnboardingView.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Pain/PainTrackingView.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Programs/ProgramsListView.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Export/ExportView.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Insights/InsightsView.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Share/WorkoutShareCardView.swift
  - SundeeFundee/Sources/SundeeFundeeKit/UI/Theme/AppTheme.swift
autonomous: true
requirements:
  - AUD-04

must_haves:
  truths:
    - "Every interactive element (button, toggle, slider, picker, NavigationLink, text field) has a meaningful accessibilityLabel"
    - "Decorative images are marked with .accessibilityHidden(true)"
    - "Chart views provide accessibilityValue describing their data"
    - "StatCard values and labels are grouped into single accessibility elements"
    - "Tab items in MainTabView have descriptive accessibility labels"
  artifacts:
    - path: "SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Dashboard/DashboardView.swift"
      provides: "Dashboard with full VoiceOver labeling on all interactive elements"
      contains: "accessibilityLabel"
    - path: "SundeeFundee/Sources/SundeeFundeeKit/UI/Theme/AppTheme.swift"
      provides: "StatCard with grouped accessibility element"
      contains: "accessibilityElement"
    - path: "SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Settings/SettingsView.swift"
      provides: "Settings with labeled toggles, pickers, links"
      contains: "accessibilityLabel"
  key_links:
    - from: "SundeeFundeeApp.swift MainTabView"
      to: "Tab items"
      via: ".tabItem accessibilityLabel"
      pattern: "accessibilityLabel.*tab"
    - from: "DashboardView quickActionsCard"
      to: "NavigationLink destinations"
      via: "accessibilityHint on shortcut buttons"
      pattern: "accessibilityHint"
    - from: "StatCard"
      to: "VoiceOver"
      via: "accessibilityElement combining value + label"
      pattern: "\\.accessibilityElement\\(children: \\.ignore\\)"
---

<objective>
Add comprehensive VoiceOver accessibility labels, hints, and value descriptions to every interactive element across all SwiftUI views.

Purpose: Ensure the app is fully usable by people relying on VoiceOver (AUD-04). Currently only 4 elements in the entire app have accessibilityLabel -- the rest are unlabeled icons, buttons, and decorative images that VoiceOver reads as meaningless or skips entirely.

Output: All 22 UI view files updated with VoiceOver modifiers. StatCard component gains grouped accessibility. Chart views gain data descriptions. AuthView and MainTabView gain labeled interactive elements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@SundeeFundee/Sources/SundeeFundeeKit/UI/Theme/AppTheme.swift
@SundeeFundee/Sources/SundeeFundeeKit/UI/App/SundeeFundeeApp.swift

<interfaces>
<!-- Key contracts executor needs. All in SundeeFundeeKit/UI/ -->

From AppTheme.swift:
- AppTheme.Background.cream, .navy, .white, .card
- AppTheme.Text.primary, .secondary, .cream, .gold, .orange, .white
- AppTheme.Accent.gold, .goldLight, .goldDark, .orange, .orangeLight
- AppTheme.Semantic.success, .warning, .error, .info
- AppTheme.Typography.* (displayLarge through monoSmall)
- AppTheme.Spacing.* (xs through xxxl)
- ArtDecoCard<Content: View> -- card container
- StatCard -- value + label card
- ArtDecoButtonStyle -- button style enum

From SundeeFundeeApp.swift:
- MainTabView -- TabView with 7 tabs (dashboard, workouts, programs, maxes, analytics, benchmarks, settings)
- AuthView -- sign-in / guest flow
- Tab enum -- 7 cases
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add VoiceOver labels to core views (MainTabView, AuthView, Dashboard, Settings, Onboarding)</name>
  <files>
    SundeeFundee/Sources/SundeeFundeeKit/UI/App/SundeeFundeeApp.swift
    SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Dashboard/DashboardView.swift
    SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Settings/SettingsView.swift
    SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Onboarding/OnboardingView.swift
    SundeeFundee/Sources/SundeeFundeeKit/UI/Theme/AppTheme.swift
  </files>
  <action>
Per AUD-04 and CONTEXT.md decision "Add .accessibilityLabel() and .accessibilityHint() to all interactive elements without labels":

**MainTabView (SundeeFundeeApp.swift):**
- Each `.tabItem` already has `Label("Dashboard", ...)` etc., which SwiftUI reads. Verify tab items read correctly. Add `.accessibilityHint("Navigate to ...")` on each tab view for clarity.

**AuthView (SundeeFundeeApp.swift):**
- "Sign in with Apple" button: add `.accessibilityHint("Sign in using your Apple ID")`
- "Continue as Guest" button: add `.accessibilityHint("Use the app without an account")`
- App logo image (figure.strengthtraining.traditional): mark `.accessibilityHidden(true)` (decorative)

**DashboardView.swift:**
- Cycle phase banner NavigationLink: add `.accessibilityElement(children: .combine)` then `.accessibilityLabel("Cycle phase: \(phase)")` and `.accessibilityHint("Tap to view cycle calendar")`
- Confidence percentage text + circle: combine into single element, `.accessibilityLabel("Phase confidence: \(Int(confidence * 100)) percent")`
- "Generate" button: add `.accessibilityLabel("Generate AI workout")`, `.accessibilityHint("Creates a workout based on your cycle phase")`
- Quick action NavigationLinks ("Log Max", "Pain Log", "Benchmarks"): add `.accessibilityHint("Navigate to ...")` on each
- "View All Insights" NavigationLink: add `.accessibilityHint("View detailed training insights")`
- Decorative images (phase icons, checkmark icons): mark `.accessibilityHidden(true)` where they duplicate adjacent text meaning
- `statCards` HStack: add `.accessibilityElement(children: .contain)` so VoiceOver reads each StatCard individually

**AppTheme.swift -- StatCard:**
- Group value and label into a single accessibility element:
  ```
  .accessibilityElement(children: .combine)
  .accessibilityLabel("\(value) \(label)")
  ```

**SettingsView.swift:**
- "Sign Out" button: add `.accessibilityHint("Sign out of your account")`
- "Delete All Data & Account" button: add `.accessibilityHint("Permanently delete all data. This cannot be undone.")`
- Website, Privacy Policy, Terms links: already have visible text, verify VoiceOver reads the link destination
- Cycle settings slider already has `.accessibilityLabel("Cycle length, \(Int(cycleLength)) days")` -- verify this is correct

**OnboardingView.swift:**
- Progress bar: add `.accessibilityLabel("Step \(viewModel.currentStep + 1) of 4")` and `.accessibilityValue("\(viewModel.currentStep + 1)")`
- Experience level buttons: add `.accessibilityHint("Select \(title) experience level")`
- Goal option buttons: add `.accessibilityHint("Select \(title) as your primary goal")`
- Weight Unit picker: already has visible label, verify accessibility
- "Back" button: add `.accessibilityLabel("Go back")`
- "Next" button: add `.accessibilityLabel("Next step")`
- "Get Started" button: add `.accessibilityHint("Complete onboarding and start using the app")`
- The TabView page control: add `.accessibilityLabel("Onboarding step \(viewModel.currentStep + 1)")`

**General rules for ALL views:**
- `Image(systemName:)` used as decorative icons (chevrons, dividers, dots) get `.accessibilityHidden(true)` when adjacent text already conveys meaning
- `NavigationLink` wrapping visual content: add `.accessibilityHint("Double tap to navigate")` only when the destination is not obvious from the label
- Empty state illustrations: mark decorative images `.accessibilityHidden(true)`
- `Toggle`: ensure all have `.accessibilityLabel()` if the visible label is separate from the toggle
  </action>
  <verify>
    <automated>cd /Users/dustinober/Projects/sundee-fundee/SundeeFundeeApp && xcodebuild -project SundeeFundee.xcodeproj -scheme SundeeFundee -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <done>All interactive elements in MainTabView, AuthView, DashboardView, SettingsView, OnboardingView, and StatCard have accessibilityLabel and appropriate accessibilityHint. Build compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add VoiceOver labels to remaining views (Workouts, Analytics, Benchmarks, Programs, Pain, Export, Insights, Cycle Calendar)</name>
  <files>
    SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Workouts/WorkoutsListView.swift
    SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Workouts/WorkoutDetailView.swift
    SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Workouts/AIWorkoutView.swift
    SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Workouts/ExercisePickerView.swift
    SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Analytics/AnalyticsView.swift
    SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Analytics/CycleCorrelationChart.swift
    SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Analytics/VolumeChart.swift
    SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Analytics/StrengthProgressionChart.swift
    SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Analytics/FrequencyChart.swift
    SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Benchmarks/BenchmarksListView.swift
    SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Cycle/CycleCalendarView.swift
    SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Maxes/MaxesListView.swift
    SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Pain/PainTrackingView.swift
    SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Programs/ProgramsListView.swift
    SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Export/ExportView.swift
    SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Insights/InsightsView.swift
    SundeeFundee/Sources/SundeeFundeeKit/UI/Views/Share/WorkoutShareCardView.swift
  </files>
  <action>
Per AUD-04 and CONTEXT.md decision "Add .accessibilityLabel() and .accessibilityHint() to all interactive elements without labels":

**WorkoutsListView.swift:**
- "Add Workout" toolbar button already has `.accessibilityLabel("Add Workout")` -- keep it
- Empty state image: `.accessibilityHidden(true)`
- WorkoutRowContent completion checkmark: add `.accessibilityLabel("Completed")` and `.accessibilityHidden(false)` (it conveys meaning)
- Duration clock icon + text: combine via `.accessibilityElement(children: .combine)` so VoiceOver reads "12 min" together
- Exercise tags: add `.accessibilityLabel("Exercises: \(exercises.joined(separator: ", "))")` on the HStack containing them
- "+ N more" text: ensure it reads naturally

**WorkoutDetailView.swift:**
- Share button (square.and.arrow.up): add `.accessibilityLabel("Share workout")`
- "Finish" toolbar button: add `.accessibilityHint("Complete all remaining sets and finish the workout")`
- Set completion toggle buttons (checkmark.circle/circle): add `.accessibilityLabel("Set \(setNumber), \(repDisplay(set)) reps at \(weightDisplay(set.prescribedWeight)). \(set.isComplete ? "Completed" : "Not completed")")`
- "Completed" / "In Progress" labels: already use Label which VoiceOver reads
- Volume stat card value: VoiceOver will read the formatted value via the StatCard accessibilityElement from Task 1

**AIWorkoutView.swift:**
- Focus option buttons: add `.accessibilityHint("Select \(title) focus area")`
- Energy option buttons: add `.accessibilityHint("Select \(title) energy level")`
- Equipment option buttons: add `.accessibilityHint("Select \(title) equipment access")`
- Duration picker segments: verify VoiceOver reads segment labels
- "Generate Workout" button: add `.accessibilityHint("Create a personalized workout based on your selections")`
- "Start This Workout" button: add `.accessibilityHint("Begin the generated workout now")`
- "Regenerate" button: add `.accessibilityHint("Generate a different workout")`
- Phase multiplier badge (e.g. "80%"): add `.accessibilityLabel("Cycle phase multiplier: \(percentage) percent")`
- Cycle phase card: combine into single accessibility element

**ExercisePickerView.swift:**
- Search field: verify placeholder text is read by VoiceOver (TextField placeholder is accessible by default)
- Exercise selection buttons: ensure the exercise name is the label
- Category headers: verify section headers are read

**AnalyticsView.swift:**
- Time range picker buttons ("1M", "3M", "6M", "1Y", "All"): add `.accessibilityLabel("\(range.shortLabel) time range")` and `.accessibilityHint("Filter analytics to \(range.shortLabel)")` and `.accessibilityAddTraits(viewModel.selectedTimeRange == range ? .isSelected : [])` so VoiceOver announces selection state
- Loading state ProgressView: add `.accessibilityLabel("Loading analytics data")`
- Error state "Retry" button: add `.accessibilityHint("Reload analytics data")`

**Chart views (CycleCorrelationChart, VolumeChart, StrengthProgressionChart, FrequencyChart):**
- Each chart card: add `.accessibilityElement(children: .ignore)` then `.accessibilityLabel(chartSummary)` where chartSummary is a text description like "Cycle correlation chart showing average volume by menstrual phase"
- Empty state: add `.accessibilityLabel("No data available for this chart")`
- For charts with data: generate a summary string programmatically and use `.accessibilityValue(summaryText)`. Example for CycleCorrelationChart: "Menstrual: \(value), Follicular: \(value), Ovulation: \(value), Luteal: \(value)"
- This is critical per CONTEXT.md: "Which specific elements need VoiceOver labels" is at Claude's discretion. Charts MUST have data descriptions.

**BenchmarksListView.swift:**
- Category picker buttons: add `.accessibilityLabel("\(category) category")` and `.accessibilityAddTraits(viewModel.selectedCategory == category ? .isSelected : [])`
- BenchmarkRow NavigationLinks: add `.accessibilityHint("View benchmark details and log results")`
- Readiness emoji (e.g. peak/moderate/recovery): replace with `.accessibilityLabel(readiness.tier.rawValue)` since emojis are not VoiceOver-friendly
- Intensity dots: add `.accessibilityLabel("Intensity: \(benchmark.intensity.rawValue) out of 5")`
- "Enroll" button: add `.accessibilityHint("Enroll in this training program")`
- "Log Result" toolbar button: add `.accessibilityLabel("Log benchmark result")`

**CycleCalendarView.swift:**
- Month navigation already has `.accessibilityLabel("Previous Month")` and `"Next Month"` -- keep
- Day cells: add `.accessibilityLabel` describing the day, e.g. "\(dayNumber), \(monthName), \(phaseName ?? ""), cycle day \(cycleDay), \(isPeriodLogged ? "period logged" : "")"` on each dayCellView
- Phase legend items: ensure `.accessibilityLabel("\(label) phase")` on each legend item
- Phase detail card: combine into `.accessibilityElement(children: .combine)` with descriptive label

**MaxesListView.swift:**
- "Add One-Rep Max" toolbar button already has `.accessibilityLabel("Add One-Rep Max")` -- keep
- Max row items: add `.accessibilityLabel("\(max.exerciseName), \(Int(max.weight)) \(max.unit.rawValue)")` so VoiceOver reads the full entry
- "Log Your First Max" button: add `.accessibilityHint("Record your first one-rep max lift")`
- OneRepMaxEntryView segmented unit picker: add `.accessibilityLabel("Weight unit")` if not already provided by Picker label

**PainTrackingView.swift:**
- "Log Pain" toolbar button already has `.accessibilityLabel("Log Pain")` -- keep
- "Log Pain" card button: add `.accessibilityHint("Open pain logging form")`
- Body region buttons: ensure region.displayName is the accessibilityLabel
- Pain type buttons: ensure type.displayName is the accessibilityLabel
- Pain intensity slider already has `.accessibilityLabel("Pain intensity, \(viewModel.painIntensity) out of 10")` -- keep
- Active injuries banner: add `.accessibilityLabel("Active injuries: \(injuryCount) injuries")`
- PainLogRow intensity circle + number: combine into `.accessibilityLabel("Pain intensity: \(log.intensity) out of 10, \(log.painType.displayName)")`
- Substitution section: ensure substitute items read naturally

**ProgramsListView.swift:**
- "Enroll" button: add `.accessibilityHint("Start this program")`
- Program row checkmark (checkmark.seal.fill): add `.accessibilityLabel("Currently enrolled")`
- Duration/sessions labels: ensure they read as "X weeks, Y sessions per week"

**ExportView.swift:**
- "Export My Data" button: add `.accessibilityHint("Generate a JSON file of all your data")`
- Category count badges: ensure VoiceOver reads "\(categoryName): \(count) records"
- ShareLink: add `.accessibilityLabel("Share exported data file")`

**InsightsView.swift:**
- Trend severity icons: add `.accessibilityLabel(severity.description)` instead of relying on icon only
- Plateau exercise items: add `.accessibilityLabel("\(plateau.exerciseName), stalled at \(Int(plateau.currentWeight)) \(plateau.unit.rawValue), \(plateau.stallCount) attempts, \(plateau.daysSinceBest) days since personal best")`
- Empty state image: `.accessibilityHidden(true)`

**WorkoutShareCardView.swift:**
- This is a rendered image (not interactive), but add accessibility labels for VoiceOver users who may encounter it in the share sheet: combine content into a single `.accessibilityElement(children: .combine)` with a description like "Workout share card: \(workout.name), \(workout.exercises.count) exercises"

**General rule for ALL views:**
- Every `.buttonStyle(.plain)` element must have explicit `.accessibilityLabel()` because plain button style strips default accessibility
- Every SF Symbol image used as the sole content of a button (no text) must have `.accessibilityLabel()`
- Segmented pickers: verify the label text is accessible
  </action>
  <verify>
    <automated>cd /Users/dustinober/Projects/sundee-fundee/SundeeFundeeApp && xcodebuild -project SundeeFundee.xcodeproj -scheme SundeeFundee -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <done>All interactive elements across Workouts, Analytics, Benchmarks, Cycle Calendar, Maxes, Pain, Programs, Export, and Insights views have meaningful accessibilityLabel and accessibilityHint. Chart views have data summaries as accessibilityValue. Build compiles without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VoiceOver output | Accessibility labels are spoken aloud -- must not leak sensitive data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | I (Information Disclosure) | accessibilityLabel on pain logs | accept | Pain data is user-entered and only spoken via VoiceOver to the device user, same as visible UI |
| T-16-02 | I (Information Disclosure) | accessibilityLabel on cycle phase | accept | Cycle phase is already visible on screen; VoiceOver does not add new data exposure |
</threat_model>

<verification>
1. Build compiles with zero errors after all accessibility modifiers added
2. Grep for `Image(systemName:` without a corresponding `.accessibilityLabel` or `.accessibilityHidden(true)` within 3 lines -- should find zero unlabeled icon-only images used as buttons
3. Grep for `.buttonStyle(.plain)` without `.accessibilityLabel` -- should find zero plain buttons without labels
</verification>

<success_criteria>
- Every button with `.buttonStyle(.plain)` has an explicit `.accessibilityLabel()`
- Every `Image(systemName:)` used as the sole content of a button has `.accessibilityLabel()`
- Decorative images have `.accessibilityHidden(true)`
- Chart views have `.accessibilityValue()` describing their data
- StatCard groups value + label into a single accessibility element
- Build compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-accessibility/16-01-SUMMARY.md`
</output>
