---
phase: 09-onboarding-resume-eligibility-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flutter_app/lib/features/auth/domain/onboarding_eligibility_evaluator.dart
  - flutter_app/lib/features/auth/data/auth_repository.dart
  - flutter_app/lib/domain/models/user_model.dart
  - flutter_app/test/features/auth/domain/onboarding_eligibility_evaluator_test.dart
  - flutter_app/test/features/auth/data/auth_repository_onboarding_bootstrap_test.dart
  - flutter_app/test/domain/models/user_model_test.dart
autonomous: true
must_haves:
  truths:
    - "Returning users with complete onboarding evidence are not routed to Resume Onboarding only because onboardingComplete is false."
    - "Users missing required onboarding fields are routed deterministically: workout history keeps resume required, max-only history can bypass resume, and no evidence remains resume required."
    - "Authenticated bootstrap failures (profile read timeout/error or legacy evidence probe timeout/error) conservatively default to Resume Onboarding."
    - "Onboarding completeness is decided before injury-profile gating so users do not see Resume Onboarding when they should be completing injury details."
  artifacts:
    - path: flutter_app/lib/features/auth/domain/onboarding_eligibility_evaluator.dart
      provides: "Single decision engine for onboarding eligibility and legacy evidence interpretation"
      contains: "OnboardingEligibility"
    - path: flutter_app/lib/features/auth/data/auth_repository.dart
      provides: "Auth bootstrap integration of evaluator, bounded legacy probes, and non-blocking auto-heal writes"
      contains: "authStateChanges"
    - path: flutter_app/test/features/auth/domain/onboarding_eligibility_evaluator_test.dart
      provides: "Decision-matrix coverage for ONB-04/ONB-05 and locked phase rules"
      min_lines: 80
    - path: flutter_app/test/features/auth/data/auth_repository_onboarding_bootstrap_test.dart
      provides: "Repository-level bootstrap fallback/timeout and ordering regression coverage"
      min_lines: 90
  key_links:
    - from: flutter_app/lib/features/auth/data/auth_repository.dart
      to: flutter_app/lib/features/auth/domain/onboarding_eligibility_evaluator.dart
      via: "Auth bootstrap delegates onboarding routing decisions to one evaluator contract"
      pattern: "OnboardingEligibility|evaluate"
    - from: flutter_app/lib/features/auth/data/auth_repository.dart
      to: flutter_app/lib/domain/models/user_model.dart
      via: "Completeness and injury gating derive from normalized user profile fields"
      pattern: "hasRequiredOnboardingAnswers|requiresInjuryProfileCompletion"
    - from: flutter_app/lib/features/auth/data/auth_repository.dart
      to: flutter_app/test/features/auth/data/auth_repository_onboarding_bootstrap_test.dart
      via: "Timeout/error and legacy-history decision outcomes are asserted by repository tests"
      pattern: "resumeOnboarding|timeout|legacy"
---

<objective>
Implement deterministic onboarding eligibility at auth bootstrap to remove false resume prompts for completed users while preserving conservative resume behavior for genuinely incomplete profiles.

Purpose: ONB-04 and ONB-05 require one consistent decision policy that can interpret legacy profile/history evidence and survive bootstrap failures.
Output: A centralized onboarding eligibility evaluator wired into auth bootstrap with bounded legacy evidence probing, non-blocking auto-heal, and matrix-level tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-onboarding-resume-eligibility-hardening/09-CONTEXT.md
@.planning/phases/09-onboarding-resume-eligibility-hardening/09-RESEARCH.md
@flutter_app/lib/features/auth/data/auth_repository.dart
@flutter_app/lib/domain/models/user_model.dart
@flutter_app/lib/features/auth/domain/auth_state.dart
@flutter_app/test/domain/models/user_model_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a single onboarding eligibility evaluator with explicit decision reasons</name>
  <files>
    flutter_app/lib/features/auth/domain/onboarding_eligibility_evaluator.dart
    flutter_app/lib/domain/models/user_model.dart
    flutter_app/test/features/auth/domain/onboarding_eligibility_evaluator_test.dart
    flutter_app/test/domain/models/user_model_test.dart
  </files>
  <action>
    Introduce a dedicated evaluator that receives normalized profile state + legacy evidence flags and returns a typed onboarding decision with reason metadata.

    Encode Phase 09 locked rules exactly:
    - `onboardingComplete == true` always resolves complete.
    - Required onboarding completeness key is `name`.
    - If `name` is present while `onboardingComplete == false`, treat as complete and mark auto-heal required.
    - If `name` missing and workout history exists, require resume onboarding.
    - If `name` missing and only max-history evidence exists, allow complete path.
    - If required fields missing and no qualifying evidence, require resume onboarding.

    If any path explicitly validates gender completion, accept only normalized `male` and `female` values for that explicit-completion check (exclude `preferNotToSay` in that path).
  </action>
  <verify>
    cd flutter_app && flutter test test/features/auth/domain/onboarding_eligibility_evaluator_test.dart test/domain/models/user_model_test.dart -r expanded
  </verify>
  <done>
    A single decision contract exists and tests prove it matches the locked phase matrix for complete users, incomplete users, and legacy evidence variants.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire evaluator into auth bootstrap with bounded legacy probes and conservative failure fallback</name>
  <files>
    flutter_app/lib/features/auth/data/auth_repository.dart
    flutter_app/lib/features/auth/domain/onboarding_eligibility_evaluator.dart
    flutter_app/test/features/auth/data/auth_repository_onboarding_bootstrap_test.dart
  </files>
  <action>
    Refactor `authStateChanges()` to delegate onboarding decisions to the evaluator and remove contradictory ad-hoc branching.

    Add bounded legacy evidence probes for workout-history vs max-history signals under authenticated profile bootstrap:
    - Inspect workout evidence separately from max evidence (`maxLifts`/`oneRepMaxes`).
    - Apply a deterministic timeout for probe completion.
    - On probe timeout/error, default to `resumeOnboarding`.

    Add non-blocking auto-heal for contradictory complete states (`name` present + flag false) so routing is not blocked by write latency.
    Ensure ordering is completeness decision first, injury-profile gating second.
  </action>
  <verify>
    cd flutter_app && flutter test test/features/auth/data/auth_repository_onboarding_bootstrap_test.dart -r expanded
  </verify>
  <done>
    Auth bootstrap produces deterministic onboarding statuses, honors legacy evidence rules, performs non-blocking auto-heal, and falls back to Resume Onboarding on bootstrap uncertainty.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add repository-level regression matrix for ONB-04 and ONB-05 bootstrap outcomes</name>
  <files>
    flutter_app/test/features/auth/data/auth_repository_onboarding_bootstrap_test.dart
    flutter_app/test/features/auth/domain/onboarding_eligibility_evaluator_test.dart
  </files>
  <action>
    Add matrix coverage that explicitly asserts:
    - complete profile users bypass resume onboarding;
    - missing-name + workout-history users remain resume-required;
    - missing-name + max-only-history users bypass resume;
    - missing-name + no-history users remain resume-required;
    - bootstrap timeout/error resolves to resume onboarding;
    - injury-profile completion gating is only evaluated after onboarding completeness is satisfied.

    Keep tests deterministic by injecting fake profile/evidence states rather than relying on live backend timing.
  </action>
  <verify>
    cd flutter_app && flutter test test/features/auth/domain/onboarding_eligibility_evaluator_test.dart test/features/auth/data/auth_repository_onboarding_bootstrap_test.dart -r expanded
  </verify>
  <done>
    ONB-04/ONB-05 decision branches are locked by targeted automated tests and fail fast if false-resume behavior is reintroduced.
  </done>
</task>

</tasks>

<verification>
Run onboarding bootstrap decision suites:
- `cd flutter_app && flutter test test/features/auth/domain/onboarding_eligibility_evaluator_test.dart test/features/auth/data/auth_repository_onboarding_bootstrap_test.dart test/domain/models/user_model_test.dart -r expanded`
</verification>

<success_criteria>
- Auth bootstrap no longer routes complete users to `resumeOnboarding` because of stale `onboardingComplete` flag state.
- Legacy history heuristics distinguish workout-history and max-only-history records exactly as Phase 09 context requires.
- Bootstrap timeout/error paths conservatively default to `resumeOnboarding`.
- Deterministic tests cover ONB-04/ONB-05 outcome matrix.
</success_criteria>

<output>
After completion, create `.planning/phases/09-onboarding-resume-eligibility-hardening/09-01-SUMMARY.md`.
</output>
