# Phase 2: Domain Layer Port - Context

**Gathered:** 2026-03-14
**Status:** Ready for planning

<domain>
## Phase Boundary

Port all 23 iOS Domain/ Swift files to TypeScript with 100% Jest test coverage and verified numeric/date parity against the Swift originals. This covers: cycle calculations/adaptation, injury adaptation engine, benchmark catalog/scoring, pain trend analysis, rehab session generation, weight calculations, 1RM estimation, AI workout models, history merging, phase transition advisor, readiness survey, and all supporting domain types. No UI, no repositories, no Firestore — pure TypeScript with zero framework dependencies.

</domain>

<decisions>
## Implementation Decisions

### Directory & module structure
- Domain layer lives at `SundeeFundeeRN/src/domain/`
- Subdirectories regrouped by concern (not mirroring Swift layout) — e.g., `src/domain/cycle/` for CycleCalculations + CycleAdaptationPolicy + CycleProgramGenerator
- Files named in kebab-case: `cycle-calculations.ts`, `injury-adaptation-engine.ts`
- Each subdirectory gets a barrel `index.ts` that re-exports public API
- Consumers import from subdirectory barrels: `import { inferCyclePhase } from 'src/domain/cycle'`

### Date/time handling
- Local time throughout — matches how Swift Foundation.Date + Calendar work in the iOS app
- No timezone normalization to UTC in the domain layer; dates stay in user's local timezone
- This means date comparisons behave identically to the Swift originals

### Parity verification
- Shared JSON fixture files with inputs and expected outputs
- Fixtures generated by running a Swift script that imports Domain types, feeds known inputs, and dumps input/output pairs to JSON
- Floating-point parity enforced to 4 decimal places (matches success criteria for 1RM formulas)
- 50+ test cases for 1RM estimation formulas; comprehensive fixture coverage for cycle phase inference, injury adaptation, benchmark scoring

### Benchmark scoring
- `roundsAndReps` encoding ported as-is: `rounds * 10000 + reps` in a single number
- Higher is better; decode: `rounds = Math.floor(value / 10000)`, `reps = value % 10000`
- Maintains data compatibility and parity with Swift implementation

### Claude's Discretion
- Date library choice (date-fns, dayjs, native Date, or other) — pick what works best for the cycle calculation patterns
- Whether domain functions accept/return plain Date objects or another format
- Enum representation in TypeScript (string unions, const objects, or TS enums)
- Struct/type modeling (interfaces + pure functions vs classes with getters)
- Nullability convention (null vs undefined for absent values) — consider Firestore semantics
- Shared fixture file location (co-located vs separate directory)
- Exact subdirectory groupings for the regrouped-by-concern layout

</decisions>

<specifics>
## Specific Ideas

- The Swift Domain layer is 23 files with zero framework dependencies — this should port cleanly to TypeScript with the same "pure logic, no framework" constraint
- The iOS app enforces 100% line coverage on Domain/ — the TypeScript port should match this standard
- CycleCalculations is the most date-heavy module — it infers menstrual cycle phase from period log sequences and predicts future phases. This is the primary driver for date library choice.
- The existing iOS test suite is the source of truth for generating parity fixtures

</specifics>

<code_context>
## Existing Code Insights

### Reusable Assets
- Jest testing infrastructure already configured in SundeeFundeeRN (from Phase 1)
- `__mocks__/` directory established for Firebase, Expo modules
- No existing domain TypeScript code — this is greenfield

### Established Patterns
- Platform-specific file extensions (.native.ts / .web.ts) for platform branching — not relevant for pure domain code but good to know
- Phase 1 decisions: SessionProvider pattern, auth hooks pattern — domain layer is independent of these

### Integration Points
- Domain types will be consumed by Phase 3 repositories (Firestore/AsyncStorage) and Phase 4+ UI features
- Barrel exports from src/domain/ subdirectories will be the public API surface
- Benchmark scoring encoding must match what Firestore will store in Phase 3+

### Swift Source Files (23 files to port)
- `AIWorkout/`: GeneratedWorkout, OfflineWorkoutGenerator, WorkoutGenerationContext
- `Calculations/`: BarbellDefaults, PlateCalculation, WeightCalculations, WeightUnitConversion
- `History/`: HistoryItem
- Cycle: CycleAdaptationPolicy, CycleCalculations, CycleProgramGenerator
- Injury: InjuryAdaptationEngine, LoadAdjustmentPolicy, PhaseTransitionAdvisor, PainTrendAnalyzer, RehabSessionGenerator, RecoveryPhase, BodyLocation
- Benchmarks: BenchmarkCatalog
- Other: CelebrationEvent, ProgramAvailability, ReadinessSurvey, WODTemplateType

</code_context>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope

</deferred>

---

*Phase: 02-domain-layer-port*
*Context gathered: 2026-03-14*
