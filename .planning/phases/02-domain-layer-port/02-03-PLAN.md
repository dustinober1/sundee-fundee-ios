---
phase: 02-domain-layer-port
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - SundeeFundeeRN/src/domain/injury/recovery-phase.ts
  - SundeeFundeeRN/src/domain/injury/body-location.ts
  - SundeeFundeeRN/src/domain/injury/load-adjustment-policy.ts
  - SundeeFundeeRN/src/domain/injury/injury-adaptation-engine.ts
  - SundeeFundeeRN/src/domain/injury/pain-trend-analyzer.ts
  - SundeeFundeeRN/src/domain/injury/phase-transition-advisor.ts
  - SundeeFundeeRN/src/domain/injury/rehab-session-generator.ts
  - SundeeFundeeRN/src/domain/injury/index.ts
  - SundeeFundeeRN/src/domain/__fixtures__/injury-adaptation.json
  - SundeeFundeeRN/src/domain/__tests__/injury.test.ts
autonomous: true
requirements:
  - INJR-02
  - INJR-04
  - INJR-05
  - INJR-06

must_haves:
  truths:
    - "Injury adaptation engine substitutes or removes contraindicated exercises for every body location and recovery phase combination"
    - "mostRestrictive function returns the earliest (most restrictive) recovery phase from a list"
    - "Pain trend analyzer correctly classifies improving vs worsening trends from pain logs"
    - "Phase transition advisor suggests progression when pain logs meet threshold criteria"
    - "Rehab session generator creates targeted exercises for the given injury profile and recovery phase"
    - "Load adjustment policy applies correct multipliers based on recovery phase"
  artifacts:
    - path: "SundeeFundeeRN/src/domain/injury/injury-adaptation-engine.ts"
      provides: "Exercise substitution/removal based on injury profiles"
      exports: ["adaptProgram", "mostRestrictive"]
      min_lines: 100
    - path: "SundeeFundeeRN/src/domain/injury/pain-trend-analyzer.ts"
      provides: "Trailing average and improving/worsening trend detection"
      exports: ["analyzeTrend"]
    - path: "SundeeFundeeRN/src/domain/injury/phase-transition-advisor.ts"
      provides: "Recovery phase progression suggestions"
      exports: ["evaluateTransition", "meetsThreshold"]
    - path: "SundeeFundeeRN/src/domain/injury/rehab-session-generator.ts"
      provides: "Targeted rehab session exercises"
      exports: ["generateSession"]
    - path: "SundeeFundeeRN/src/domain/injury/index.ts"
      provides: "Barrel re-export of all injury functions"
  key_links:
    - from: "SundeeFundeeRN/src/domain/injury/injury-adaptation-engine.ts"
      to: "SundeeFundeeRN/src/domain/injury/load-adjustment-policy.ts"
      via: "load multiplier lookup by recovery phase"
      pattern: "import.*loadMultiplier.*from.*load-adjustment"
    - from: "SundeeFundeeRN/src/domain/injury/injury-adaptation-engine.ts"
      to: "SundeeFundeeRN/src/domain/types/index.ts"
      via: "RECOVERY_PHASE_ORDER for mostRestrictive comparison"
      pattern: "RECOVERY_PHASE_ORDER"
    - from: "SundeeFundeeRN/src/domain/injury/phase-transition-advisor.ts"
      to: "SundeeFundeeRN/src/domain/types/index.ts"
      via: "PainLog, RecoveryPhase types"
      pattern: "import.*RecoveryPhase.*from.*types"
---

<objective>
Port the 7 injury domain files (RecoveryPhase, BodyLocation, LoadAdjustmentPolicy, InjuryAdaptationEngine, PainTrendAnalyzer, PhaseTransitionAdvisor, RehabSessionGenerator) to TypeScript with fixture-driven parity tests and 100% coverage.

Purpose: Delivers the injury modification engine that automatically adapts workouts for users with active injuries — a key safety and personalization feature.
Output: `src/domain/injury/` fully ported and tested with parity fixtures.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-domain-layer-port/02-CONTEXT.md
@.planning/phases/02-domain-layer-port/02-RESEARCH.md
@.planning/phases/02-domain-layer-port/02-01-SUMMARY.md

@SundeeFundee/Domain/InjuryAdaptationEngine.swift
@SundeeFundee/Domain/LoadAdjustmentPolicy.swift
@SundeeFundee/Domain/PhaseTransitionAdvisor.swift
@SundeeFundee/Domain/PainTrendAnalyzer.swift
@SundeeFundee/Domain/RehabSessionGenerator.swift
@SundeeFundee/Domain/RecoveryPhase.swift
@SundeeFundee/Domain/BodyLocation.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port injury types, load adjustment, pain trend, and phase transition</name>
  <files>
    SundeeFundeeRN/src/domain/injury/recovery-phase.ts
    SundeeFundeeRN/src/domain/injury/body-location.ts
    SundeeFundeeRN/src/domain/injury/load-adjustment-policy.ts
    SundeeFundeeRN/src/domain/injury/pain-trend-analyzer.ts
    SundeeFundeeRN/src/domain/injury/phase-transition-advisor.ts
    SundeeFundeeRN/src/domain/__tests__/injury.test.ts
  </files>
  <behavior>
    - RecoveryPhase helpers: phaseIndex('acute') = 0, isMoreRestrictive('acute', 'rehab') = true
    - BodyLocation: all body locations from Swift source are present, parse/encode round-trip correctly
    - LoadAdjustmentPolicy: loadMultiplier('acute') returns lowest multiplier, loadMultiplier('resolved') returns 1.0
    - analyzeTrend with empty logs returns { trailingAverage: 0, isImproving: false }
    - analyzeTrend with decreasing pain values returns isImproving: true
    - analyzeTrend with increasing pain values returns isImproving: false
    - analyzeTrend correctly splits recent window into newer/older halves per Swift prefix/suffix pattern
    - meetsThreshold returns true when N recent logs are all at or below max pain level
    - meetsThreshold returns false when fewer than N logs exist
    - evaluateTransition suggests 'rehab' when acute logs show 3 entries at pain <= 5
    - evaluateTransition suggests 'lightLoad' when rehab logs show 3 entries at pain <= 3
    - evaluateTransition suggests 'returnToPlay' when lightLoad logs show 3 entries at pain <= 2
    - evaluateTransition suggests 'resolved' when returnToPlay logs show 5 entries at pain <= 1
  </behavior>
  <action>
    Port the 5 simpler injury files. Read each Swift source file and translate line by line.

    1. `recovery-phase.ts`: If RecoveryPhase types are already in `src/domain/types/`, this file provides helper functions — phaseIndex, isMoreRestrictive, nextPhase. Import RECOVERY_PHASE_ORDER from types.

    2. `body-location.ts`: Port all body location values and any parse/encode helpers from BodyLocation.swift. If types are in `src/domain/types/`, this file adds helpers.

    3. `load-adjustment-policy.ts`: Port LoadAdjustmentPolicy.swift — lookup table mapping RecoveryPhase to load multiplier. Export `getLoadMultiplier(phase: RecoveryPhase): number`.

    4. `pain-trend-analyzer.ts`: Port PainTrendAnalyzer.swift. CRITICAL: Match the Swift prefix/suffix splitting pattern exactly (see research pitfall 3). `recent = logs.slice(0, windowSize)`, `newerHalf = recent.slice(0, halfSize)`, `olderHalf = recent.slice(recent.length - halfSize)`. Export `analyzeTrend`.

    5. `phase-transition-advisor.ts`: Port PhaseTransitionAdvisor.swift. Export `meetsThreshold` and `evaluateTransition`. Thresholds: acute->rehab: 3 logs <=5, rehab->lightLoad: 3 logs <=3, lightLoad->returnToPlay: 3 logs <=2, returnToPlay->resolved: 5 logs <=1.

    Write tests FIRST in `__tests__/injury.test.ts`. Test each module in its own describe block.
  </action>
  <verify>
    <automated>cd SundeeFundeeRN && npx jest src/domain/__tests__/injury.test.ts --testNamePattern="(RecoveryPhase|BodyLocation|LoadAdjustment|PainTrend|PhaseTransition)" --coverage --collectCoverageFrom='src/domain/injury/(recovery-phase|body-location|load-adjustment-policy|pain-trend-analyzer|phase-transition-advisor).ts'</automated>
  </verify>
  <done>5 injury support files ported, pain trend split logic matches Swift exactly, phase transition thresholds verified, 100% line coverage on all 5 files</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port InjuryAdaptationEngine and RehabSessionGenerator</name>
  <files>
    SundeeFundeeRN/src/domain/injury/injury-adaptation-engine.ts
    SundeeFundeeRN/src/domain/injury/rehab-session-generator.ts
    SundeeFundeeRN/src/domain/injury/index.ts
    SundeeFundeeRN/src/domain/__fixtures__/injury-adaptation.json
    SundeeFundeeRN/src/domain/__tests__/injury.test.ts
  </files>
  <behavior>
    - mostRestrictive(['rehab', 'acute']) returns 'acute'
    - mostRestrictive(['resolved']) returns 'resolved'
    - mostRestrictive([]) returns undefined
    - adaptProgram with shoulder injury in acute phase removes overhead press exercises
    - adaptProgram with knee injury substitutes squats with upper body alternatives
    - adaptProgram with resolved injury makes no modifications
    - adaptProgram with multiple injuries applies the most restrictive phase's rules
    - generateSession for knee rehab phase returns knee-specific rehab exercises
    - generateSession for shoulder acute phase returns gentle mobility exercises
    - All contraindication rules from Swift lookup tables are ported
  </behavior>
  <action>
    Port the 2 most complex injury files. These have the largest lookup tables.

    1. `injury-adaptation-engine.ts` (356 lines Swift): This is the largest file. Port the full contraindication rules table, regression table, and the `adaptProgram` function. Key details:
       - `mostRestrictive` uses `RECOVERY_PHASE_ORDER.indexOf()` to compare phases (research pitfall 4)
       - `adaptProgram` iterates exercises, checks each against active injuries, substitutes or removes contraindicated ones
       - The contraindication rules map (bodyLocation, recoveryPhase) -> list of contraindicated exercise patterns
       - The regression table maps contraindicated exercises to safe alternatives
       - Port every entry in the lookup tables — do not summarize or skip entries

    2. `rehab-session-generator.ts` (75 lines Swift): Port the recovery map that maps (bodyLocation, recoveryPhase) -> rehab exercises. Export `generateSession(injury: InjuryProfile): RehabSession`.

    3. `index.ts`: Barrel re-export all public functions from all 7 injury files.

    4. Create `__fixtures__/injury-adaptation.json` with test cases covering:
       - Each body location with acute phase (most restrictive)
       - Mixed injuries (multiple body locations)
       - Exercise substitution scenarios
       - Edge case: no injuries (passthrough)

    Add tests to existing `__tests__/injury.test.ts`. Test the full adaptation pipeline with realistic exercise lists and injury profiles.
  </action>
  <verify>
    <automated>cd SundeeFundeeRN && npx jest src/domain/__tests__/injury.test.ts --coverage --collectCoverageFrom='src/domain/injury/**/*.ts'</automated>
  </verify>
  <done>InjuryAdaptationEngine with full lookup tables ported, RehabSessionGenerator ported, barrel exports working, fixture-driven tests cover all body location x recovery phase combinations, 100% line coverage on src/domain/injury/</done>
</task>

</tasks>

<verification>
- `cd SundeeFundeeRN && npx tsc --noEmit` — no type errors
- `cd SundeeFundeeRN && npx jest src/domain/__tests__/injury.test.ts --coverage` — 100% on injury/
- mostRestrictive correctly returns earliest phase in RECOVERY_PHASE_ORDER
- Pain trend split matches Swift prefix/suffix behavior
- Phase transition thresholds match documented values
- `import { adaptProgram, analyzeTrend, evaluateTransition, generateSession } from 'src/domain/injury'` resolves
</verification>

<success_criteria>
- 7 injury files ported to TypeScript with pure functions
- Contraindication lookup tables fully ported from Swift (no missing entries)
- mostRestrictive uses RECOVERY_PHASE_ORDER.indexOf for comparison
- Pain trend analyzer splits halves per Swift prefix/suffix pattern
- Phase transition thresholds: acute->rehab 3@<=5, rehab->lightLoad 3@<=3, lightLoad->returnToPlay 3@<=2, returnToPlay->resolved 5@<=1
- Rehab session generator produces exercises for all body location + phase combinations
- Barrel index.ts re-exports all public API
- 100% line coverage on src/domain/injury/
</success_criteria>

<output>
After completion, create `.planning/phases/02-domain-layer-port/02-03-SUMMARY.md`
</output>
