---
phase: 02-domain-layer-port
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SundeeFundeeRN/src/domain/types/index.ts
  - SundeeFundeeRN/src/domain/calculations/weight-calculations.ts
  - SundeeFundeeRN/src/domain/calculations/epley-formula.ts
  - SundeeFundeeRN/src/domain/calculations/plate-calculation.ts
  - SundeeFundeeRN/src/domain/calculations/weight-unit-conversion.ts
  - SundeeFundeeRN/src/domain/calculations/barbell-defaults.ts
  - SundeeFundeeRN/src/domain/calculations/index.ts
  - SundeeFundeeRN/src/domain/__fixtures__/epley-formula.json
  - SundeeFundeeRN/src/domain/__fixtures__/weight-calculations.json
  - SundeeFundeeRN/src/domain/__tests__/calculations.test.ts
  - SundeeFundeeRN/package.json
autonomous: true
requirements:
  - WORK-06
  - MAX-03

must_haves:
  truths:
    - "1RM estimation (Epley) produces identical output to Swift for 50+ test cases to 4 decimal places"
    - "Personal record detection correctly identifies when a new estimate exceeds previous max"
    - "Weight snapping rounds barbell weight to nearest valid plate combination"
    - "Weight unit conversion between lbs and kg is numerically accurate"
    - "All shared domain types are defined and exported for downstream module consumption"
  artifacts:
    - path: "SundeeFundeeRN/src/domain/types/index.ts"
      provides: "Shared domain interfaces — ExerciseValue, ProgramExercise, InjuryProfile, PainLog, PeriodLog, CycleSettings, etc."
      min_lines: 80
    - path: "SundeeFundeeRN/src/domain/calculations/epley-formula.ts"
      provides: "1RM estimation and PR detection"
      exports: ["estimated1RM", "isPR"]
    - path: "SundeeFundeeRN/src/domain/calculations/weight-calculations.ts"
      provides: "Weight snapping, rounding, target weight logic"
      exports: ["roundToNearestFive", "snapBarbellWeightLb", "snapBarbellWeightKg", "isPersonalRecord"]
    - path: "SundeeFundeeRN/src/domain/calculations/index.ts"
      provides: "Barrel re-export of all calculation functions"
    - path: "SundeeFundeeRN/src/domain/__fixtures__/epley-formula.json"
      provides: "50+ parity test cases for 1RM estimation"
      min_lines: 50
  key_links:
    - from: "SundeeFundeeRN/src/domain/__tests__/calculations.test.ts"
      to: "SundeeFundeeRN/src/domain/__fixtures__/epley-formula.json"
      via: "fixture-driven test.each"
      pattern: "test\\.each.*cases"
    - from: "SundeeFundeeRN/src/domain/calculations/weight-calculations.ts"
      to: "SundeeFundeeRN/src/domain/types/index.ts"
      via: "import of WeightUnit type"
      pattern: "import.*WeightUnit.*from.*types"
---

<objective>
Install date-fns, define all shared domain types, port the 5 calculation files (WeightCalculations, EpleyFormula, PlateCalculation, WeightUnitConversion, BarbellDefaults), create parity fixtures, and achieve 100% test coverage on the calculations subdomain.

Purpose: Establishes the type foundation every other domain plan depends on, and delivers the first complete subdomain (calculations) with verified Swift parity.
Output: `src/domain/types/` with all shared interfaces, `src/domain/calculations/` fully ported and tested, `src/domain/__fixtures__/` with parity test data.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-domain-layer-port/02-CONTEXT.md
@.planning/phases/02-domain-layer-port/02-RESEARCH.md

@SundeeFundee/Domain/Calculations/WeightCalculations.swift
@SundeeFundee/Domain/Calculations/PlateCalculation.swift
@SundeeFundee/Domain/Calculations/WeightUnitConversion.swift
@SundeeFundee/Domain/Calculations/BarbellDefaults.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install date-fns + create shared domain types + fixtures directory</name>
  <files>
    SundeeFundeeRN/package.json
    SundeeFundeeRN/src/domain/types/index.ts
    SundeeFundeeRN/src/domain/__fixtures__/epley-formula.json
    SundeeFundeeRN/src/domain/__fixtures__/weight-calculations.json
  </files>
  <behavior>
    - ExerciseValue discriminated union: { kind: 'fixed', value } | { kind: 'range', lo, hi } | { kind: 'amrap' } | { kind: 'text', value }
    - CyclePhase, RecoveryPhase, BodyLocation as string union types
    - WeightUnit type ('lb' | 'kg')
    - PeriodLog, CycleSettings, InjuryProfile, PainLog, ProgramExercise interfaces with fields matching Swift domain function usage
    - Gender, WorkoutFocus, EnergyLevel, EquipmentAccess as string unions
    - Fixture JSON contains 50+ cases for Epley formula with weight, reps, expected1RM fields
    - Fixture JSON contains weight snapping test cases
  </behavior>
  <action>
    1. `cd SundeeFundeeRN && npm install date-fns`
    2. Create `src/domain/types/index.ts` with ALL shared domain interfaces and type unions. Read every Swift Domain file to identify the minimum interface fields actually used by domain functions. Use string unions (not TS enums) per research. Use `undefined` for absent values (not null). Key types to define:
       - ExerciseValue (discriminated union with 'kind' discriminant)
       - CyclePhase = 'menstrual' | 'follicular' | 'ovulation' | 'luteal'
       - RecoveryPhase = 'acute' | 'rehab' | 'lightLoad' | 'returnToPlay' | 'resolved' (with RECOVERY_PHASE_ORDER array)
       - BodyLocation (all body locations from BodyLocation.swift)
       - WeightUnit = 'lb' | 'kg'
       - PeriodLog { startDate: Date; endDate?: Date }
       - CycleSettings { averageCycleLengthDays, averagePeriodLengthDays, lutealPhaseLengthDays }
       - InjuryProfile { bodyLocation: BodyLocation; recoveryPhase: RecoveryPhase; ... }
       - PainLog { date: Date; painLevel: number; injuryId: string }
       - ProgramExercise, ProgramSession, Program interfaces (fields used by CycleProgramGenerator and InjuryAdaptationEngine)
       - Gender, WorkoutFocus, EnergyLevel, EquipmentAccess, ReadinessTier
       - BenchmarkScoringType = 'forTime' | 'amrap' | 'maxLoad'
       - BenchmarkDefinition interface
       - WorkoutTemplate types for OfflineWorkoutGenerator
       - scaleExerciseValue function (ported from Swift, used by cycle and injury engines)
       - clamp utility function
       - swiftRound helper for parity-critical rounding
    3. Create `src/domain/__fixtures__/epley-formula.json` — generate 50+ cases by computing `weight * (1 + reps / 30)` for weights [45, 65, 95, 100, 135, 185, 225, 275, 315, 405] x reps [1, 2, 3, 5, 8, 10, 12, 15, 20]. Include edge cases: reps=1 returns weight, very high reps.
    4. Create `src/domain/__fixtures__/weight-calculations.json` — test cases for roundToNearestFive, snapBarbellWeightLb, snapBarbellWeightKg, plate calculation scenarios.
  </action>
  <verify>
    <automated>cd SundeeFundeeRN && npx tsc --noEmit src/domain/types/index.ts && node -e "const f = require('./src/domain/__fixtures__/epley-formula.json'); console.log(f.cases.length + ' epley cases'); if (f.cases.length < 50) process.exit(1)"</automated>
  </verify>
  <done>date-fns installed, all shared domain types compile with no errors, 50+ Epley fixture cases exist, weight calculation fixtures exist</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port calculations subdomain with 100% test coverage</name>
  <files>
    SundeeFundeeRN/src/domain/calculations/epley-formula.ts
    SundeeFundeeRN/src/domain/calculations/weight-calculations.ts
    SundeeFundeeRN/src/domain/calculations/plate-calculation.ts
    SundeeFundeeRN/src/domain/calculations/weight-unit-conversion.ts
    SundeeFundeeRN/src/domain/calculations/barbell-defaults.ts
    SundeeFundeeRN/src/domain/calculations/index.ts
    SundeeFundeeRN/src/domain/__tests__/calculations.test.ts
  </files>
  <behavior>
    - estimated1RM(100, 5) returns 116.6667 (to 4 decimal places)
    - estimated1RM(weight, 1) returns weight unchanged
    - isPR(120, 100) returns true; isPR(80, 100) returns false; isPR(100, undefined) returns true
    - roundToNearestFive(123) returns 125; roundToNearestFive(122) returns 120
    - snapBarbellWeightLb(150, 45) snaps plate load to nearest 5 + bar weight
    - lbToKg(100) returns ~45.3592; kgToLb(100) returns ~220.462
    - plateCalculation for 225lb returns [45, 45] plates per side
    - barbellDefaults provides standard bar weights for lb and kg
    - All 50+ Epley fixture cases pass to 4 decimal places
  </behavior>
  <action>
    Port each Swift calculation file to TypeScript, reading the Swift source line by line. Follow these patterns:

    1. `epley-formula.ts`: Export `estimated1RM(weight: number, reps: number): number` and `isPR(newEstimate: number, currentMax: number | undefined): boolean`. Guard `reps <= 1` returns weight. Formula: `weight * (1 + reps / 30)`.

    2. `weight-calculations.ts`: Port all static functions from WeightCalculations.swift — `roundToNearestFive`, `snapBarbellWeightLb`, `snapBarbellWeightKg`, `isPersonalRecord`, and any helper functions. Use `swiftRound` from types where needed for parity.

    3. `plate-calculation.ts`: Port PlateCalculation.swift — calculates plates needed per side for a target weight. Must handle both lb and kg plate sets.

    4. `weight-unit-conversion.ts`: Port WeightUnitConversion.swift — `lbToKg`, `kgToLb`, conversion constants. Do NOT port locale-dependent formatting (leave to UI layer per research pitfall 5).

    5. `barbell-defaults.ts`: Port BarbellDefaults.swift — standard bar weights, plate inventories for lb and kg.

    6. `index.ts`: Barrel re-export all public functions and types from the 5 files above.

    7. `__tests__/calculations.test.ts`: Write tests FIRST (TDD). Use fixture-driven `test.each` for Epley formula (load from `__fixtures__/epley-formula.json`). Use `toBeCloseTo(expected, 4)` for floating-point comparisons. Test all edge cases: zero weight, zero reps, reps=1, very large values. Test weight snapping, plate calculation, unit conversion. Construct test dates with `new Date(year, monthIndex, day)` not ISO strings.

    IMPORTANT: All functions must be pure (no mutation of inputs). Use `undefined` not `null` for absent values.
  </action>
  <verify>
    <automated>cd SundeeFundeeRN && npx jest src/domain/__tests__/calculations.test.ts --coverage --collectCoverageFrom='src/domain/calculations/**/*.ts'</automated>
  </verify>
  <done>All 5 calculation files ported, barrel exports working, 50+ Epley parity test cases pass to 4 decimal places, 100% line coverage on src/domain/calculations/</done>
</task>

</tasks>

<verification>
- `cd SundeeFundeeRN && npx tsc --noEmit` — no type errors in domain code
- `cd SundeeFundeeRN && npx jest src/domain --coverage` — 100% coverage on calculations
- Epley formula parity: all 50+ fixture cases pass with `toBeCloseTo(expected, 4)`
- `import { estimated1RM, isPR, roundToNearestFive } from 'src/domain/calculations'` resolves correctly
</verification>

<success_criteria>
- date-fns installed and importable
- All shared domain types defined in src/domain/types/index.ts and compile cleanly
- 5 calculation files ported with pure functions matching Swift signatures
- Barrel index.ts re-exports all public API from calculations/
- 50+ fixture-driven parity tests pass for Epley formula
- 100% line coverage on src/domain/calculations/
- No TypeScript compiler errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-domain-layer-port/02-01-SUMMARY.md`
</output>
