---
phase: 02-domain-layer-port
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - SundeeFundeeRN/src/domain/cycle/cycle-calculations.ts
  - SundeeFundeeRN/src/domain/cycle/cycle-adaptation-policy.ts
  - SundeeFundeeRN/src/domain/cycle/cycle-program-generator.ts
  - SundeeFundeeRN/src/domain/cycle/index.ts
  - SundeeFundeeRN/src/domain/__fixtures__/cycle-calculations.json
  - SundeeFundeeRN/src/domain/__fixtures__/cycle-adaptation.json
  - SundeeFundeeRN/src/domain/__tests__/cycle.test.ts
autonomous: true
requirements:
  - CYAD-01
  - CYAD-02
  - CYAD-03

must_haves:
  truths:
    - "Cycle phase inference produces identical output to Swift for any period log sequence"
    - "Workout load multipliers adjust based on current cycle phase (menstrual reduces, ovulation increases)"
    - "Set and rep targets scale with phase-specific blended multipliers clamped to [0.75, 1.25]"
    - "Readiness score feeds into adaptation blending via readiness tier lookup"
    - "Cycle program generator adapts a full program session for cycle phase and readiness"
  artifacts:
    - path: "SundeeFundeeRN/src/domain/cycle/cycle-calculations.ts"
      provides: "Phase inference, phase boundaries, cycle status from period logs"
      exports: ["calculateCycleStatus", "getPhaseBoundaries", "inferCurrentPhase"]
    - path: "SundeeFundeeRN/src/domain/cycle/cycle-adaptation-policy.ts"
      provides: "Phase-aware load/set/rep multiplier blending with readiness integration"
      exports: ["applyPhaseAdjustment", "blendMultiplier", "resolveReadinessTier"]
    - path: "SundeeFundeeRN/src/domain/cycle/cycle-program-generator.ts"
      provides: "Full program session adaptation for cycle phase"
      exports: ["adaptProgram"]
    - path: "SundeeFundeeRN/src/domain/__fixtures__/cycle-calculations.json"
      provides: "Parity test data for phase inference from period logs"
  key_links:
    - from: "SundeeFundeeRN/src/domain/cycle/cycle-adaptation-policy.ts"
      to: "SundeeFundeeRN/src/domain/types/index.ts"
      via: "CyclePhase, ReadinessTier, ExerciseValue imports"
      pattern: "import.*CyclePhase.*from.*types"
    - from: "SundeeFundeeRN/src/domain/cycle/cycle-program-generator.ts"
      to: "SundeeFundeeRN/src/domain/cycle/cycle-adaptation-policy.ts"
      via: "calls applyPhaseAdjustment to transform exercises"
      pattern: "applyPhaseAdjustment"
    - from: "SundeeFundeeRN/src/domain/cycle/cycle-calculations.ts"
      to: "date-fns"
      via: "startOfDay, differenceInCalendarDays, addDays for calendar math"
      pattern: "import.*from.*date-fns"
---

<objective>
Port the 3 cycle domain files (CycleCalculations, CycleAdaptationPolicy, CycleProgramGenerator) to TypeScript with fixture-driven parity tests and 100% coverage.

Purpose: Delivers the cycle-aware training adaptation engine — the core differentiating feature. Phase inference from period logs, load/rep scaling by phase, and readiness integration are all verified against Swift outputs.
Output: `src/domain/cycle/` fully ported and tested with parity fixtures.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-domain-layer-port/02-CONTEXT.md
@.planning/phases/02-domain-layer-port/02-RESEARCH.md
@.planning/phases/02-domain-layer-port/02-01-SUMMARY.md

@SundeeFundee/Domain/CycleCalculations.swift
@SundeeFundee/Domain/CycleAdaptationPolicy.swift
@SundeeFundee/Domain/CycleProgramGenerator.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port CycleCalculations with date-fns parity</name>
  <files>
    SundeeFundeeRN/src/domain/cycle/cycle-calculations.ts
    SundeeFundeeRN/src/domain/__fixtures__/cycle-calculations.json
    SundeeFundeeRN/src/domain/__tests__/cycle.test.ts
  </files>
  <behavior>
    - getPhaseBoundaries with default settings (28 cycle, 5 period, 14 luteal) returns correct day ranges for each phase
    - inferCurrentPhase on day 3 of cycle returns 'menstrual', day 8 returns 'follicular', day 14 returns 'ovulation', day 20 returns 'luteal'
    - calculateCycleStatus with empty period logs returns undefined
    - calculateCycleStatus with a period log starting 10 days ago correctly identifies current phase
    - Date arithmetic uses date-fns startOfDay/differenceInCalendarDays/addDays (not millisecond math)
    - Test dates constructed with new Date(year, monthIndex, day) — never ISO strings
  </behavior>
  <action>
    Port CycleCalculations.swift line by line to `cycle-calculations.ts`. This is the most date-sensitive module.

    CRITICAL date-fns mappings:
    - `Calendar.current.startOfDay(for:)` -> `startOfDay(date)` from date-fns
    - `calendar.dateComponents([.day], from:, to:).day` -> `differenceInCalendarDays(to, from)` from date-fns
    - `Calendar.current.date(byAdding: .day, value:, to:)` -> `addDays(date, n)` from date-fns

    Export: `calculateCycleStatus`, `getPhaseBoundaries`, `inferCurrentPhase`, `predictNextPhases`, and any helper functions from the Swift original.

    Use CyclePhase, PeriodLog, CycleSettings from `src/domain/types/`.

    Create `__fixtures__/cycle-calculations.json` with test cases for:
    - Phase boundary computation for various cycle/period/luteal lengths
    - Phase inference at specific days within a cycle
    - Edge cases: very short cycles (21 days), very long cycles (35 days), reference date on phase boundary

    Write tests FIRST in `__tests__/cycle.test.ts`. Use `test.each` with fixture data for phase boundaries. Test each phase independently. Ensure all dates use `new Date(year, monthIndex, day)` constructor.
  </action>
  <verify>
    <automated>cd SundeeFundeeRN && npx jest src/domain/__tests__/cycle.test.ts --testNamePattern="CycleCalculations" --coverage --collectCoverageFrom='src/domain/cycle/cycle-calculations.ts'</automated>
  </verify>
  <done>CycleCalculations ported, phase inference matches Swift for all fixture cases, 100% line coverage on cycle-calculations.ts</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port CycleAdaptationPolicy and CycleProgramGenerator with readiness integration</name>
  <files>
    SundeeFundeeRN/src/domain/cycle/cycle-adaptation-policy.ts
    SundeeFundeeRN/src/domain/cycle/cycle-program-generator.ts
    SundeeFundeeRN/src/domain/cycle/index.ts
    SundeeFundeeRN/src/domain/__fixtures__/cycle-adaptation.json
    SundeeFundeeRN/src/domain/__tests__/cycle.test.ts
  </files>
  <behavior>
    - blendMultiplier(0.8, 1.0, 1.0) returns 0.8 (full application of target)
    - blendMultiplier(0.5, 0.5, 1.0) clamps result to minimum 0.75
    - blendMultiplier(1.5, 1.0, 1.0) clamps result to maximum 1.25
    - applyPhaseAdjustment for menstrual phase reduces load multiplier
    - applyPhaseAdjustment for ovulation phase increases load multiplier
    - resolveReadinessTier maps readiness score ranges to tiers
    - Readiness tier affects the readinessScale parameter in blendMultiplier
    - adaptProgram applies phase adjustment to each exercise's sets/reps/weight in a session
    - ExerciseValue 'fixed' values scale by multiplier with Math.max(1, ...) floor
    - ExerciseValue 'range' values scale both lo and hi
    - ExerciseValue 'amrap' and 'text' pass through unchanged
  </behavior>
  <action>
    Port CycleAdaptationPolicy.swift and CycleProgramGenerator.swift to TypeScript.

    1. `cycle-adaptation-policy.ts`: Port `blendMultiplier`, `applyPhaseAdjustment`, `resolveReadinessTier`, and the phase multiplier lookup tables. Use `clamp` from types. The multiplier tables define load/set/rep targets per CyclePhase. Readiness tiers (high/moderate/low/veryLow) map to readinessScale values. Confidence scale is derived from cycle data quality.

    2. `cycle-program-generator.ts`: Port `adaptProgram` which takes a program session, current cycle phase, readiness score, and applies phase adjustment to each exercise. Uses `scaleExerciseValue` from types and `applyPhaseAdjustment` from the adaptation policy.

    3. `index.ts`: Barrel re-export all public functions from all 3 cycle files.

    4. Create `__fixtures__/cycle-adaptation.json` with test cases for blendMultiplier at various target/readiness/confidence combinations, including clamping edge cases.

    Write tests FIRST. Add to existing `__tests__/cycle.test.ts`. Test the full pipeline: phase -> adaptation policy -> program generator. Verify clamping boundaries [0.75, 1.25].

    IMPORTANT: scaleExerciseValue uses Math.max(1, ...) to floor scaled values at 1 (never 0 reps/sets). This is a parity-critical detail.
  </action>
  <verify>
    <automated>cd SundeeFundeeRN && npx jest src/domain/__tests__/cycle.test.ts --coverage --collectCoverageFrom='src/domain/cycle/**/*.ts'</automated>
  </verify>
  <done>CycleAdaptationPolicy and CycleProgramGenerator ported, blendMultiplier clamping verified, readiness integration tested, barrel exports working, 100% line coverage on src/domain/cycle/</done>
</task>

</tasks>

<verification>
- `cd SundeeFundeeRN && npx tsc --noEmit` — no type errors
- `cd SundeeFundeeRN && npx jest src/domain/__tests__/cycle.test.ts --coverage` — 100% on cycle/
- Phase boundary fixture cases all pass
- Multiplier clamping at [0.75, 1.25] verified for edge cases
- `import { calculateCycleStatus, applyPhaseAdjustment, adaptProgram } from 'src/domain/cycle'` resolves
</verification>

<success_criteria>
- 3 cycle files ported to TypeScript with pure functions
- Phase inference matches Swift for all fixture test cases
- Multiplier blending clamps correctly at [0.75, 1.25]
- Readiness tier integration feeds into adaptation blending
- CycleProgramGenerator adapts exercises through the full pipeline
- Barrel index.ts re-exports all public API
- 100% line coverage on src/domain/cycle/
</success_criteria>

<output>
After completion, create `.planning/phases/02-domain-layer-port/02-02-SUMMARY.md`
</output>
