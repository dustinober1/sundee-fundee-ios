---
phase: 02-visual-progress
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/app/progress/page.tsx
autonomous: true

must_haves:
  truths:
    - "User sees all three charts on the /progress page in a logical layout"
    - "1RM chart, volume chart, and heatmap all render without errors"
    - "Page works for new users with no data (empty states visible, no crashes)"
    - "Page is responsive on mobile viewports"
  artifacts:
    - path: "src/app/progress/page.tsx"
      provides: "Progress page with all three chart cards and responsive layout"
      contains: "WeightProgressChart"
  key_links:
    - from: "src/app/progress/page.tsx"
      to: "src/components/progress/weight-progress-chart.tsx"
      via: "import + render in Card"
      pattern: "WeightProgressChart"
    - from: "src/app/progress/page.tsx"
      to: "src/components/progress/weekly-volume-chart.tsx"
      via: "import + render in Card"
      pattern: "WeeklyVolumeChart"
    - from: "src/app/progress/page.tsx"
      to: "src/components/progress/workout-heatmap.tsx"
      via: "import + render in Card"
      pattern: "WorkoutHeatmap"
---

<objective>
Integrate all three charts into the /progress page — layout the 1RM chart, volume chart, and heatmap in Cards with proper titles, responsive spacing, and verify the complete page works end-to-end.

Purpose: This is the user-facing integration point where all visualization work comes together. Without this, the chart components exist but are invisible to users.
Output: Updated `src/app/progress/page.tsx` with all three charts rendered in a polished layout.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-visual-progress/02-RESEARCH.md
@.planning/phases/02-visual-progress/02-01-SUMMARY.md
@.planning/phases/02-visual-progress/02-02-SUMMARY.md

@src/app/progress/page.tsx
@src/components/progress/cycle-filter.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite progress page with all three chart sections</name>
  <files>src/app/progress/page.tsx</files>
  <action>
Rewrite `src/app/progress/page.tsx` to include all three visualization charts. Keep the existing `CycleFilter` component and its conditional cycle-phase card.

**Layout (top to bottom):**

1. **Page header:** `<h1>` "Progress" (keep existing)

2. **CycleFilter** (keep existing — `onFilterChange` sets `filter` state)

3. **1RM Progress Card** — `Card` with:
   - `CardHeader` → `CardTitle`: "Estimated 1RM Progress"
   - `CardDescription` (from shadcn): "Track your estimated one-rep max over time"
   - `CardContent` → `<WeightProgressChart />`
   - This component includes its own exercise selector internally (built in Plan 01)

4. **Weekly Volume Card** — `Card className="mt-6"` with:
   - `CardHeader` → `CardTitle`: "Weekly Volume"
   - `CardDescription`: "Total weight × reps lifted per week"
   - `CardContent` → `<WeeklyVolumeChart />`

5. **Workout Frequency Card** — `Card className="mt-6"` with:
   - `CardHeader` → `CardTitle`: "Training Frequency"
   - `CardDescription`: "Your workout activity over the last year"
   - `CardContent` → `<WorkoutHeatmap />`

6. **Conditional Cycle Phase Card** (keep existing — only shows when `filter === 'cycle-phase'`)

**Imports to add:**
- `CardDescription` from `@/components/ui/card` (add to existing Card import)
- `WeeklyVolumeChart` from `@/components/progress/weekly-volume-chart`
- `WorkoutHeatmap` from `@/components/progress/workout-heatmap`
- `WeightProgressChart` import already exists — keep it

**Imports to keep:**
- `useState` from React
- `Card`, `CardContent`, `CardHeader`, `CardTitle` from shadcn
- `WeightProgressChart` from progress components
- `CycleFilter` from progress components

**Styling notes:**
- Keep `min-h-screen p-4 pb-20` on outer div (matches existing)
- Each Card after the first gets `className="mt-6"` for vertical spacing
- The heatmap card content may need `overflow-x-auto` on the `CardContent` div to handle horizontal scroll on very narrow mobile screens (the heatmap is naturally wide)

Keep `'use client'` directive.
  </action>
  <verify>
Run `npx tsc --noEmit` — no type errors.
Run `npm run build` — production build succeeds.
Run `npm run lint` — no lint errors.
Verify all three charts are imported: `grep -E "WeightProgressChart|WeeklyVolumeChart|WorkoutHeatmap" src/app/progress/page.tsx` — all three present.
  </verify>
  <done>
Progress page renders all three chart cards (1RM line chart, weekly volume bar chart, workout frequency heatmap) in vertical layout with proper Card headers/descriptions. Existing CycleFilter and cycle-phase card preserved. Build passes, lint clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: End-to-end build + verification</name>
  <files>src/app/progress/page.tsx</files>
  <action>
Run the full verification suite to confirm everything works together:

1. `npm run build` — full production build must succeed with zero errors. This proves all imports resolve, all types check, and the page compiles.

2. `npm run lint` — zero lint errors across all modified/new files.

3. Verify no unused imports or dead code in the new files:
   - `src/hooks/use-1rm-progress.ts`
   - `src/hooks/use-weekly-volume.ts`
   - `src/hooks/use-workout-frequency.ts`
   - `src/components/progress/weight-progress-chart.tsx`
   - `src/components/progress/weekly-volume-chart.tsx`
   - `src/components/progress/workout-heatmap.tsx`
   - `src/app/progress/page.tsx`

4. If any build or lint errors are found, fix them. Common issues to watch for:
   - Missing `'use client'` directives on hook files that use React hooks
   - recharts 3.x Tooltip formatter type mismatches — use explicit cast if needed
   - react-activity-calendar types — may need `@ts-expect-error` if type declarations are imperfect (check if `@types/react-activity-calendar` exists, if not the package includes its own types)
   - `CardDescription` may not be exported from shadcn Card — check `src/components/ui/card.tsx` and use it only if it exists, otherwise use a `<p className="text-sm text-muted-foreground">` instead

5. Run existing tests to ensure nothing is broken: `npx vitest run --reporter=verbose 2>&1 | tail -20`

If all checks pass, the phase is complete.
  </action>
  <verify>
`npm run build` exits 0.
`npm run lint` exits 0.
`npx vitest run` — existing tests still pass (no regressions).
  </verify>
  <done>
Full project builds cleanly, lint passes, existing tests pass. All Phase 02 files compile without errors. The /progress page is ready to render all three visualizations.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` — zero errors, production build succeeds
2. `npm run lint` — zero lint errors
3. `npx vitest run` — existing tests pass (no regressions)
4. `grep -c "WeightProgressChart\|WeeklyVolumeChart\|WorkoutHeatmap" src/app/progress/page.tsx` — returns 3+ (all three imported and used)
5. All 7 phase files exist:
   - `src/hooks/use-1rm-progress.ts`
   - `src/hooks/use-weekly-volume.ts`
   - `src/hooks/use-workout-frequency.ts`
   - `src/components/progress/weight-progress-chart.tsx`
   - `src/components/progress/weekly-volume-chart.tsx`
   - `src/components/progress/workout-heatmap.tsx`
   - `src/app/progress/page.tsx`
</verification>

<success_criteria>
- /progress page shows three chart cards: 1RM Progress, Weekly Volume, Training Frequency
- Each card has a descriptive title and subtitle
- All charts handle empty state gracefully (no crashes with zero data)
- Heatmap card has overflow handling for mobile
- Existing CycleFilter functionality preserved
- Build, lint, and tests all pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-progress/02-03-SUMMARY.md`
</output>
