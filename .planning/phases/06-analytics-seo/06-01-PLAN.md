---
phase: 06-analytics-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pwa/index.html
  - pwa/src/routes/SignIn.tsx
  - pwa/src/routes/WorkoutSession.tsx
  - pwa/src/routes/Settings.tsx
  - pwa/src/entitlements/stripe-checkout.ts
  - pwa/src/firebase/analytics.ts
autonomous: true
requirements:
  - QUAL-02
  - QUAL-03

must_haves:
  truths:
    - "Sharing the app URL on Slack, iMessage, or Twitter shows a card with title, description, and image"
    - "Key user actions (sign in, workout complete, subscription start) fire as Firebase Analytics events"
  artifacts:
    - path: "pwa/index.html"
      provides: "OG and Twitter meta tags"
      contains: "og:title"
    - path: "pwa/src/routes/SignIn.tsx"
      provides: "Analytics event on successful sign-in"
      contains: "logEvent"
    - path: "pwa/src/routes/WorkoutSession.tsx"
      provides: "Analytics event on workout completion"
      contains: "logEvent"
    - path: "pwa/src/entitlements/stripe-checkout.ts"
      provides: "Analytics event on subscription start"
      contains: "logEvent"
  key_links:
    - from: "pwa/src/routes/SignIn.tsx"
      to: "pwa/src/firebase/analytics.ts"
      via: "import logEvent"
      pattern: "logEvent.*sign_in"
    - from: "pwa/src/routes/WorkoutSession.tsx"
      to: "pwa/src/firebase/analytics.ts"
      via: "import logEvent"
      pattern: "logEvent.*workout_complete"
---

<objective>
Add SEO meta tags for social sharing and instrument key user actions with Firebase Analytics events.

Purpose: Complete the app's public-facing metadata so shared links render rich previews, and wire analytics events so user behavior is visible in Firebase DebugView.
Output: index.html with OG/Twitter meta tags; logEvent calls on sign-in, workout completion, and subscription checkout.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@pwa/index.html
@pwa/src/firebase/analytics.ts
@pwa/src/routes/SignIn.tsx
@pwa/src/routes/WorkoutSession.tsx
@pwa/src/entitlements/stripe-checkout.ts
@pwa/src/routes/Settings.tsx

<interfaces>
<!-- Key exports from analytics module the executor needs -->

From pwa/src/firebase/analytics.ts:
```typescript
export function initAnalytics(): void;
export async function logEvent(
  eventName: string,
  params?: Record<string, string | number | boolean>,
): Promise<void>;
export async function setUserProperties(props: {
  subscriptionTier: 'free' | 'premium';
  cycleTrackingEnabled: boolean;
}): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OG and Twitter meta tags to index.html</name>
  <files>pwa/index.html</files>
  <action>
Add the following meta tags inside the `<head>` element of pwa/index.html, after the existing `<title>` tag:

```html
<!-- Open Graph -->
<meta property="og:type" content="website" />
<meta property="og:title" content="Sundee Fundee — Strength Training, Your Way" />
<meta property="og:description" content="Cycle-aware strength training with AI workouts, program tracking, and smart recovery. Free to start." />
<meta property="og:image" content="https://sundeefundee.com/og-image.png" />
<meta property="og:url" content="https://sundeefundee.com" />

<!-- Twitter Card -->
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content="Sundee Fundee — Strength Training, Your Way" />
<meta name="twitter:description" content="Cycle-aware strength training with AI workouts, program tracking, and smart recovery. Free to start." />
<meta name="twitter:image" content="https://sundeefundee.com/og-image.png" />
```

Also add a `<meta name="description">` tag:
```html
<meta name="description" content="Cycle-aware strength training with AI workouts, program tracking, and smart recovery. Free to start." />
```

For the OG image: copy the existing `pwa/public/icon-512.png` to `pwa/public/og-image.png` (same icon works as a social preview card image). If icon-512.png doesn't exist, check for any 512px PNG in pwa/public/ and use that.
  </action>
  <verify>
    <automated>grep -c "og:title" pwa/index.html && grep -c "twitter:card" pwa/index.html && test -f pwa/public/og-image.png && echo "PASS"</automated>
  </verify>
  <done>index.html contains og:title, og:description, og:image, og:url, twitter:card, twitter:title, twitter:description, twitter:image meta tags. og-image.png exists in public/.</done>
</task>

<task type="auto">
  <name>Task 2: Instrument key user actions with Firebase Analytics events</name>
  <files>pwa/src/routes/SignIn.tsx, pwa/src/routes/WorkoutSession.tsx, pwa/src/entitlements/stripe-checkout.ts, pwa/src/routes/Settings.tsx</files>
  <action>
Import `logEvent` from `../firebase/analytics` (adjust relative path per file) and add event calls at these points:

**SignIn.tsx** — fire after each successful sign-in method:
- After `signInWithEmailAndPassword` succeeds (line ~36 area): `await logEvent('login', { method: 'email' });`
- After `createUserWithEmailAndPassword` succeeds (line ~32 area): `await logEvent('sign_up', { method: 'email' });`
- After `signInWithGoogle` succeeds: `await logEvent('login', { method: 'google' });`
- After `signInWithApple` succeeds: `await logEvent('login', { method: 'apple' });`
- After `signInAnonymously` succeeds: `await logEvent('login', { method: 'guest' });`
Use Firebase recommended event names: `login` for sign-in, `sign_up` for account creation.

**WorkoutSession.tsx** — fire when workout is saved/completed:
- Find the function that saves the completed workout (likely calls `workoutRepo.saveWorkout` or similar). Add after the successful save: `await logEvent('workout_complete', { exercise_count: session.exercises.length, duration_seconds: Math.floor(elapsed / 1000) });`
- The logEvent call should be fire-and-forget (non-blocking) — wrap in void or catch to prevent save failure.

**stripe-checkout.ts** — fire before redirect to Stripe:
- In `redirectToCheckout`, just before `window.location.href = data.url`: `void logEvent('begin_checkout', { price_id: priceId });`
- Use Firebase recommended event name `begin_checkout`.

**Settings.tsx** — fire when user taps "Manage Subscription":
- Find the manage subscription button handler. Add: `void logEvent('subscription_manage');`

All logEvent calls should be non-blocking (use `void logEvent(...)` or fire-and-forget pattern) so they never block user flows. Analytics failures must be silent.
  </action>
  <verify>
    <automated>cd /Users/dustinober/Projects/Sundee-Fundee/pwa && grep -l "logEvent" src/routes/SignIn.tsx src/routes/WorkoutSession.tsx src/entitlements/stripe-checkout.ts src/routes/Settings.tsx | wc -l | grep -q "4" && echo "PASS"</automated>
  </verify>
  <done>logEvent calls exist in SignIn.tsx (login + sign_up events with method param), WorkoutSession.tsx (workout_complete with exercise count and duration), stripe-checkout.ts (begin_checkout with price_id), and Settings.tsx (subscription_manage). All calls are non-blocking.</done>
</task>

</tasks>

<verification>
1. `grep -c "og:title" pwa/index.html` returns 1
2. `grep -c "twitter:card" pwa/index.html` returns 1
3. `grep -r "logEvent" pwa/src/routes/SignIn.tsx` shows login/sign_up events
4. `grep -r "logEvent" pwa/src/routes/WorkoutSession.tsx` shows workout_complete event
5. `grep -r "logEvent" pwa/src/entitlements/stripe-checkout.ts` shows begin_checkout event
6. `cd pwa && npx vitest run` passes (no regressions)
7. `cd pwa && npx tsc -b --noEmit` passes (type check)
</verification>

<success_criteria>
- OG/Twitter meta tags in index.html render a rich preview card when URL is pasted in Slack/iMessage/Twitter
- Firebase Analytics events fire for: sign-in (all methods), sign-up, workout completion, checkout start, subscription management
- No existing tests broken
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/06-analytics-seo/06-01-SUMMARY.md`
</output>
