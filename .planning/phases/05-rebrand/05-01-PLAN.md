---
phase: 05-rebrand
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/components/onboarding/onboarding-wizard.tsx
  - package.json
  - README.md
  - CLAUDE.md
autonomous: true

must_haves:
  truths:
    - "Browser tab reads 'Sundee-Fundee' on every route (dashboard, workout, progress, settings)"
    - "Onboarding welcome text says 'Welcome to Sundee-Fundee'"
    - "No residual 'Strength' brand-name references in UI copy or metadata"
    - "package.json name, README heading, and CLAUDE.md reflect 'Sundee-Fundee'"
    - "All 11 existing Playwright E2E tests pass without modification"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root metadata with new brand name and description"
      contains: "Sundee-Fundee"
    - path: "src/components/onboarding/onboarding-wizard.tsx"
      provides: "Updated onboarding welcome text"
      contains: "Welcome to Sundee-Fundee"
    - path: "package.json"
      provides: "Updated npm package name"
      contains: '"name": "sundee-fundee"'
    - path: "README.md"
      provides: "Updated project heading"
      contains: "Sundee-Fundee"
    - path: "CLAUDE.md"
      provides: "Updated project description"
      contains: "Sundee-Fundee"
  key_links:
    - from: "src/app/layout.tsx"
      to: "browser tab title (all routes)"
      via: "Next.js App Router metadata export — single root layout controls all routes"
      pattern: 'title: "Sundee-Fundee"'
    - from: "grep -r 'Strength' src/"
      to: "verification"
      via: "Must return ONLY frozen domain identifiers (StrengthApp, StrengthDatabase, analyzeStrengthPatterns, etc.)"
      pattern: "zero brand-name hits"
---

<objective>
Replace all brand-name references from "Strength" to "Sundee-Fundee" across source code, metadata, and config files — with zero functional regression.

Purpose: Users and developers see "Sundee-Fundee" consistently everywhere the app name appears (browser tabs, metadata, page copy, config files). This is a text-only rebrand — no new packages, no architectural changes.

Output: 5 files edited with brand name updated; verification via grep and full E2E suite.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-rebrand/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all brand-name references</name>
  <files>
    src/app/layout.tsx
    src/components/onboarding/onboarding-wizard.tsx
    package.json
    README.md
    CLAUDE.md
  </files>
  <action>
Make exactly 6 text replacements across 5 files. Each replacement is a single string swap — no structural changes.

**Source code (2 edits):**

1. `src/app/layout.tsx` line 18: Change `title: "Strength - Workout Tracker"` → `title: "Sundee-Fundee"`
2. `src/app/layout.tsx` line 19: Change `description: "Track your workouts, build strength"` → `description: "Track your workouts and fitness goals"`
3. `src/components/onboarding/onboarding-wizard.tsx` line 60: Change `'Welcome to Strength'` → `'Welcome to Sundee-Fundee'`

**Config/docs (3 edits):**

4. `package.json` line 2: Change `"name": "strength"` → `"name": "sundee-fundee"`
5. `README.md` line 1: Change `# Strength - Workout Tracking App` → `# Sundee-Fundee - Workout Tracking App`
6. `CLAUDE.md` ~line 6: Change `**Strength** is a mobile-first` → `**Sundee-Fundee** is a mobile-first`

**CRITICAL — DO NOT TOUCH these frozen identifiers:**
- `super('StrengthApp')` in `src/lib/db/dexie.ts` — changing silently destroys ALL user data
- `StrengthDatabase` class name in `src/lib/db/dexie.ts` — frozen DB class
- `'strength'` in PrimaryGoal union in `src/types/user.ts` — fitness term, not brand
- `<SelectItem value="strength">Build Strength</SelectItem>` in onboarding-wizard.tsx — frozen fitness term
- `analyzeStrengthPatterns`, `predictStrengthWindow`, `PhaseStrengthProfile` — domain identifiers
- `indexedDB.open('StrengthApp')` in E2E tests — frozen DB reference
  </action>
  <verify>
Run `grep -r "Strength" src/ README.md CLAUDE.md package.json .planning/` and confirm output contains ONLY frozen domain identifiers:
- `src/lib/db/dexie.ts`: `StrengthApp`, `StrengthDatabase`
- `src/lib/cycle-calculations.ts`: `analyzeStrengthPatterns`, `predictStrengthWindow`, `PhaseStrengthProfile`
- `src/types/cycle.ts`: `PhaseStrengthProfile`
- `src/contexts/cycle-context.tsx`: `strengthProfile`, `PhaseStrengthProfile`

Zero brand-name hits (no "Strength - Workout Tracker", no "Welcome to Strength", no "build strength" in metadata).

Also verify: `grep -n "Sundee-Fundee" src/app/layout.tsx src/components/onboarding/onboarding-wizard.tsx` shows the new brand name in both files.
  </verify>
  <done>All 6 text replacements made. grep confirms zero brand-name "Strength" references remain in src/ — only frozen domain identifiers survive.</done>
</task>

<task type="auto">
  <name>Task 2: Run full E2E suite to confirm zero regression</name>
  <files>no files modified — verification only</files>
  <action>
Run the full Playwright E2E test suite to confirm all 11 tests pass without modification. The tests do NOT assert the old brand title or onboarding welcome text, so they should pass unchanged.

Run: `npx playwright test`

If any test fails, investigate whether it's related to the rebrand edits (it shouldn't be — research confirmed no E2E test checks brand strings). If a test fails for an unrelated reason, note it but do not modify tests.
  </action>
  <verify>
`npx playwright test` exits with 0. All 11 tests pass. No test was modified.
  </verify>
  <done>Exit code 0. Terminal shows 11 passed, 0 failed. No test files were modified.</done>
</task>

</tasks>

<verification>
1. `grep -r "Strength" src/ README.md CLAUDE.md package.json .planning/` — returns ONLY frozen domain identifiers (StrengthApp, StrengthDatabase, analyzeStrengthPatterns, predictStrengthWindow, PhaseStrengthProfile, strengthProfile). Zero brand-name hits.
2. `grep -n "Sundee-Fundee" src/app/layout.tsx` — shows title on line 18
3. `grep -n "Sundee-Fundee" src/components/onboarding/onboarding-wizard.tsx` — shows welcome text on line 60
4. `grep '"name": "sundee-fundee"' package.json` — confirms package.json updated
5. `head -1 README.md` — shows "# Sundee-Fundee - Workout Tracking App"
6. `grep "Sundee-Fundee" CLAUDE.md` — shows updated brand reference
7. `npx playwright test` — all 11 tests pass, exit code 0
</verification>

<success_criteria>
- Browser tab title metadata reads "Sundee-Fundee" (via layout.tsx root metadata export)
- Onboarding says "Welcome to Sundee-Fundee"
- `grep -r "Strength" src/` returns only frozen domain identifiers
- package.json, README.md, CLAUDE.md all reflect "Sundee-Fundee"
- All 11 E2E tests pass without modification
</success_criteria>

<output>
After completion, create `.planning/phases/05-rebrand/05-01-SUMMARY.md`
</output>
