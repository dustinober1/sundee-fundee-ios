---
phase: 17-device-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - .planning/phases/17-device-verification/17-TRIAGE.md
autonomous: false
requirements:
  - VERIFY-01
  - VERIFY-02

must_haves:
  truths:
    - "All 20 degraded-tier items are verified or fixed on iOS simulator"
    - "All 5 cosmetic-tier items are verified via screenshot"
    - "Weight unit toggling works correctly across all relevant screens"
    - "Cycle tab visibility matches user opt-in state"
    - "PR toast renders visually (haptic deferred to Phase 18)"
    - "Charts render correctly (1RM line chart, pain trend chart)"
    - "Delete account flow completes on simulator"
  artifacts:
    - path: ".planning/phases/17-device-verification/17-TRIAGE.md"
      provides: "Updated triage report with all 25 degraded + cosmetic items"
      contains: "VERIFIED\\|FIXED\\|DEFERRED"
  key_links:
    - from: "17-TRIAGE.md"
      to: "17-01-SUMMARY.md"
      via: "Appends to existing triage report"
      pattern: "Status column updated"
---

<objective>
Degraded-tier and cosmetic-tier verification sweep on iOS simulator.

Purpose: Verify all 25 remaining non-blocker items (20 degraded + 5 cosmetic). These are visual, behavioral, and integration items that should work correctly on simulator. Fix bugs immediately as found. Items requiring physical device or live external services are marked as deferred with rationale.

Output: Complete triage report for all non-deferred iOS simulator items. Any code bugs fixed.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-device-verification/17-CONTEXT.md
@.planning/phases/17-device-verification/17-RESEARCH.md
@.planning/phases/17-device-verification/17-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Degraded-tier verification sweep (20 items)</name>
  <files>.planning/phases/17-device-verification/17-TRIAGE.md</files>
  <action>
Systematically verify all 20 degraded-tier items on iOS simulator. Fix bugs immediately as found. Use iOS Simulator MCP tools for automation.

**Degraded items grouped by feature area:**

**Auth/Platform (2 items):**
- P1-3: Firestore user doc sync — sign in, check user doc appears at /users/{uid}. Use Firebase Emulator if configured; otherwise verify via app behavior (profile loads correctly).
- P1-4: Platform button differentiation — iOS shows Apple button only; verify no Google button on iOS sim.

**Workout UI/Behavior (4 items):**
- P4-2: Rest timer background persistence — start rest timer, lock screen, return. Timer shows correct remaining time. Note: real notification deferred to P18.
- P4-3: ForTime/AMRAP/EMOM modes — verify countdown plays, timers work. Note: audio/notification limited on sim.
- P4-4: PR detection toast — log higher weight, verify orange PR toast at top + PR badge on set row. Note: haptic deferred to P18.
- P4-5: Exercise detail 1RM chart — Maxes tab, tap exercise, verify orange line chart + 5-row PR table renders.

**Differentiating Features (3 items):**
- P5-1: AI workout generation — if Gemini API key is available, test live generation. If not, mark as "degraded — requires live API key" and verify the UI flow up to the API call point. Do NOT block on this.
- P5-2: Cycle tab visibility — verify cycleOptIn=true shows Cycle tab; cycleOptIn=false hides it.
- P5-4: Pain trend chart — log 3+ pain readings, verify LineChart renders with trend direction text. Use Firebase Emulator if available.
- P5-5: WOD card — verify Dashboard shows WOD content (may need Firebase Emulator with seeded WOD doc).

**Polish Features (3 items):**
- P7-1: Weight unit toggle — change lbs to kg in Settings, verify Maxes tab and workout session show kg immediately.
- P7-2: Delete account flow — type DELETE, spinner, function executes, AsyncStorage cleared, goodbye screen, Done to sign-in. Use Firebase Emulator.

**Cycle Adaptation (3 items):**
- P8-1: Cycle banner on Dashboard — for opted-in user with period logs, verify CyclePhaseBanner shows phase info.
- P8-2: Adaptation indicator in workout — verify adaptation banner shows load adjustment for opted-in user.
- P8-3: Opted-out user sees no cycle UI — verify no CyclePhaseBanner, no adaptation banner.

**Guest Migration (1 item):**
- P9-1: Guest migration retry — guest, generate data, upgrade, kill mid-migration, relaunch, all data in Firestore. Test on simulator with Firebase Emulator.

**Weight Unit Threading (2 items):**
- P10-3: Weight unit on workout detail — set kg, open completed workout from History, verify all weights in kg including Volume MetaStat.
- P16-1: kg display on program session — Settings kg, program session, ExerciseRow badges show kg.

**Pain Log (1 item):**
- P12-2: Pain trend chart with real data — 3+ entries, trend line renders (not empty state).

For each item: take screenshot, verify expected behavior, update triage report. If broken, fix code immediately and run `cd SundeeFundeeRN && npx jest --passWithNoTests` to confirm no regressions.

Mark items requiring live external services (P5-1 if no API key, P5-5 if no WOD data in emulator) as "degraded — service dependency" with clear rationale.
  </action>
  <verify>
    <automated>cd /Users/dustinober/Projects/Sundee-Fundee/SundeeFundeeRN && npx jest --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>All 20 degraded items have status in triage report. Any code fixes pass test suite. Items requiring external services documented with rationale.</done>
</task>

<task type="auto">
  <name>Task 2: Cosmetic-tier verification sweep (5 items)</name>
  <files>.planning/phases/17-device-verification/17-TRIAGE.md</files>
  <action>
Verify all 5 cosmetic-tier items via iOS simulator screenshots. These are visual polish items.

**Cosmetic items:**
- P7-3: Export data share sheet — Settings, Export Data, CSV, verify native iOS share sheet appears with .zip attachment.
- P10-1: Goodbye screen — after delete account, verify no back arrow/header visible on goodbye screen.
- P10-2: Web CSV export — this is web-only, will be checked in Plan 03 web smoke test. Mark as "deferred to Plan 03 web test" in triage report.
- P15-1: AdaptationChip visual — verify orange pill shows adaptation context ("Adapting for: Luteal phase...") on AI preview screen for user with cycle/injury/readiness data.
- P15-2: AdaptationChip hidden — verify chip does not appear for user with no cycle tracking, no injuries, no readiness survey.

For each: screenshot and record in triage report. Fix any visual issues found.
  </action>
  <verify>
    <automated>cd /Users/dustinober/Projects/Sundee-Fundee/SundeeFundeeRN && npx jest --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>All 5 cosmetic items have status in triage report. Visual rendering confirmed via screenshots.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of degraded and cosmetic sweep</name>
  <action>Review degraded and cosmetic sweep results and approve or request fixes.</action>
  <what-built>All 25 degraded + cosmetic items verified or fixed on iOS simulator. Triage report updated with full status for all non-deferred items.</what-built>
  <how-to-verify>
    1. Review updated triage report at `.planning/phases/17-device-verification/17-TRIAGE.md`
    2. Confirm all 25 items show VERIFIED, FIXED, or DEFERRED (with rationale) status
    3. If any items were FIXED, review the code changes
    4. Optionally: spot-check a few items on simulator (e.g., weight unit toggle, cycle tab visibility)
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to address</resume-signal>
</task>

</tasks>

<verification>
- All 20 degraded items documented in triage report
- All 5 cosmetic items documented in triage report
- No test regressions from bug fixes
- Items requiring external services clearly marked with rationale
</verification>

<success_criteria>
- Complete triage coverage: all 25 degraded + cosmetic items have documented status
- Visual items confirmed via simulator screenshots
- Weight unit, cycle adaptation, and chart rendering all verified correct
- Test suite green after any code changes
</success_criteria>

<output>
After completion, create `.planning/phases/17-device-verification/17-02-SUMMARY.md`
</output>
