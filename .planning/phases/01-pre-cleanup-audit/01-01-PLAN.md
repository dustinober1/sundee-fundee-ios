---
phase: 01-pre-cleanup-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-pre-cleanup-audit/01-01-PLAN.md
  - .planning/phases/01-pre-cleanup-audit/AUDIT.md
autonomous: true
requirements:
  - VERF-01

must_haves:
  truths:
    - "Developer can view complete inventory of all directories to be deleted with file counts"
    - "Developer can view complete inventory of all root-level config files with their purposes"
    - "Developer can view cross-reference scan results showing which iOS files (if any) reference directories being deleted"
    - "Developer can confirm the Xcode project builds successfully before any cleanup begins"
  artifacts:
    - path: ".planning/phases/01-pre-cleanup-audit/AUDIT.md"
      provides: "Comprehensive pre-cleanup audit document"
      contains: "## Directories to Delete"
  key_links:
    - from: "AUDIT.md"
      to: "Phase 9 verification"
      via: "checklist of references to verify are gone"
      pattern: "cross-reference scan"
---

<objective>
Produce a comprehensive pre-cleanup audit document that inventories everything being deleted, identifies all cross-references, and confirms the Xcode project builds cleanly before any changes are made.

Purpose: Phase 9 (Cross-Reference Verification) needs a baseline to verify against. Without this audit, there is no way to confirm all references to deleted code were cleaned up.

Output: `.planning/phases/01-pre-cleanup-audit/AUDIT.md` containing full inventory, cross-reference map, and build verification status.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pre-cleanup-audit/01-CONTEXT.md

<interfaces>
<!-- No code interfaces needed — this is a pure documentation/audit phase. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scan repository and produce comprehensive audit document</name>
  <files>.planning/phases/01-pre-cleanup-audit/AUDIT.md</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/STATE.md
    .gitignore
    readme.md
    package.json
    SundeeFundee/Package.swift
    SundeeFundeeApp/project.yml
  </read_first>
  <action>
Run a comprehensive repository scan and produce `AUDIT.md` in the phase directory. The document must contain these sections:

**Section 1: Directories to Delete**
For each of these directories, run `find <dir> -type f | wc -l` for file count and `du -sh <dir>` for size. List every subdirectory and its purpose:
- `web-app/` -- Next.js PWA (main web application)
- `firebase/` -- Cloud Functions for AI workout generation
- `backend/` -- Wrappers for backend tooling (teenybase, wrangler)
- `src-backend/` -- Backend worker source
- `scripts/` -- Python marketing screenshot generators
- `docs/` -- Screenshots, app store copy, superpowers docs, TODO
- `plans/` -- Historical planning docs (monetization roadmap, delivery spec)
- `.agents/` -- Agent skill configurations (25 ASC skills)
- `.github/` -- GitHub Actions workflows
- `.blitz/` -- Blitz configuration
- `.codex/` -- Codex configuration
- `.gemini/` -- Gemini AI configuration
- `.playwright-mcp/` -- Playwright MCP settings
- `.workflow-audit/` -- Workflow audit snapshot
- `.xcodebuildmcp/` -- XcodeBuildMCP config

**Section 2: Root-Level Files to Remove**
For each root file, state its purpose and whether it is needed by iOS:
- `package.json` -- Node.js dependencies (react-native-purchases, teenybase). NOT needed for iOS.
- `package-lock.json` -- npm lockfile. NOT needed.
- `firebase.json` -- Firebase project config. NOT needed.
- `firestore.indexes.json` -- Firestore index definitions. NOT needed.
- `.firebaserc` -- Firebase project alias. NOT needed.
- `.dev.vars` -- Dev environment variables (Cloudflare). NOT needed.
- `wrangler.toml` -- Cloudflare Workers config. NOT needed.
- `teenybase.ts` -- Teenybase type definitions. NOT needed.
- `opencode.json` -- OpenCode config. NOT needed.
- `skills-lock.json` -- Agent skills lockfile. NOT needed.
- `backlog.md` -- Backlog tracking. NOT needed.
- `AGENTS.md` -- Agent instructions (references XcodeBuildMCP). Needs rewriting, not removal.
- `Logo.jpeg` -- App logo image. KEEP -- may be useful for iOS.
- `.mcp.json` -- MCP server config. KEEP -- contains XcodeBuildMCP reference.
- `readme.md` -- Project readme. KEEP -- needs rewrite in Phase 7.

**Section 3: Cross-Reference Scan Results**
Run these scans and report results:
1. `grep -rn "web-app" SundeeFundee/ SundeeFundeeApp/` -- any iOS code referencing web-app directory
2. `grep -rn "firebase" SundeeFundee/ SundeeFundeeApp/` -- any iOS code referencing Firebase
3. `grep -rn "wod-dashboard" SundeeFundee/ SundeeFundeeApp/` -- any iOS code referencing admin dashboard
4. `grep -rn "backend" SundeeFundee/ SundeeFundeeApp/` -- any iOS code referencing backend
5. `grep -rn "scripts/" SundeeFundee/ SundeeFundeeApp/` -- any iOS code referencing scripts directory
6. `grep -rn "screenshots/" SundeeFundee/ SundeeFundeeApp/` -- any iOS code referencing screenshots

Known finding: `SundeeFundee/Sources/SundeeFundeeKit/DataLayer/HealthClientFactory.swift` line 6 contains the word "screenshots" in a comment about mock client usage. This is a code comment, NOT an import or file reference. Safe to ignore.

**Section 4: iOS Build Verification**
Run `cd SundeeFundeeApp && xcodebuild -project SundeeFundee.xcodeproj -scheme SundeeFundee -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -20` and record the result. The project must build successfully before cleanup begins.

**Section 5: Hidden Directories to Preserve**
These hidden directories are for iOS tooling and must NOT be deleted:
- `.claude/` -- GSD workflow and Claude configuration (ALSO contains .planning/ path reference)
- `.git/` -- Git repository
- `.planning/` -- Current cleanup project planning artifacts
- `.xcodebuildmcp/` -- Consider keeping if XcodeBuildMCP skill is used

**Section 6: .gitignore Assessment**
List entries in `.gitignore` that reference non-iOS tooling:
- `functions/node_modules/` -- Firebase Functions specific
- `node_modules/` -- Node.js specific
- `.next/` -- Next.js specific
- `dist/` -- Generic web build output
- `__pycache__/` -- Python cache (used by scripts/)
- `coverage/` -- Web app test coverage
- `.vercel` -- Vercel deployment
- `.env*.local` -- Web app env files

Write the complete AUDIT.md document with all sections populated with actual scan results. Do not use placeholder text -- every section must contain real data from the repository.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-pre-cleanup-audit/AUDIT.md && grep -c "## Directories to Delete" .planning/phases/01-pre-cleanup-audit/AUDIT.md && grep -c "## Cross-Reference Scan" .planning/phases/01-pre-cleanup-audit/AUDIT.md && grep -c "## iOS Build" .planning/phases/01-pre-cleanup-audit/AUDIT.md</automated>
  </verify>
  <done>AUDIT.md exists in phase directory with all 6 sections populated with real scan data, file counts, sizes, cross-reference results, and build status</done>
</task>

<task type="auto">
  <name>Task 2: Verify Xcode project builds and tests pass</name>
  <files>(no file changes -- verification only)</files>
  <read_first>
    SundeeFundee/Package.swift
    SundeeFundeeApp/project.yml
  </read_first>
  <action>
Run the Xcode project build and Swift tests to establish a baseline that the iOS codebase is healthy before any cleanup begins. Record the results in AUDIT.md (append to the iOS Build Verification section created by Task 1).

Steps:
1. Build the Xcode project: `cd /Users/dustinober/Projects/sundee-fundee/SundeeFundeeApp && xcodebuild -project SundeeFundee.xcodeproj -scheme SundeeFundee -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5`
2. If the build succeeds, run Swift tests: `cd /Users/dustinober/Projects/sundee-fundee/SundeeFundee && swift test 2>&1 | tail -10`
3. Record build status (success/failure), test count, and any failures in AUDIT.md

If the build fails, note the failure but DO NOT attempt to fix it -- the audit's purpose is to document current state. The fix would be a separate task outside this phase.

Important: This task only reads and verifies. It does not modify any source files.
  </action>
  <verify>
    <automated>grep -c "build.*success\|BUILD SUCCEEDED\|Tests.*passed\|Test Suite" .planning/phases/01-pre-cleanup-audit/AUDIT.md</automated>
  </verify>
  <done>AUDIT.md contains build verification result (BUILD SUCCEEDED or failure details) and test results (if build succeeded). No source files were modified.</done>
</task>

</tasks>

<verification>
1. AUDIT.md exists with all 6 sections containing real data (no placeholders)
2. Every directory marked for deletion has file count and size recorded
3. Cross-reference scan results are present with grep output
4. Xcode build result is documented
5. No source files were modified during this audit phase
</verification>

<success_criteria>
- AUDIT.md in phase directory with comprehensive inventory of all directories, root files, cross-references, and build status
- Xcode project build result recorded (success or documented failure)
- Zero source file modifications -- this phase is read-only
</success_criteria>

<output>
After completion, create `.planning/phases/01-pre-cleanup-audit/01-01-SUMMARY.md`
</output>
