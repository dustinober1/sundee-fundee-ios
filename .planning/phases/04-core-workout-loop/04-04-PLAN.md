---
phase: 04-core-workout-loop
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - SundeeFundeeRN/src/domain/history/history-item.ts
  - SundeeFundeeRN/src/domain/history/history-filter.ts
  - SundeeFundeeRN/src/domain/history/index.ts
  - SundeeFundeeRN/src/domain/progress/progress-data.ts
  - SundeeFundeeRN/src/domain/progress/index.ts
  - SundeeFundeeRN/src/domain/__tests__/history-filter.test.ts
  - SundeeFundeeRN/src/domain/__tests__/progress-data.test.ts
autonomous: true
requirements:
  - WORK-07
  - WORK-08
  - WORK-10
  - MAX-02

must_haves:
  truths:
    - "History items include 'custom' source type alongside 'aiWorkout' and 'program'"
    - "History can be filtered by source: all, ai, program, custom"
    - "Progress data transforms exercise max history into chart-ready time series"
    - "Volume progress data aggregates total volume per workout for a given exercise"
  artifacts:
    - path: "SundeeFundeeRN/src/domain/history/history-filter.ts"
      provides: "History source filtering function"
      exports: ["filterHistoryBySource", "HistorySourceFilter"]
    - path: "SundeeFundeeRN/src/domain/progress/progress-data.ts"
      provides: "Chart data preparation from exercise maxes and workout history"
      exports: ["prepare1RMChartData", "prepareVolumeChartData", "prepareRepRangePRs"]
  key_links:
    - from: "SundeeFundeeRN/src/domain/history/history-filter.ts"
      to: "history-item.ts"
      via: "imports HistoryItem for filtering"
      pattern: "import.*HistoryItem.*from.*history-item"
    - from: "SundeeFundeeRN/src/domain/progress/progress-data.ts"
      to: "src/domain/pr-detection/pr-types.ts"
      via: "imports ExerciseMax for chart data"
      pattern: "import.*ExerciseMax.*from.*pr-detection"
---

<objective>
Expand history domain with custom source filtering and create progress data helpers for chart visualization.

Purpose: History needs the 'custom' source type to display user-built workouts. Progress charts need pure data transformation functions to convert raw max records into chart-ready arrays. Both are pure domain functions.
Output: History filtering function, progress chart data preparation functions, all tested.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-core-workout-loop/04-CONTEXT.md
@.planning/phases/04-core-workout-loop/04-RESEARCH.md

<interfaces>
From src/domain/history/history-item.ts (current):
```typescript
export type HistoryItemSource =
  | { kind: 'aiWorkout' }
  | { kind: 'program'; name: string };
export interface HistoryItem {
  id: string; title: string; source: HistoryItemSource; completedAt: Date;
  exerciseCount: number; durationSeconds: number; originalID: string; isAIWorkout: boolean;
}
export function getSourceLabel(source: HistoryItemSource): string;
```

From src/domain/pr-detection/pr-types.ts (Plan 01):
```typescript
export interface ExerciseMax {
  exerciseId: string; exerciseName: string; repRange: TrackedRepRange;
  weight: number; estimated1RM: number; achievedAt: string;
}
export type TrackedRepRange = 1 | 3 | 5 | 8 | 10;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand HistoryItemSource with 'custom' and add filtering</name>
  <files>
    SundeeFundeeRN/src/domain/history/history-item.ts,
    SundeeFundeeRN/src/domain/history/history-filter.ts,
    SundeeFundeeRN/src/domain/history/index.ts,
    SundeeFundeeRN/src/domain/__tests__/history-filter.test.ts
  </files>
  <behavior>
    - HistoryItemSource now includes { kind: 'custom' }
    - getSourceLabel({ kind: 'custom' }) returns 'Custom Workout'
    - filterHistoryBySource(items, 'all') returns all items
    - filterHistoryBySource(items, 'ai') returns only { kind: 'aiWorkout' } items
    - filterHistoryBySource(items, 'program') returns only { kind: 'program' } items
    - filterHistoryBySource(items, 'custom') returns only { kind: 'custom' } items
    - groupHistoryByDate(items) groups items by date string (Today, Yesterday, date format)
    - HistoryItem gains optional totalVolume and workoutName fields
  </behavior>
  <action>
    1. Update `history-item.ts`:
       - Add `{ kind: 'custom' }` to HistoryItemSource union
       - Add `totalVolume?: number` and `workoutName?: string` to HistoryItem
       - Update `getSourceLabel` to handle 'custom' -> 'Custom Workout'

    2. Create `history-filter.ts`:
       - `HistorySourceFilter = 'all' | 'ai' | 'program' | 'custom'`
       - `filterHistoryBySource(items: HistoryItem[], filter: HistorySourceFilter): HistoryItem[]`
         - 'all' returns items unchanged
         - 'ai' filters for source.kind === 'aiWorkout'
         - 'program' filters for source.kind === 'program'
         - 'custom' filters for source.kind === 'custom'
       - `groupHistoryByDate(items: HistoryItem[]): { label: string; items: HistoryItem[] }[]`
         - Groups by date, labels: 'Today', 'Yesterday', or formatted date (e.g., 'Mar 12')
         - Sorted most recent first
         - Uses date-fns `isToday`, `isYesterday`, `format` if available, otherwise manual date comparison

    3. Update barrel index.ts to re-export new functions
  </action>
  <verify>
    <automated>cd /Users/dustinober/Projects/Sundee-Fundee/SundeeFundeeRN && npx jest src/domain/__tests__/history-filter.test.ts --no-cache</automated>
  </verify>
  <done>HistoryItemSource includes 'custom', filtering works for all 4 source types, date grouping produces labeled sections</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create progress data preparation functions for charts</name>
  <files>
    SundeeFundeeRN/src/domain/progress/progress-data.ts,
    SundeeFundeeRN/src/domain/progress/index.ts,
    SundeeFundeeRN/src/domain/__tests__/progress-data.test.ts
  </files>
  <behavior>
    - prepare1RMChartData(maxes) returns sorted array of { date: string, value: number } from estimated1RM values
    - prepare1RMChartData([]) returns empty array
    - prepare1RMChartData filters to single exerciseId and uses best estimated1RM per date
    - prepareVolumeChartData(workoutRecords, exerciseId) returns { date: string, value: number } from total volume per workout
    - prepareRepRangePRs(maxes, exerciseId) returns { repRange: number, weight: number, date: string }[] for each tracked rep range
    - prepareRepRangePRs returns null entries for rep ranges with no data
  </behavior>
  <action>
    1. Create `src/domain/progress/progress-data.ts`:
       - `prepare1RMChartData(maxes: ExerciseMax[], exerciseId: string): { date: string; value: number }[]`
         - Filter maxes by exerciseId
         - For each unique date (achievedAt truncated to date), take the highest estimated1RM
         - Sort by date ascending
         - Return as chart-ready array

       - `prepareVolumeChartData(records: WorkoutRecord[], exerciseId: string): { date: string; value: number }[]`
         - Import CompletedExercise from WorkoutRepo types
         - For each record that has exercises array, find matching exerciseId
         - Sum weight * reps across all sets for that exercise
         - Return { date: completedAt truncated, value: totalVolume } sorted by date

       - `prepareRepRangePRs(maxes: ExerciseMax[], exerciseId: string): { repRange: TrackedRepRange; weight: number | null; date: string | null }[]`
         - For each TRACKED_REP_RANGE, find best weight for this exercise
         - Return array of 5 entries (one per rep range), null weight/date if no data

    2. Create barrel `src/domain/progress/index.ts`
  </action>
  <verify>
    <automated>cd /Users/dustinober/Projects/Sundee-Fundee/SundeeFundeeRN && npx jest src/domain/__tests__/progress-data.test.ts --no-cache</automated>
  </verify>
  <done>Chart data preparation functions produce correctly sorted, aggregated data. 1RM chart data deduplicates by date (best per day). Volume chart sums across sets. Rep-range PRs return all 5 tracked ranges with null for missing data.</done>
</task>

</tasks>

<verification>
```bash
cd SundeeFundeeRN && npx jest src/domain/__tests__/history-filter.test.ts src/domain/__tests__/progress-data.test.ts --no-cache
```
Existing history tests still pass:
```bash
cd SundeeFundeeRN && npx jest --passWithNoTests
```
</verification>

<success_criteria>
- HistoryItemSource includes 'custom' kind (backward compatible)
- History filtering handles all 4 source types
- Date grouping produces Today/Yesterday/formatted labels
- 1RM chart data sorted by date, best per day
- Volume chart data aggregates per-exercise totals
- Rep-range PRs cover all 5 tracked ranges
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-workout-loop/04-04-SUMMARY.md`
</output>
