---
phase: 18-foundation-config-build-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified: []
autonomous: false
requirements: [SEC-03]
user_setup:
  - service: apple-developer
    why: "Register physical iOS device for DeviceCheck verification"
    env_vars: []
    dashboard_config:
      - task: "Register iOS device UDID in Apple Developer portal"
        location: "Apple Developer > Devices > Add Device (or use `eas device:create`)"
  - service: app-store-connect
    why: "Provide App Store Connect App ID for eas.json submit config"
    env_vars: []
    dashboard_config:
      - task: "Look up numeric ascAppId"
        location: "App Store Connect > Your App > General > App Information"

must_haves:
  truths:
    - "EAS development build completes successfully for iOS and Android"
    - "New dev build installs and launches on physical iOS device without native module errors"
    - "New dev build installs and launches on Android device/emulator without native module errors"
    - "Firebase App Check dashboard shows DeviceCheck (iOS) and Play Integrity (Android) providers registered"
  artifacts: []
  key_links:
    - from: "EAS dev build"
      to: "@react-native-firebase/messaging native module"
      via: "app.json plugin registration + static linking"
      pattern: "Messaging module loads without error"
    - from: "EAS dev build"
      to: "@react-native-firebase/crashlytics native module"
      via: "app.json plugin registration + static linking"
      pattern: "Crashlytics module loads without error"
    - from: "EAS dev build"
      to: "@react-native-firebase/analytics native module"
      via: "static linking only (no plugin)"
      pattern: "Analytics module loads without error"
---

<objective>
Trigger an EAS development build that includes all three new native Firebase modules, then verify on physical devices that modules initialize correctly and App Check production attestation is active.

Purpose: Produce the new EAS dev build that unblocks all subsequent v1.1 phases (19-23). Confirm App Check production attestation works on real hardware.
Output: Working EAS dev build installed on physical iOS and Android devices.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-foundation-config-build-infrastructure/18-CONTEXT.md
@.planning/phases/18-foundation-config-build-infrastructure/18-RESEARCH.md
@.planning/phases/18-foundation-config-build-infrastructure/18-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trigger EAS development builds for iOS and Android</name>
  <files></files>
  <action>
1. Before building, verify the ascAppId placeholder in eas.json has been replaced by the user. If it still says "REPLACE_WITH_APP_STORE_CONNECT_APP_ID", pause and ask the user for the actual value. Update eas.json with the real ascAppId.

2. Trigger iOS EAS development build:
   ```bash
   eas build --platform ios --profile development
   ```
   Use EAS managed credentials (per locked decision). If prompted to register the iOS device, follow the flow.

3. Trigger Android EAS development build:
   ```bash
   eas build --platform android --profile development
   ```

4. Monitor both builds. If either fails:
   - Check build logs via `eas build:list` and `eas build:view`
   - Common failure: CocoaPods static linking error = missing forceStaticLinking entry
   - Common failure: Gradle error = crashlytics plugin misconfigured
   - Fix config and retrigger

5. Once builds complete, note the build URLs/QR codes for installation.
  </action>
  <verify>
    <automated>eas build:list --limit 2 --json 2>&1 | head -20</automated>
  </verify>
  <done>
    - Both iOS and Android EAS development builds complete with status "finished"
    - Build artifacts available for download/installation
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify builds on physical devices and confirm App Check attestation</name>
  <action>
User verifies the new EAS dev builds work on physical devices and that Firebase modules initialize correctly.

**What was built:** New EAS development builds for iOS and Android containing @react-native-firebase/messaging, /crashlytics, and /analytics native modules. App Check configured for production attestation on non-dev builds.

**Pre-requisites (user must complete):**
1. Register your physical iOS device UDID in Apple Developer portal (or run `eas device:create` to register via web)
2. Provide your App Store Connect numeric App ID if not already done

**iOS Physical Device:**
1. Install the new EAS dev build on your physical iOS device (scan QR or use Expo Go link)
2. Launch the app — confirm it opens without crash
3. Open the Expo dev tools console (shake device or check Metro bundler output)
4. Look for these console logs on launch:
   - `[Crashlytics] Initialized` (or similar)
   - `[Analytics] Initialized` (or similar)
   - `[Messaging] Initialized` (or similar)
   - No `FAILED` messages for any Firebase module
5. Check Firebase Console > App Check: verify DeviceCheck provider shows activity from iOS

**Android Device/Emulator:**
1. Install the new EAS dev build on Android device or emulator
2. Launch the app — confirm it opens without crash
3. Check Metro bundler console for same Firebase module initialization logs
4. Check Firebase Console > App Check: verify Play Integrity provider shows activity from Android

**Firebase Console Verification:**
1. Go to Firebase Console > App Check
2. Confirm DeviceCheck (iOS) and Play Integrity (Android) are registered as providers
3. Per locked decision: attestation is enabled but NOT enforced (enforcement deferred to Phase 22)

**Expected outcome:** App launches cleanly on both platforms, all three new Firebase modules initialize without errors, App Check dashboard shows production attestation providers active.
  </action>
  <verify>
    <automated>echo "Human verification checkpoint — no automated command"</automated>
  </verify>
  <done>
    - User confirms app launches on iOS physical device without native module errors
    - User confirms app launches on Android device/emulator without native module errors
    - User confirms Firebase Console shows DeviceCheck and Play Integrity providers active
    - User types "approved" or describes issues
  </done>
</task>

</tasks>

<verification>
1. EAS dev builds complete for both platforms (check `eas build:list`)
2. App launches on physical iOS device without native module errors
3. App launches on Android device/emulator without native module errors
4. Firebase Console > App Check shows DeviceCheck + Play Integrity providers active
5. Console logs confirm messaging, crashlytics, and analytics modules initialized
</verification>

<success_criteria>
- EAS dev builds for iOS and Android complete successfully
- Builds install and run on physical devices
- All three new Firebase native modules (messaging, crashlytics, analytics) load without errors
- Firebase App Check dashboard shows production attestation providers (DeviceCheck iOS, Play Integrity Android)
- User confirms via checkpoint that everything works
</success_criteria>

<output>
After completion, create `.planning/phases/18-foundation-config-build-infrastructure/18-02-SUMMARY.md`
</output>
