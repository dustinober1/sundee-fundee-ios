---
phase: 20-notification-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/hooks/useRestTimer.ts
  - src/components/notifications/NotificationPermissionModal.tsx
  - app/(app)/workout-session.tsx
  - app/(app)/_layout.tsx
autonomous: true
requirements:
  - NOTIF-01
  - NOTIF-02
  - NOTIF-03

must_haves:
  truths:
    - "Rest timer no longer eagerly requests notification permission"
    - "After first workout completion, user sees Art Deco permission modal (not the OS dialog directly)"
    - "Tapping Enable on the modal fires the OS permission dialog"
    - "Tapping Not Now dismisses modal and sets AsyncStorage flag so it never re-appears"
    - "Rest timer notification only schedules when restTimerAlertsEnabled is true"
    - "After permission granted, FCM token is registered for authenticated non-web users"
    - "Tapping rest timer notification deep-links back to workout-session"
    - "Android notification channels created at app layout load"
  artifacts:
    - path: "src/components/notifications/NotificationPermissionModal.tsx"
      provides: "Art Deco styled pre-permission modal"
      min_lines: 50
    - path: "src/hooks/useRestTimer.ts"
      provides: "Refactored rest timer without eager permission, with toggle guard"
      contains: "restTimerAlertsEnabled"
    - path: "app/(app)/workout-session.tsx"
      provides: "First-workout detection and modal trigger, no eager permission"
    - path: "app/(app)/_layout.tsx"
      provides: "Android channels, notification deep-link listener, FCM token registration on auth"
  key_links:
    - from: "app/(app)/workout-session.tsx"
      to: "src/components/notifications/NotificationPermissionModal.tsx"
      via: "showNotifPermissionModal state"
      pattern: "NotificationPermissionModal"
    - from: "app/(app)/_layout.tsx"
      to: "src/services/notificationService.ts"
      via: "registerFCMToken import"
      pattern: "registerFCMToken"
    - from: "src/hooks/useRestTimer.ts"
      to: "src/repositories/SettingsRepo.ts"
      via: "restTimerAlertsEnabled check"
      pattern: "restTimerAlertsEnabled"
---

<objective>
Wire the notification permission flow and rest timer refactoring: remove all eager permission requests, create the Art Deco permission modal shown after first workout, add the rest timer toggle guard, create Android notification channels, add the notification deep-link listener, and register the FCM token after auth.

Purpose: This is the core behavioral wiring — after this plan, users experience the correct permission flow (deferred, respectful) and the rest timer respects their toggle preference.
Output: Working permission modal, refactored useRestTimer, Android channels, FCM token registration, deep-link handling.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-notification-infrastructure/20-CONTEXT.md
@.planning/phases/20-notification-infrastructure/20-RESEARCH.md
@.planning/phases/20-notification-infrastructure/20-01-SUMMARY.md

@src/hooks/useRestTimer.ts
@app/(app)/workout-session.tsx
@app/(app)/_layout.tsx
@src/theme/colors.ts
@__mocks__/expo-notifications.ts

<interfaces>
<!-- From Plan 01 outputs -->

From src/repositories/SettingsRepo.ts (after Plan 01):
```typescript
export interface AppSettings {
  weightUnit: 'lb' | 'kg';
  defaultRestDuration: number;
  restTimerAlertsEnabled: boolean;   // default true
  workoutRemindersEnabled: boolean;  // default false
  wodAlertsEnabled: boolean;         // default true
  subscriptionAlertsEnabled: boolean; // default true
  reminderHour: number;              // default 7
  reminderMinute: number;            // default 0
}
```

From src/services/notificationService.ts (after Plan 01):
```typescript
export async function scheduleDailyReminder(hour: number, minute: number, cyclePhase: CyclePhase | null): Promise<void>;
export async function cancelDailyReminder(): Promise<void>;
export async function registerFCMToken(uid: string, isGuest: boolean): Promise<void>;
```

From src/hooks/useNotificationPermission.ts (after Plan 01):
```typescript
export function useNotificationPermission(): {
  status: 'granted' | 'denied' | 'undetermined';
  isGranted: boolean;
  isDenied: boolean;
  checkPermission: () => Promise<void>;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create permission modal, refactor useRestTimer, remove eager permission from workout-session</name>
  <files>
    src/components/notifications/NotificationPermissionModal.tsx,
    src/hooks/useRestTimer.ts,
    app/(app)/workout-session.tsx
  </files>
  <action>
    1. **Create src/components/notifications/NotificationPermissionModal.tsx:**
       - Art Deco styled modal (cream background #F4F0DF, navy text #0D1A40, orange accent #F2731A)
       - Title: "Stay on Track" (value proposition framing, not permission nag)
       - Body: Brief description of what notifications enable — rest timer alerts, workout reminders, WOD drops
       - Two buttons: "Enable" (orange, primary) and "Not Now" (text-only, secondary)
       - Props: `visible: boolean`, `onEnable: () => void`, `onDismiss: () => void`
       - Uses React Native Modal component (consistent with existing PaywallModal pattern)
       - No business logic in the modal — just presentation

    2. **Refactor src/hooks/useRestTimer.ts:**
       - Remove lines 98-102 (the eager permission request block: getPermissionsAsync + requestPermissionsAsync)
       - Add `restTimerAlertsEnabled` parameter to the hook: `useRestTimer(defaultSeconds = 90, restTimerAlertsEnabled = true)`
       - In the `start` function, guard the notification scheduling with `if (restTimerAlertsEnabled)` — if disabled, skip scheduleNotificationAsync but still start the visual countdown timer
       - Add Android channelId to notification content: `...(Platform.OS === 'android' && { android: { channelId: 'rest-timer' } })`
       - Add deep-link data to notification content: `data: { url: '/workout-session' }`
       - Import Platform from react-native

    3. **Refactor app/(app)/workout-session.tsx:**
       - Remove lines 270-279 (the entire eager requestNotifPermission useEffect block)
       - Add first-workout detection after workout save in the finish/complete handler:
         - Constants: `FIRST_WORKOUT_DONE_KEY = '@sundee/first_workout_done'`, `NOTIF_PERMISSION_ASKED_KEY = '@sundee/notif_permission_asked'`
         - After successful workout save: check `AsyncStorage.getItem(FIRST_WORKOUT_DONE_KEY)`
         - If null (first workout): `setItem(FIRST_WORKOUT_DONE_KEY, 'true')`, then check NOTIF_PERMISSION_ASKED_KEY
         - If not already asked: `setShowNotifPermissionModal(true)`
       - Add state: `const [showNotifPermissionModal, setShowNotifPermissionModal] = useState(false)`
       - Add modal handlers:
         - `handleEnableNotifications`: set NOTIF_PERMISSION_ASKED_KEY, hide modal, call `Notifications.requestPermissionsAsync()`, if granted call `void registerFCMToken(user.uid, isGuest)` (import from notificationService)
         - `handleNotNow`: set NOTIF_PERMISSION_ASKED_KEY via void fire-and-forget, hide modal
       - Render `<NotificationPermissionModal visible={showNotifPermissionModal} onEnable={handleEnableNotifications} onDismiss={handleNotNow} />` in the JSX
       - Pass `settings.restTimerAlertsEnabled` to useRestTimer hook
  </action>
  <verify>
    <automated>npx jest --passWithNoTests src/hooks/__tests__/useRestTimer.test.ts -x</automated>
  </verify>
  <done>
    - useRestTimer no longer calls requestPermissionsAsync or getPermissionsAsync
    - useRestTimer accepts restTimerAlertsEnabled param and skips notification scheduling when false
    - workout-session.tsx no longer has the eager permission useEffect
    - workout-session.tsx shows NotificationPermissionModal after first workout completion
    - NotificationPermissionModal renders with Art Deco styling and two buttons
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Android channels, deep-link listener, and FCM token registration in app layout</name>
  <files>
    app/(app)/_layout.tsx
  </files>
  <action>
    1. **Create Android notification channels** at module level (alongside existing setNotificationHandler):
       - Import Platform from react-native
       - After the existing `Notifications.setNotificationHandler({...})` block:
       - `if (Platform.OS === 'android') { ... }` — create two channels:
         - 'rest-timer': name "Rest Timer", importance HIGH, sound 'default', vibrationPattern [0, 250, 250, 250], lightColor '#F2731A'
         - 'reminders': name "Workout Reminders", importance DEFAULT, sound 'default'
       - Use `void` prefix (fire-and-forget, async at module level)

    2. **Add notification response listener** for deep-linking when user taps a notification:
       - Inside the AppLayout component, add a useEffect:
       - `Notifications.addNotificationResponseReceivedListener((response) => { ... })`
       - Extract `url` from `response.notification.request.content.data?.url`
       - If url exists, call `router.push(url as never)` (import router from expo-router — already available via Redirect/Stack imports, add useRouter)
       - Return cleanup: `sub.remove()`

    3. **Wire FCM token registration** after auth resolves:
       - In the existing useEffect that runs when `user` changes (the onboarding check), after confirming user is authenticated and not guest:
       - Call `void registerFCMToken(user.uid, isGuest)` (import from notificationService)
       - Also set up token refresh listener: `messaging().onTokenRefresh(() => void registerFCMToken(user.uid, isGuest))` — wrap in try/catch with require pattern consistent with other RNFB usage
       - Return cleanup for the token refresh unsubscribe
       - Guard: only if Platform.OS !== 'web' && !isGuest
  </action>
  <verify>
    <automated>npx jest --passWithNoTests -x</automated>
  </verify>
  <done>
    - Android channels 'rest-timer' (HIGH) and 'reminders' (DEFAULT) created at module load
    - Notification tap deep-links to the URL stored in notification data
    - FCM token registered after auth resolves for non-guest, non-web users
    - Token refresh listener wired with cleanup
    - Full test suite passes
  </done>
</task>

</tasks>

<verification>
- `grep -r "requestPermissionsAsync" src/hooks/useRestTimer.ts` returns zero hits
- `grep -r "requestPermissionsAsync" app/\(app\)/workout-session.tsx` returns zero hits (only in modal handler, not in useEffect)
- `grep "restTimerAlertsEnabled" src/hooks/useRestTimer.ts` confirms toggle guard
- `grep "NotificationPermissionModal" app/\(app\)/workout-session.tsx` confirms modal rendered
- `grep "setNotificationChannelAsync" app/\(app\)/_layout.tsx` confirms Android channels
- `grep "registerFCMToken" app/\(app\)/_layout.tsx` confirms FCM wiring
- `npx jest --passWithNoTests -x` — full suite green
</verification>

<success_criteria>
- Zero eager permission requests remain in the codebase (only in modal Enable handler)
- Rest timer notification respects restTimerAlertsEnabled toggle
- First-workout completion triggers the Art Deco permission modal exactly once
- Android has proper notification channels for rest-timer and reminders
- FCM token is registered to Firestore after auth + permission grant
- Notification tap deep-links to workout-session
</success_criteria>

<output>
After completion, create `.planning/phases/20-notification-infrastructure/20-02-SUMMARY.md`
</output>
