---
phase: 20-notification-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/repositories/SettingsRepo.ts
  - src/services/notificationService.ts
  - src/domain/notifications/notification-copy.ts
  - src/hooks/useNotificationPermission.ts
  - src/repositories/__tests__/SettingsRepo.test.ts
  - src/services/__tests__/notificationService.test.ts
  - src/domain/__tests__/notification-copy.test.ts
autonomous: true
requirements:
  - NOTIF-06
  - NOTIF-07
  - NOTIF-08

must_haves:
  truths:
    - "AppSettings interface has 4 granular notification toggle fields and reminderHour/reminderMinute with correct defaults"
    - "notificationService can schedule a daily reminder with cancel-and-reschedule pattern"
    - "notificationService can register FCM token to Firestore (skips web and guest)"
    - "Cycle-phase copy returns a distinct one-line string for each of the 4 CyclePhase values"
    - "Generic copy rotates through 3-5 motivational variations"
    - "useNotificationPermission hook exposes current OS permission status"
  artifacts:
    - path: "src/repositories/SettingsRepo.ts"
      provides: "Extended AppSettings with 4 toggles + reminder time"
      contains: "restTimerAlertsEnabled"
    - path: "src/services/notificationService.ts"
      provides: "scheduleDailyReminder, cancelDailyReminder, registerFCMToken"
      exports: ["scheduleDailyReminder", "cancelDailyReminder", "registerFCMToken"]
    - path: "src/domain/notifications/notification-copy.ts"
      provides: "getCycleAwareCopy, getGenericCopy"
      exports: ["getCycleAwareCopy", "getGenericCopy", "CYCLE_COPY", "GENERIC_COPY"]
    - path: "src/hooks/useNotificationPermission.ts"
      provides: "Hook returning permission status"
      exports: ["useNotificationPermission"]
  key_links:
    - from: "src/services/notificationService.ts"
      to: "src/domain/notifications/notification-copy.ts"
      via: "import getCycleAwareCopy/getGenericCopy"
      pattern: "getCycleAwareCopy|getGenericCopy"
    - from: "src/services/notificationService.ts"
      to: "expo-notifications"
      via: "scheduleNotificationAsync, cancelScheduledNotificationAsync"
      pattern: "Notifications\\.schedule|Notifications\\.cancel"
---

<objective>
Build the notification foundation layer: extend AppSettings with per-type notification preferences, create the notification service (daily reminder scheduling, FCM token registration), create pure domain logic for cycle-phase-aware copy, and add a permission status hook.

Purpose: All subsequent notification wiring (permission modal, rest timer toggle, Settings UI, FCM token) depends on these types, services, and domain functions existing first.
Output: Extended AppSettings interface, notificationService module, notification-copy domain module, useNotificationPermission hook, and unit tests for all.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-notification-infrastructure/20-CONTEXT.md
@.planning/phases/20-notification-infrastructure/20-RESEARCH.md

@src/repositories/SettingsRepo.ts
@src/repositories/FirestoreSettingsRepo.ts
@src/repositories/LocalSettingsRepo.ts
@src/repositories/CycleRepo.ts
@src/domain/types/index.ts
@src/domain/cycle/cycle-calculations.ts
@src/hooks/useRestTimer.ts
@__mocks__/expo-notifications.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/repositories/SettingsRepo.ts:
```typescript
export interface AppSettings {
  weightUnit: 'lb' | 'kg';
  notificationsEnabled: boolean;
  defaultRestDuration: number;
}
export const DEFAULT_SETTINGS: AppSettings = {
  weightUnit: 'lb',
  notificationsEnabled: true,
  defaultRestDuration: 90,
};
export interface SettingsRepository {
  saveSettings(uid: string, settings: AppSettings): Promise<void>;
  getSettings(uid: string): Promise<AppSettings | null>;
}
export function getSettingsRepo(isGuest: boolean): SettingsRepository;
```

From src/domain/types/index.ts:
```typescript
export type CyclePhase = 'menstrual' | 'follicular' | 'ovulation' | 'luteal';
```

From src/domain/cycle/cycle-calculations.ts:
```typescript
export function calculateCycleStatus(
  periodLogs: PeriodLog[], settings: CycleSettings, referenceDate?: Date
): { currentPhase: CyclePhase; cycleDay: number; ... } | undefined;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend AppSettings and create notification domain + service modules</name>
  <files>
    src/repositories/SettingsRepo.ts,
    src/domain/notifications/notification-copy.ts,
    src/services/notificationService.ts,
    src/hooks/useNotificationPermission.ts
  </files>
  <behavior>
    - AppSettings has restTimerAlertsEnabled (default true), workoutRemindersEnabled (default false), wodAlertsEnabled (default true), subscriptionAlertsEnabled (default true), reminderHour (default 7), reminderMinute (default 0)
    - notificationsEnabled boolean is removed from AppSettings
    - DEFAULT_SETTINGS reflects all new defaults
    - getCycleAwareCopy('menstrual') returns string containing "Menstrual"
    - getCycleAwareCopy('follicular') returns string containing "follicular" (case insensitive)
    - getCycleAwareCopy('ovulation') returns string referencing peak/energy
    - getCycleAwareCopy('luteal') returns string referencing "listen to your body" or similar
    - getGenericCopy() returns a string from the GENERIC_COPY array (non-empty)
    - GENERIC_COPY has 4+ variations
    - scheduleDailyReminder(hour, minute, cyclePhase) cancels existing ID from AsyncStorage, schedules new DailyTrigger with repeats:true, stores new ID
    - scheduleDailyReminder with null cyclePhase uses getGenericCopy()
    - cancelDailyReminder() cancels stored ID and removes from AsyncStorage
    - registerFCMToken(uid, isGuest) skips when Platform.OS === 'web'
    - registerFCMToken(uid, isGuest) skips when isGuest === true
    - registerFCMToken writes fcmToken and fcmTokenUpdatedAt to /users/{uid} with merge:true
    - useNotificationPermission returns { status, isGranted, isDenied, checkPermission }
  </behavior>
  <action>
    1. **Extend AppSettings in src/repositories/SettingsRepo.ts:**
       - Remove `notificationsEnabled: boolean` field
       - Add: `restTimerAlertsEnabled: boolean` (default true), `workoutRemindersEnabled: boolean` (default false), `wodAlertsEnabled: boolean` (default true), `subscriptionAlertsEnabled: boolean` (default true), `reminderHour: number` (default 7), `reminderMinute: number` (default 0)
       - Update DEFAULT_SETTINGS to match
       - No changes needed to FirestoreSettingsRepo or LocalSettingsRepo — they serialize the full AppSettings object. Old Firestore docs missing new fields get defaults via `{ ...DEFAULT_SETTINGS, ...stored }` spread in consumers.

    2. **Create src/domain/notifications/notification-copy.ts** (pure domain, zero dependencies):
       - Export `CYCLE_COPY: Record<CyclePhase, string>` with one-line encouraging copy per phase:
         - menstrual: "Menstrual phase — gentle movement counts. You've got this."
         - follicular: "Follicular phase — great day for strength PRs."
         - ovulation: "Peak energy today — make it count."
         - luteal: "Luteal phase — listen to your body. Consistent beats heroic."
       - Export `GENERIC_COPY: string[]` with 4 variations: "Time to train.", "Your workout is waiting.", "Show up today.", "Strength is built one session at a time."
       - Export `getCycleAwareCopy(phase: CyclePhase): string` — returns CYCLE_COPY[phase]
       - Export `getGenericCopy(): string` — returns random element from GENERIC_COPY
       - Import CyclePhase from `../../domain/types`

    3. **Create src/services/notificationService.ts:**
       - Import * as Notifications from 'expo-notifications', AsyncStorage, Platform
       - Import getCycleAwareCopy, getGenericCopy from domain
       - Constants: `DAILY_REMINDER_KEY = '@sundee/daily_reminder_id'`
       - Export `async function scheduleDailyReminder(hour: number, minute: number, cyclePhase: CyclePhase | null): Promise<void>`:
         - Cancel existing: read AsyncStorage DAILY_REMINDER_KEY, cancel if exists
         - Build body using cyclePhase ? getCycleAwareCopy(cyclePhase) : getGenericCopy()
         - Schedule with DailyTriggerInput: `{ type: SchedulableTriggerInputTypes.DAILY, hour, minute, repeats: true }`
         - Content: title "Time to train", body from above, sound: true, android channelId 'reminders'
         - Store returned ID in AsyncStorage
       - Export `async function cancelDailyReminder(): Promise<void>`:
         - Read and cancel stored ID, remove from AsyncStorage
       - Export `async function registerFCMToken(uid: string, isGuest: boolean): Promise<void>`:
         - Guard: if Platform.OS === 'web' return
         - Guard: if isGuest return
         - try/catch: require('@react-native-firebase/messaging').default
         - Get token via messaging().getToken()
         - If token, write to Firestore /users/{uid} with merge: true: { fcmToken: token, fcmTokenUpdatedAt: new Date().toISOString() }
         - Use getFirestoreInstance() from src/firebase/firestore (consistent with existing pattern)
         - Warn on error, do not throw

    4. **Create src/hooks/useNotificationPermission.ts:**
       - Uses expo-notifications getPermissionsAsync
       - Returns `{ status: 'granted' | 'denied' | 'undetermined', isGranted: boolean, isDenied: boolean, checkPermission: () => Promise<void> }`
       - Checks permission on mount and exposes checkPermission for re-checking (useFocusEffect in consumers)
  </action>
  <verify>
    <automated>npx jest --passWithNoTests src/repositories/__tests__/SettingsRepo.test.ts src/domain/__tests__/notification-copy.test.ts src/services/__tests__/notificationService.test.ts -x</automated>
  </verify>
  <done>
    - AppSettings has 6 new fields, notificationsEnabled removed, DEFAULT_SETTINGS correct
    - notification-copy module exports 4 cycle copy strings and 4+ generic variations
    - notificationService exports scheduleDailyReminder, cancelDailyReminder, registerFCMToken
    - useNotificationPermission hook exports permission status
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Update existing tests for AppSettings schema change</name>
  <files>
    src/hooks/__tests__/useRestTimer.test.ts,
    src/repositories/__tests__/SettingsRepo.test.ts
  </files>
  <action>
    1. **Update src/hooks/__tests__/useRestTimer.test.ts:**
       - The test at line 175 asserts `expect(Notifications.requestPermissionsAsync).toHaveBeenCalled()` — this assertion must be REMOVED because the eager permission request is being removed from useRestTimer in Plan 02. For now, just remove the permission-related assertions so the test doesn't break when Plan 02 removes the code. If there's a dedicated "requests notification permission" test case, remove the entire test case.

    2. **Update existing SettingsRepo tests** (if they reference `notificationsEnabled`):
       - Search for `notificationsEnabled` references and update to use the new granular fields
       - Add test cases for new DEFAULT_SETTINGS values (all 6 new fields)

    3. **Run full test suite** to confirm no regressions from the AppSettings interface change. Any test referencing the old `notificationsEnabled` field will fail — fix all occurrences.
  </action>
  <verify>
    <automated>npx jest --passWithNoTests -x</automated>
  </verify>
  <done>
    - Full test suite passes with no references to old notificationsEnabled field
    - useRestTimer tests no longer assert eager permission request
    - SettingsRepo tests validate all new AppSettings defaults
  </done>
</task>

</tasks>

<verification>
- `npx jest --passWithNoTests -x` — full suite green
- `grep -r "notificationsEnabled" src/ app/ --include="*.ts" --include="*.tsx"` returns zero hits (old field fully removed)
- `grep -r "restTimerAlertsEnabled" src/repositories/SettingsRepo.ts` confirms new field exists
- `grep -r "scheduleDailyReminder" src/services/notificationService.ts` confirms export exists
</verification>

<success_criteria>
- AppSettings interface extended with 6 new fields; old notificationsEnabled removed everywhere
- notification-copy domain module exports cycle-aware and generic copy functions
- notificationService exports schedule/cancel/registerFCM functions
- useNotificationPermission hook exports permission status
- All tests pass including updated existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/20-notification-infrastructure/20-01-SUMMARY.md`
</output>
