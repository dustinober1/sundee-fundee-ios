---
phase: 01-critical-bug-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - _legacy-swift/SundeeFundee/Repositories/Gemini/GeminiPromptBuilder.swift
  - _legacy-swift/SundeeFundee/Repositories/Gemini/GeminiResponseParser.swift
  - _legacy-swift/SundeeFundee/Domain/AIWorkout/GeneratedWorkout.swift
  - _legacy-swift/SundeeFundeTests/GeminiPromptBuilderTests.swift
  - _legacy-swift/SundeeFundeTests/GeminiResponseParserTests.swift
autonomous: true
requirements:
  - FIX-01

must_haves:
  truths:
    - "When weightUnit is kg, the system prompt prescribes kg equipment lists (20kg bar, metric plates, metric dumbbells/kettlebells)"
    - "When weightUnit is kg, known maxes are converted from stored lbs to kg before embedding in the prompt"
    - "When weightUnit is kg, body weight is converted to kg in the user prompt"
    - "The Gemini response schema field is named 'weight' (not 'weightLb') and includes a 'weightUnit' field"
    - "GeminiResponseParser and GeneratedWorkout model use 'weight' and 'weightUnit' instead of 'weightLb'"
  artifacts:
    - path: "_legacy-swift/SundeeFundee/Repositories/Gemini/GeminiPromptBuilder.swift"
      provides: "Unit-parameterized system prompt and user prompt with max/body weight conversion"
      contains: "systemPrompt(weightUnit:"
    - path: "_legacy-swift/SundeeFundee/Repositories/Gemini/GeminiResponseParser.swift"
      provides: "Parsing 'weight' and 'weightUnit' fields from Gemini response"
      contains: "weightUnit"
    - path: "_legacy-swift/SundeeFundee/Domain/AIWorkout/GeneratedWorkout.swift"
      provides: "GeneratedWorkout model with weight + weightUnit properties"
      contains: "weightUnit"
  key_links:
    - from: "GeminiPromptBuilder.systemPrompt(weightUnit:)"
      to: "WorkoutGenerationContext.weightUnit"
      via: "Unit passed from context to prompt builder"
      pattern: "systemPrompt\\(weightUnit:"
    - from: "GeminiResponseParser"
      to: "GeneratedWorkout"
      via: "Parses weight and weightUnit from JSON response"
      pattern: "weight.*weightUnit"
---

<objective>
Fix AI workout generation to prescribe weights in the user's selected unit (FIX-01). Parameterize the system prompt with unit-specific equipment lists, convert maxes and body weight, and rename the response schema field from `weightLb` to `weight` with an accompanying `weightUnit` field.

Purpose: kg users currently receive lbs-based prescriptions with imperial equipment values. This makes AI workouts unusable for metric users.
Output: Unit-aware prompt builder, cleaned response schema, updated parser and model.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-critical-bug-fixes/01-CONTEXT.md
@.planning/phases/01-critical-bug-fixes/01-RESEARCH.md

@_legacy-swift/SundeeFundee/Repositories/Gemini/GeminiPromptBuilder.swift
@_legacy-swift/SundeeFundee/Repositories/Gemini/GeminiResponseParser.swift
@_legacy-swift/SundeeFundee/Domain/AIWorkout/GeneratedWorkout.swift
@_legacy-swift/SundeeFundee/Domain/AIWorkout/WorkoutGenerationContext.swift
@_legacy-swift/SundeeFundee/Domain/Calculations/WeightUnitConversion.swift
@_legacy-swift/SundeeFundeTests/GeminiPromptBuilderTests.swift
@_legacy-swift/SundeeFundeTests/GeminiResponseParserTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize system prompt and user prompt by weight unit (FIX-01 core)</name>
  <files>_legacy-swift/SundeeFundee/Repositories/Gemini/GeminiPromptBuilder.swift</files>
  <action>
**Replace static `systemPrompt` with `systemPrompt(weightUnit:)` function:**

Per the locked decision: maintain SEPARATE kg and lbs equipment lists with real gym values (do NOT convert the lbs list mathematically).

```swift
static func systemPrompt(weightUnit: String) -> String {
    let isKg = weightUnit.lowercased().contains("kg")
    // ... build prompt with unit-specific sections
}
```

**Kg equipment values (real metric gym equipment):**
- Bar: Men 20 kg, Women 15 kg
- Plates per side: 25, 20, 15, 10, 5, 2.5, 1.25 kg
- Dumbbells: 4, 6, 8, 10, 12, 14, 16, 20, 24, 28, 32 kg
- Kettlebells: 8, 12, 16, 20, 24, 28, 32 kg

**Lbs equipment values (keep existing):**
- Bar: Men 45 lb, Women 35 lb
- Plates per side: 45, 35, 25, 10, 5, 2.5 lb
- Dumbbells: existing lbs list from current prompt
- Kettlebells: existing lbs list from current prompt

**Update the response schema** in `GeminiPromptBuilder.responseSchema` (or wherever it defines the JSON schema sent to Gemini):
- Rename `"weightLb"` to `"weight"` (type: number)
- Add `"weightUnit"` (type: string) to the schema
- Update any descriptions to say "weight in the prescribed unit" instead of "weight in pounds"

**Update `userPrompt(from:)` to convert maxes and body weight:**

When `context.weightUnit` is kg:
1. Convert each `max.weightLb` to kg using `WeightUnitConversion` (divide by `poundsPerKilogram` constant, round to nearest integer)
2. Label as "kg" instead of "lb"
3. Convert body weight similarly if it's embedded in the prompt

Find all callers of `systemPrompt` (it was a static property, now a function) and update them to pass `context.weightUnit`. The caller is likely in `GeminiWorkoutService` or wherever the API request is built.
  </action>
  <verify>
    <automated>cd /Users/dustinober/Projects/Sundee-Fundee/_legacy-swift && grep -c "weightLb" SundeeFundee/Repositories/Gemini/GeminiPromptBuilder.swift</automated>
    Expect 0 — no remaining `weightLb` references in the prompt builder.
  </verify>
  <done>
- systemPrompt is a function accepting weightUnit, with separate native equipment lists for lbs and kg
- userPrompt converts maxes and body weight to the user's selected unit
- Response schema requests "weight" and "weightUnit" instead of "weightLb"
- All callers of systemPrompt updated to pass weightUnit
  </done>
</task>

<task type="auto">
  <name>Task 2: Update response parser, model, and tests for weight/weightUnit fields (FIX-01 completion)</name>
  <files>_legacy-swift/SundeeFundee/Repositories/Gemini/GeminiResponseParser.swift, _legacy-swift/SundeeFundee/Domain/AIWorkout/GeneratedWorkout.swift, _legacy-swift/SundeeFundeTests/GeminiPromptBuilderTests.swift, _legacy-swift/SundeeFundeTests/GeminiResponseParserTests.swift</files>
  <action>
**GeneratedWorkout.swift:**
- Rename the `weightLb` property to `weight` (type: Double or Int, matching current type)
- Add `weightUnit: String` property (defaults to "lb" for backward compatibility with any stored records)
- Update any `CodingKeys` if present
- Search the ENTIRE codebase for references to `weightLb` on GeneratedWorkout and update them all. Check: display views (WorkoutPreviewView, WorkoutExecutionView, etc.), any view models, any mappers.

**GeminiResponseParser.swift:**
- Update JSON key parsing from `"weightLb"` to `"weight"`
- Parse new `"weightUnit"` field from the response
- Handle backward compatibility: if `"weightLb"` is present but `"weight"` is not (cached/old responses), fall back to reading `"weightLb"` with unit "lb"

**GeminiPromptBuilderTests.swift — Add kg-specific tests:**
1. Test: `systemPrompt(weightUnit: "kg")` contains "20 kg bar" and does NOT contain "45 lb bar"
2. Test: `systemPrompt(weightUnit: "lb")` contains "45 lb bar" and does NOT contain "20 kg bar"
3. Test: `userPrompt` with kg unit converts a 225 lb max to approximately 102 kg
4. Test: `userPrompt` with lb unit keeps 225 lb max as-is
5. Test: response schema contains "weight" and "weightUnit" (not "weightLb")

**GeminiResponseParserTests.swift — Update/add tests:**
1. Update existing tests that reference `weightLb` in mock JSON to use `weight` + `weightUnit`
2. Add test: parser handles response with `weight` and `weightUnit: "kg"`
3. Add test: parser handles backward-compatible response with `weightLb` (no `weightUnit`)

**Codebase-wide `weightLb` sweep:**
After the above changes, search the entire `_legacy-swift/SundeeFundee/` directory for remaining `weightLb` references. Any display code that reads `exercise.weightLb` must be updated to `exercise.weight`. The display should already use the unit from the model (or from user settings) for formatting.
  </action>
  <verify>
    <automated>cd /Users/dustinober/Projects/Sundee-Fundee/_legacy-swift && xcodebuild test -scheme SundeeFundee -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:SundeeFundeTests/GeminiPromptBuilderTests -only-testing:SundeeFundeTests/GeminiResponseParserTests 2>&1 | tail -20</automated>
  </verify>
  <done>
- GeneratedWorkout uses `weight` + `weightUnit` instead of `weightLb`
- GeminiResponseParser parses `weight` and `weightUnit` with backward compatibility
- All codebase references to `weightLb` on GeneratedWorkout are updated
- New test cases verify kg prompt, kg max conversion, and updated response parsing
- All GeminiPromptBuilder and GeminiResponseParser tests pass
  </done>
</task>

</tasks>

<verification>
- `grep -rn "weightLb" _legacy-swift/SundeeFundee/` returns 0 results (no remaining weightLb references in production code)
- `grep -c "systemPrompt(weightUnit:" _legacy-swift/SundeeFundee/Repositories/Gemini/GeminiPromptBuilder.swift` returns >= 1
- GeminiPromptBuilderTests and GeminiResponseParserTests all pass
</verification>

<success_criteria>
1. System prompt uses native kg equipment list when user's unit is kg
2. Known maxes are converted to kg in the user prompt when unit is kg
3. Response schema field is "weight" + "weightUnit" (not "weightLb")
4. All references to weightLb across the codebase are updated
5. Tests verify both lbs and kg paths
</success_criteria>

<output>
After completion, create `.planning/phases/01-critical-bug-fixes/01-03-SUMMARY.md`
</output>
