---
phase: 01-critical-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _legacy-swift/SundeeFundee/App/AppModelContainer.swift
  - _legacy-swift/SundeeFundee/Services/SubscriptionService.swift
  - _legacy-swift/SundeeFundeTests/SubscriptionServiceTests.swift
autonomous: true
requirements:
  - FIX-04
  - FIX-05

must_haves:
  truths:
    - "App boots without crash when SwiftData migration is needed on the local persistent store path"
    - "SubscriptionService.currentTier is .free immediately after init(), regardless of any UserDefaults value"
    - "StoreKit verification upgrades tier to premium only after async loadStatus() completes"
  artifacts:
    - path: "_legacy-swift/SundeeFundee/App/AppModelContainer.swift"
      provides: "migrationPlan parameter on localPersistent case"
      contains: "migrationPlan: AppSchemaMigrationPlan.self"
    - path: "_legacy-swift/SundeeFundee/Services/SubscriptionService.swift"
      provides: "Init defaults to .free with no UserDefaults read"
  key_links:
    - from: "AppModelContainer.makeContainer(.localPersistent)"
      to: "AppSchemaMigrationPlan"
      via: "migrationPlan parameter"
      pattern: "migrationPlan:\\s*AppSchemaMigrationPlan\\.self"
    - from: "SubscriptionService.init()"
      to: "loadStatus()"
      via: "startObservingTransactions only, no UserDefaults"
      pattern: "init\\(\\).*startObservingTransactions"
---

<objective>
Fix two app initialization bugs: SwiftData migration plan missing on local store path (FIX-05), and SubscriptionService reading cached tier from UserDefaults on cold launch (FIX-04).

Purpose: Both bugs affect app boot correctness. FIX-05 can cause a crash if migration is needed on the local store. FIX-04 creates a brief premium access window before StoreKit verification completes.
Output: Two corrected initialization paths with updated tests.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-critical-bug-fixes/01-CONTEXT.md
@.planning/phases/01-critical-bug-fixes/01-RESEARCH.md

@_legacy-swift/SundeeFundee/App/AppModelContainer.swift
@_legacy-swift/SundeeFundee/Services/SubscriptionService.swift
@_legacy-swift/SundeeFundeTests/SubscriptionServiceTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add migrationPlan to local persistent store path (FIX-05)</name>
  <files>_legacy-swift/SundeeFundee/App/AppModelContainer.swift</files>
  <action>
In `AppModelContainer.swift`, find the `makeContainer` function's `.localPersistent` case (around line 95-96). It currently creates a `ModelContainer` WITHOUT the `migrationPlan:` parameter. Add `migrationPlan: AppSchemaMigrationPlan.self` to match the `.cloudKit` case (line 93).

Before (approximate):
```swift
case .localPersistent:
    let localConfig = ModelConfiguration(schema: schema, isStoredInMemoryOnly: false, cloudKitDatabase: .none)
    return try ModelContainer(for: schema, configurations: [localConfig])
```

After:
```swift
case .localPersistent:
    let localConfig = ModelConfiguration(schema: schema, isStoredInMemoryOnly: false, cloudKitDatabase: .none)
    return try ModelContainer(for: schema, migrationPlan: AppSchemaMigrationPlan.self, configurations: [localConfig])
```

This is a one-line parameter addition. Do not change any other code in this file.
  </action>
  <verify>
    <automated>cd /Users/dustinober/Projects/Sundee-Fundee/_legacy-swift && grep -n "migrationPlan" SundeeFundee/App/AppModelContainer.swift | wc -l</automated>
    Expect 2 occurrences (one for cloudKit case, one for localPersistent case). Before the fix there was only 1.
  </verify>
  <done>Both `.cloudKit` and `.localPersistent` cases in `makeContainer` include `migrationPlan: AppSchemaMigrationPlan.self`. The local persistent store path will apply migrations on schema version changes instead of failing.</done>
</task>

<task type="auto">
  <name>Task 2: Remove UserDefaults tier cache from SubscriptionService init (FIX-04)</name>
  <files>_legacy-swift/SundeeFundee/Services/SubscriptionService.swift, _legacy-swift/SundeeFundeTests/SubscriptionServiceTests.swift</files>
  <action>
**In `SubscriptionService.swift`:**

1. Find `init()` (around line 69-70). Remove the `UserDefaults.standard.string(forKey:...)` read that sets `currentTier` from cached value. The `currentTier` property should start as `.free` (its default property value). The init should only call `startObservingTransactions()`.

After fix, init should look like:
```swift
init() {
    // currentTier starts as .free from property declaration default
    // StoreKit verification via loadStatus() is the single source of truth
    startObservingTransactions()
}
```

2. Do NOT remove `UserDefaults` writes in `setTier` or elsewhere -- only remove the READ in `init()`. Other code may use the cached value for analytics or non-gating purposes.

**In `SubscriptionServiceTests.swift`:**

1. Find the test `restoresTierFromUserDefaults` (or similar name that tests UserDefaults cache restoration). DELETE this entire test -- the behavior it validates is intentionally removed.
2. Verify the existing `defaultsToFree` test (or equivalent) still makes sense -- it should confirm that a freshly created `SubscriptionService` has `.free` tier. Keep this test.
3. If any other tests set UserDefaults before creating SubscriptionService expecting it to read, update them to not depend on that behavior.
  </action>
  <verify>
    <automated>cd /Users/dustinober/Projects/Sundee-Fundee/_legacy-swift && xcodebuild test -scheme SundeeFundee -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:SundeeFundeTests/SubscriptionServiceTests 2>&1 | tail -20</automated>
  </verify>
  <done>SubscriptionService.init() no longer reads UserDefaults for tier. Cold launch always starts at .free tier. The `restoresTierFromUserDefaults` test is deleted. Remaining SubscriptionService tests pass.</done>
</task>

</tasks>

<verification>
- `grep -n "migrationPlan" _legacy-swift/SundeeFundee/App/AppModelContainer.swift` shows 2 lines
- `grep -n "UserDefaults" _legacy-swift/SundeeFundee/Services/SubscriptionService.swift` does NOT appear in `init()`
- SubscriptionServiceTests pass without `restoresTierFromUserDefaults`
</verification>

<success_criteria>
1. AppModelContainer applies migration plan to BOTH store paths (cloudKit and localPersistent)
2. SubscriptionService.init() defaults to .free without reading UserDefaults
3. All SubscriptionService tests pass after removing the stale cache test
</success_criteria>

<output>
After completion, create `.planning/phases/01-critical-bug-fixes/01-01-SUMMARY.md`
</output>
