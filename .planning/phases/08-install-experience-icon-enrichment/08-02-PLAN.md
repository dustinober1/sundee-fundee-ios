---
phase: 08-install-experience-icon-enrichment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard/offline-banner.tsx
  - src/components/dashboard/active-cycles-card.tsx
  - src/components/dashboard/cycle-widget.tsx
  - src/components/program/workout-session-view.tsx
  - src/components/program/exercise-card-v2.tsx
  - src/components/layout/bottom-navigation.tsx
autonomous: true

must_haves:
  truths:
    - "OfflineBanner displays WifiOff Lucide icon with no inline SVG remaining"
    - "ActiveCyclesCard header shows Trophy icon next to title"
    - "CycleWidget header shows Flame icon; progress/focus area shows BarChart2"
    - "ExerciseCardV2 header shows Dumbbell icon; prescribed info shows Target icon"
    - "WorkoutSessionView Complete button shows CircleCheck icon; session header shows AlarmClockCheck"
    - "Bottom navigation Workout tab uses Activity icon (not ClipboardPlus)"
  artifacts:
    - path: "src/components/dashboard/offline-banner.tsx"
      provides: "WifiOff Lucide icon replacing inline SVG"
      contains: "WifiOff"
    - path: "src/components/dashboard/active-cycles-card.tsx"
      provides: "Trophy icon in card header"
      contains: "Trophy"
    - path: "src/components/dashboard/cycle-widget.tsx"
      provides: "Flame and BarChart2 icons"
      contains: "Flame"
    - path: "src/components/program/exercise-card-v2.tsx"
      provides: "Dumbbell and Target icons"
      contains: "Dumbbell"
    - path: "src/components/program/workout-session-view.tsx"
      provides: "CircleCheck and AlarmClockCheck icons"
      contains: "CircleCheck"
    - path: "src/components/layout/bottom-navigation.tsx"
      provides: "Activity icon for Workout tab"
      contains: "Activity"
  key_links:
    - from: "src/components/dashboard/offline-banner.tsx"
      to: "lucide-react"
      via: "named import"
      pattern: "import.*WifiOff.*from 'lucide-react'"
    - from: "src/components/layout/bottom-navigation.tsx"
      to: "lucide-react"
      via: "named import"
      pattern: "import.*Activity.*from 'lucide-react'"
---

<objective>
Enrich all major UI surfaces with consistent, meaningful Lucide icons — replacing the inline SVG in OfflineBanner, adding contextual icons to dashboard cards, adding fitness-specific icons to workout/logging components, and updating bottom navigation for semantic alignment.

Purpose: ICON-01 through ICON-04 — the app currently has sparse icon usage. This plan adds a consistent visual vocabulary so users can scan UI surfaces and immediately understand purpose.
Output: Six existing files modified with Lucide icon imports. No new files created.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-install-experience-icon-enrichment/08-RESEARCH.md
@src/components/dashboard/offline-banner.tsx
@src/components/dashboard/active-cycles-card.tsx
@src/components/dashboard/cycle-widget.tsx
@src/components/program/exercise-card-v2.tsx
@src/components/program/workout-session-view.tsx
@src/components/layout/bottom-navigation.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: ICON-01 + ICON-02 — OfflineBanner WifiOff + Dashboard card icons</name>
  <files>
    src/components/dashboard/offline-banner.tsx
    src/components/dashboard/active-cycles-card.tsx
    src/components/dashboard/cycle-widget.tsx
  </files>
  <action>
**ICON-01: OfflineBanner (`src/components/dashboard/offline-banner.tsx`)**
1. Add import: `import { WifiOff } from 'lucide-react';`
2. Delete the entire inline `<svg>...</svg>` block (the wifi-off icon comment through closing `</svg>` tag — approximately lines 12–23 in current file).
3. Replace it with: `<WifiOff className="h-4 w-4 shrink-0" aria-hidden="true" />`
4. No other changes to the component. Keep all existing Tailwind classes on the wrapper div.

**ICON-02: ActiveCyclesCard (`src/components/dashboard/active-cycles-card.tsx`)**
1. Add import: `import { Trophy } from 'lucide-react';`
2. In BOTH `<CardTitle>` instances (the empty-state card and the active-programs card), wrap the text in a flex container with the Trophy icon:
   - Change `<CardTitle>Active Programs</CardTitle>` to:
     ```
     <CardTitle className="flex items-center gap-2">
       <Trophy className="h-5 w-5 text-yellow-500" />
       Active Programs
     </CardTitle>
     ```
3. Do this for both the empty-state return block and the main return block (there are exactly 2 `<CardTitle>` elements).

**ICON-02: CycleWidget (`src/components/dashboard/cycle-widget.tsx`)**
1. Add `Flame` and `BarChart2` to the existing `import { Calendar, ArrowRight } from 'lucide-react';` line — making it `import { Calendar, ArrowRight, Flame, BarChart2 } from 'lucide-react';`.
2. In the active cycle view (second return block), change the `<CardTitle className="text-lg">Cycle Phase</CardTitle>` to:
   ```
   <CardTitle className="text-lg flex items-center gap-2">
     <Flame className="h-5 w-5 text-orange-500" />
     Cycle Phase
   </CardTitle>
   ```
3. In the empty-state return block, change `<CardTitle className="text-lg">Cycle Tracking</CardTitle>` to:
   ```
   <CardTitle className="text-lg flex items-center gap-2">
     <Flame className="h-5 w-5 text-orange-500" />
     Cycle Tracking
   </CardTitle>
   ```
4. Add `BarChart2` to the "Today's Focus" heading inside the recommendation block. Change:
   `<h4 className="font-medium text-sm mb-1">Today&apos;s Focus</h4>` to:
   ```
   <h4 className="font-medium text-sm mb-1 flex items-center gap-1.5">
     <BarChart2 className="h-4 w-4 text-muted-foreground" />
     Today&apos;s Focus
   </h4>
   ```
  </action>
  <verify>
Run `npx tsc --noEmit` — no type errors.
Run `grep -n "svg" src/components/dashboard/offline-banner.tsx` — should return NO results (inline SVG fully removed).
Run `grep "WifiOff" src/components/dashboard/offline-banner.tsx` — should return the import and usage.
Run `grep "Trophy" src/components/dashboard/active-cycles-card.tsx` — should show import and 2 usages.
Run `grep "Flame" src/components/dashboard/cycle-widget.tsx` — should show import and 2 usages.
Run `grep "BarChart2" src/components/dashboard/cycle-widget.tsx` — should show import and 1 usage.
  </verify>
  <done>
OfflineBanner uses `<WifiOff>` with zero inline SVG remaining. ActiveCyclesCard has Trophy icon in both card headers. CycleWidget has Flame in both card headers and BarChart2 in "Today's Focus" heading. All compile cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: ICON-03 + ICON-04 — Workout page icons + bottom nav update</name>
  <files>
    src/components/program/exercise-card-v2.tsx
    src/components/program/workout-session-view.tsx
    src/components/layout/bottom-navigation.tsx
  </files>
  <action>
**ICON-03: ExerciseCardV2 (`src/components/program/exercise-card-v2.tsx`)**
1. Add import: `import { Dumbbell, Target } from 'lucide-react';`
2. In the `<CardTitle>` element (currently `<CardTitle className="capitalize">{displayName}</CardTitle>`), add the Dumbbell icon:
   ```
   <CardTitle className="capitalize flex items-center gap-2">
     <Dumbbell className="h-5 w-5 text-muted-foreground shrink-0" />
     {displayName}
   </CardTitle>
   ```
3. In the prescribed weight paragraph (currently `<p className="text-muted-foreground text-sm">@ {Math.round(exercise.percent1RM * 100)}% 1RM ({prescribedWeight} lbs)</p>`), add the Target icon:
   ```
   <p className="text-muted-foreground text-sm flex items-center gap-1.5">
     <Target className="h-4 w-4 shrink-0" />
     @ {Math.round(exercise.percent1RM * 100)}% 1RM ({prescribedWeight} lbs)
   </p>
   ```

**ICON-03: WorkoutSessionView (`src/components/program/workout-session-view.tsx`)**
1. Add import: `import { CircleCheck, AlarmClockCheck } from 'lucide-react';`
2. In the session header area, add AlarmClockCheck next to the session name. Change:
   ```
   <h1 className="text-2xl font-bold">{session.sessionName}</h1>
   ```
   to:
   ```
   <h1 className="text-2xl font-bold flex items-center gap-2">
     <AlarmClockCheck className="h-6 w-6 text-primary shrink-0" />
     {session.sessionName}
   </h1>
   ```
3. In the "Complete Workout" `<Button>`, add CircleCheck icon. Change:
   ```
   Complete Workout
   ```
   to:
   ```
   <CircleCheck className="h-5 w-5 mr-2" />
   Complete Workout
   ```

**ICON-04: BottomNavigation (`src/components/layout/bottom-navigation.tsx`)**
1. In the import line, replace `ClipboardPlus` with `Activity`:
   Change: `import { LayoutDashboard, Dumbbell, ClipboardPlus, TrendingUp } from 'lucide-react';`
   To: `import { LayoutDashboard, Dumbbell, Activity, TrendingUp } from 'lucide-react';`
2. In the `navItems` array, change the Workout entry:
   From: `{ href: '/workout', icon: ClipboardPlus, label: 'Workout' },`
   To: `{ href: '/workout', icon: Activity, label: 'Workout' },`

Why `Activity` not something else: `Activity` (heartbeat/ECG line) represents active exercise and is visually distinct from `Dumbbell` (used for Programs tab). `ClipboardPlus` was semantically clipboard-oriented, not fitness-oriented.
  </action>
  <verify>
Run `npx tsc --noEmit` — no type errors.
Run `npm run build` — builds successfully.
Run `npx playwright test` — all 11 E2E tests pass.
Run `grep "ClipboardPlus" src/components/layout/bottom-navigation.tsx` — should return NO results.
Run `grep "Activity" src/components/layout/bottom-navigation.tsx` — should return import and usage.
Run `grep "Dumbbell" src/components/program/exercise-card-v2.tsx` — should show import and usage.
Run `grep "CircleCheck" src/components/program/workout-session-view.tsx` — should show import and usage.
  </verify>
  <done>
ExerciseCardV2 shows Dumbbell in header and Target on prescribed weight line. WorkoutSessionView shows AlarmClockCheck in session header and CircleCheck on Complete button. Bottom nav uses Activity for Workout tab. Build succeeds. All 11 E2E tests pass.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds
3. `npx playwright test` — all 11 tests pass
4. `grep -rn "svg" src/components/dashboard/offline-banner.tsx` returns nothing (no inline SVG)
5. `grep -rn "ClipboardPlus" src/` returns nothing (fully removed)
6. All 8 required Lucide icons are imported somewhere in src/:
   - `grep -r "WifiOff\|Trophy\|Flame\|BarChart2\|Dumbbell\|AlarmClockCheck\|Target\|CircleCheck" src/ --include="*.tsx" --include="*.ts" | grep "import"` shows all 8
7. `grep -r "Activity" src/components/layout/bottom-navigation.tsx` confirms new Workout tab icon
</verification>

<success_criteria>
- ICON-01: OfflineBanner uses `<WifiOff>` — zero inline SVG
- ICON-02: Dashboard cards show Trophy (ActiveCyclesCard), Flame (CycleWidget header), BarChart2 (Today's Focus heading)
- ICON-03: ExerciseCardV2 shows Dumbbell + Target; WorkoutSessionView shows AlarmClockCheck + CircleCheck
- ICON-04: Bottom nav Workout tab uses Activity icon (ClipboardPlus fully removed)
- Build + all 11 E2E tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-install-experience-icon-enrichment/08-02-SUMMARY.md`
</output>
