---
phase: 06-subscriptions-and-monetization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - SundeeFundeeRN/src/entitlements/useEntitlements.ts
  - SundeeFundeeRN/src/entitlements/EntitlementContext.tsx
  - SundeeFundeeRN/src/entitlements/__tests__/useEntitlements.test.ts
  - SundeeFundeeRN/src/components/paywall/PaywallModal.tsx
  - SundeeFundeeRN/src/components/paywall/PremiumBadge.tsx
  - SundeeFundeeRN/src/components/paywall/__tests__/PaywallModal.test.tsx
  - SundeeFundeeRN/src/components/paywall/__tests__/PremiumBadge.test.tsx
  - SundeeFundeeRN/app/_layout.tsx
  - SundeeFundeeRN/__mocks__/react-native-purchases.ts
autonomous: true
requirements: [SUBS-01, SUBS-04]
user_setup:
  - service: revenuecat
    why: "Mobile in-app purchase entitlements"
    env_vars:
      - name: EXPO_PUBLIC_RC_APPLE_KEY
        source: "RevenueCat Dashboard -> Project -> API Keys -> Apple public API key"
      - name: EXPO_PUBLIC_RC_GOOGLE_KEY
        source: "RevenueCat Dashboard -> Project -> API Keys -> Google public API key"
    dashboard_config:
      - task: "Create 'premium' entitlement and map to App Store / Play Store subscription products"
        location: "RevenueCat Dashboard -> Project -> Entitlements -> New"
      - task: "Create Offering with monthly ($9.99) and annual ($59.99) packages"
        location: "RevenueCat Dashboard -> Project -> Offerings -> New"

must_haves:
  truths:
    - "Authenticated user on iOS/Android has Purchases.logIn(firebaseUID) called on sign-in"
    - "useEntitlements returns isPremium=true when RevenueCat reports active 'premium' entitlement on mobile"
    - "useEntitlements returns isPremium=true when Firestore premiumEntitlement.active is true on web"
    - "Entitlement status updates in real-time via addCustomerInfoUpdateListener on mobile"
    - "PaywallModal shows monthly and annual pricing with annual pre-selected as Best Value"
    - "PaywallModal shows 'Create Account to Subscribe' for guest users instead of purchase CTA"
    - "PremiumBadge component renders a lock/premium indicator for gated features"
  artifacts:
    - path: "SundeeFundeeRN/src/entitlements/useEntitlements.ts"
      provides: "Upgraded entitlement hook with listener (mobile) and Firestore read (web)"
      exports: ["useEntitlements", "EntitlementState", "PREMIUM_ENTITLEMENT_ID"]
    - path: "SundeeFundeeRN/src/entitlements/EntitlementContext.tsx"
      provides: "Context provider wrapping useEntitlements for app-wide access"
      exports: ["EntitlementProvider", "useEntitlementContext"]
    - path: "SundeeFundeeRN/src/components/paywall/PaywallModal.tsx"
      provides: "Full-screen paywall modal with offerings and purchase flow"
      exports: ["PaywallModal"]
    - path: "SundeeFundeeRN/src/components/paywall/PremiumBadge.tsx"
      provides: "Small badge/lock icon component for premium feature indicators"
      exports: ["PremiumBadge"]
  key_links:
    - from: "SundeeFundeeRN/app/_layout.tsx"
      to: "react-native-purchases"
      via: "Purchases.logIn(user.uid) in handleUserSignIn callback"
      pattern: "Purchases\\.logIn"
    - from: "SundeeFundeeRN/src/entitlements/useEntitlements.ts"
      to: "react-native-purchases"
      via: "addCustomerInfoUpdateListener for real-time mobile updates"
      pattern: "addCustomerInfoUpdateListener"
    - from: "SundeeFundeeRN/src/entitlements/useEntitlements.ts"
      to: "Firestore /users/{uid}"
      via: "onSnapshot for web entitlement state"
      pattern: "onSnapshot.*premiumEntitlement"
---

<objective>
Upgrade the entitlement infrastructure and build the paywall UI components.

Purpose: The existing useEntitlements hook is a Phase 1 stub that always returns false on web and does a one-shot check on mobile. This plan upgrades it to real-time entitlement tracking (mobile listener + web Firestore), wires RevenueCat logIn to the auth flow, and creates the PaywallModal and PremiumBadge components that Plan 03 will use to gate premium features.

Output: Working entitlement hook with real-time updates, PaywallModal with RevenueCat offerings/purchase flow, PremiumBadge component, and RevenueCat identity wired to auth.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-subscriptions-and-monetization/06-CONTEXT.md
@.planning/phases/06-subscriptions-and-monetization/06-RESEARCH.md

<interfaces>
<!-- Existing code the executor needs -->

From SundeeFundeeRN/src/entitlements/useEntitlements.ts:
```typescript
export const PREMIUM_ENTITLEMENT_ID = 'premium';
export interface EntitlementState {
  isPremium: boolean;
  isLoading: boolean;
}
export function useEntitlements(): EntitlementState;
```

From SundeeFundeeRN/src/auth/AuthContext.tsx:
```typescript
export interface Session {
  user: AuthUser | null;
  isLoading: boolean;
  isGuest: boolean;
  signOut: () => Promise<void>;
}
export function SessionProvider({ children, onUserSignIn }: SessionProviderProps): React.JSX.Element;
export function useSession(): Session;
```

From SundeeFundeeRN/app/_layout.tsx:
```typescript
function configureRevenueCat(): void; // Already calls Purchases.configure on mobile
function buildUserProfile(user: AuthUser): UserProfile;
export default function RootLayout(): React.JSX.Element;
// handleUserSignIn callback already persists user profile — add RC logIn here
```

From SundeeFundeeRN/__mocks__/react-native-purchases.ts:
```typescript
const Purchases = {
  configure: jest.fn(),
  getCustomerInfo: jest.fn().mockResolvedValue(mockCustomerInfo),
  logIn: jest.fn().mockResolvedValue({ customerInfo: mockCustomerInfo, created: false }),
  logOut: jest.fn().mockResolvedValue(mockCustomerInfo),
  purchaseProduct: jest.fn().mockResolvedValue({ customerInfo: mockCustomerInfo }),
  restorePurchases: jest.fn().mockResolvedValue(mockCustomerInfo),
  // NOTE: missing addCustomerInfoUpdateListener, getOfferings, purchasePackage — must add
};
```

From SundeeFundeeRN/src/theme/colors.ts:
```typescript
export const CREAM, NAVY, ORANGE, CREAM_LIGHT, NAVY_DARK, ORANGE_DARK, ORANGE_LIGHT, RED, GREY, GREY_LIGHT;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade useEntitlements hook and wire RC logIn to auth</name>
  <files>
    SundeeFundeeRN/src/entitlements/useEntitlements.ts,
    SundeeFundeeRN/src/entitlements/EntitlementContext.tsx,
    SundeeFundeeRN/src/entitlements/__tests__/useEntitlements.test.ts,
    SundeeFundeeRN/app/_layout.tsx,
    SundeeFundeeRN/__mocks__/react-native-purchases.ts
  </files>
  <behavior>
    - useEntitlements on mobile: initial getCustomerInfo check sets isPremium
    - useEntitlements on mobile: addCustomerInfoUpdateListener updates isPremium in real-time
    - useEntitlements on mobile: listener cleanup on unmount calls subscription.remove()
    - useEntitlements on web: reads Firestore /users/{uid} field premiumEntitlement.active via onSnapshot
    - useEntitlements on web: returns isPremium=false when premiumEntitlement is absent or active=false
    - useEntitlements returns isLoading=true initially, false after first check completes
    - EntitlementContext provides isPremium and isLoading to descendants
    - RootLayout handleUserSignIn calls Purchases.logIn(user.uid) for non-guest, non-web users
    - RootLayout handleUserSignIn does NOT call Purchases.logIn for guest users
    - RC mock includes addCustomerInfoUpdateListener, getOfferings, purchasePackage
  </behavior>
  <action>
    1. Update `SundeeFundeeRN/__mocks__/react-native-purchases.ts`:
       - Add `addCustomerInfoUpdateListener: jest.fn().mockReturnValue({ remove: jest.fn() })` to mock
       - Add `getOfferings: jest.fn().mockResolvedValue({ current: { monthly: null, annual: null } })`
       - Add `purchasePackage: jest.fn().mockResolvedValue({ customerInfo: mockCustomerInfo })`
       - Add `LOG_LEVEL: { DEBUG: 'DEBUG' }` and `setLogLevel: jest.fn()`

    2. Upgrade `SundeeFundeeRN/src/entitlements/useEntitlements.ts`:
       - Accept optional `uid: string | null` parameter (from useSession().user?.uid)
       - On web: if uid is provided, use `onSnapshot(doc(db, 'users', uid))` to listen for `premiumEntitlement.active` field changes. Import `getFirestoreWeb` from `@/src/firebase/firestore` (existing web Firestore getter). Return cleanup unsubscribe function.
       - On mobile: keep existing getCustomerInfo initial check. Add `Purchases.addCustomerInfoUpdateListener((info) => { ... })` that updates isPremium. Return `subscription.remove` cleanup.
       - Re-run effect when uid changes (add to dependency array)
       - Export `PREMIUM_ENTITLEMENT_ID` for use by PaywallModal

    3. Create `SundeeFundeeRN/src/entitlements/EntitlementContext.tsx`:
       - Create context with `{ isPremium: boolean, isLoading: boolean }`
       - `EntitlementProvider` wraps children, calls `useEntitlements(uid)` with uid from `useSession()`
       - `useEntitlementContext()` hook reads from context, throws if outside provider
       - This context avoids every premium screen needing to independently call useEntitlements

    4. Update `SundeeFundeeRN/app/_layout.tsx`:
       - In `handleUserSignIn`, after persisting user profile, add RevenueCat logIn:
         ```
         if (Platform.OS !== 'web' && !user.isAnonymous) {
           try {
             const Purchases = require('react-native-purchases').default;
             await Purchases.logIn(user.uid);
           } catch { /* RC logIn failure is non-fatal */ }
         }
         ```
       - Wrap `<Stack>` with `<EntitlementProvider>` inside `<SessionProvider>`
       - In signOut handler (via SessionProvider), add `Purchases.logOut()` for mobile non-web

    5. Write tests in `SundeeFundeeRN/src/entitlements/__tests__/useEntitlements.test.ts`:
       - Test: mobile path calls getCustomerInfo, sets isPremium based on entitlements.active
       - Test: mobile path registers addCustomerInfoUpdateListener, updates state on callback
       - Test: mobile path cleans up listener on unmount
       - Test: web path (mock Platform.OS = 'web') returns isPremium=false without calling Purchases
       - Test: isLoading transitions from true to false after check completes
       - Use renderHook from @testing-library/react-native
  </action>
  <verify>
    <automated>cd /Users/dustinober/Projects/sundee-fundee/SundeeFundeeRN && npx jest src/entitlements/ --no-coverage</automated>
  </verify>
  <done>
    - useEntitlements returns real-time entitlement status on mobile via listener
    - useEntitlements reads Firestore premiumEntitlement on web
    - EntitlementProvider provides app-wide entitlement state
    - Purchases.logIn(firebaseUID) called on non-guest auth
    - RC mock updated with all needed methods
    - All entitlement tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build PaywallModal and PremiumBadge components</name>
  <files>
    SundeeFundeeRN/src/components/paywall/PaywallModal.tsx,
    SundeeFundeeRN/src/components/paywall/PremiumBadge.tsx,
    SundeeFundeeRN/src/components/paywall/__tests__/PaywallModal.test.tsx,
    SundeeFundeeRN/src/components/paywall/__tests__/PremiumBadge.test.tsx
  </files>
  <behavior>
    - PaywallModal renders as full-screen modal when visible=true
    - PaywallModal displays 4 premium feature descriptions: AI Workouts, Cycle Adaptation, Program Access, Injury Adaptation
    - PaywallModal fetches RevenueCat offerings on mount (mobile) and displays monthly/annual pricing
    - PaywallModal pre-selects annual plan with "Best Value" / "Save 50%" badge
    - PaywallModal shows "Start 7-Day Free Trial" CTA text
    - PaywallModal calls Purchases.purchasePackage on subscribe tap (mobile)
    - PaywallModal shows "Create Account to Subscribe" when user is guest, routes to sign-in on tap
    - PaywallModal on web shows Stripe pricing ($7.99/mo, $47.99/yr) and calls createCheckoutSession Cloud Function on subscribe
    - PaywallModal calls onDismiss when close button tapped
    - PaywallModal calls onSubscribed after successful purchase
    - PremiumBadge renders small lock icon or "Premium" text badge
    - PremiumBadge accepts optional compact prop for inline usage
  </behavior>
  <action>
    1. Create `SundeeFundeeRN/src/components/paywall/PremiumBadge.tsx`:
       - Simple component with lock icon text and "Premium" label
       - Props: `{ compact?: boolean }` — compact shows just the lock icon, full shows "Premium" text + icon
       - Art Deco styling: ORANGE background tint (ORANGE_LIGHT), ORANGE text, small rounded badge
       - Export as named export

    2. Create `SundeeFundeeRN/src/components/paywall/PaywallModal.tsx`:
       - Props: `{ visible: boolean; onDismiss: () => void; onSubscribed: () => void }`
       - Use React Native `Modal` with `animationType="slide"` and `transparent`
       - Layout: Full-screen overlay with Art Deco styling (NAVY gradient-like background via solid color, CREAM content card)
       - Header: "Unlock Premium" title in CREAM on NAVY background
       - Feature list: 4 items with icon (text emoji or simple text icon) and one-liner description per CONTEXT.md:
         - AI Workouts: "Personalized AI-generated workouts"
         - Cycle Adaptation: "Auto-adjust training to your cycle"
         - Programs: "Access the full program catalog"
         - Injury Adaptation: "Smart exercise substitutions"
       - Pricing section:
         - On mobile: fetch `Purchases.getOfferings()` on mount. Show monthly and annual packages with localized prices from the offering. If offerings unavailable, show hardcoded $9.99/mo and $59.99/yr.
         - On web: show $7.99/mo and $47.99/yr (per CONTEXT.md pricing decision)
         - Annual card pre-selected with "Best Value - Save 50%" badge
         - Monthly card shown but visually de-emphasized (smaller, no badge)
       - CTA button:
         - If guest (check `useSession().isGuest`): show "Create Account to Subscribe" in ORANGE, onPress routes to `/sign-in`
         - If mobile authenticated: "Start 7-Day Free Trial" button, calls `Purchases.purchasePackage(selectedPackage)`. On success check `customerInfo.entitlements.active` for premium, call `onSubscribed()`. Handle `userCancelled` error silently.
         - If web authenticated: "Start 7-Day Free Trial" button, calls `createCheckoutSession` Cloud Function via `httpsCallable` from Firebase, gets back `{ url }`, opens URL with `Linking.openURL(url)` or `window.open(url)` on web. Pass `successUrl: 'https://sundeefundee.com/subscription/success'` and `cancelUrl: window.location.href` (web) or deep link back.
       - Close button: X in top-right corner, calls `onDismiss`
       - Platform-only pricing shown (no cross-platform pricing per App Store guideline)

    3. Write tests:
       - `PaywallModal.test.tsx`:
         - Renders feature list with 4 premium features
         - Shows annual plan pre-selected with "Best Value" badge
         - Shows "Create Account to Subscribe" for guest users
         - Calls onDismiss when close button pressed
         - Calls purchasePackage when subscribe tapped (mock successful purchase)
         - Calls onSubscribed after successful purchase
       - `PremiumBadge.test.tsx`:
         - Renders "Premium" text in default mode
         - Renders compact badge without full text
  </action>
  <verify>
    <automated>cd /Users/dustinober/Projects/sundee-fundee/SundeeFundeeRN && npx jest src/components/paywall/ --no-coverage</automated>
  </verify>
  <done>
    - PaywallModal shows Art Deco styled paywall with 4 premium features, pricing cards, and purchase flow
    - Annual plan pre-selected with "Best Value" badge per user decision
    - Guest users see "Create Account to Subscribe" per user decision
    - PremiumBadge renders lock/premium indicator for inline use
    - All paywall component tests pass
  </done>
</task>

</tasks>

<verification>
- `cd /Users/dustinober/Projects/sundee-fundee/SundeeFundeeRN && npx jest src/entitlements/ src/components/paywall/ --no-coverage` passes all tests
- useEntitlements has real-time listener on mobile and Firestore read on web
- Purchases.logIn wired in auth flow
- PaywallModal handles mobile (RevenueCat) and web (Stripe checkout) purchase flows
</verification>

<success_criteria>
- useEntitlements returns accurate real-time entitlement status on all platforms
- PaywallModal correctly presents premium features and handles purchase on mobile and web
- Guest users see account creation prompt instead of purchase CTA
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-subscriptions-and-monetization/06-02-SUMMARY.md`
</output>
