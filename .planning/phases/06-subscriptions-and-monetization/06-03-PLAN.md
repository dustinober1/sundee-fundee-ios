---
phase: 06-subscriptions-and-monetization
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - SundeeFundeeRN/app/(app)/(tabs)/settings.tsx
  - SundeeFundeeRN/app/(app)/(tabs)/__tests__/settings.test.tsx
  - SundeeFundeeRN/src/components/paywall/TrialBanner.tsx
  - SundeeFundeeRN/src/components/paywall/TrialEndedModal.tsx
  - SundeeFundeeRN/src/components/paywall/__tests__/TrialBanner.test.tsx
  - SundeeFundeeRN/src/components/paywall/__tests__/TrialEndedModal.test.tsx
  - SundeeFundeeRN/app/(app)/(tabs)/dashboard.tsx
  - SundeeFundeeRN/app/(app)/ai-workout/config.tsx
  - SundeeFundeeRN/app/(app)/(tabs)/programs/index.tsx
  - SundeeFundeeRN/app/(app)/(tabs)/cycle.tsx
  - SundeeFundeeRN/app/(app)/injuries/[id].tsx
autonomous: true
requirements: [SUBS-04, SUBS-05, SUBS-03, SUBS-01]
must_haves:
  truths:
    - "Free user tapping AI workout config sees PaywallModal"
    - "Free user tapping program catalog sees PaywallModal"
    - "Free user sees lock badge on cycle adaptation toggle"
    - "Free user sees lock badge on injury adaptation section"
    - "Subscribed user accesses all premium features without paywall"
    - "Settings shows Subscription section with plan name, renewal date, and Manage link for subscribed users"
    - "Settings shows Restore Purchases button on mobile"
    - "Days 6-7 of trial shows subtle dismissable banner on dashboard"
    - "Trial-ended modal shows once on first app launch after expiry"
    - "Manage Subscription deep-links to App Store on iOS, Play Store on Android, Stripe Portal on web"
  artifacts:
    - path: "SundeeFundeeRN/app/(app)/(tabs)/settings.tsx"
      provides: "Subscription management section in settings"
      contains: "Subscription"
    - path: "SundeeFundeeRN/app/(app)/(tabs)/__tests__/settings.test.tsx"
      provides: "Unit tests for settings subscription management"
    - path: "SundeeFundeeRN/src/components/paywall/TrialBanner.tsx"
      provides: "Subtle trial countdown banner for days 6-7"
      exports: ["TrialBanner"]
    - path: "SundeeFundeeRN/src/components/paywall/TrialEndedModal.tsx"
      provides: "One-time trial-ended modal"
      exports: ["TrialEndedModal"]
  key_links:
    - from: "SundeeFundeeRN/app/(app)/ai-workout/config.tsx"
      to: "SundeeFundeeRN/src/components/paywall/PaywallModal.tsx"
      via: "useEntitlementContext + showPaywall state"
      pattern: "useEntitlementContext|PaywallModal"
    - from: "SundeeFundeeRN/app/(app)/(tabs)/programs/index.tsx"
      to: "SundeeFundeeRN/src/components/paywall/PaywallModal.tsx"
      via: "useEntitlementContext + showPaywall state"
      pattern: "useEntitlementContext|PaywallModal"
    - from: "SundeeFundeeRN/app/(app)/(tabs)/settings.tsx"
      to: "react-native-purchases"
      via: "restorePurchases and getCustomerInfo for subscription display"
      pattern: "restorePurchases|getCustomerInfo"
    - from: "SundeeFundeeRN/app/(app)/(tabs)/dashboard.tsx"
      to: "SundeeFundeeRN/src/components/paywall/TrialBanner.tsx"
      via: "rendered conditionally based on trial status"
      pattern: "TrialBanner"
---

<objective>
Gate premium features behind entitlement checks, add subscription management to settings, and implement trial UX (banner + ended modal).

Purpose: This is the user-facing completion of the monetization system. Free users see premium feature badges and get the paywall on interaction. Subscribed users access everything seamlessly. Trial users get subtle reminders at the end. All users can manage their subscription from settings.

Output: All 4 premium features gated, settings subscription section, trial banner and ended modal, restore purchases.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-subscriptions-and-monetization/06-CONTEXT.md
@.planning/phases/06-subscriptions-and-monetization/06-RESEARCH.md
@.planning/phases/06-subscriptions-and-monetization/06-01-SUMMARY.md
@.planning/phases/06-subscriptions-and-monetization/06-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 outputs -->

From SundeeFundeeRN/src/entitlements/EntitlementContext.tsx:
```typescript
export function EntitlementProvider({ children }: { children: React.ReactNode }): React.JSX.Element;
export function useEntitlementContext(): { isPremium: boolean; isLoading: boolean };
```

From SundeeFundeeRN/src/components/paywall/PaywallModal.tsx:
```typescript
export function PaywallModal(props: {
  visible: boolean;
  onDismiss: () => void;
  onSubscribed: () => void;
}): React.JSX.Element;
```

From SundeeFundeeRN/src/components/paywall/PremiumBadge.tsx:
```typescript
export function PremiumBadge(props: { compact?: boolean }): React.JSX.Element;
```

From SundeeFundeeRN/src/entitlements/useEntitlements.ts:
```typescript
export const PREMIUM_ENTITLEMENT_ID = 'premium';
export function useEntitlements(uid?: string | null): EntitlementState;
```

From SundeeFundeeRN/src/auth/AuthContext.tsx:
```typescript
export function useSession(): Session; // { user, isLoading, isGuest, signOut }
```

From SundeeFundeeRN/app/(app)/(tabs)/settings.tsx:
```typescript
// Existing sections: user card, create account (guest), rest timer, about, sign out
// Integration point: add Subscription section between rest timer and about
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate premium features and build trial UX components</name>
  <files>
    SundeeFundeeRN/app/(app)/ai-workout/config.tsx,
    SundeeFundeeRN/app/(app)/(tabs)/programs/index.tsx,
    SundeeFundeeRN/app/(app)/(tabs)/cycle.tsx,
    SundeeFundeeRN/app/(app)/(tabs)/dashboard.tsx,
    SundeeFundeeRN/app/(app)/injuries/[id].tsx,
    SundeeFundeeRN/src/components/paywall/TrialBanner.tsx,
    SundeeFundeeRN/src/components/paywall/TrialEndedModal.tsx,
    SundeeFundeeRN/src/components/paywall/__tests__/TrialBanner.test.tsx,
    SundeeFundeeRN/src/components/paywall/__tests__/TrialEndedModal.test.tsx
  </files>
  <behavior>
    - AI workout config: shows PaywallModal when non-premium user taps "Generate" (config screen still visible, paywall overlays)
    - Program catalog: shows PaywallModal when non-premium user tries to enroll (catalog browsing stays free, enrollment is gated)
    - Cycle tab: adaptation toggle disabled with PremiumBadge when non-premium; tapping shows PaywallModal
    - Injury [id]: adaptation/substitution section shows PremiumBadge when non-premium; tapping shows PaywallModal
    - Dashboard: renders TrialBanner when trial has <= 2 days remaining; banner onSubscribe opens PaywallModal
    - TrialBanner: renders dismissable banner with "Your trial ends in X days" text
    - TrialBanner: only visible when periodType is TRIAL and days remaining <= 2 (days 6-7 of 7-day trial)
    - TrialBanner: stores dismissal in AsyncStorage to not re-show in same session
    - TrialBanner: has "Subscribe" CTA that opens PaywallModal
    - TrialEndedModal: shown once on first launch after trial expiry
    - TrialEndedModal: lists premium features the user accessed during trial
    - TrialEndedModal: has "Subscribe" and "Continue with Free" buttons
    - TrialEndedModal: "Continue with Free" persists dismissal to AsyncStorage (shown once per CONTEXT.md)
  </behavior>
  <action>
    1. Create `SundeeFundeeRN/src/components/paywall/TrialBanner.tsx`:
       - Props: `{ onSubscribe: () => void }`
       - Uses dynamic require for `react-native-purchases` on mobile (Platform.OS !== 'web')
       - On mount: check `Purchases.getCustomerInfo()` for `entitlements.active.premium.periodType === 'TRIAL'` and `expirationDate`
       - Calculate days remaining. Only render if <= 2 days remaining (covers days 6-7 of trial).
       - On web: do not render (web trial tracking would need Firestore field — keep simple for v1)
       - Layout: subtle cream/orange banner with text "Your trial ends in {N} day{s}. Subscribe to keep premium features." and small "Subscribe" button
       - Dismissable via X button. Store `trialBannerDismissed` in AsyncStorage. Check on mount.
       - Art Deco styling: ORANGE_LIGHT background, NAVY text, ORANGE subscribe button

    2. Create `SundeeFundeeRN/src/components/paywall/TrialEndedModal.tsx`:
       - Props: `{ visible: boolean; onDismiss: () => void; onSubscribe: () => void }`
       - Layout: Modal with "Your Trial Has Ended" header
       - Feature summary: "You used: AI Workouts, Programs, Cycle Adaptation" (static list of premium features)
       - Two buttons: "Subscribe Now" (ORANGE, calls onSubscribe) and "Continue with Free" (text button, calls onDismiss)
       - Art Deco styling consistent with PaywallModal

    3. Gate AI workout config (`app/(app)/ai-workout/config.tsx`):
       - Import `useEntitlementContext` and `PaywallModal`
       - Add `const { isPremium } = useEntitlementContext()`
       - Add `[showPaywall, setShowPaywall]` state
       - On "Generate Workout" button press: if !isPremium, `setShowPaywall(true)` and return
       - Render `<PaywallModal visible={showPaywall} onDismiss={...} onSubscribed={...} />`
       - After subscribe, dismiss paywall and proceed with generation

    4. Gate program enrollment (`app/(app)/(tabs)/programs/index.tsx`):
       - Import `useEntitlementContext`, `PaywallModal`, `PremiumBadge`
       - Show `<PremiumBadge compact />` next to each program card when !isPremium
       - On "Enroll" tap: if !isPremium, show PaywallModal
       - Catalog browsing (viewing program details) stays free per user decision

    5. Gate cycle adaptation (`app/(app)/(tabs)/cycle.tsx`):
       - Import `useEntitlementContext`, `PaywallModal`, `PremiumBadge`
       - Cycle tracking (period logging, phase viewing) stays free per user decision
       - The adaptation toggle (if exists) or adaptation indicator section: show `<PremiumBadge />` when !isPremium
       - Tapping the adaptation section when !isPremium shows PaywallModal

    6. Gate injury adaptation (`app/(app)/injuries/[id].tsx`):
       - Import `useEntitlementContext`, `PaywallModal`, `PremiumBadge`
       - Injury profile, pain logging, trends stay free per user decision
       - The adaptation/substitution section: show `<PremiumBadge />` when !isPremium
       - Tapping adaptation section when !isPremium shows PaywallModal

    7. Wire TrialBanner into dashboard (`app/(app)/(tabs)/dashboard.tsx`):
       - Import `TrialBanner` from `@/src/components/paywall/TrialBanner`
       - Import `PaywallModal` from `@/src/components/paywall/PaywallModal`
       - Add `[showPaywall, setShowPaywall]` state
       - Render `<TrialBanner onSubscribe={() => setShowPaywall(true)} />` near the top of the dashboard content (below header, above main dashboard cards)
       - Render `<PaywallModal visible={showPaywall} onDismiss={() => setShowPaywall(false)} onSubscribed={() => setShowPaywall(false)} />`
       - TrialBanner handles its own visibility logic internally (only shows when trial <= 2 days remaining), so it can be rendered unconditionally — it returns null when not applicable

    8. Write tests:
       - `TrialBanner.test.tsx`: renders when trial has <= 2 days remaining, does not render when > 2 days, dismisses and stores to AsyncStorage, calls onSubscribe
       - `TrialEndedModal.test.tsx`: renders feature list, calls onSubscribe on button press, calls onDismiss on "Continue with Free"
  </action>
  <verify>
    <automated>cd /Users/dustinober/Projects/sundee-fundee/SundeeFundeeRN && npx jest src/components/paywall/ --no-coverage</automated>
  </verify>
  <done>
    - AI workout generation gated behind premium
    - Program enrollment gated behind premium (browsing free)
    - Cycle adaptation gated behind premium (tracking free)
    - Injury adaptation gated behind premium (profile/pain logging free)
    - TrialBanner rendered in dashboard, shows on days 6-7, dismissable
    - TrialEndedModal shows once after expiry
    - All paywall component tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add subscription management to Settings</name>
  <files>
    SundeeFundeeRN/app/(app)/(tabs)/settings.tsx,
    SundeeFundeeRN/app/(app)/(tabs)/__tests__/settings.test.tsx
  </files>
  <behavior>
    - Settings renders "Subscription" section header between rest timer and about sections
    - Subscribed user: settings shows plan name (e.g., "Premium Annual"), renewal date, and "Manage Subscription" link
    - Subscribed user on trial: settings shows "Free Trial - expires {date}" instead of plan name
    - Non-subscribed user: settings shows "Unlock Premium" card with "View Plans" button that opens PaywallModal
    - "Manage Subscription" on iOS: calls Linking.openURL with App Store subscription URL
    - "Manage Subscription" on Android: calls Linking.openURL with managementURL from customerInfo
    - "Manage Subscription" on web: shows "Manage on sundeefundee.com" text
    - "Restore Purchases" button visible only on mobile (Platform.OS !== 'web')
    - "Restore Purchases" calls Purchases.restorePurchases() and shows success/failure Alert
    - "Restore Purchases" button disabled while restore is in progress
    - TrialEndedModal shown once on settings mount when trial has ended and AsyncStorage key trialEndedModalShown is not set
  </behavior>
  <action>
    1. Create `SundeeFundeeRN/app/(app)/(tabs)/__tests__/settings.test.tsx`:
       - Mock dependencies: `@/src/entitlements/EntitlementContext` (useEntitlementContext), `@/src/auth/AuthContext` (useSession), `react-native-purchases`, `@react-native-async-storage/async-storage`, `react-native` (Linking, Alert, Platform)
       - Test: renders "Subscription" section heading
       - Test: when isPremium=true, shows plan name and "Manage Subscription" link
       - Test: when isPremium=false, shows "Unlock Premium" card with "View Plans" button
       - Test: tapping "View Plans" opens PaywallModal (check visible prop becomes true)
       - Test: "Restore Purchases" button visible on mobile, calls restorePurchases on press
       - Test: "Restore Purchases" button NOT rendered when Platform.OS is 'web'
       - Test: "Manage Subscription" on iOS calls Linking.openURL with App Store subscriptions URL
       - Use `@testing-library/react-native` render and fireEvent

    2. Update `SundeeFundeeRN/app/(app)/(tabs)/settings.tsx`:
       - Import `useEntitlementContext` from `@/src/entitlements/EntitlementContext`
       - Import `PaywallModal` from `@/src/components/paywall/PaywallModal`
       - Import `Linking` from `react-native`

    3. Add state: `subscriptionInfo` object holding `{ planName, renewalDate, isTrialing, managementURL }` — loaded on mount from RevenueCat (mobile) or Firestore (web).

    4. On mount (mobile, non-guest): call `Purchases.getCustomerInfo()` to get:
       - Active entitlement name -> planName (e.g., "Premium Monthly" or "Premium Annual")
       - expirationDate -> renewalDate
       - periodType -> isTrialing
       - managementURL -> for Android

    5. Add "Subscription" section between "Rest Timer" and "About" sections:
       - **If subscribed (isPremium):** Show card with:
         - Plan name (e.g., "Premium Annual")
         - Renewal/expiration date formatted
         - If trialing: show "Free Trial - expires {date}"
         - "Manage Subscription" row:
           - iOS: `Linking.openURL('itms-apps://apps.apple.com/account/subscriptions')`
           - Android: `Linking.openURL(managementURL)` from customerInfo
           - Web: Link to Stripe Customer Portal URL (for v1, show "Manage on sundeefundee.com" text)
       - **If not subscribed:** Show "Unlock Premium" card with brief feature list and "View Plans" button that opens PaywallModal

    6. Add "Restore Purchases" button:
       - Only visible on mobile (Platform.OS !== 'web') for subscribed or unsubscribed users
       - Required by Apple App Review
       - On press: call `Purchases.restorePurchases()`, show success/failure feedback via Alert
       - Loading state while restore is in progress (disable button)

    7. Add `showPaywall` state and render `<PaywallModal>` at bottom of component

    8. Add `TrialEndedModal` integration:
       - On settings mount, check if trial has ended and modal hasn't been shown (AsyncStorage key `trialEndedModalShown`)
       - If trial ended and not shown: display TrialEndedModal
       - On dismiss: set `trialEndedModalShown` in AsyncStorage

    9. Style subscription section consistently with existing settings sections using established patterns (sectionTitle, settingRow styles).
  </action>
  <verify>
    <automated>cd /Users/dustinober/Projects/sundee-fundee/SundeeFundeeRN && npx jest app/\(app\)/\(tabs\)/__tests__/settings --no-coverage</automated>
  </verify>
  <done>
    - Settings shows Subscription section with plan info for subscribed users
    - Settings shows "View Plans" for non-subscribed users
    - Manage Subscription deep-links to platform-appropriate subscription management
    - Restore Purchases button visible on mobile, calls RevenueCat restorePurchases
    - TrialEndedModal shown once after trial expiry from settings/dashboard
    - Settings tests pass including all subscription management assertions
  </done>
</task>

</tasks>

<verification>
- `cd /Users/dustinober/Projects/sundee-fundee/SundeeFundeeRN && npx jest --no-coverage` passes all tests (full suite)
- All 4 premium features show paywall for non-premium users
- Settings subscription management section present
- Trial UX components render correctly
</verification>

<success_criteria>
- Free user is blocked by paywall on AI workout, program enrollment, cycle adaptation, and injury adaptation
- Free user can still browse programs, log cycle data, create injury profiles
- Subscribed user accesses all features without interruption
- Settings shows subscription status and management for subscribed users
- Trial banner appears on days 6-7, trial-ended modal appears once after expiry
- Restore Purchases works on mobile
- All tests pass including settings subscription tests
</success_criteria>

<output>
After completion, create `.planning/phases/06-subscriptions-and-monetization/06-03-SUMMARY.md`
</output>
