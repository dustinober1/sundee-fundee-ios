---
phase: 02-female-cycle-aware-program-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flutter_app/lib/domain/models/program_models.dart
  - flutter_app/lib/domain/calculations/cycle_program_generator.dart
  - flutter_app/lib/domain/calculations/cycle_adaptation_policy.dart
  - flutter_app/lib/features/programs/data/squad_squat_program.dart
  - flutter_app/lib/features/cycle/providers.dart
  - flutter_app/test/domain/cycle_program_generator_test.dart
  - flutter_app/test/domain/cycle_adaptation_policy_test.dart
  - flutter_app/test/domain/program_models_test.dart
autonomous: true
must_haves:
  truths:
    - "Cycle-aware prescriptions adjust load and sets/reps by phase while preserving session identity and week structure."
    - "Missing phase data falls back to last-known phase, and low confidence scales adjustments conservatively without blocking workouts."
    - "Readiness and cycle phase conflicts resolve through a deterministic blend where phase sets direction and readiness controls magnitude."
  artifacts:
    - path: flutter_app/lib/domain/calculations/cycle_adaptation_policy.dart
      provides: "Single source of truth for phase multipliers, readiness modifiers, confidence scaling, and clamp guardrails"
    - path: flutter_app/lib/domain/calculations/cycle_program_generator.dart
      provides: "Policy-driven adaptation preserving sessionId/program shape"
    - path: flutter_app/lib/domain/models/program_models.dart
      provides: "Per-phase adaptation settings schema for backend compatibility"
    - path: flutter_app/test/domain/cycle_program_generator_test.dart
      provides: "Required phase multiplier matrix coverage for PROG-03"
  key_links:
    - from: flutter_app/lib/domain/calculations/cycle_program_generator.dart
      to: flutter_app/lib/domain/calculations/cycle_adaptation_policy.dart
      via: "Generator delegates all multiplier and fallback decisions to policy APIs"
      pattern: "resolveReadinessTier|resolveConfidenceScale|applyPhaseAdjustment"
    - from: flutter_app/lib/domain/models/program_models.dart
      to: flutter_app/lib/features/programs/data/squad_squat_program.dart
      via: "Program schema exposes optional per-phase adjustment payload and fallback-safe defaults"
      pattern: "cycleAdjustmentProfile|phaseSettings|fromJson/toJson"
---

<objective>
Establish deterministic cycle adaptation primitives and data contracts before provider/UI wiring.

Purpose: Make adaptation behavior explicit, testable, and reusable across program and workout flows.
Output: Domain policy + generator refactor + model updates + unit tests proving phase multiplier behavior.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-female-cycle-aware-program-generation/02-CONTEXT.md
@.planning/phases/02-female-cycle-aware-program-generation/02-RESEARCH.md
@flutter_app/lib/domain/calculations/cycle_calculations.dart
@flutter_app/lib/domain/calculations/cycle_program_generator.dart
@flutter_app/lib/domain/models/program_models.dart
@flutter_app/lib/features/programs/data/squad_squat_program.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add per-phase adaptation settings and fallback metadata to domain models</name>
  <files>
    flutter_app/lib/domain/models/program_models.dart
    flutter_app/lib/features/programs/data/squad_squat_program.dart
    flutter_app/test/domain/program_models_test.dart
  </files>
  <action>
    Extend program model serialization with optional per-phase adaptation settings that can be persisted in Firestore and safely ignored by legacy documents.

    Include defaults so existing programs still parse unchanged, and add model tests for:
    - legacy JSON without adaptation fields,
    - full per-phase settings payload round-trip,
    - null/partial values using safe fallbacks.
  </action>
  <verify>
    cd flutter_app && flutter test test/domain/program_models_test.dart
  </verify>
  <done>
    Program model supports backend per-phase settings while preserving backward compatibility for current data.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor cycle generator to policy-driven adaptation with readiness + confidence blending</name>
  <files>
    flutter_app/lib/domain/calculations/cycle_adaptation_policy.dart
    flutter_app/lib/domain/calculations/cycle_program_generator.dart
    flutter_app/lib/features/cycle/providers.dart
  </files>
  <action>
    Create a policy layer that centralizes:
    - baseline phase multipliers (existing in-app behavior),
    - readiness tier modifiers (`low|neutral|high`),
    - confidence scaling (`high|medium|low`) and conservative fallback rules.

    Update `CycleProgramGenerator` to consume the policy and preserve all `sessionId`/week ordering, touching only prescription values. Implement deterministic fallback order:
    current phase -> last known phase -> conservative neutral.
  </action>
  <verify>
    cd flutter_app && flutter test test/domain/cycle_adaptation_policy_test.dart
  </verify>
  <done>
    Generator behavior is deterministic, policy-backed, and honors locked context decisions for blending and fallback.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add exhaustive generator matrix tests for roadmap success criteria</name>
  <files>
    flutter_app/test/domain/cycle_program_generator_test.dart
    flutter_app/test/domain/cycle_adaptation_policy_test.dart
  </files>
  <action>
    Add unit matrix tests that explicitly cover:
    - menstrual, follicular, ovulation, and luteal phase prescriptions,
    - readiness conflict behavior (phase direction + bounded magnitude),
    - low-confidence reduced-adjustment behavior,
    - fallback when cycle data is missing or partial.

    Assert both load percentages and sets/reps outcomes for heavy-day prescriptions so "noticeable adaptation" is protected from regression.
  </action>
  <verify>
    cd flutter_app && flutter test test/domain/cycle_program_generator_test.dart test/domain/cycle_adaptation_policy_test.dart
  </verify>
  <done>
    Phase multiplier and blend logic are covered with deterministic unit tests satisfying Phase 2 criterion #2.
  </done>
</task>

</tasks>

<verification>
Run all domain-level tests touched by this plan and confirm policy + generator + model suites pass.
</verification>

<success_criteria>
- Program schema can persist per-phase adaptation settings without breaking legacy data.
- Cycle adaptation is policy-driven with fallback and confidence scaling.
- Generator tests explicitly validate phase multipliers and blend behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-female-cycle-aware-program-generation/02-01-SUMMARY.md`.
</output>
