---
phase: 14-release-hardening-cutover-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flutter_app/test/parity_gates/calculations_parity_test.dart
autonomous: true

must_haves:
  truths:
    - "Calculations parity test file exists and validates epley, roundToNearestFive, getNextRecommendedWeight against known v1.1 expected values"
    - "Test covers all SessionResult cases (first, success, failure) for getNextRecommendedWeight"
    - "Test passes, confirming calculations.dart is a correct port of calculations.ts"
  artifacts:
    - path: "flutter_app/test/parity_gates/calculations_parity_test.dart"
      provides: "Calculations parity gate unit tests"
      min_lines: 60
  key_links:
    - from: "flutter_app/test/parity_gates/calculations_parity_test.dart"
      to: "flutter_app/lib/core/recommendations/calculations.dart"
      via: "import and function calls"
      pattern: "import.*calculations.dart"
---

<objective>
Create calculations parity gate test to verify Flutter calculations.dart is a correct port of v1.1 calculations.ts.

Purpose: PLAT-02 requires equivalent behavior outcomes across platforms. The calculations are the foundation of all recommendation logic. A dedicated parity test provides permanent verification that the Flutter port produces identical results to v1.1.

Output: flutter_app/test/parity_gates/calculations_parity_test.dart with parameterized assertions against known v1.1 expected values.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-release-hardening-cutover-safety/14-RESEARCH.md

@flutter_app/lib/core/recommendations/calculations.dart
@src/lib/calculations.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create calculations parity gate test</name>
  <files>flutter_app/test/parity_gates/calculations_parity_test.dart</files>
  <action>
    Create `flutter_app/test/parity_gates/calculations_parity_test.dart` with unit tests:

    1. Create parity_gates directory under flutter_app/test/ if not exists

    2. Test `roundToNearestFive()`:
       - 72.0 -> 70.0
       - 73.0 -> 75.0
       - 72.5 -> 75.0
       - 100.0 -> 100.0
       - 2.5 -> 5.0

    3. Test `epley()` (must match v1.1 formula: weight * (1 + clamp(reps,1,10) / 30)):
       - (200, 1) -> 200.0  (reps=1 returns weight unchanged)
       - (200, 5) -> 233.33... (200 * 1.1667)
       - (200, 10) -> 266.67... (200 * 1.333)
       - (200, 15) -> 266.67... (reps capped at 10)
       - (100, 8) -> 126.67... (100 * 1.2667)

    4. Test `getNextRecommendedWeight()`:
       - first: (100, first, 200) -> 140.0  (70% of 1RM = 140)
       - success: (100, success, 200) -> 105.0  (+5 rounded)
       - failure: (100, failure, 200) -> 100.0  (-5 but floor at 50% = 100)
       - failure at floor: (95, failure, 200) -> 100.0  (can't go below floor)

    5. Test `wasSetSuccessful()`:
       - reps met, no prescribedWeight -> true
       - reps not met -> false
       - reps met, weight met -> true
       - reps met, weight not met -> false

    6. Test `wasSessionSuccessful()`:
       - empty list -> true (vacuous success, matches v1.1)
       - all sets successful -> true
       - one set failed -> false

    7. Test `isPersonalRecord()`:
       - weight > previousMax -> true
       - weight <= previousMax -> false

    8. Test `detectPlateau()`:
       - fewer than 3 weights -> false
       - variance < 5 -> true (plateau)
       - variance >= 5 -> false (progress)

    Use `closeTo(expected, 0.01)` matcher for floating point comparisons.

    Import:
    ```dart
    import 'package:flutter_test/flutter_test.dart';
    import 'package:sundee_fundee/core/recommendations/calculations.dart';
    ```
  </action>
  <verify>
    - `mkdir -p flutter_app/test/parity_gates` if needed
    - `cd flutter_app && flutter test test/parity_gates/calculations_parity_test.dart` exits 0
    - All assertions pass against known v1.1 expected values
  </verify>
  <done>Calculations parity gate test exists and passes. All 8 functions in calculations.dart are verified to produce v1.1-equivalent outputs.</done>
</task>

</tasks>

<verification>
- `cd flutter_app && flutter test test/parity_gates/calculations_parity_test.dart` — all tests pass
- Test covers: roundToNearestFive, epley, getNextRecommendedWeight (3 cases), wasSetSuccessful, wasSessionSuccessful, isPersonalRecord, detectPlateau
- flutter analyze clean
</verification>

<success_criteria>
- calculations_parity_test.dart exists under flutter_app/test/parity_gates/
- All 8 calculation functions have explicit parity assertions
- Tests pass with flutter test
- PLAT-02 calculations parity requirement is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/14-release-hardening-cutover-safety/14-01-SUMMARY.md`
</output>
