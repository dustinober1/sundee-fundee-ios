---
phase: 10-ui-polish-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SundeeFundeeRN/app/(app)/workout-detail.tsx
  - SundeeFundeeRN/app/(app)/_layout.tsx
  - SundeeFundeeRN/src/export/exportData.ts
  - SundeeFundeeRN/app/(app)/__tests__/workout-detail.test.tsx
  - SundeeFundeeRN/app/(app)/__tests__/goodbye.test.tsx
  - SundeeFundeeRN/src/export/__tests__/exportData.test.ts
  - SundeeFundeeRN/package.json
autonomous: true
requirements:
  - PLAT-05
must_haves:
  truths:
    - "Workout detail screen displays weights in the user's chosen unit (lbs or kg), not hardcoded lbs"
    - "Goodbye screen after account deletion has no system back button or header"
    - "Web CSV export downloads a single zip file, not 7 individual files"
  artifacts:
    - path: "SundeeFundeeRN/app/(app)/workout-detail.tsx"
      provides: "Weight display using formatWeight utility"
      contains: "formatWeight"
    - path: "SundeeFundeeRN/app/(app)/_layout.tsx"
      provides: "Stack.Screen entry for goodbye route"
      contains: "goodbye"
    - path: "SundeeFundeeRN/src/export/exportData.ts"
      provides: "JSZip web export bundling"
      contains: "jszip"
  key_links:
    - from: "SundeeFundeeRN/app/(app)/workout-detail.tsx"
      to: "src/utils/formatWeight.ts"
      via: "import formatWeight"
      pattern: "formatWeight\\(.*weightUnit\\)"
    - from: "SundeeFundeeRN/app/(app)/workout-detail.tsx"
      to: "src/repositories/SettingsRepo.ts"
      via: "getSettingsRepo to load weightUnit"
      pattern: "getSettingsRepo"
    - from: "SundeeFundeeRN/src/export/exportData.ts"
      to: "jszip"
      via: "dynamic import in web branch"
      pattern: "import\\('jszip'\\)"
---

<objective>
Fix three UI polish bugs identified during post-launch audit: (1) workout detail screen hardcodes " lbs" instead of using the user's weight unit preference, (2) goodbye screen after account deletion shows a system back button because it lacks a Stack.Screen declaration, (3) web CSV export triggers 7 separate browser download dialogs instead of bundling into a single zip.

Purpose: Close remaining PLAT-05 gap and improve UX for account deletion and web export flows.
Output: Three bug fixes with test coverage, all in existing files.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ui-polish-fixes/10-RESEARCH.md

@SundeeFundeeRN/app/(app)/workout-detail.tsx
@SundeeFundeeRN/app/(app)/_layout.tsx
@SundeeFundeeRN/src/export/exportData.ts
@SundeeFundeeRN/src/utils/formatWeight.ts
@SundeeFundeeRN/src/repositories/SettingsRepo.ts

<interfaces>
From src/utils/formatWeight.ts:
```typescript
export function formatWeight(lbs: number, unit: WeightUnit): string;
export function formatWeightNumeric(lbs: number, unit: WeightUnit): number;
```

From src/repositories/SettingsRepo.ts:
```typescript
export function getSettingsRepo(isGuest: boolean): SettingsRepository;
// AppSettings includes weightUnit: 'lb' | 'kg'
```

From src/domain/types/index.ts:
```typescript
export type WeightUnit = 'lb' | 'kg';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread weightUnit into workout-detail.tsx via formatWeight</name>
  <files>SundeeFundeeRN/app/(app)/workout-detail.tsx, SundeeFundeeRN/app/(app)/__tests__/workout-detail.test.tsx</files>
  <behavior>
    - Test 1: CompletedExerciseSection renders weight with "kg" suffix when weightUnit is 'kg'
    - Test 2: CompletedExerciseSection renders weight with "lbs" suffix when weightUnit is 'lb'
    - Test 3: AIExerciseSection renders weightLb converted to kg when weightUnit is 'kg'
    - Test 4: MetaStat for Volume uses formatWeight (shows kg when unit is kg)
    - Test 5: Zero-weight sets render as dash regardless of unit
  </behavior>
  <action>
    1. Create `SundeeFundeeRN/app/(app)/__tests__/workout-detail.test.tsx` with tests for the three sub-components (CompletedExerciseSection, AIExerciseSection, MetaStat Volume). Export the sub-components or test through the main screen with mocked repos. Since the sub-components are module-private, test via the main WorkoutDetailScreen by mocking useSession, useLocalSearchParams, getWorkoutRepo, and getSettingsRepo. Verify rendered text includes "kg" or "lbs" based on mocked settings.

    2. In `workout-detail.tsx`:
       - Import `formatWeight` from `@/src/utils/formatWeight.ts`
       - Import `getSettingsRepo` from `@/src/repositories/SettingsRepo`
       - Import `WeightUnit` type from `@/src/domain/types`
       - Add `weightUnit` state: `const [weightUnit, setWeightUnit] = useState<WeightUnit>('lb')`
       - Add useEffect to load weightUnit from settings repo (same pattern as workout-session.tsx): load via `getSettingsRepo(isGuest).getSettings(user.uid)`, set weightUnit from result. Include `user` and `isGuest` in deps.
       - Pass `weightUnit` as prop to `CompletedExerciseSection` and `AIExerciseSection`
       - Update CompletedExerciseSection signature to accept `weightUnit: WeightUnit` prop
       - Replace `{set.weight > 0 ? \`\${set.weight} lbs\` : '\u2014'}` with `{set.weight > 0 ? formatWeight(set.weight, weightUnit) : '\u2014'}`
       - Update AIExerciseSection signature to accept `weightUnit: WeightUnit` prop
       - Replace `\`\${exercise.weightLb} lbs\`` with `formatWeight(exercise.weightLb, weightUnit)`
       - Replace `\`\${record.totalVolume} lbs\`` in the MetaStat Volume with `formatWeight(record.totalVolume, weightUnit)`
  </action>
  <verify>
    <automated>cd SundeeFundeeRN && npx jest --testPathPattern="workout-detail" --passWithNoTests</automated>
  </verify>
  <done>All three hardcoded " lbs" instances in workout-detail.tsx replaced with formatWeight(value, weightUnit). Tests confirm kg rendering when unit is kg and lbs rendering when unit is lb. Zero-weight values still show dash.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add goodbye Stack.Screen and install JSZip for web zip export</name>
  <files>SundeeFundeeRN/app/(app)/_layout.tsx, SundeeFundeeRN/app/(app)/__tests__/goodbye.test.tsx, SundeeFundeeRN/src/export/exportData.ts, SundeeFundeeRN/src/export/__tests__/exportData.test.ts, SundeeFundeeRN/package.json</files>
  <behavior>
    - Test 1 (goodbye): Goodbye screen renders without crash and shows a "Done" or action button
    - Test 2 (_layout): The goodbye Stack.Screen is declared (verify by checking rendered output or snapshot)
    - Test 3 (exportData): Web CSV export calls JSZip to create a single zip blob download (not 7 individual downloads)
    - Test 4 (exportData): Mobile CSV export still uses react-native-zip-archive (unchanged)
  </behavior>
  <action>
    **Goodbye Stack.Screen:**
    1. In `app/(app)/_layout.tsx`, add after the `ai-workout` Stack.Screen entry:
       ```
       <Stack.Screen name="goodbye" options={{ headerShown: false }} />
       ```
    2. Create `app/(app)/__tests__/goodbye.test.tsx` that renders the goodbye screen and verifies it mounts without error and contains expected text/button. Mock useSession and useRouter.

    **JSZip web export:**
    3. Install JSZip: `cd SundeeFundeeRN && npm install jszip && npm install --save-dev @types/jszip`
    4. In `src/export/exportData.ts`:
       - Update `triggerWebDownload` signature to accept `Blob | string` as first param:
         ```typescript
         function triggerWebDownload(content: Blob | string, filename: string, mimeType: string): void {
           const blob = content instanceof Blob ? content : new Blob([content], { type: mimeType });
           // rest unchanged
         }
         ```
       - Replace the web CSV branch (lines 200-206) with:
         ```typescript
         if (isWeb) {
           const JSZip = (await import('jszip')).default;
           const jszip = new JSZip();
           for (const file of csvFiles) {
             jszip.file(file.name, file.content);
           }
           const zipBlob = await jszip.generateAsync({ type: 'blob' });
           triggerWebDownload(zipBlob, `sundee-fundee-export-${dateStr}.zip`, 'application/zip');
           return;
         }
         ```
    5. In `src/export/__tests__/exportData.test.ts`, add a new test block for the web zip behavior:
       - Mock `Platform.OS` as `'web'` using `Object.defineProperty(Platform, 'OS', { value: 'web', configurable: true })`
       - Mock `jszip` module: `jest.mock('jszip', () => { ... })` returning a mock with `file()` and `generateAsync()` methods
       - Call `exportUserData` with format 'csv' and verify `generateAsync` was called with `{ type: 'blob' }` and that only one download link was created (not 7)
       - Restore `Platform.OS` in afterEach
  </action>
  <verify>
    <automated>cd SundeeFundeeRN && npx jest --testPathPattern="goodbye|exportData" --passWithNoTests</automated>
  </verify>
  <done>goodbye Stack.Screen declared with headerShown:false in _layout.tsx. JSZip installed and web CSV export creates a single zip download. Existing mobile export path unchanged. All new and existing export tests pass.</done>
</task>

</tasks>

<verification>
Run the full test suite to confirm no regressions:
```bash
cd SundeeFundeeRN && npx jest --coverage
```

All existing tests pass. New tests for workout-detail, goodbye, and web zip export are green.
</verification>

<success_criteria>
1. Opening a workout detail screen with kg preference shows all weights in kg (e.g., "60.0 kg" not "132.3 lbs")
2. Goodbye screen after account deletion has no header bar or back button
3. Web CSV export triggers exactly one browser download (a .zip file), not 7 separate CSV downloads
4. All tests pass including new coverage for the three fixes
</success_criteria>

<output>
After completion, create `.planning/phases/10-ui-polish-fixes/10-01-SUMMARY.md`
</output>
