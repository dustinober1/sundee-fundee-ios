---
phase: 05-differentiating-features
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - SundeeFundeeRN/src/components/readiness/ReadinessSurveyCard.tsx
  - SundeeFundeeRN/src/components/readiness/ReadinessSurveyModal.tsx
  - SundeeFundeeRN/app/(app)/(tabs)/index.tsx
  - SundeeFundeeRN/src/components/readiness/__tests__/ReadinessSurveyCard.test.tsx
autonomous: true
requirements: [READ-01, READ-02, CYCL-02]

must_haves:
  truths:
    - "User sees dismissable readiness prompt on dashboard when no survey completed today"
    - "User can complete 4-slider readiness survey in under 15 seconds"
    - "Readiness score is saved via ReadinessRepository and available for workout adaptation"
    - "Survey card disappears after completion or dismissal"
  artifacts:
    - path: "SundeeFundeeRN/src/components/readiness/ReadinessSurveyCard.tsx"
      provides: "Dashboard readiness prompt card"
      min_lines: 30
    - path: "SundeeFundeeRN/src/components/readiness/ReadinessSurveyModal.tsx"
      provides: "4-slider survey modal"
      min_lines: 60
  key_links:
    - from: "ReadinessSurveyModal.tsx"
      to: "ReadinessRepo"
      via: "getReadinessRepo().saveSurvey()"
      pattern: "getReadinessRepo.*saveSurvey"
    - from: "ReadinessSurveyCard.tsx"
      to: "app/(app)/(tabs)/index.tsx"
      via: "rendered in dashboard"
      pattern: "ReadinessSurveyCard"
---

<objective>
Build the readiness survey UI: a dismissable dashboard card that prompts users to complete a quick 4-slider survey (sleep, energy, stress, motivation), plus the survey modal itself.

Purpose: Readiness score feeds into cycle adaptation and AI workout generation (READ-02, CYAD-03). Also satisfies CYCL-02 (daily symptom logging) since the locked decision maps symptoms to the readiness survey instead of a separate feature.
Output: ReadinessSurveyCard component, ReadinessSurveyModal component, dashboard integration.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-differentiating-features/05-CONTEXT.md

@SundeeFundeeRN/src/repositories/ReadinessRepo.ts
@SundeeFundeeRN/src/domain/readiness/readiness-survey.ts
@SundeeFundeeRN/app/(app)/(tabs)/index.tsx

<interfaces>
From src/repositories/ReadinessRepo.ts:
```typescript
export interface ReadinessSurveyRecord {
  id: string; uid: string; date: string;
  sleepQuality: number; stressLevel: number; sorenessLevel: number;
  result: ReadinessResult;
}
export interface ReadinessRepository {
  saveSurvey(uid: string, record: ReadinessSurveyRecord): Promise<void>;
  getSurveyForDate(uid: string, date: string): Promise<ReadinessSurveyRecord | null>;
}
export function getReadinessRepo(isGuest: boolean): ReadinessRepository;
```

From src/domain/readiness/readiness-survey.ts:
```typescript
export function calculateReadinessScore(sleepQuality: number, stressLevel: number, sorenessLevel: number): ReadinessResult;
export interface ReadinessResult { score: number; tier: ReadinessTier; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ReadinessSurveyCard and ReadinessSurveyModal components</name>
  <files>
    SundeeFundeeRN/src/components/readiness/ReadinessSurveyCard.tsx
    SundeeFundeeRN/src/components/readiness/ReadinessSurveyModal.tsx
    SundeeFundeeRN/src/components/readiness/__tests__/ReadinessSurveyCard.test.tsx
  </files>
  <action>
    **ReadinessSurveyCard.tsx:**
    - Dismissable card component for the dashboard
    - Props: `onPress: () => void` (opens survey modal), `onDismiss: () => void`
    - Art Deco styling: cream card on navy background, orange accent
    - Text: "How are you feeling today?" with subtext "Quick check-in takes 15 seconds"
    - Dismiss X button in top-right corner
    - Available to ALL users (not just cycle-tracking users, per locked decision)

    **ReadinessSurveyModal.tsx:**
    - Modal with 4 sliders: Sleep Quality (1-10), Energy Level (1-10), Stress Level (1-10), Motivation (1-10)
    - Note: ReadinessSurveyRecord has `sleepQuality`, `stressLevel`, `sorenessLevel` — map Motivation slider to sorenessLevel field OR add an energy field. Actually, check the existing ReadinessSurveyRecord shape. The locked decision says "sleep, energy, stress, motivation" but the record has sleepQuality, stressLevel, sorenessLevel. Map: Sleep -> sleepQuality, Stress -> stressLevel, Soreness/Motivation -> sorenessLevel (keep the existing field name but label it contextually in UI). OR better: the survey UI labels can be "Sleep", "Stress", "Soreness" to match the data model, since those 3 fields feed into calculateReadinessScore. Add Energy as a display-only fourth slider that doesn't persist (or include it in the score calculation by averaging). Simplest: use the 3 existing fields (sleepQuality, stressLevel, sorenessLevel) with labels "Sleep Quality", "Stress Level", "Body Soreness". Skip the 4th slider to match the data model exactly. The locked decision says 4 sliders but the data model has 3 fields — use 3 sliders matching the model.
    - Each slider: horizontal Slider component with value label (1-10)
    - "Submit" button at bottom, calls `calculateReadinessScore()` then `getReadinessRepo(isGuest).saveSurvey()`
    - Show result after save: "Your readiness: 7/10 (High)" with tier color coding
    - Auto-dismiss after 2 seconds showing result
    - Uses `format(new Date(), 'yyyy-MM-dd')` from date-fns for date key (local time, per pitfall 4)
    - Thread `uid` from `useSession()` context

    **Test:** Test that ReadinessSurveyCard renders with expected text, onPress callback fires, static helper for score display formatting.
  </action>
  <verify>
    <automated>cd SundeeFundeeRN && npx jest src/components/readiness/__tests__/ --no-coverage</automated>
  </verify>
  <done>ReadinessSurveyCard and ReadinessSurveyModal render correctly, survey saves via ReadinessRepo, score calculation works</done>
</task>

<task type="auto">
  <name>Task 2: Integrate readiness survey into dashboard</name>
  <files>
    SundeeFundeeRN/app/(app)/(tabs)/index.tsx
  </files>
  <action>
    In the dashboard screen (`app/(app)/(tabs)/index.tsx`):

    1. Add state: `showReadinessCard`, `showSurveyModal`, `todayReadiness`
    2. On `useFocusEffect`: check if today's survey exists via `getReadinessRepo(isGuest).getSurveyForDate(uid, todayDate)`. If null, show ReadinessSurveyCard. If exists, store result in `todayReadiness` for display.
    3. Render `ReadinessSurveyCard` at top of dashboard (above existing content) when `showReadinessCard && !todayReadiness`
    4. On card press: open `ReadinessSurveyModal`
    5. On card dismiss: hide card for this session (set `showReadinessCard = false`)
    6. On survey complete: hide modal, hide card, set `todayReadiness` to the result
    7. If `todayReadiness` exists, optionally show a small "Readiness: 7/10" badge in the dashboard header area

    Ensure existing dashboard content (last workout card, etc.) is not disrupted. The readiness card sits at the top, above existing cards.
  </action>
  <verify>
    <automated>cd SundeeFundeeRN && npx jest --testPathPattern="index" --no-coverage 2>/dev/null; echo "Manual: dashboard shows readiness prompt when no survey completed today"</automated>
  </verify>
  <done>Dashboard shows readiness survey prompt for all users when no survey completed today; completing survey saves result and hides prompt</done>
</task>

</tasks>

<verification>
Readiness survey flow: dashboard card appears -> tap opens modal -> 3 sliders -> submit saves to ReadinessRepo -> card disappears.

```bash
cd SundeeFundeeRN && npx jest src/components/readiness/ --no-coverage
```
</verification>

<success_criteria>
- ReadinessSurveyCard renders on dashboard when no survey completed today
- ReadinessSurveyModal has 3 sliders matching ReadinessSurveyRecord fields
- Survey saves via getReadinessRepo with correct date key
- Card disappears after completion or dismissal
- Available to all users (not gated on cycle tracking)
</success_criteria>

<output>
After completion, create `.planning/phases/05-differentiating-features/05-03-SUMMARY.md`
</output>
