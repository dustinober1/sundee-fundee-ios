---
phase: 10-verification-evidence-and-regression-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flutter_app/integration_test/support/firebase_emulator_test_harness.dart
  - flutter_app/integration_test/critical_access_flow_test.dart
  - flutter_app/pubspec.yaml
  - .github/workflows/flutter-release.yml
  - flutter_app/test/features/auth/data/auth_repository_onboarding_bootstrap_test.dart
  - flutter_app/test/features/workouts/presentation/workout_write_resilience_test.dart
autonomous: true
must_haves:
  truths:
    - "Automated integration coverage exercises the authenticated UAT path (`login -> dashboard -> programs -> workout start`) and asserts expected data/load/write outcomes."
    - "Automated coverage includes unauthenticated guard-path behavior for Home, Programs, and Workout surfaces."
    - "Automated assertions explicitly fail on permission-denied signatures and onboarding false-prompt regressions for the canonical verification account."
    - "CI runs emulator-backed integration checks as a dedicated quality lane so regressions are caught before release builds."
  artifacts:
    - path: flutter_app/integration_test/critical_access_flow_test.dart
      provides: "End-to-end emulator-backed UAT-path assertions including checkpoint and negative-regression checks"
      contains: "login -> dashboard -> programs -> workout start"
    - path: flutter_app/integration_test/support/firebase_emulator_test_harness.dart
      provides: "Deterministic Firebase emulator bootstrap and canonical verification-account helpers for integration runs"
      contains: "useAuthEmulator|useFirestoreEmulator"
    - path: .github/workflows/flutter-release.yml
      provides: "Dedicated CI gate that executes emulator-backed integration flow checks"
      contains: "firebase emulators:exec"
    - path: flutter_app/test/features/workouts/presentation/workout_write_resilience_test.dart
      provides: "Regression lock for workout write enqueue/replay and sync-gated finalization behavior"
      min_lines: 80
  key_links:
    - from: flutter_app/integration_test/critical_access_flow_test.dart
      to: flutter_app/integration_test/support/firebase_emulator_test_harness.dart
      via: "Integration flow setup must use deterministic emulator wiring and canonical account fixtures before checkpoint assertions"
      pattern: "initializeHarness|seedVerificationAccount|checkpoint"
    - from: flutter_app/integration_test/critical_access_flow_test.dart
      to: flutter_app/test/features/auth/data/auth_repository_onboarding_bootstrap_test.dart
      via: "Integration-level no-false-prompt assertions align with repository-level onboarding regression matrix"
      pattern: "resumeOnboarding|onboardingComplete|legacy"
    - from: .github/workflows/flutter-release.yml
      to: flutter_app/integration_test/critical_access_flow_test.dart
      via: "CI quality lane executes emulator-backed integration test command using Firebase-enabled dart defines"
      pattern: "emulators:exec|integration_test/critical_access_flow_test.dart"
---

<objective>
Create deterministic emulator-backed automated evidence for QA-01 across authenticated Home/Programs/Workout critical paths plus unauthenticated guards.

Purpose: Phase 10 must prove repaired access/onboarding behavior stays stable beyond widget-level coverage by exercising real auth/firestore contracts.
Output: Integration harness + critical-flow integration suite + CI guardrail that fails on permission-denied or onboarding false-prompt regressions.
</objective>

<execution_context>
@/Users/dustinober/.codex/get-shit-done/references/ui-brand.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-verification-evidence-and-regression-guardrails/10-CONTEXT.md
@.planning/phases/10-verification-evidence-and-regression-guardrails/10-RESEARCH.md
@.github/workflows/flutter-release.yml
@flutter_app/lib/firebase/firebase_bootstrap.dart
@flutter_app/lib/bootstrap.dart
@flutter_app/test/features/auth/data/auth_repository_onboarding_bootstrap_test.dart
@flutter_app/test/features/workouts/presentation/workout_write_resilience_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build deterministic Firebase emulator integration harness for canonical verification account runs</name>
  <files>
    flutter_app/integration_test/support/firebase_emulator_test_harness.dart
    flutter_app/pubspec.yaml
  </files>
  <action>
    Introduce an integration-test harness that initializes Firebase with emulator wiring before any auth/session actions:
    - initialize `IntegrationTestWidgetsFlutterBinding` and Firebase app setup;
    - require `ENABLE_FIREBASE=true` and `USE_FIREBASE_EMULATORS=true` for integration command paths;
    - connect Auth and Firestore emulator hosts with deterministic host selection per runtime target (web/simulator/emulator);
    - expose a helper for canonical verification-account sign-in/bootstrap preparation;
    - fail fast when emulator mode is not active to avoid accidental live-backend test runs.

    Keep harness scope narrow to phase-critical verification behavior; do not hand-roll parallel auth/mock stacks.
  </action>
  <verify>
    cd flutter_app && flutter test integration_test/critical_access_flow_test.dart --dart-define=ENABLE_FIREBASE=true --dart-define=USE_FIREBASE_EMULATORS=true -r compact
  </verify>
  <done>
    Integration runs have one deterministic bootstrap path that always targets Firebase emulators and can authenticate the canonical verification account safely.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement critical-flow integration suite with positive checkpoint and negative regression assertions</name>
  <files>
    flutter_app/integration_test/critical_access_flow_test.dart
    flutter_app/test/features/auth/data/auth_repository_onboarding_bootstrap_test.dart
    flutter_app/test/features/workouts/presentation/workout_write_resilience_test.dart
  </files>
  <action>
    Add the phase-critical integration suite for `login -> dashboard -> programs -> workout start` using emulator-backed auth/firestore:
    - assert each checkpoint loads expected data/state for the verification account;
    - assert workout start and initial session progress writes persist and recover correctly through queue/replay paths;
    - assert unauthenticated guard behavior for Home/Programs/Workout surfaces (no unauthorized data render, no unstable tab errors);
    - explicitly assert absence of permission-denied and onboarding false-prompt signatures in visible surfaces and routed auth status.

    Extend targeted existing tests only where needed to keep onboarding and workout-write regression guarantees aligned with integration assertions.
  </action>
  <verify>
    cd flutter_app && flutter test integration_test/critical_access_flow_test.dart test/features/auth/data/auth_repository_onboarding_bootstrap_test.dart test/features/workouts/presentation/workout_write_resilience_test.dart --dart-define=ENABLE_FIREBASE=true --dart-define=USE_FIREBASE_EMULATORS=true -r compact
  </verify>
  <done>
    QA-01 has deterministic automated coverage for authenticated and unauthenticated critical paths, including negative assertions for both reported regressions.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add dedicated CI emulator-integration quality lane and artifact output</name>
  <files>
    .github/workflows/flutter-release.yml
  </files>
  <action>
    Update CI to run emulator-backed integration checks as an explicit quality gate:
    - add a dedicated job (separate from default `flutter test`) that installs Firebase CLI/runtime prerequisites and runs `firebase emulators:exec --only auth,firestore`;
    - execute the critical integration command with required dart defines;
    - publish integration logs/results as build artifacts for verification evidence traceability;
    - wire release build jobs to depend on this gate once stable.

    Keep failure attribution clear by preserving separation between widget/unit quality and emulator integration quality.
  </action>
  <verify>
    rg -n "emulators:exec|integration_test/critical_access_flow_test.dart|quality-integration|upload-artifact" .github/workflows/flutter-release.yml
  </verify>
  <done>
    CI blocks regressions with a reproducible emulator-backed integration lane and keeps logs available for phase evidence.
  </done>
</task>

</tasks>

<verification>
Run the phase-critical automated command set:
- `cd flutter_app && flutter test integration_test/critical_access_flow_test.dart --dart-define=ENABLE_FIREBASE=true --dart-define=USE_FIREBASE_EMULATORS=true -r compact`
- `cd flutter_app && flutter test test/features/auth/data/auth_repository_onboarding_bootstrap_test.dart test/features/workouts/presentation/workout_write_resilience_test.dart -r compact`
- `rg -n "emulators:exec|integration_test/critical_access_flow_test.dart" .github/workflows/flutter-release.yml`
</verification>

<success_criteria>
- Emulator-backed integration suite covers login/dashboard/programs/workout-start authenticated path and explicit unauthenticated guards.
- Automated assertions fail on permission-denied signatures and onboarding false-prompt regressions.
- Workout start/progress write and replay-recovery behavior is covered in automated verification.
- CI executes and gates on the dedicated emulator-backed integration lane.
</success_criteria>

<output>
After completion, create `.planning/phases/10-verification-evidence-and-regression-guardrails/10-01-SUMMARY.md`.
</output>
