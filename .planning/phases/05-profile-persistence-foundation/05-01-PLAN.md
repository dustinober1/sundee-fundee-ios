---
phase: 05-profile-persistence-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestore.rules
  - flutter_app/lib/domain/models/user_model.dart
  - flutter_app/lib/domain/models/injury_profile_model.dart
  - flutter_app/lib/features/profile/data/profile_repository.dart
  - flutter_app/lib/features/profile/providers.dart
  - flutter_app/test/domain/models/user_model_test.dart
  - flutter_app/test/features/profile/data/profile_repository_test.dart
  - flutter_app/test/features/profile/data/firestore_profile_rules_test.dart
autonomous: true
must_haves:
  truths:
    - "Legacy and current profile documents normalize into one stable onboarding and injury schema without crash paths."
    - "Onboarding completion and injury-required completeness can be computed from profile data, not a single boolean flag."
    - "Cross-device profile reconciliation is deterministic using profile update timestamps, and failed default writes do not block usage."
  artifacts:
    - path: flutter_app/lib/domain/models/injury_profile_model.dart
      provides: "Canonical injury profile model with required fields, active/resolved status, and history retention support"
    - path: flutter_app/lib/features/profile/data/profile_repository.dart
      provides: "Single profile persistence layer for normalization, defaults/migration writes, and timestamp reconciliation"
    - path: firestore.rules
      provides: "Owner-safe profile schema validation for onboarding and injury structures"
    - path: flutter_app/test/features/profile/data/profile_repository_test.dart
      provides: "Deterministic coverage for normalization, conflict reconciliation, and default-write fallback behavior"
  key_links:
    - from: flutter_app/lib/features/profile/data/profile_repository.dart
      to: flutter_app/lib/domain/models/user_model.dart
      via: "Repository maps raw firestore snapshots into validated profile models before routing/UI consumers read state"
      pattern: "fromJson|normalize|requiredFields"
    - from: flutter_app/lib/features/profile/data/profile_repository.dart
      to: firestore.rules
      via: "Repository writes match rule-enforced schema and owner constraints"
      pattern: "onboarding|injuryProfile|profileUpdatedAt"
---

<objective>
Create the canonical Phase 5 profile data contract and persistence foundation before routing or settings UX changes.

Purpose: Ensure onboarding and injury state is reliably stored, normalized, and reconcilable across sessions/devices.
Output: Profile domain/repository layer + rules contract + foundational tests for migration and conflict behavior.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-profile-persistence-foundation/05-CONTEXT.md
@.planning/phases/05-profile-persistence-foundation/05-RESEARCH.md
@flutter_app/lib/domain/models/user_model.dart
@flutter_app/lib/features/auth/data/auth_repository.dart
@firestore.rules
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical onboarding and injury profile models with legacy-safe normalization</name>
  <files>
    flutter_app/lib/domain/models/user_model.dart
    flutter_app/lib/domain/models/injury_profile_model.dart
    flutter_app/test/domain/models/user_model_test.dart
  </files>
  <action>
    Extend profile modeling to support Phase 5 requirements:
    - hybrid onboarding completeness fields (flag + required answers),
    - required injury fields (`location`, `movementLimitations`, `recoveryGoal`, `status`),
    - multiple concurrent active injuries,
    - soft-resolve semantics (`status=resolved`, `resolvedAt`) instead of hard delete,
    - explicit schema version and profile update timestamp parsing.

    Keep normalization backward-compatible with existing documents (`displayName`, `onboardingComplete`, and missing injury structures) so legacy accounts receive safe in-memory defaults.
  </action>
  <verify>
    cd flutter_app && flutter test test/domain/models/user_model_test.dart
  </verify>
  <done>
    Profile models parse legacy/current data safely and expose deterministic completeness checks for onboarding and injury requirements.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build profile repository for defaults, timestamp reconciliation, and resilient writes</name>
  <files>
    flutter_app/lib/features/profile/data/profile_repository.dart
    flutter_app/lib/features/profile/providers.dart
    flutter_app/test/features/profile/data/profile_repository_test.dart
  </files>
  <action>
    Implement a profile repository that:
    - streams profile snapshots with metadata needed for conflict handling,
    - applies client-side defaults for missing legacy fields and syncs them with merge writes,
    - reconciles local/server profile conflicts using most-recent `profileUpdatedAt` (server wins on tie),
    - treats failed default/profile writes as non-blocking and records retryable operations for later attempts.

    Expose Riverpod providers so downstream auth/routing/settings flows can consume one canonical profile stream.
  </action>
  <verify>
    cd flutter_app && flutter test test/features/profile/data/profile_repository_test.dart
  </verify>
  <done>
    A single repository API provides normalized profile state, deterministic conflict decisions, and retry-friendly write behavior.
  </done>
</task>

<task type="auto">
  <name>Task 3: Harden Firestore rules for profile shape and owner-safe writes</name>
  <files>
    firestore.rules
    flutter_app/test/features/profile/data/firestore_profile_rules_test.dart
  </files>
  <action>
    Update Firestore rules so `/users/{uid}` profile writes enforce:
    - owner-only read/write access,
    - required onboarding/injury field types and allowed status values,
    - rejection of malformed or unknown top-level profile keys for this phase.

    Add emulator-backed rule tests that cover valid profile payloads, invalid injury payloads, and owner isolation.
  </action>
  <verify>
    firebase emulators:exec --only firestore "cd flutter_app && flutter test test/features/profile/data/firestore_profile_rules_test.dart"
  </verify>
  <done>
    Profile schema and ownership constraints are enforceable at rules layer with executable regression tests.
  </done>
</task>

</tasks>

<verification>
Run model/repository tests plus firestore rule tests to prove normalization, reconciliation, and schema constraints are stable before routing/UI integration.
</verification>

<success_criteria>
- Canonical onboarding + injury profile schema exists with safe legacy normalization.
- Profile repository supports defaults sync, timestamp reconciliation, and non-blocking write failures.
- Firestore rules enforce owner-safe profile structure with automated coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/05-profile-persistence-foundation/05-01-SUMMARY.md`.
</output>
