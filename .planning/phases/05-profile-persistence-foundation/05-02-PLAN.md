---
phase: 05-profile-persistence-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - flutter_app/lib/features/auth/domain/auth_state.dart
  - flutter_app/lib/features/auth/data/auth_repository.dart
  - flutter_app/lib/features/auth/providers.dart
  - flutter_app/lib/features/auth/presentation/onboarding_screen.dart
  - flutter_app/lib/features/auth/presentation/loading_screen.dart
  - flutter_app/lib/app/router.dart
  - flutter_app/test/features/auth/data/auth_repository_test.dart
  - flutter_app/test/features/auth/presentation/router_redirect_test.dart
  - flutter_app/test/features/auth/presentation/onboarding_screen_test.dart
autonomous: true
must_haves:
  truths:
    - "Returning authenticated users with complete required onboarding data bypass onboarding by default on all devices."
    - "Partial or contradictory profile state triggers explicit resume/restart decision paths instead of silent loops or crashes."
    - "Missing required injury fields blocks plan flow until injury inputs are completed, while migration default-write failures remain non-blocking with user warning."
  artifacts:
    - path: flutter_app/lib/features/auth/data/auth_repository.dart
      provides: "Bootstrap session state machine using profile repository data, hybrid completeness checks, and conflict handling"
    - path: flutter_app/lib/app/router.dart
      provides: "Route redirects for onboarding resume/restart and injury-required gates"
    - path: flutter_app/lib/features/auth/presentation/onboarding_screen.dart
      provides: "Resume/restart onboarding UX and contradictory-legacy-state handling"
    - path: flutter_app/test/features/auth/presentation/router_redirect_test.dart
      provides: "Deterministic redirect coverage for all new bootstrap states"
  key_links:
    - from: flutter_app/lib/features/auth/data/auth_repository.dart
      to: flutter_app/lib/app/router.dart
      via: "Auth bootstrap statuses map directly to redirect destinations without ambiguous fallthrough"
      pattern: "needsOnboarding|resumeOnboarding|needsInjuryProfile|authenticated"
    - from: flutter_app/lib/features/auth/presentation/onboarding_screen.dart
      to: flutter_app/lib/features/auth/data/auth_repository.dart
      via: "Resume/restart selection and completion actions persist profile updates through repository APIs"
      pattern: "resume|restart|completeOnboarding"
---

<objective>
Implement Phase 5 bootstrap and routing behavior that honors hybrid onboarding/injury gates and legacy conflict handling.

Purpose: Remove repeat onboarding friction while preserving safety gates and deterministic state transitions.
Output: Auth bootstrap state machine + routing updates + onboarding decision UX + test coverage.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-profile-persistence-foundation/05-CONTEXT.md
@.planning/phases/05-profile-persistence-foundation/05-RESEARCH.md
@.planning/phases/05-profile-persistence-foundation/05-01-SUMMARY.md
@flutter_app/lib/features/auth/data/auth_repository.dart
@flutter_app/lib/features/auth/providers.dart
@flutter_app/lib/app/router.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor auth bootstrap to hybrid completeness and timestamp-based reconciliation</name>
  <files>
    flutter_app/lib/features/auth/domain/auth_state.dart
    flutter_app/lib/features/auth/data/auth_repository.dart
    flutter_app/lib/features/auth/providers.dart
    flutter_app/test/features/auth/data/auth_repository_test.dart
  </files>
  <action>
    Replace simple `onboardingComplete` gating with a bootstrap state machine driven by normalized profile data:
    - onboarding completion requires flag plus required-field validation,
    - contradictory legacy state (flag true but missing required answers) is explicitly surfaced,
    - local/server conflicts resolve by most-recent timestamp (server tie-breaker),
    - missing required injury fields map to a dedicated injury-required auth state.

    Preserve non-blocking behavior when migration/default writes fail by emitting warning-capable session metadata rather than forcing logout.
  </action>
  <verify>
    cd flutter_app && flutter test test/features/auth/data/auth_repository_test.dart
  </verify>
  <done>
    Auth bootstrap transitions are deterministic for complete, partial, contradictory, and injury-incomplete profiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add resume/restart onboarding and migration notice UX flows</name>
  <files>
    flutter_app/lib/features/auth/presentation/onboarding_screen.dart
    flutter_app/lib/features/auth/presentation/loading_screen.dart
    flutter_app/test/features/auth/presentation/onboarding_screen_test.dart
  </files>
  <action>
    Update onboarding UX so partially completed users can choose resume from last step or restart from step 1.

    Add contradictory-state handling dialog (keep existing path vs redo onboarding) and show a one-time lightweight migration notice for legacy users after defaults are applied.
    Keep copy concise and non-blocking.
  </action>
  <verify>
    cd flutter_app && flutter test test/features/auth/presentation/onboarding_screen_test.dart
  </verify>
  <done>
    Onboarding decision flows and migration notice behavior match locked context decisions without forcing full reruns for completed users.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update router redirects for new bootstrap states and safety gates</name>
  <files>
    flutter_app/lib/app/router.dart
    flutter_app/test/features/auth/presentation/router_redirect_test.dart
  </files>
  <action>
    Extend routing logic to support the new auth states:
    - allow authenticated users with complete data to bypass onboarding,
    - route partial/contradictory users to onboarding decision surfaces,
    - block plan flows until required injury fields are answered.

    Ensure legal route exceptions still work for unauthenticated users and no redirect loops are introduced.
  </action>
  <verify>
    cd flutter_app && flutter test test/features/auth/presentation/router_redirect_test.dart
  </verify>
  <done>
    Redirect behavior fully covers all bootstrap states and preserves ONB-01/ONB-03 safety/UX requirements.
  </done>
</task>

</tasks>

<verification>
Run auth repository + onboarding + router tests and confirm all new bootstrap states behave deterministically across restart/login scenarios.
</verification>

<success_criteria>
- Hybrid onboarding and injury gating replaces boolean-only routing.
- Resume/restart and contradictory-state UX is implemented for partial legacy profiles.
- Routing enforces injury-required blocking without regressions in existing auth/legal flows.
</success_criteria>

<output>
After completion, create `.planning/phases/05-profile-persistence-foundation/05-02-SUMMARY.md`.
</output>
