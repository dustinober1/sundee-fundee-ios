---
phase: 03-directory-deletion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web-app/ (deleted)
  - firebase/ (deleted)
  - backend/ (deleted)
autonomous: true
requirements:
  - ARCH-02
  - ARCH-03
  - ARCH-04
  - ARCH-05

must_haves:
  truths:
    - "Repository no longer contains web-app/ directory"
    - "Repository no longer contains firebase/ directory"
    - "Repository no longer contains wod-dashboard/ directory (if it existed)"
    - "Repository no longer contains backend/ directory"
    - "Git history shows detailed commit message explaining what was deleted and why"
  artifacts:
    - path: ".planning/phases/03-directory-deletion/03-01-SUMMARY.md"
      provides: "Directory deletion summary"
---

<objective>
Remove all multi-platform directories from the repository. These are backed up in the archive created in Phase 2. AUDIT.md from Phase 1 confirmed zero iOS dependencies on these directories.
</objective>

<tasks>

<task type="auto">
  <name>Delete multi-platform directories</name>
  <files>web-app/, firebase/, backend/</files>
  <read_first>
    .planning/phases/01-pre-cleanup-audit/AUDIT.md
  </read_first>
  <action>
Delete the following directories from the repository using git rm:
1. `git rm -r web-app/` — Next.js PWA (main web application, 211 files)
2. `git rm -r firebase/` — Cloud Functions for AI workout generation (12 files)
3. `git rm -r backend/` — Backend tooling wrappers (teenybase, wrangler)

Do NOT delete: SundeeFundee/, SundeeFundeeApp/, .planning/, .git/, .claude/

Commit with message: "chore: remove web-app, firebase, backend directories (archived in sundee-fundee-archive-2026-04-08.zip)"
  </action>
  <acceptance_criteria>
    - `test ! -d web-app/` — web-app directory no longer exists
    - `test ! -d firebase/` — firebase directory no longer exists
    - `test ! -d backend/` — backend directory no longer exists
    - `git log --oneline -1 | grep -i "remove.*web-app"` — commit references deletion
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. web-app/ directory does not exist
2. firebase/ directory does not exist
3. backend/ directory does not exist
4. wod-dashboard/ does not exist (if it existed before)
5. SundeeFundee/ and SundeeFundeeApp/ still exist and are intact
6. Git commit references what was deleted
</verification>
