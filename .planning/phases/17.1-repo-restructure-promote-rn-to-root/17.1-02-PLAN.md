---
phase: 17.1-repo-restructure-promote-rn-to-root
plan: 02
type: execute
wave: 2
depends_on: ["17.1-01"]
files_modified:
  - .gitignore
  - firebase.json
  - CLAUDE.md
  - package.json (npm install regenerates package-lock.json)
autonomous: true
requirements:
  - RESTRUCTURE-02
must_haves:
  truths:
    - "All 1327 tests pass from the repo root with `npx jest --passWithNoTests`"
    - "Expo config validates without errors from repo root"
    - "Firebase CLI can resolve firestore.rules at root level"
    - "CLAUDE.md commands work without `cd SundeeFundeeRN &&` prefix"
    - "Generated directories (ios/, android/, .expo/, dist/) are gitignored at root level"
  artifacts:
    - path: ".gitignore"
      provides: "Merged gitignore with Expo-specific entries (ios/, android/, .expo/, dist/, web-build/, coverage/)"
      contains: "/ios"
    - path: "firebase.json"
      provides: "Updated rules path pointing to root-level firestore.rules"
      contains: "firestore.rules"
    - path: "CLAUDE.md"
      provides: "Updated project instructions with root-level commands"
    - path: "node_modules/"
      provides: "Fresh npm install at repo root"
  key_links:
    - from: "firebase.json"
      to: "firestore.rules"
      via: "rules path reference"
      pattern: '"rules":\\s*"firestore\\.rules"'
    - from: "jest.config.js"
      to: "src/"
      via: "moduleNameMapper @/* alias"
      pattern: "@/\\*"
    - from: "CLAUDE.md"
      to: "app/"
      via: "Directory references in documentation"
      pattern: "app/"
---

<objective>
Update configuration files, merge .gitignore, install dependencies at root, and validate the full test suite passes after the restructure.

Purpose: Ensure the promoted repo layout is fully functional — all configs point to correct paths, dependencies install cleanly, and the 1327-test regression suite confirms no breakage.

Output: Working repo where `npx jest`, `npx expo start`, and `firebase deploy` all work from root.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.1-repo-restructure-promote-rn-to-root/17.1-RESEARCH.md
@.planning/phases/17.1-repo-restructure-promote-rn-to-root/17.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .gitignore, firebase.json, and install dependencies</name>
  <files>.gitignore, firebase.json, package-lock.json</files>
  <action>
Step 1 — Merge SundeeFundeeRN/.gitignore entries into root .gitignore. The root .gitignore currently has Xcode, macOS, secrets, and node_modules entries. Add these Expo/RN-specific entries that were in the now-removed SundeeFundeeRN/.gitignore:

```
# Expo
.expo/
dist/
web-build/

# Native (generated by Expo prebuild)
/ios
/android
.kotlin/

# Metro
.metro-health-check*

# Jest coverage (generated output, should not be tracked)
coverage/

# Expo auto-generated
expo-env.d.ts

# Debug
npm-debug.*
yarn-debug.*
yarn-error.*

# TypeScript
*.tsbuildinfo
```

Keep all existing root .gitignore entries (Xcode patterns are still relevant for _legacy-swift/ reference). Remove duplicate entries (`.DS_Store`, `*.pem`, `.env*`, `node_modules` are already in root .gitignore).

Also add `google-services.json` to .gitignore (it is an Android secret file, like GoogleService-Info.plist which is already gitignored).

Step 2 — Update firebase.json: change `"rules": "SundeeFundeeRN/firestore.rules"` to `"rules": "firestore.rules"`. The rest of firebase.json (functions config) stays the same.

Step 3 — Run `npm install` from repo root. This creates a fresh node_modules/ at root based on the promoted package.json. The old SundeeFundeeRN/node_modules/ was gitignored and is gone with the directory.

Step 4 — Verify npm install succeeded: `ls node_modules/expo` should exist.
  </action>
  <verify>
    <automated>grep -q '/ios' .gitignore && grep -q '/android' .gitignore && grep -q 'coverage/' .gitignore && echo "gitignore OK" && grep -q '"firestore.rules"' firebase.json && echo "firebase.json OK" && test -d node_modules/expo && echo "npm install OK"</automated>
  </verify>
  <done>.gitignore has all Expo/RN entries merged. firebase.json points to root-level firestore.rules. npm install completed successfully at repo root.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md and validate full test suite</name>
  <files>CLAUDE.md</files>
  <action>
Step 1 — Update CLAUDE.md to reflect the new repo structure. Key changes:

a) Remove "Active codebase: `SundeeFundeeRN/`" — the RN app is now at root. Update to say the RN app is at the repo root.

b) Remove all `cd SundeeFundeeRN &&` prefixes from commands. For example:
- `cd SundeeFundeeRN && npm install` becomes `npm install`
- `cd SundeeFundeeRN && npx jest --passWithNoTests` becomes `npx jest --passWithNoTests`
- `cd SundeeFundeeRN && npx expo start` becomes `npx expo start`

c) Update "Key Directories" section to remove `SundeeFundeeRN/` prefix:
- `app/` (not `SundeeFundeeRN/app/`)
- `src/domain/` (not `SundeeFundeeRN/src/domain/`)
- etc.

d) Add `_legacy-swift/` to the directory section noting it contains archived Swift code.

e) Update "Legacy iOS App" section to note files are now in `_legacy-swift/` subdirectory.

f) Remove the xcodegen command since the .xcodeproj is archived.

g) Update the "React Native App" header — remove `(SundeeFundeeRN/)` suffix since it is now at root.

h) Update the Architecture section's file paths to remove `SundeeFundeeRN/` prefix.

Step 2 — Run the full test suite to validate the restructure:
```bash
npx jest --passWithNoTests
```
All 1327 tests must pass. If any fail, investigate — the restructure should not change any code logic, only file locations. Common cause: a hardcoded path in a test or mock that references `SundeeFundeeRN/`.

Step 3 — Validate Expo config:
```bash
npx expo config --type introspect 2>&1 | head -20
```
Should show valid config without path errors.

Step 4 — Commit all config updates:
```
docs(17.1-02): update configs and CLAUDE.md for root-level RN app
```
  </action>
  <verify>
    <automated>npx jest --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>All 1327 tests pass from repo root. CLAUDE.md reflects new directory structure with no SundeeFundeeRN/ references in commands. Expo config validates. firebase.json resolves firestore.rules at root.</done>
</task>

</tasks>

<verification>
- `npx jest --passWithNoTests` — all 1327 tests pass (full regression)
- `npx expo config --type introspect` — no errors
- `grep -c 'SundeeFundeeRN' firebase.json` returns 0
- `grep -c 'cd SundeeFundeeRN' CLAUDE.md` returns 0
- `grep '/ios' .gitignore` returns a match
- `git status` shows clean working tree after commits
</verification>

<success_criteria>
- All 1327 tests pass from repo root
- Expo config validates without errors
- firebase.json references root-level firestore.rules (not SundeeFundeeRN/ path)
- CLAUDE.md has zero references to `cd SundeeFundeeRN`
- Root .gitignore covers ios/, android/, .expo/, dist/, web-build/, coverage/
- npm install completed successfully at root
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-repo-restructure-promote-rn-to-root/17.1-02-SUMMARY.md`
</output>
