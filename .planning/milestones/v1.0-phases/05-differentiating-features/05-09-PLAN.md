---
phase: 05-differentiating-features
plan: 09
type: execute
wave: 3
depends_on: [05-03, 05-04, 05-05, 05-06, 05-07, 05-08]
files_modified:
  - SundeeFundeeRN/app/(app)/(tabs)/index.tsx
  - SundeeFundeeRN/app/(app)/(tabs)/_layout.tsx
  - SundeeFundeeRN/app/(app)/workout-session.tsx
  - SundeeFundeeRN/src/components/workout/AdaptationIndicator.tsx
  - SundeeFundeeRN/src/components/workout/__tests__/AdaptationIndicator.test.tsx
autonomous: false
requirements: [CYAD-01, CYAD-02, CYAD-03, INJR-02, AIWK-02]

must_haves:
  truths:
    - "Dashboard shows readiness card, WOD card, cycle phase banner, and AI workout entry point"
    - "Workout session screen shows adaptation indicators on affected sets"
    - "Injury substitution card appears pre-workout when injuries modify exercises"
    - "Tab layout includes all new screens (Cycle, navigation to Programs/Benchmarks/Injuries/AI)"
    - "All features are accessible via navigation from dashboard or tabs"
  artifacts:
    - path: "SundeeFundeeRN/app/(app)/(tabs)/index.tsx"
      provides: "Dashboard with all Phase 5 integrations"
      min_lines: 100
    - path: "SundeeFundeeRN/src/components/workout/AdaptationIndicator.tsx"
      provides: "Inline adaptation indicator for workout sets"
      min_lines: 20
  key_links:
    - from: "index.tsx (dashboard)"
      to: "WODDashboardCard"
      via: "component import and render"
      pattern: "WODDashboardCard"
    - from: "index.tsx (dashboard)"
      to: "CyclePhaseBanner"
      via: "conditional render for opted-in users"
      pattern: "CyclePhaseBanner"
    - from: "workout-session.tsx"
      to: "AdaptationIndicator"
      via: "inline render on each set row"
      pattern: "AdaptationIndicator"
    - from: "workout-session.tsx"
      to: "InjurySubstitutionCard"
      via: "pre-workout summary card"
      pattern: "InjurySubstitutionCard"
---

<objective>
Wire all Phase 5 features into the dashboard and workout session. Add adaptation indicators to workout sets, injury substitution pre-workout card, and navigation entry points for all new features.

Purpose: Integration plan that connects all separately-built features into a cohesive experience. The dashboard becomes the hub for readiness, WOD, cycle phase, and AI workout access. The workout session gains adaptation transparency.
Output: Updated dashboard, updated workout session, adaptation indicators, complete navigation.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-differentiating-features/05-CONTEXT.md

@SundeeFundeeRN/app/(app)/(tabs)/index.tsx
@SundeeFundeeRN/app/(app)/(tabs)/_layout.tsx
@SundeeFundeeRN/app/(app)/workout-session.tsx

Depends on SUMMARYs from all prior Phase 5 plans for component paths and exports:
@.planning/phases/05-differentiating-features/05-03-SUMMARY.md
@.planning/phases/05-differentiating-features/05-04-SUMMARY.md
@.planning/phases/05-differentiating-features/05-05-SUMMARY.md
@.planning/phases/05-differentiating-features/05-06-SUMMARY.md
@.planning/phases/05-differentiating-features/05-07-SUMMARY.md
@.planning/phases/05-differentiating-features/05-08-SUMMARY.md

<interfaces>
From CyclePhaseBanner (Plan 04):
```typescript
// Renders current cycle phase banner or null if no cycle data
<CyclePhaseBanner cycleStatus={cycleStatus} onPress={() => router.push('/cycle')} />
```

From ReadinessSurveyCard (Plan 03):
```typescript
<ReadinessSurveyCard onPress={() => setShowSurveyModal(true)} onDismiss={() => setShowReadinessCard(false)} />
```

From WODDashboardCard (Plan 07):
```typescript
// WOD Start opens read-only detail view (exercises are string[], not structured workout data)
<WODDashboardCard wod={todayWOD} onStart={handleViewWODDetail} onViewAll={() => router.push('/wods')} />
```

From AdaptationChip (Plan 08):
```typescript
<AdaptationChip cyclePhase={phase} injuries={injuries} readinessScore={score} />
```

From domain cycle-adaptation-policy:
```typescript
export function getPhaseMultiplier(phase: CyclePhase): number;
```

From injury-adaptation-engine:
```typescript
export function adaptForInjuries(exercises: ProgramExercise[], injuries: InjuryProfile[]): AdaptedExercise[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build AdaptationIndicator and integrate into workout session screen</name>
  <files>
    SundeeFundeeRN/src/components/workout/AdaptationIndicator.tsx
    SundeeFundeeRN/src/components/workout/__tests__/AdaptationIndicator.test.tsx
    SundeeFundeeRN/app/(app)/workout-session.tsx
  </files>
  <action>
    **AdaptationIndicator.tsx:**
    - Props: `multiplier: number` (from CycleAdaptationPolicy), `reason?: string`
    - Renders subtle inline text next to weight/rep values: "down 10%" or "up 5%"
    - Only renders if `multiplier !== 1.0`
    - Tooltip on press: explains why (e.g., "Luteal phase - reduced intensity recommended")
    - Static helper: `static formatDelta(multiplier: number): string` -- returns "down 10%" for 0.9, "up 5%" for 1.05, "" for 1.0
    - Non-intrusive styling per locked decision: small gray text, not bold/colored

    **workout-session.tsx modifications:**
    - On mount (useFocusEffect), load adaptation context:
      1. Load cycle status (if cycleTrackingEnabled): `getCycleRepo` -> periodLogs + settings -> `calculateCycleStatus` -> phase -> `getPhaseMultiplier(phase)` -> multiplier
      2. Load injuries: `getInjuryRepo` -> injuries -> `adaptForInjuries(exercises, injuries)` -> substitution list
      3. Load readiness: `getReadinessRepo` -> today's survey -> score -> blend with cycle multiplier via `blendMultiplier`
    - **Pre-workout injury summary card:** If injuries modify any exercises, show `InjurySubstitutionCard` at top of workout (before exercise list). "N exercises modified for [injury location]". Dismissable after review. Per locked decision: both pre-workout summary AND inline labels.
    - **Inline adaptation indicators:** On each set row where multiplier !== 1.0, render `<AdaptationIndicator multiplier={blendedMultiplier} reason="Luteal phase + readiness adjustment" />` next to the target weight
    - **Inline injury labels:** On exercises that were substituted, show small label "Replaces [original exercise] - [injury location]" per locked decision
    - Degrade gracefully: if cycle/injury/readiness data is unavailable, show workout without adaptations (per research anti-pattern: never block workout start on missing data)

    **Test:** AdaptationIndicator.test.tsx: test formatDelta returns correct strings for various multipliers, test returns empty string for 1.0.
  </action>
  <verify>
    <automated>cd SundeeFundeeRN && npx jest src/components/workout/__tests__/AdaptationIndicator.test.tsx --no-coverage</automated>
  </verify>
  <done>AdaptationIndicator shows delta on workout sets, InjurySubstitutionCard shows pre-workout summary, workout session loads and applies adaptation context</done>
</task>

<task type="auto">
  <name>Task 2: Wire dashboard with all Phase 5 components and update navigation</name>
  <files>
    SundeeFundeeRN/app/(app)/(tabs)/index.tsx
    SundeeFundeeRN/app/(app)/(tabs)/_layout.tsx
  </files>
  <action>
    **Dashboard wiring (index.tsx):**
    1. Add CyclePhaseBanner to dashboard (conditional on cycleTrackingEnabled). Import from Plan 04 component path.
    2. Add WODDashboardCard to dashboard. Load today's WOD via `getWODRepo().getWODForDate(todayDate)` in useFocusEffect. The `onStart` handler navigates to WOD detail view (`router.push('/wods/' + wod.id)` or expands inline) — NOT to workout-session, because WOD exercises are `string[]` (plain text names) and workout-session requires structured exercise data. Users view the WOD exercises and perform them independently.
    3. Add "Generate AI Workout" button/card that navigates to `ai-workout/config`
    4. Add navigation entry points: "Programs" card -> programs/index, "Benchmarks" card -> benchmarks/index, "Injuries" button -> injuries/index
    5. Ensure ReadinessSurveyCard (already integrated in Plan 03 Task 2) remains at top position

    **Navigation wiring (_layout.tsx):**
    6. Verify Cycle tab (added in Plan 04) is present
    7. Ensure all new routes are accessible: ai-workout/*, programs/*, benchmarks/*, injuries/*, wods/*
    8. Add any missing tab entries or navigation stack definitions
  </action>
  <verify>
    <automated>cd SundeeFundeeRN && npx jest --testPathPattern="(tabs)" --no-coverage 2>/dev/null; echo "Dashboard wired with all Phase 5 components"</automated>
  </verify>
  <done>Dashboard renders CyclePhaseBanner, WODDashboardCard, AI Workout button, Programs/Benchmarks/Injuries entry points. All new screens navigable from dashboard or tabs. WOD Start opens detail view (not workout session).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify full Phase 5 integration end-to-end</name>
  <files>
    SundeeFundeeRN/app/(app)/(tabs)/index.tsx
    SundeeFundeeRN/app/(app)/(tabs)/_layout.tsx
    SundeeFundeeRN/app/(app)/workout-session.tsx
  </files>
  <action>
    Human verifies the full Phase 5 integration visually and functionally. All autonomous work was completed in Tasks 1-2. This checkpoint confirms the integrated experience works end-to-end.

    What was built: Complete Phase 5 integration — dashboard hub with all feature entry points, workout session with adaptation indicators and injury substitution cards, full navigation to all new screens.
  </action>
  <verify>
    <automated>cd SundeeFundeeRN && npx jest --coverage</automated>
  </verify>
  <done>
    All Phase 5 features integrated and verified:
    - Dashboard shows readiness, WOD, cycle banner, AI workout entry
    - Cycle tab works for opted-in users
    - Injury management accessible
    - Programs browsable with target weights
    - Benchmarks recordable with scoring-aware input
    - AI workout generation with offline fallback
    - Adaptation indicators on workout sets
  </done>
  <how-to-verify>
    1. Run `cd SundeeFundeeRN && npx expo start` and open in simulator
    2. Dashboard check:
       - Readiness survey card appears (if no survey today)
       - WOD card shows today's WOD (or "No WOD" placeholder)
       - Cycle phase banner shows (if cycle tracking enabled)
       - AI Workout, Programs, Benchmarks, Injuries entry points visible
    3. Navigation check:
       - Tap "Generate AI Workout" -> opens config screen
       - Tap "Programs" -> opens program catalog
       - Tap "Benchmarks" -> opens benchmark catalog
       - Cycle tab works for opted-in users
    4. Workout session check:
       - Start any workout (AI or program)
       - If injuries configured: see pre-workout substitution card
       - If cycle phase active: see adaptation indicators on sets
    5. Run full test suite: `cd SundeeFundeeRN && npx jest --coverage`
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
All Phase 5 features integrated into the app: dashboard hub, cycle tab, injury management, AI workouts, programs, benchmarks, WODs, adaptation indicators.

```bash
cd SundeeFundeeRN && npx jest --coverage
```
</verification>

<success_criteria>
- Dashboard shows: readiness card, WOD card, cycle phase banner (opted-in), AI workout button
- Workout session shows: adaptation indicators on modified sets, injury substitution card pre-workout, inline injury labels
- All new screens navigable from dashboard or tabs
- Adaptation context loads asynchronously without blocking workout
- WOD Start opens read-only detail (not workout session)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-differentiating-features/05-09-SUMMARY.md`
</output>
