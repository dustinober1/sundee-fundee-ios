---
phase: 05-differentiating-features
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - SundeeFundeeRN/functions/src/generateWorkout.ts
  - SundeeFundeeRN/functions/package.json
  - SundeeFundeeRN/functions/src/index.ts
  - SundeeFundeeRN/functions/__tests__/generateWorkout.test.ts
autonomous: true
requirements: [AIWK-01, AIWK-02]

must_haves:
  truths:
    - "Authenticated user can generate a workout in under 60 seconds"
    - "Workout generation incorporates cyclePhase, injuries, and readinessScore from WorkoutGenerationContext for personalized adaptation"
    - "Unauthenticated requests are rejected with appropriate error"
    - "Malformed AI responses degrade gracefully with retry"
  artifacts:
    - path: "SundeeFundeeRN/functions/src/generateWorkout.ts"
      provides: "Gemini-powered onCall Cloud Function"
      contains: "GoogleGenerativeAI"
    - path: "SundeeFundeeRN/functions/package.json"
      provides: "@google/generative-ai dependency"
      contains: "@google/generative-ai"
    - path: "SundeeFundeeRN/functions/__tests__/generateWorkout.test.ts"
      provides: "Unit tests for Cloud Function"
      min_lines: 30
  key_links:
    - from: "functions/src/generateWorkout.ts"
      to: "@google/generative-ai"
      via: "Gemini SDK import"
      pattern: "GoogleGenerativeAI"
    - from: "functions/src/generateWorkout.ts"
      to: "WorkoutGenerationContext"
      via: "prompt includes cyclePhase, activeInjuries, readinessTier"
      pattern: "cyclePhase|activeInjuries|readinessTier"
---

<objective>
Migrate the Firebase Cloud Function from Anthropic Claude to Google Gemini for AI workout generation, ensuring the Gemini prompt includes all context fields for personalized adaptation (AIWK-02).

Purpose: The Cloudflare Worker (being replaced) used Gemini. The Cloud Function currently uses Anthropic. Per locked decision, switch to Gemini and add minInstances: 1 for cold start mitigation. The prompt must carry cyclePhase, activeInjuries, and readinessTier from WorkoutGenerationContext so Gemini can personalize workouts.
Output: Updated Cloud Function using @google/generative-ai, with GEMINI_API_KEY secret, minInstances config, JSON response mode, and context-aware prompt.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-differentiating-features/05-CONTEXT.md
@.planning/phases/05-differentiating-features/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate generateWorkout Cloud Function from Anthropic to Gemini with context-aware prompt</name>
  <files>
    SundeeFundeeRN/functions/src/generateWorkout.ts
    SundeeFundeeRN/functions/package.json
    SundeeFundeeRN/functions/src/index.ts
    SundeeFundeeRN/functions/__tests__/generateWorkout.test.ts
  </files>
  <action>
    1. Read existing `functions/src/generateWorkout.ts` to understand current Anthropic implementation (auth checks, prompt structure, response validation, retry logic, Firestore save).

    2. In `functions/package.json`: add `"@google/generative-ai": "^0.21.0"` to dependencies. Remove `@anthropic-ai/sdk` if present.

    3. In `generateWorkout.ts`:
       - Replace Anthropic SDK import with: `import { GoogleGenerativeAI } from "@google/generative-ai";`
       - Define secret: `const GEMINI_API_KEY = defineSecret("GEMINI_API_KEY");`
       - Update onCall config to: `{ minInstances: 1, memory: "256MiB", timeoutSeconds: 60, secrets: [GEMINI_API_KEY], concurrency: 80 }`
       - Initialize Gemini: `const genAI = new GoogleGenerativeAI(GEMINI_API_KEY.value()); const model = genAI.getGenerativeModel({ model: "gemini-2.0-flash" });`
       - Use `responseMimeType: "application/json"` in generationConfig to get native JSON responses (per research recommendation) — eliminates need for response parsing.
       - Translate Anthropic message format to Gemini format: `systemInstruction` for system prompt, `contents` for user message.
       - **CRITICAL (AIWK-02):** Verify the migrated Gemini prompt explicitly includes these WorkoutGenerationContext fields in the user message:
         - `cyclePhase` — e.g., "User is in luteal phase" (or omit line if null)
         - `activeInjuries` — e.g., "Active injuries: Right knee (subacute) — avoid deep squats" (or omit if empty array)
         - `readinessTier` — e.g., "Readiness: fatigued — reduce volume" (or omit if null)
         These fields drive personalized adaptation. If the existing Anthropic prompt includes them, preserve them exactly. If missing, add them to the user message section of the prompt.
       - Keep existing auth check (`request.auth` validation).
       - Keep existing Firestore save logic (saves generated workout to `generatedWorkouts` collection for audit).
       - Keep existing retry logic for malformed responses. With `responseMimeType: "application/json"`, retries should be less frequent.
       - Model name: use `"gemini-2.0-flash"` (stable, fast, cost-effective for structured JSON generation).

    4. If `functions/src/index.ts` re-exports the function, ensure it still works after the migration.

    5. Run `cd SundeeFundeeRN/functions && npm install` to install new dependency.

    6. Create `functions/__tests__/generateWorkout.test.ts`:
       - Test that the prompt builder includes cyclePhase when provided
       - Test that the prompt builder includes activeInjuries when provided
       - Test that the prompt builder includes readinessTier when provided
       - Test that null/empty context fields are gracefully omitted from prompt
       - Extract the prompt-building logic into a testable pure function (e.g., `buildPrompt(context: WorkoutGenerationContext): string`) so it can be tested without calling Gemini.

    Note: The GEMINI_API_KEY secret must be set via `firebase functions:secrets:set GEMINI_API_KEY` — this is a user_setup item, not automatable.
  </action>
  <verify>
    <automated>cd SundeeFundeeRN/functions && npm install && npx tsc --noEmit && npx jest __tests__/generateWorkout.test.ts --no-coverage</automated>
  </verify>
  <done>Cloud Function compiles with Gemini SDK, Anthropic SDK removed, minInstances: 1 configured, JSON response mode enabled, prompt includes cyclePhase/activeInjuries/readinessTier from WorkoutGenerationContext, tests pass</done>
</task>

</tasks>

<verification>
Cloud Function TypeScript compiles without errors. Package.json includes @google/generative-ai and does not include @anthropic-ai/sdk. Prompt includes context fields for personalization.

```bash
cd SundeeFundeeRN/functions && npx tsc --noEmit && grep -c "generative-ai" package.json && npx jest __tests__/ --no-coverage
```
</verification>

<success_criteria>
- generateWorkout.ts uses GoogleGenerativeAI (not Anthropic)
- minInstances: 1 set on function config
- responseMimeType: "application/json" configured
- GEMINI_API_KEY secret referenced (not hardcoded)
- Prompt includes cyclePhase, activeInjuries, readinessTier from WorkoutGenerationContext (AIWK-02)
- Unit tests verify context fields appear in generated prompt
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-differentiating-features/05-02-SUMMARY.md`
</output>
