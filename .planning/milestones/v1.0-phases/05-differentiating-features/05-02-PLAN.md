---
phase: 05-differentiating-features
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pwa/src/components/SkeletonCard.tsx
  - pwa/src/components/SkeletonCard.module.css
  - pwa/src/components/SkeletonCard.test.tsx
  - pwa/src/routes/Dashboard.tsx
  - pwa/src/routes/Programs.tsx
  - pwa/src/routes/History.tsx
  - pwa/src/routes/Cycle.tsx
  - pwa/src/routes/Maxes.tsx
autonomous: true
requirements:
  - UX-02

must_haves:
  truths:
    - "Navigating to Dashboard before data loads shows shimmer skeleton cards, not blank space"
    - "Navigating to Programs before data loads shows shimmer skeleton cards, not a spinner"
    - "Navigating to History before data loads shows shimmer skeleton cards, not a spinner"
    - "Navigating to Cycle before data loads shows shimmer skeleton cards, not a spinner"
    - "Navigating to Maxes before data loads shows shimmer skeleton cards, not a spinner"
  artifacts:
    - path: "pwa/src/components/SkeletonCard.tsx"
      provides: "Reusable shimmer skeleton placeholder"
      exports: ["SkeletonCard"]
    - path: "pwa/src/components/SkeletonCard.module.css"
      provides: "CSS shimmer animation keyframes"
      contains: "@keyframes shimmer"
  key_links:
    - from: "pwa/src/routes/Dashboard.tsx"
      to: "pwa/src/components/SkeletonCard.tsx"
      via: "import and render when isLoading is true"
      pattern: "import.*SkeletonCard"
    - from: "pwa/src/routes/Programs.tsx"
      to: "pwa/src/components/SkeletonCard.tsx"
      via: "import and render when isLoading is true"
      pattern: "import.*SkeletonCard"
    - from: "pwa/src/routes/History.tsx"
      to: "pwa/src/components/SkeletonCard.tsx"
      via: "import and render when isLoading is true"
      pattern: "import.*SkeletonCard"
    - from: "pwa/src/routes/Cycle.tsx"
      to: "pwa/src/components/SkeletonCard.tsx"
      via: "import and render when isLoading is true"
      pattern: "import.*SkeletonCard"
    - from: "pwa/src/routes/Maxes.tsx"
      to: "pwa/src/components/SkeletonCard.tsx"
      via: "import and render when isLoading is true"
      pattern: "import.*SkeletonCard"
---

<objective>
Replace spinner loading states with shimmer skeleton cards on all data-fetching routes.

Purpose: Users should see content-shaped placeholders during data fetches instead of blank space or generic spinners (UX-02).
Output: Reusable SkeletonCard component, wired into Dashboard, Programs, History, Cycle, and Maxes routes.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.0-phases/05-differentiating-features/05-RESEARCH.md

@pwa/src/theme/theme.css
@pwa/src/routes/Dashboard.tsx
@pwa/src/routes/Programs.tsx
@pwa/src/routes/History.tsx
@pwa/src/routes/Cycle.tsx
@pwa/src/routes/Maxes.tsx

<interfaces>
<!-- Design tokens from theme.css needed for shimmer -->
--color-grey-light: #E0DDD4  (skeleton base color)
--color-cream-light: #FAF7EF (shimmer highlight sweep)
--radius-sm: 8px
--radius-md: 12px
--space-sm: 8px
--space-md: 16px

<!-- Existing loading patterns in routes -->
<!-- History, Programs, Maxes: useState(true) + spinner div in isLoading branch -->
<!-- Cycle: early return with spinner div when isLoading -->
<!-- Dashboard: NO isLoading state — needs to be added -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SkeletonCard component with tests</name>
  <files>
    pwa/src/components/SkeletonCard.tsx
    pwa/src/components/SkeletonCard.module.css
    pwa/src/components/SkeletonCard.test.tsx
  </files>
  <behavior>
    - SkeletonCard with default props renders 4 shimmer div elements
    - SkeletonCard with count=2 renders exactly 2 shimmer div elements
    - SkeletonCard with count=6 renders exactly 6 shimmer div elements
    - Each shimmer div has the CSS module class for the card style
  </behavior>
  <action>
    Write failing tests first (RED), then implement.

    **Tests:** Use vitest + @testing-library/react. Render SkeletonCard with various `count` props. Assert the number of shimmer elements rendered using `container.querySelectorAll` or test IDs.

    **SkeletonCard.tsx:**
    ```typescript
    interface SkeletonCardProps {
      count?: number;   // Number of skeleton cards to render (default: 4)
      height?: number;  // Height of each card in px (default: 80)
    }
    export function SkeletonCard({ count = 4, height = 80 }: SkeletonCardProps)
    ```
    - Render `count` div elements, each with the `.card` CSS module class and `style={{ height }}`
    - Use `Array.from({ length: count })` to generate elements
    - Each div gets `aria-hidden="true"` for accessibility (decorative placeholder)

    **SkeletonCard.module.css:**
    ```css
    @keyframes shimmer {
      0%   { background-position: -200% 0; }
      100% { background-position:  200% 0; }
    }
    .card {
      background: linear-gradient(
        90deg,
        var(--color-grey-light) 25%,
        var(--color-cream-light) 50%,
        var(--color-grey-light) 75%
      );
      background-size: 200% 100%;
      animation: shimmer 1.4s ease-in-out infinite;
      border-radius: var(--radius-sm);
      margin-bottom: var(--space-sm);
    }
    ```
  </action>
  <verify>
    <automated>cd pwa && npx vitest run src/components/SkeletonCard.test.tsx</automated>
  </verify>
  <done>SkeletonCard renders the correct number of shimmer div elements with CSS animation class. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SkeletonCard into all 5 data-fetching routes</name>
  <files>
    pwa/src/routes/Dashboard.tsx
    pwa/src/routes/Programs.tsx
    pwa/src/routes/History.tsx
    pwa/src/routes/Cycle.tsx
    pwa/src/routes/Maxes.tsx
  </files>
  <action>
    Replace existing spinner/blank loading states with SkeletonCard in all five routes.

    **Programs.tsx, History.tsx, Maxes.tsx** (same pattern for all three):
    - Add `import { SkeletonCard } from '../components/SkeletonCard';`
    - Replace the `isLoading ? (<div className={styles.center}><div className={styles.spinner} /></div>)` branch with `isLoading ? (<SkeletonCard count={4} height={72} />)` — adjust height to approximate the real card height in each route
    - Remove the `.spinner` and `.center` CSS classes from each route's `.module.css` if they are only used for the loading spinner (check first — `.center` may be used elsewhere)

    **Cycle.tsx** (early-return pattern):
    - Add `import { SkeletonCard } from '../components/SkeletonCard';`
    - Replace the early return `if (isLoading) return <div className={styles.center}><div className={styles.spinner} /></div>;` with `if (isLoading) return <div className={styles.page}><h1 className={styles.title}>Cycle Tracking</h1><SkeletonCard count={3} height={100} /></div>;` — keep the page title visible during loading for context
    - Remove spinner CSS if no longer used

    **Dashboard.tsx** (special case — no isLoading state):
    - Add `import { SkeletonCard } from '../components/SkeletonCard';`
    - Add `const [isLoading, setIsLoading] = useState(true);` state
    - Merge the existing data-fetching logic (profile + todayWOD) into a single useEffect with `Promise.all`. Set `isLoading` to `false` in the `.finally()` block after both fetches complete
    - In the render, wrap the main content in an `isLoading` ternary: show `<SkeletonCard count={3} height={80} />` when loading, show existing content when loaded
    - Keep the greeting/header visible during loading (skeleton replaces only the data cards area)

    **Height guidelines** (approximate real card heights):
    - Dashboard: 80px (WOD card + quick-links)
    - Programs: 72px (program list cards)
    - History: 72px (workout history cards)
    - Cycle: 100px (cycle tracking cards are taller)
    - Maxes: 64px (max lift rows are compact)
  </action>
  <verify>
    <automated>cd pwa && npx vitest run && npx tsc -b --noEmit</automated>
  </verify>
  <done>All 5 routes show SkeletonCard during loading instead of spinners or blank space. TypeScript compiles with no errors. Full test suite passes.</done>
</task>

</tasks>

<verification>
cd pwa && npx vitest run src/components/SkeletonCard.test.tsx && npx tsc -b --noEmit
</verification>

<success_criteria>
- SkeletonCard component exists and renders configurable shimmer cards
- Dashboard, Programs, History, Cycle, and Maxes all import and render SkeletonCard during loading
- No spinners remain in the 5 data-fetching routes
- Dashboard has isLoading state that controls skeleton visibility
- Full test suite and TypeScript compilation pass
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/05-differentiating-features/05-02-SUMMARY.md`
</output>
