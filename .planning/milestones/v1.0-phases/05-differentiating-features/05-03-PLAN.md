---
phase: 05-differentiating-features
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - SundeeFundeeRN/src/repositories/ReadinessRepo.ts
  - SundeeFundeeRN/src/repositories/FirestoreReadinessRepo.ts
  - SundeeFundeeRN/src/repositories/LocalReadinessRepo.ts
  - SundeeFundeeRN/src/repositories/__tests__/FirestoreReadinessRepo.test.ts
  - SundeeFundeeRN/src/repositories/__tests__/LocalReadinessRepo.test.ts
  - SundeeFundeeRN/src/domain/readiness/readiness-survey.ts
  - SundeeFundeeRN/src/domain/__tests__/ai-workout.test.ts
  - SundeeFundeeRN/src/components/readiness/ReadinessSurveyCard.tsx
  - SundeeFundeeRN/src/components/readiness/ReadinessSurveyModal.tsx
  - SundeeFundeeRN/app/(app)/(tabs)/index.tsx
  - SundeeFundeeRN/src/components/readiness/__tests__/ReadinessSurveyCard.test.tsx
autonomous: true
requirements: [READ-01, READ-02, CYCL-02]

must_haves:
  truths:
    - "User sees dismissable readiness prompt on dashboard when no survey completed today"
    - "User can complete 4-slider readiness survey (sleep, energy, stress, motivation) in under 15 seconds"
    - "Readiness score is saved via ReadinessRepository and available for workout adaptation"
    - "Survey card disappears after completion or dismissal"
    - "CYCL-02 satisfied via readiness survey per locked decision: sleep/energy/stress/motivation sliders capture equivalent symptom data"
  artifacts:
    - path: "SundeeFundeeRN/src/components/readiness/ReadinessSurveyCard.tsx"
      provides: "Dashboard readiness prompt card"
      min_lines: 30
    - path: "SundeeFundeeRN/src/components/readiness/ReadinessSurveyModal.tsx"
      provides: "4-slider survey modal"
      min_lines: 60
  key_links:
    - from: "ReadinessSurveyModal.tsx"
      to: "ReadinessRepo"
      via: "getReadinessRepo().saveSurvey()"
      pattern: "getReadinessRepo.*saveSurvey"
    - from: "ReadinessSurveyCard.tsx"
      to: "app/(app)/(tabs)/index.tsx"
      via: "rendered in dashboard"
      pattern: "ReadinessSurveyCard"
---

<objective>
Build the readiness survey UI: a dismissable dashboard card that prompts users to complete a quick 4-slider survey (sleep, energy, stress, motivation), plus the survey modal itself. Extends the data model with an `energyLevel` field per locked decision requiring 4 sliders.

Purpose: Readiness score feeds into cycle adaptation and AI workout generation (READ-02, CYAD-03). Also satisfies CYCL-02 (daily symptom logging) since the locked decision maps symptoms to the readiness survey instead of a separate feature — sleep/energy/stress/motivation sliders capture equivalent symptom data.
Output: ReadinessSurveyCard component, ReadinessSurveyModal component, dashboard integration, extended data model.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-differentiating-features/05-CONTEXT.md

@SundeeFundeeRN/src/repositories/ReadinessRepo.ts
@SundeeFundeeRN/src/domain/readiness/readiness-survey.ts
@SundeeFundeeRN/app/(app)/(tabs)/index.tsx

<interfaces>
From src/repositories/ReadinessRepo.ts (will be extended):
```typescript
export interface ReadinessSurveyRecord {
  id: string; uid: string; date: string;
  sleepQuality: number; stressLevel: number; sorenessLevel: number;
  // NEW: energyLevel field to be added per locked decision
  result: ReadinessResult;
}
export interface ReadinessRepository {
  saveSurvey(uid: string, record: ReadinessSurveyRecord): Promise<void>;
  getSurveyForDate(uid: string, date: string): Promise<ReadinessSurveyRecord | null>;
}
export function getReadinessRepo(isGuest: boolean): ReadinessRepository;
```

From src/domain/readiness/readiness-survey.ts (will be extended):
```typescript
export function calculateReadinessScore(sleepQuality: number, stressLevel: number, sorenessLevel: number): ReadinessResult;
// Will add energyLevel parameter
export interface ReadinessResult { score: number; tier: ReadinessTier; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend data model with energyLevel and build survey components</name>
  <files>
    SundeeFundeeRN/src/repositories/ReadinessRepo.ts
    SundeeFundeeRN/src/repositories/FirestoreReadinessRepo.ts
    SundeeFundeeRN/src/repositories/LocalReadinessRepo.ts
    SundeeFundeeRN/src/repositories/__tests__/FirestoreReadinessRepo.test.ts
    SundeeFundeeRN/src/repositories/__tests__/LocalReadinessRepo.test.ts
    SundeeFundeeRN/src/domain/readiness/readiness-survey.ts
    SundeeFundeeRN/src/domain/__tests__/ai-workout.test.ts
    SundeeFundeeRN/src/components/readiness/ReadinessSurveyCard.tsx
    SundeeFundeeRN/src/components/readiness/ReadinessSurveyModal.tsx
    SundeeFundeeRN/src/components/readiness/__tests__/ReadinessSurveyCard.test.tsx
  </files>
  <action>
    **Step 1: Extend ReadinessSurveyRecord with energyLevel field (locked decision: 4 sliders)**

    In `ReadinessRepo.ts`: Add `energyLevel: number;` to `ReadinessSurveyRecord` interface (between stressLevel and result). This makes it optional-compatible: existing records without energyLevel will have `undefined` which is handled by defaulting to 5 in the scoring function.

    In `readiness-survey.ts`: Update `calculateReadinessScore` signature to accept 4 parameters:
    ```typescript
    export function calculateReadinessScore(
      sleepQuality: number,
      stressLevel: number,
      sorenessLevel: number,
      energyLevel: number = 5,  // default for backward compatibility
    ): ReadinessResult
    ```
    Adjust weights: SLEEP_WEIGHT=0.3, STRESS_WEIGHT=0.25, SORENESS_WEIGHT=0.25, ENERGY_WEIGHT=0.2. Energy is not inverted (higher = better, like sleep). Update the formula accordingly.

    Update all existing test call sites in `ai-workout.test.ts` that call `calculateReadinessScore(a, b, c)` to still pass with the new default parameter — verify tests pass without modification since the 4th param defaults to 5.

    Update repo implementations (FirestoreReadinessRepo, LocalReadinessRepo) to include energyLevel in save/load. Update repo test fixtures to include energyLevel.

    **Step 2: Build ReadinessSurveyCard.tsx**
    - Dismissable card component for the dashboard
    - Props: `onPress: () => void` (opens survey modal), `onDismiss: () => void`
    - Art Deco styling: cream card on navy background, orange accent
    - Text: "How are you feeling today?" with subtext "Quick check-in takes 15 seconds"
    - Dismiss X button in top-right corner
    - Available to ALL users (not just cycle-tracking users, per locked decision)

    **Step 3: Build ReadinessSurveyModal.tsx**
    - Modal with 4 sliders per locked decision:
      1. Sleep Quality (1-10) -> maps to `sleepQuality`
      2. Energy Level (1-10) -> maps to `energyLevel` (NEW field)
      3. Stress Level (1-10) -> maps to `stressLevel`
      4. Motivation (1-10) -> maps to `sorenessLevel` (labeled "Body Soreness / Motivation" — represents physical readiness factor)
    - Each slider: horizontal Slider component with value label (1-10)
    - "Submit" button at bottom, calls `calculateReadinessScore(sleep, stress, soreness, energy)` then `getReadinessRepo(isGuest).saveSurvey()`
    - Show result after save: "Your readiness: 7/10 (High)" with tier color coding
    - Auto-dismiss after 2 seconds showing result
    - Uses `format(new Date(), 'yyyy-MM-dd')` from date-fns for date key (local time, per pitfall 4)
    - Thread `uid` from `useSession()` context

    **Step 4: Test**
    Test that ReadinessSurveyCard renders with expected text, onPress callback fires, static helper for score display formatting.
  </action>
  <verify>
    <automated>cd SundeeFundeeRN && npx jest src/components/readiness/__tests__/ src/repositories/__tests__/FirestoreReadinessRepo.test.ts src/repositories/__tests__/LocalReadinessRepo.test.ts src/domain/__tests__/ai-workout.test.ts --no-coverage</automated>
  </verify>
  <done>ReadinessSurveyRecord has energyLevel field, calculateReadinessScore accepts 4 params with backward-compatible default, ReadinessSurveyCard and ReadinessSurveyModal render correctly with 4 sliders, survey saves via ReadinessRepo, existing tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Integrate readiness survey into dashboard</name>
  <files>
    SundeeFundeeRN/app/(app)/(tabs)/index.tsx
  </files>
  <action>
    In the dashboard screen (`app/(app)/(tabs)/index.tsx`):

    1. Add state: `showReadinessCard`, `showSurveyModal`, `todayReadiness`
    2. On `useFocusEffect`: check if today's survey exists via `getReadinessRepo(isGuest).getSurveyForDate(uid, todayDate)`. If null, show ReadinessSurveyCard. If exists, store result in `todayReadiness` for display.
    3. Render `ReadinessSurveyCard` at top of dashboard (above existing content) when `showReadinessCard && !todayReadiness`
    4. On card press: open `ReadinessSurveyModal`
    5. On card dismiss: hide card for this session (set `showReadinessCard = false`)
    6. On survey complete: hide modal, hide card, set `todayReadiness` to the result
    7. If `todayReadiness` exists, optionally show a small "Readiness: 7/10" badge in the dashboard header area

    Ensure existing dashboard content (last workout card, etc.) is not disrupted. The readiness card sits at the top, above existing cards.
  </action>
  <verify>
    <automated>cd SundeeFundeeRN && npx jest --testPathPattern="index" --no-coverage 2>/dev/null; echo "Manual: dashboard shows readiness prompt when no survey completed today"</automated>
  </verify>
  <done>Dashboard shows readiness survey prompt for all users when no survey completed today; completing survey saves result and hides prompt; 4 sliders match locked decision (sleep, energy, stress, motivation)</done>
</task>

</tasks>

<verification>
Readiness survey flow: dashboard card appears -> tap opens modal -> 4 sliders (sleep, energy, stress, motivation) -> submit saves to ReadinessRepo -> card disappears.

```bash
cd SundeeFundeeRN && npx jest src/components/readiness/ src/repositories/__tests__/ --no-coverage
```
</verification>

<success_criteria>
- ReadinessSurveyRecord includes energyLevel field
- calculateReadinessScore accepts 4 parameters (backward-compatible default on energyLevel)
- ReadinessSurveyCard renders on dashboard when no survey completed today
- ReadinessSurveyModal has 4 sliders matching locked decision: sleep, energy, stress, motivation
- Survey saves via getReadinessRepo with correct date key
- Card disappears after completion or dismissal
- Available to all users (not gated on cycle tracking)
- CYCL-02 covered: readiness survey captures equivalent symptom data
</success_criteria>

<output>
After completion, create `.planning/phases/05-differentiating-features/05-03-SUMMARY.md`
</output>
