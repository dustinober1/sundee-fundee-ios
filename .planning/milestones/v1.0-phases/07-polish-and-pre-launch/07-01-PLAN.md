---
phase: 07-polish-and-pre-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SundeeFundeeRN/src/utils/formatWeight.ts
  - SundeeFundeeRN/src/utils/__tests__/formatWeight.test.ts
  - SundeeFundeeRN/src/firebase/appCheck.ts
  - SundeeFundeeRN/app/(app)/(tabs)/settings.tsx
  - SundeeFundeeRN/app/(app)/_layout.tsx
  - SundeeFundeeRN/app/(app)/(tabs)/history.tsx
  - SundeeFundeeRN/app/(app)/(tabs)/maxes.tsx
  - SundeeFundeeRN/src/components/workout/SetRow.tsx
  - SundeeFundeeRN/package.json
autonomous: true
requirements:
  - PLAT-04
  - PLAT-05

must_haves:
  truths:
    - "User can toggle between lbs and kg in Settings"
    - "All weight displays update immediately when unit changes"
    - "Typing 60 in kg mode stores 132.28 lbs internally"
    - "Kg values display rounded to nearest 0.5 kg"
    - "App Check initializes before any Firebase service call"
  artifacts:
    - path: "SundeeFundeeRN/src/utils/formatWeight.ts"
      provides: "Weight display formatting and input parsing"
      exports: ["formatWeight", "parseWeightInput", "formatWeightNumeric"]
    - path: "SundeeFundeeRN/src/utils/__tests__/formatWeight.test.ts"
      provides: "Unit tests for weight formatting"
      min_lines: 40
    - path: "SundeeFundeeRN/src/firebase/appCheck.ts"
      provides: "App Check initialization with debug/production provider switching"
      exports: ["initAppCheck"]
  key_links:
    - from: "SundeeFundeeRN/app/(app)/(tabs)/settings.tsx"
      to: "SundeeFundeeRN/src/repositories/SettingsRepo.ts"
      via: "getSettingsRepo().saveSettings with weightUnit field"
      pattern: "saveSettings.*weightUnit"
    - from: "SundeeFundeeRN/app/(app)/_layout.tsx"
      to: "SundeeFundeeRN/src/firebase/appCheck.ts"
      via: "initAppCheck() called before SessionProvider renders"
      pattern: "initAppCheck"
---

<objective>
Add weight unit switching (lbs/kg) throughout the app and initialize Firebase App Check.

Purpose: Users who train with metric weights can see all values in kg. App Check secures all Firebase calls against unauthenticated access.
Output: formatWeight utility with tests, weight unit toggle in Settings, all weight displays updated, App Check initialized at app startup.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-polish-and-pre-launch/07-CONTEXT.md
@.planning/phases/07-polish-and-pre-launch/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From SundeeFundeeRN/src/repositories/SettingsRepo.ts:
```typescript
export interface AppSettings {
  weightUnit: 'lb' | 'kg';
  notificationsEnabled: boolean;
  defaultRestDuration: number;
}

export const DEFAULT_SETTINGS: AppSettings = {
  weightUnit: 'lb',
  notificationsEnabled: true,
  defaultRestDuration: 90,
};

export interface SettingsRepository {
  saveSettings(uid: string, settings: AppSettings): Promise<void>;
  getSettings(uid: string): Promise<AppSettings | null>;
}

export function getSettingsRepo(isGuest: boolean): SettingsRepository;
```

From SundeeFundeeRN/src/domain/calculations/weight-unit-conversion.ts:
```typescript
export const POUNDS_PER_KG = 2.2046226218;
export function lbToKg(lb: number): number;
export function kgToLb(kg: number): number;
export function valueFromKilograms(kilograms: number, unit: WeightUnit): number;
export function kilogramsFrom(value: number, unit: WeightUnit): number;
```

From SundeeFundeeRN/src/domain/types/index.ts:
```typescript
export type WeightUnit = 'lb' | 'kg';
```

From SundeeFundeeRN/src/firebase/ (existing files):
```
app.ts, auth.ts, auth.web.ts, firestore.ts
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create formatWeight utility, App Check init, and install dependencies</name>
  <files>
    SundeeFundeeRN/src/utils/formatWeight.ts,
    SundeeFundeeRN/src/utils/__tests__/formatWeight.test.ts,
    SundeeFundeeRN/src/firebase/appCheck.ts,
    SundeeFundeeRN/package.json
  </files>
  <behavior>
    - formatWeight(132.28, 'kg') returns "60.0 kg"
    - formatWeight(132.28, 'lb') returns "132.3 lbs"
    - formatWeight(135, 'kg') returns "61.5 kg" (rounds to nearest 0.5)
    - formatWeightNumeric(132.28, 'kg') returns 60.0 (number, not string)
    - parseWeightInput('60', 'kg') returns ~132.28 (stored as lbs)
    - parseWeightInput('135', 'lb') returns 135
    - parseWeightInput('', 'kg') returns 0 (handles empty string)
    - parseWeightInput('abc', 'lb') returns NaN
  </behavior>
  <action>
    1. Install new dependencies:
       ```bash
       cd SundeeFundeeRN && npm install @react-native-firebase/app-check
       cd SundeeFundeeRN && npx expo install expo-file-system expo-sharing
       cd SundeeFundeeRN && npm install react-native-zip-archive
       ```
       Use --legacy-peer-deps if needed (React 19 peer support gap, same as RevenueCat install).

    2. Create `SundeeFundeeRN/src/utils/formatWeight.ts`:
       - `formatWeight(lbs: number, unit: 'lb' | 'kg'): string` — converts lbs to display string with unit suffix. Kg values rounded to nearest 0.5 via `Math.round(kg * 2) / 2`, displayed with 1 decimal. Lbs values rounded to 1 decimal.
       - `formatWeightNumeric(lbs: number, unit: 'lb' | 'kg'): number` — same conversion but returns raw number (for input fields showing numeric value).
       - `parseWeightInput(value: string, unit: 'lb' | 'kg'): number` — parses user input in their preferred unit, returns stored lbs. For kg: multiply by POUNDS_PER_KG. For lb: return as-is. Empty string returns 0.
       - Import `lbToKg`, `kgToLb` from `@/src/domain/calculations/weight-unit-conversion`.
       - Pure functions, no React dependencies.

    3. Create `SundeeFundeeRN/src/utils/__tests__/formatWeight.test.ts`:
       - Test all behaviors listed above.
       - Test edge cases: 0 lbs, very large numbers, negative numbers.

    4. Create `SundeeFundeeRN/src/firebase/appCheck.ts`:
       - Export `initAppCheck(): Promise<void>`.
       - Use `ReactNativeFirebaseAppCheckProvider` with `configure()`.
       - Debug provider when `__DEV__` is true (reads `process.env.EXPO_PUBLIC_APP_CHECK_DEBUG_TOKEN`).
       - Production: `appAttestWithDeviceCheckFallback` on iOS, `playIntegrity` on Android.
       - Guard against double-init with module-level `initialized` boolean.
       - Set `isTokenAutoRefreshEnabled: true`.
       - Web platform: skip App Check entirely (Platform.OS === 'web' early return) since web doesn't use RNF.
  </action>
  <verify>
    <automated>cd /Users/dustinober/Projects/sundee-fundee/SundeeFundeeRN && npx jest src/utils/__tests__/formatWeight.test.ts --passWithNoTests</automated>
  </verify>
  <done>formatWeight utility passes all tests. appCheck.ts exports initAppCheck. Dependencies installed.</done>
</task>

<task type="auto">
  <name>Task 2: Wire weight unit toggle into Settings and thread formatWeight through all weight displays</name>
  <files>
    SundeeFundeeRN/app/(app)/(tabs)/settings.tsx,
    SundeeFundeeRN/app/(app)/_layout.tsx,
    SundeeFundeeRN/app/(app)/(tabs)/history.tsx,
    SundeeFundeeRN/app/(app)/(tabs)/maxes.tsx,
    SundeeFundeeRN/src/components/workout/SetRow.tsx
  </files>
  <action>
    1. **Settings screen** (`app/(app)/(tabs)/settings.tsx`):
       - Add "Weight Unit" section between "Rest Timer" and "Subscription" sections.
       - Use same bottom-sheet/picker pattern as the rest timer duration picker.
       - Two options: "lbs" and "kg". Current selection shown in row, tapping opens picker.
       - On change: save to settings via `getSettingsRepo(isGuest).saveSettings(uid, { ...settings, weightUnit: newUnit })`.
       - Settings screen already loads AppSettings on mount — extend to display current weightUnit.

    2. **App layout** (`app/(app)/_layout.tsx`):
       - Call `initAppCheck()` before the component tree renders.
       - Add `useEffect` at the top of AppLayout that calls `initAppCheck()` once.
       - This must run before SessionProvider triggers any Firestore calls.
       - Import from `@/src/firebase/appCheck`.

    3. **Thread formatWeight through weight displays**:
       - The Settings screen already loads `AppSettings` which has `weightUnit`.
       - For screens that display weights (history, maxes, workout session, exercise detail, benchmarks, programs), each screen needs to:
         a. Load weightUnit from settings (or accept it as a prop/context value).
         b. Call `formatWeight(storedLbs, unit)` for display.
         c. Call `parseWeightInput(userInput, unit)` when user enters a weight.
       - **Key screens to update** (search for hardcoded 'lbs' strings and numeric weight displays):
         - `history.tsx` — workout detail weight displays
         - `maxes.tsx` — 1RM values
         - `SetRow.tsx` — weight input field in workout session (show unit suffix, parse input in user's unit)
         - Any component rendering weight numbers
       - For each screen: add a `useEffect` or inline call to `getSettingsRepo(isGuest).getSettings(uid)` to get the weightUnit, or thread it from a shared state. The simplest approach: each screen that needs weightUnit loads it from settings on mount (same pattern as settings screen).
       - Input fields: show unit suffix ('kg' or 'lbs') after the TextInput. When user types in kg mode, `parseWeightInput` converts to lbs for storage.

    4. **Art Deco consistency** (PLAT-04 partial):
       - While touching each screen for weight unit, audit and fix any spacing/typography inconsistencies:
         - Ensure all section titles use `colors.text` and consistent font size
         - Ensure all cards use consistent `colors.surface` background, same border radius, same shadow
         - Ensure consistent padding (16px horizontal, 12px vertical for sections)
       - This is a subtle polish pass, not a redesign. Fix what you see while you're in each file.
  </action>
  <verify>
    <automated>cd /Users/dustinober/Projects/sundee-fundee/SundeeFundeeRN && npx jest --testPathPattern="settings|formatWeight" --passWithNoTests</automated>
  </verify>
  <done>Weight unit toggle in Settings saves preference. All weight displays use formatWeight with user's unit. Kg values round to 0.5. Input fields accept kg and convert to lbs for storage. App Check initializes on app start. Screens have consistent Art Deco spacing.</done>
</task>

</tasks>

<verification>
1. `cd SundeeFundeeRN && npx jest src/utils/__tests__/formatWeight.test.ts` — all formatWeight tests pass
2. `cd SundeeFundeeRN && npx jest --testPathPattern="settings" --passWithNoTests` — settings tests pass
3. App Check: verify `appCheck.ts` imports and exports correctly (build check)
4. Weight unit toggle: Settings shows lbs/kg picker, changing it updates displayed weights across screens
</verification>

<success_criteria>
- formatWeight utility converts lbs to kg (rounded to 0.5) and formats with unit suffix
- parseWeightInput converts user's kg/lbs input to stored lbs
- Settings screen has Weight Unit picker between Rest Timer and Subscription
- All weight displays call formatWeight instead of hardcoding 'lbs'
- App Check initialized before Firebase calls with debug provider in dev
- Visual consistency improved on touched screens
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-and-pre-launch/07-01-SUMMARY.md`
</output>
