---
phase: 07-polish-and-pre-launch
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - functions/src/stripeWebhook.ts
  - functions/src/deleteAccount.ts
  - functions/src/index.ts
  - functions/__tests__/deleteAccount.test.ts
  - SundeeFundeeRN/app/(app)/(tabs)/settings.tsx
  - SundeeFundeeRN/app/(app)/goodbye.tsx
autonomous: true
requirements:
  - PLAT-04
  - PLAT-07

must_haves:
  truths:
    - "User can delete their account from Settings with two-step confirmation"
    - "Account deletion wipes all Firestore data, revokes RC entitlement, cancels Stripe subscription, and deletes Auth user"
    - "Guest users see 'Clear Local Data' instead of 'Delete Account'"
    - "Delete confirmation modal suggests exporting data first with a link"
    - "Post-deletion goodbye screen navigates to sign-in"
    - "stripeWebhook persists stripeSubscriptionId to user doc for server-side cancel"
  artifacts:
    - path: "functions/src/deleteAccount.ts"
      provides: "Callable Cloud Function for full account deletion"
      exports: ["deleteAccount"]
    - path: "functions/__tests__/deleteAccount.test.ts"
      provides: "Tests for delete orchestration"
      min_lines: 60
    - path: "SundeeFundeeRN/app/(app)/goodbye.tsx"
      provides: "Post-deletion goodbye screen"
      min_lines: 20
  key_links:
    - from: "functions/src/deleteAccount.ts"
      to: "firebase-admin"
      via: "db.recursiveDelete + admin.auth().deleteUser"
      pattern: "recursiveDelete|deleteUser"
    - from: "functions/src/stripeWebhook.ts"
      to: "Firestore /users/{uid}"
      via: "set stripeSubscriptionId on subscription.created"
      pattern: "stripeSubscriptionId"
    - from: "SundeeFundeeRN/app/(app)/(tabs)/settings.tsx"
      to: "functions/src/deleteAccount.ts"
      via: "httpsCallable('deleteAccount')"
      pattern: "httpsCallable.*deleteAccount"
    - from: "SundeeFundeeRN/app/(app)/(tabs)/settings.tsx"
      to: "SundeeFundeeRN/src/export/exportData.ts"
      via: "Export Data button and link in delete modal"
      pattern: "exportUserData"
---

<objective>
Build account deletion Cloud Function, wire delete and export into Settings, fix stripeWebhook subscription ID gap, and complete Art Deco polish.

Purpose: Users can fully delete their account with data wipe (required by App Store and Play Store review), and export their data before leaving. The delete flow handles Stripe, RevenueCat, Firestore, and Auth cleanup server-side.
Output: deleteAccount Cloud Function, updated stripeWebhook, Settings screen with Export Data + Delete Account sections, goodbye screen.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-polish-and-pre-launch/07-CONTEXT.md
@.planning/phases/07-polish-and-pre-launch/07-RESEARCH.md
@.planning/phases/07-polish-and-pre-launch/07-01-SUMMARY.md
@.planning/phases/07-polish-and-pre-launch/07-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From SundeeFundeeRN/src/utils/formatWeight.ts:
```typescript
export function formatWeight(lbs: number, unit: 'lb' | 'kg'): string;
export function parseWeightInput(value: string, unit: 'lb' | 'kg'): number;
export function formatWeightNumeric(lbs: number, unit: 'lb' | 'kg'): number;
```

<!-- From Plan 02 -->
From SundeeFundeeRN/src/export/exportData.ts:
```typescript
export function exportUserData(
  uid: string,
  format: 'csv' | 'json',
  weightUnit: 'lb' | 'kg',
  repos: RepoBundle,
): Promise<void>;
```

<!-- Existing Cloud Functions -->
From functions/src/stripeWebhook.ts:
```typescript
// grantRCEntitlement and revokeRCEntitlement are module-scoped helpers
// stripeWebhook is an onRequest handler
// On customer.subscription.created/updated with active status:
//   grants RC entitlement + writes premiumEntitlement to /users/{uid}
// Currently does NOT persist stripeSubscriptionId — this is the gap to fix
```

From functions/src/index.ts:
```typescript
export { generateWorkout } from "./generateWorkout";
export { createCheckoutSession } from "./createCheckoutSession";
export { stripeWebhook } from "./stripeWebhook";
// Need to add: export { deleteAccount } from "./deleteAccount";
```

From SundeeFundeeRN/src/auth/AuthContext.ts:
```typescript
// useSession() returns { user, isLoading, isGuest, signOut }
```

From SundeeFundeeRN/src/entitlements/EntitlementContext.ts:
```typescript
// useEntitlementContext() returns { tier, isLoading }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix stripeWebhook subscription ID gap and create deleteAccount Cloud Function with tests</name>
  <files>
    functions/src/stripeWebhook.ts,
    functions/src/deleteAccount.ts,
    functions/src/index.ts,
    functions/__tests__/deleteAccount.test.ts
  </files>
  <behavior>
    - stripeWebhook on customer.subscription.created writes stripeSubscriptionId to /users/{uid}
    - deleteAccount rejects unauthenticated callers with 'unauthenticated' error
    - deleteAccount calls revokeRCEntitlement (best-effort, does not throw on failure)
    - deleteAccount cancels Stripe subscription if stripeSubscriptionId exists on user doc (best-effort)
    - deleteAccount calls db.recursiveDelete on /users/{uid} (wipes all subcollections)
    - deleteAccount calls admin.auth().deleteUser(uid) last
    - deleteAccount returns { success: true } on completion
    - deleteAccount has timeoutSeconds: 540
  </behavior>
  <action>
    1. **Fix stripeWebhook** (`functions/src/stripeWebhook.ts`):
       - In the `customer.subscription.created` / `customer.subscription.updated` handler, after the existing `premiumEntitlement` write, add `stripeSubscriptionId: subscription.id` to the same `set()` call.
       - This is a one-line addition to the existing merge object. The subscription variable is already available as `event.data.object as Stripe.Subscription`.

    2. **Create deleteAccount** (`functions/src/deleteAccount.ts`):
       - Use `onCall` from `firebase-functions/v2/https` (same pattern as createCheckoutSession).
       - Secrets: `STRIPE_SECRET_KEY`, `RC_SECRET_API_KEY` (same as stripeWebhook).
       - Set `timeoutSeconds: 540` in the function config (max allowed — handles large data sets).
       - Authentication guard: `if (!request.auth) throw new HttpsError('unauthenticated', 'Must be authenticated')`.
       - Execution order (per locked decision + research):
         a. Revoke RC entitlement — call `revokeRCEntitlement(uid, rcKey)`. Best-effort: wrap in try/catch, log error but continue.
         b. Cancel Stripe subscription — read `stripeSubscriptionId` from `/users/{uid}` doc. If exists, call `stripe.subscriptions.cancel(id)`. Best-effort: catch and continue.
         c. Delete Firestore data — `await db.recursiveDelete(db.doc(\`users/${uid}\`))`. This deletes the user doc AND all subcollections (workouts, maxes, injuries, painLogs, cycleData, benchmarks, enrollments, readiness).
         d. Delete Firebase Auth user — `await admin.auth().deleteUser(uid)`.
         e. Return `{ success: true }`.
       - Extract `revokeRCEntitlement` into a shared helper file OR import it. Since it's currently module-scoped in stripeWebhook.ts, the simplest approach is to duplicate the small function in deleteAccount.ts (3 lines of fetch) rather than refactoring stripeWebhook's module structure. Alternatively, extract to `functions/src/rcHelpers.ts` and import from both files.

    3. **Update index.ts** (`functions/src/index.ts`):
       - Add `export { deleteAccount } from "./deleteAccount";`

    4. **Create tests** (`functions/__tests__/deleteAccount.test.ts`):
       - Mock `firebase-admin` (firestore, auth).
       - Mock `Stripe` constructor and `subscriptions.cancel`.
       - Mock `fetch` for RC revoke call.
       - Test: unauthenticated request throws HttpsError.
       - Test: full deletion flow calls RC revoke, reads user doc, cancels Stripe, recursiveDelete, deleteUser in order.
       - Test: missing stripeSubscriptionId skips Stripe cancel.
       - Test: RC revoke failure does not block deletion.
       - Test: Stripe cancel failure does not block deletion.
  </action>
  <verify>
    <automated>cd /Users/dustinober/Projects/sundee-fundee/functions && npx jest __tests__/deleteAccount.test.ts --passWithNoTests</automated>
  </verify>
  <done>stripeWebhook persists stripeSubscriptionId. deleteAccount Cloud Function handles full deletion flow (RC + Stripe + Firestore + Auth) with best-effort external API calls. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Export Data and Delete Account into Settings UI with goodbye screen</name>
  <files>
    SundeeFundeeRN/app/(app)/(tabs)/settings.tsx,
    SundeeFundeeRN/app/(app)/goodbye.tsx
  </files>
  <action>
    1. **Settings screen additions** (`app/(app)/(tabs)/settings.tsx`):
       Add two new sections after the Subscription section:

       a. **"Data" section** with "Export Data" row:
          - Tapping opens a modal/bottom sheet with two options: "Export as CSV (zip)" and "Export as JSON".
          - On selection: call `exportUserData(uid, format, settings.weightUnit, repos)` from Plan 02's export module.
          - Show a loading indicator while exporting. On completion, the share sheet/download triggers automatically.
          - Import `exportUserData` from `@/src/export/exportData`.
          - Build the `repos` bundle from `getWorkoutRepo(isGuest)`, `getExerciseMaxRepo(isGuest)`, etc.

       b. **"Danger Zone" section** (last section, red-tinted):
          - For authenticated users: "Delete Account" button (red text).
          - For guest users: "Clear Local Data" button (red text).

          **Delete Account flow (authenticated)**:
          - Step 1: Tap "Delete Account" opens a warning modal:
            - Title: "Delete Your Account?"
            - Body: Lists consequences — "This will permanently delete all your data, cancel any active subscriptions, and remove your account."
            - "Want to save your data first?" link that triggers the export flow.
            - Text input: "Type DELETE to confirm"
            - Confirm button: disabled until input === "DELETE"
            - Cancel button
          - Step 2: On confirm, call `httpsCallable(functions, 'deleteAccount')()` from `@react-native-firebase/functions` (or the web equivalent).
            - Show loading spinner during deletion.
            - On success: clear AsyncStorage (`AsyncStorage.clear()`), navigate to `/goodbye`.
            - On error: show Alert with error message, re-enable the button.

          **Clear Local Data flow (guest)**:
          - Simple confirmation Alert: "This will clear all your local workout data. This cannot be undone."
          - On confirm: `AsyncStorage.clear()`, navigate to sign-in screen.

    2. **Goodbye screen** (`app/(app)/goodbye.tsx`):
       - Simple full-screen view with Art Deco styling.
       - Text: "Account Deleted" heading, "Your data has been removed. Thank you for using Sundee Fundee." subtext.
       - "Done" button that navigates to `/sign-in` (or uses `router.replace('/sign-in')`).
       - Uses `colors.background`, `colors.text`, `colors.accent` from theme.

    3. **Art Deco consistency final pass** (PLAT-04):
       - Review the Settings screen holistically now that all sections are added.
       - Ensure consistent section spacing, typography, button styles across Account, Rest Timer, Weight Unit, Subscription, Data, and Danger Zone sections.
       - Danger Zone section: subtle red border or tinted background to distinguish it visually.
       - Ensure goodbye screen matches the overall Art Deco feel (cream background, navy text, orange accent on button).
  </action>
  <verify>
    <automated>cd /Users/dustinober/Projects/sundee-fundee/SundeeFundeeRN && npx jest --testPathPattern="settings" --passWithNoTests</automated>
  </verify>
  <done>Settings has Export Data section with CSV/JSON format picker and share sheet trigger. Settings has Danger Zone with Delete Account (authenticated) or Clear Local Data (guest). Delete flow: two-step confirmation with "DELETE" typing, Cloud Function call, AsyncStorage clear, navigate to goodbye. Goodbye screen shows polite farewell with Done button to sign-in. All Settings sections have consistent Art Deco styling.</done>
</task>

</tasks>

<verification>
1. `cd functions && npx jest __tests__/deleteAccount.test.ts` — Cloud Function tests pass
2. `cd SundeeFundeeRN && npx jest --testPathPattern="settings" --passWithNoTests` — settings tests pass
3. Settings screen has Export Data, Delete Account (or Clear Local Data for guests), and Danger Zone sections
4. Delete confirmation requires typing "DELETE" and includes export suggestion link
5. Goodbye screen renders with Art Deco theme and navigates to sign-in
6. stripeWebhook now persists stripeSubscriptionId
</verification>

<success_criteria>
- deleteAccount Cloud Function executes: RC revoke -> Stripe cancel -> Firestore recursiveDelete -> Auth deleteUser
- stripeWebhook writes stripeSubscriptionId to /users/{uid} on subscription.created
- Settings Export Data triggers share sheet (mobile) or download (web)
- Delete Account uses two-step confirmation with "DELETE" text input
- Guest users see "Clear Local Data" instead
- Post-deletion goodbye screen with Done button navigates to sign-in
- All Settings sections have consistent Art Deco styling
- Cloud Function tests cover auth guard, full flow, and best-effort error handling
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-and-pre-launch/07-03-SUMMARY.md`
</output>
