---
phase: 04-core-workout-loop
plan: 08
type: execute
wave: 5
depends_on:
  - 04-05
  - 04-06
  - 04-07
files_modified:
  - SundeeFundeeRN/app/(app)/(tabs)/_layout.tsx
  - SundeeFundeeRN/app/(app)/(tabs)/index.tsx
  - SundeeFundeeRN/app/(app)/_layout.tsx
  - SundeeFundeeRN/app/(app)/(tabs)/settings.tsx
autonomous: false
requirements:
  - WORK-01
  - WORK-02
  - WORK-03
  - WORK-04
  - WORK-05
  - WORK-06
  - WORK-07
  - WORK-08
  - WORK-09
  - WORK-10
  - WORK-12
  - EXEC-01
  - EXEC-02
  - EXEC-03
  - EXEC-04
  - MAX-01
  - MAX-02
  - MAX-03

must_haves:
  truths:
    - "Tab bar shows Dashboard, History, Maxes, Settings tabs"
    - "Dashboard has 'Start Workout' button navigating to workout session"
    - "Settings includes configurable default rest timer duration"
    - "All modal screens (exercise-picker, workout-session, timer-mode, workout-detail, exercise-detail) are accessible via navigation"
    - "Full workout flow works end-to-end: start workout -> add exercises -> log sets -> see PRs -> finish -> view in history"
  artifacts:
    - path: "SundeeFundeeRN/app/(app)/(tabs)/_layout.tsx"
      provides: "Updated tab layout with 4 tabs"
      contains: "history"
    - path: "SundeeFundeeRN/app/(app)/_layout.tsx"
      provides: "Stack layout with modal screens registered"
  key_links:
    - from: "SundeeFundeeRN/app/(app)/(tabs)/_layout.tsx"
      to: "history.tsx, maxes.tsx"
      via: "Tabs.Screen registration"
      pattern: "Tabs\\.Screen.*name=\"(history|maxes)\""
    - from: "SundeeFundeeRN/app/(app)/(tabs)/index.tsx"
      to: "workout-session.tsx"
      via: "Start Workout button navigates to workout session"
      pattern: "router\\.push.*workout-session"
    - from: "SundeeFundeeRN/app/(app)/_layout.tsx"
      to: "exercise-picker.tsx, workout-detail.tsx, exercise-detail.tsx, timer-mode.tsx"
      via: "Stack.Screen registration for modals"
      pattern: "Stack\\.Screen.*presentation.*modal"
---

<objective>
Wire all Phase 4 screens into navigation: update tab bar, register modal routes, add workout entry point to dashboard, add rest timer settings, and verify end-to-end.

Purpose: All domain logic, repositories, and UI screens are built — this plan connects them into a working app and verifies the complete workout flow.
Output: Updated tab bar with 4 tabs, modal routes registered, dashboard with Start Workout, settings with rest timer default. Human verification of full flow.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-core-workout-loop/04-CONTEXT.md
@.planning/phases/04-core-workout-loop/04-05-SUMMARY.md
@.planning/phases/04-core-workout-loop/04-06-SUMMARY.md
@.planning/phases/04-core-workout-loop/04-07-SUMMARY.md

<interfaces>
From app/(app)/(tabs)/_layout.tsx (current):
```typescript
// Currently has: Dashboard (index) and Settings tabs
// Needs: + History, Maxes tabs
```

From app/(app)/(tabs)/settings.tsx (current):
```typescript
// Has sign-out, profile info
// Needs: rest timer default duration setting
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire tab navigation, modal routes, dashboard entry, and settings</name>
  <files>
    SundeeFundeeRN/app/(app)/(tabs)/_layout.tsx,
    SundeeFundeeRN/app/(app)/(tabs)/index.tsx,
    SundeeFundeeRN/app/(app)/_layout.tsx,
    SundeeFundeeRN/app/(app)/(tabs)/settings.tsx
  </files>
  <action>
    1. Update `app/(app)/(tabs)/_layout.tsx`:
       - Add 2 new Tabs.Screen entries between Dashboard and Settings:
         a. "history" — title "History", icon placeholder
         b. "maxes" — title "Maxes", icon placeholder
       - Tab order: Dashboard, History, Maxes, Settings (4 tabs)
       - Workout is entered from Dashboard via "Start Workout" button (build-as-you-go per user decision, no dedicated workout tab)

    2. Update `app/(app)/_layout.tsx` (or create if not exists):
       - Register modal/stack screens:
         a. `workout-session` — presentation: 'modal' or 'card', full screen
         b. `exercise-picker` — presentation: 'modal'
         c. `timer-mode` — presentation: 'fullScreenModal' (no header, immersive)
         d. `workout-detail` — presentation: 'card'
         e. `exercise-detail` — presentation: 'card'
       - Use Stack layout wrapping (tabs) and modal screens

    3. Update `app/(app)/(tabs)/index.tsx` (Dashboard):
       - Add "Start Workout" prominent button (ORANGE background, CREAM text, large)
       - On press: `router.push('/workout-session')`
       - Add "Start Timed Workout" section below with 3 options: ForTime, AMRAP, EMOM
       - Each timed option: navigates to workout-session with timerMode param, then user adds exercises and starts timer
       - Show recent workout summary card if there's history (last workout date, name, duration)

    4. Update `app/(app)/(tabs)/settings.tsx`:
       - Add "Rest Timer" section:
         a. "Default Rest Duration" row with current value (e.g., "90 seconds")
         b. On tap: show picker or input for seconds (30, 45, 60, 90, 120, 150, 180, 240, 300)
         c. Persist to SettingsRepo (existing)
       - Read default rest duration on mount, pass to useRestTimer via context or prop

    5. Ensure notification setup:
       - In the (app) layout, configure expo-notifications:
         ```typescript
         Notifications.setNotificationHandler({
           handleNotification: async () => ({
             shouldShowAlert: true,
             shouldPlaySound: true,
             shouldSetBadge: false,
           }),
         });
         ```
       - This ensures notifications display when app is in foreground
  </action>
  <verify>
    <automated>cd /Users/dustinober/Projects/Sundee-Fundee/SundeeFundeeRN && npx jest --passWithNoTests</automated>
  </verify>
  <done>Tab bar shows Dashboard, History, Maxes, Settings. Dashboard has Start Workout and timed workout buttons. Settings has rest timer duration picker. All modal routes registered. Notification handler configured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 4 workout loop end-to-end</name>
  <action>
    Human verifies the complete workout flow works across all features built in Phase 4.
  </action>
  <what-built>
    Complete Phase 4 workout loop: exercise library with search, workout logging with set rows and ghost text, rest timer with notifications, ForTime/AMRAP/EMOM timed workouts with 3-2-1-Go countdown, PR detection with inline badges and toasts, workout history with filtering and swipe-to-delete, maxes tracking with 1RM charts and rep-range PR breakdown.
  </what-built>
  <how-to-verify>
    1. Start the app: `cd SundeeFundeeRN && npx expo start`
    2. From Dashboard, tap "Start Workout"
    3. Tap "Add Exercise" — verify muscle group grid appears
    4. Tap a muscle group — verify exercises listed
    5. Use search bar — verify filtering works
    6. Search for nonexistent exercise — verify "Create [name]" appears
    7. Select an exercise — verify it appears in workout with 3 empty set rows
    8. Enter weight and reps in a set row, tap checkmark — verify set completes
    9. Verify rest timer appears at bottom with countdown
    10. Tap "Skip" on rest timer — verify it dismisses
    11. Add another exercise, reorder via long-press drag
    12. Tap "Finish" — verify workout saves
    13. Navigate to History tab — verify workout appears with date group and source badge "Custom"
    14. Tap the workout card — verify detail screen shows exercises and sets
    15. Swipe left on a workout — verify delete option appears
    16. Navigate to Maxes tab — verify exercises appear with estimated 1RM
    17. Tap an exercise — verify 1RM chart and rep-range PR table
    18. Go to Settings — verify rest timer duration setting exists
    19. (Optional) Start a timed workout (AMRAP) — verify 3-2-1-Go countdown and timer
  </how-to-verify>
  <verify>Human confirms all 19 verification steps pass</verify>
  <done>Full workout loop verified end-to-end by user</done>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
Full test suite:
```bash
cd SundeeFundeeRN && npx jest --passWithNoTests
```
App starts without errors:
```bash
cd SundeeFundeeRN && npx expo start --web
```
</verification>

<success_criteria>
- 4 tabs visible: Dashboard, History, Maxes, Settings
- Start Workout from Dashboard opens workout session modal
- Full workout flow: add exercise -> log sets -> PR detection -> rest timer -> finish -> history
- Timed workout flow: select mode -> 3-2-1-Go -> timer running -> complete
- History: date groups, source filter, swipe delete, detail view
- Maxes: exercise list with 1RM, chart detail, rep-range PRs
- Settings: rest timer duration configurable
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-workout-loop/04-08-SUMMARY.md`
</output>
