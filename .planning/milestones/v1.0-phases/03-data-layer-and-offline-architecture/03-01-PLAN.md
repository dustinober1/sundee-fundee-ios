---
phase: 03-data-layer-and-offline-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SundeeFundeeRN/src/repositories/UserRepository.ts
  - SundeeFundeeRN/src/repositories/OnboardingProfileRepo.ts
  - SundeeFundeeRN/src/repositories/FirestoreOnboardingProfileRepo.ts
  - SundeeFundeeRN/src/repositories/LocalOnboardingProfileRepo.ts
  - SundeeFundeeRN/src/repositories/WorkoutRepo.ts
  - SundeeFundeeRN/src/repositories/FirestoreWorkoutRepo.ts
  - SundeeFundeeRN/src/repositories/LocalWorkoutRepo.ts
  - SundeeFundeeRN/src/repositories/SettingsRepo.ts
  - SundeeFundeeRN/src/repositories/FirestoreSettingsRepo.ts
  - SundeeFundeeRN/src/repositories/LocalSettingsRepo.ts
  - SundeeFundeeRN/src/repositories/ReadinessRepo.ts
  - SundeeFundeeRN/src/repositories/FirestoreReadinessRepo.ts
  - SundeeFundeeRN/src/repositories/LocalReadinessRepo.ts
  - SundeeFundeeRN/src/repositories/migration.ts
  - SundeeFundeeRN/src/repositories/index.ts
  - SundeeFundeeRN/src/firebase/firestore.ts
  - SundeeFundeeRN/src/repositories/__tests__/FirestoreOnboardingProfileRepo.test.ts
  - SundeeFundeeRN/src/repositories/__tests__/LocalOnboardingProfileRepo.test.ts
  - SundeeFundeeRN/src/repositories/__tests__/FirestoreWorkoutRepo.test.ts
  - SundeeFundeeRN/src/repositories/__tests__/LocalWorkoutRepo.test.ts
  - SundeeFundeeRN/src/repositories/__tests__/FirestoreSettingsRepo.test.ts
  - SundeeFundeeRN/src/repositories/__tests__/LocalSettingsRepo.test.ts
  - SundeeFundeeRN/src/repositories/__tests__/FirestoreReadinessRepo.test.ts
  - SundeeFundeeRN/src/repositories/__tests__/LocalReadinessRepo.test.ts
  - SundeeFundeeRN/src/repositories/__tests__/migration.test.ts
  - SundeeFundeeRN/src/repositories/__tests__/repoFactory.test.ts
autonomous: true
requirements:
  - ONBD-01
  - ONBD-02
  - WORK-11
  - AUTH-07

must_haves:
  truths:
    - "UserProfile includes onboarding fields (name, experienceLevel, primaryGoal, gender, cycleOptIn, hasCompletedOnboarding)"
    - "OnboardingProfileRepo saves and retrieves profile data via Firestore or AsyncStorage depending on auth state"
    - "WorkoutRepo saves and retrieves workout records via Firestore subcollection or AsyncStorage"
    - "SettingsRepo saves and retrieves app settings (weightUnit, notificationsEnabled)"
    - "ReadinessRepo saves and retrieves readiness survey records by date"
    - "Factory functions return Firestore implementations for authenticated users and AsyncStorage for guests"
    - "Guest-to-auth migration reads all AsyncStorage data, batch-writes to Firestore, then clears local storage"
    - "Web Firestore persistence is enabled so offline writes survive tab close"
  artifacts:
    - path: "SundeeFundeeRN/src/repositories/UserRepository.ts"
      provides: "Expanded UserProfile with onboarding fields + ExperienceLevel/PrimaryGoal types"
      contains: "hasCompletedOnboarding"
    - path: "SundeeFundeeRN/src/repositories/OnboardingProfileRepo.ts"
      provides: "OnboardingProfileRepository interface + factory function"
      exports: ["OnboardingProfile", "OnboardingProfileRepository", "getOnboardingProfileRepo"]
    - path: "SundeeFundeeRN/src/repositories/WorkoutRepo.ts"
      provides: "WorkoutRepository interface + factory function"
      exports: ["WorkoutRecord", "WorkoutRepository", "getWorkoutRepo"]
    - path: "SundeeFundeeRN/src/repositories/SettingsRepo.ts"
      provides: "SettingsRepository interface + factory function"
      exports: ["AppSettings", "DEFAULT_SETTINGS", "SettingsRepository", "getSettingsRepo"]
    - path: "SundeeFundeeRN/src/repositories/ReadinessRepo.ts"
      provides: "ReadinessRepository interface + factory function"
      exports: ["ReadinessSurveyRecord", "ReadinessRepository", "getReadinessRepo"]
    - path: "SundeeFundeeRN/src/repositories/migration.ts"
      provides: "Guest-to-auth data migration via Firestore batch writes"
      exports: ["migrateGuestDataToFirestore"]
    - path: "SundeeFundeeRN/src/repositories/index.ts"
      provides: "Barrel re-exporting all repository interfaces and factories"
    - path: "SundeeFundeeRN/src/firebase/firestore.ts"
      provides: "Web Firestore IndexedDB persistence initialization"
      contains: "persistentLocalCache"
  key_links:
    - from: "SundeeFundeeRN/src/repositories/OnboardingProfileRepo.ts"
      to: "SundeeFundeeRN/src/repositories/UserRepository.ts"
      via: "imports ExperienceLevel, PrimaryGoal, Gender types"
      pattern: "import.*from.*UserRepository"
    - from: "SundeeFundeeRN/src/repositories/FirestoreOnboardingProfileRepo.ts"
      to: "SundeeFundeeRN/src/firebase/firestore.ts"
      via: "getFirestoreInstance()"
      pattern: "getFirestoreInstance"
    - from: "SundeeFundeeRN/src/repositories/migration.ts"
      to: "SundeeFundeeRN/src/firebase/firestore.ts"
      via: "batch writes via getFirestoreInstance()"
      pattern: "batch.*commit"
---

<objective>
Build all four repository interfaces (OnboardingProfile, Workout, Settings, Readiness) with dual Firestore/AsyncStorage implementations, expand UserProfile with onboarding fields, create guest-to-auth migration helper, add web Firestore persistence, and wire factory functions.

Purpose: Establish the complete data access layer so that all subsequent UI work (onboarding screens, workout logging, settings) has typed, tested persistence ready to consume.
Output: 14 repository files, 1 migration helper, 1 barrel, 10 test files, updated firestore.ts
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-data-layer-and-offline-architecture/03-CONTEXT.md
@.planning/phases/03-data-layer-and-offline-architecture/03-RESEARCH.md

<interfaces>
<!-- Existing contracts the executor needs -->

From src/repositories/UserRepository.ts:
```typescript
export interface UserProfile {
  uid: string;
  email: string | null;
  displayName: string | null;
  isAnonymous: boolean;
  createdAt: string;
  lastSignInAt: string;
  authProvider: 'apple' | 'google' | 'email' | 'anonymous';
}

export interface UserRepository {
  createOrUpdateUser(profile: UserProfile): Promise<void>;
  getUser(uid: string): Promise<UserProfile | null>;
  deleteUser(uid: string): Promise<void>;
}
```

From src/firebase/firestore.ts:
```typescript
export function getFirestoreInstance(): FirestoreInstance;
```

From src/domain/types/index.ts:
```typescript
export type Gender = 'female' | 'male' | 'other';
export type ReadinessTier = 'low' | 'neutral' | 'high';
```

From src/domain/readiness/readiness-survey.ts:
```typescript
export interface ReadinessResult {
  score: number;
  tier: ReadinessTier;
}
```

From src/domain/ai-workout/generated-workout.ts:
```typescript
export interface GeneratedWorkout { ... }
```

From src/domain/history/history-item.ts:
```typescript
export interface HistoryItem { ... }
```

From src/repositories/FirestoreUserRepo.ts (pattern to follow):
```typescript
import { getFirestoreInstance } from '../firebase/firestore';
// Uses db.collection('users').doc(uid).set(data, { merge: true })
```

From src/repositories/LocalUserRepo.ts (pattern to follow):
```typescript
import AsyncStorage from '@react-native-async-storage/async-storage';
const USER_PROFILE_KEY = 'user_profile';
// Uses AsyncStorage.setItem(KEY, JSON.stringify(data))
```

From __mocks__/@react-native-firebase/firestore.ts:
```typescript
// Mock supports: collection().doc().set(), .get(), .delete(), .collection() (subcollections)
// Also: batch().set(), batch().commit()
// mockCollectionRef supports: .doc(), .get(), .where(), .orderBy(), .limit()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define repository interfaces, expand UserProfile, and create all implementations</name>
  <files>
    SundeeFundeeRN/src/repositories/UserRepository.ts,
    SundeeFundeeRN/src/repositories/OnboardingProfileRepo.ts,
    SundeeFundeeRN/src/repositories/FirestoreOnboardingProfileRepo.ts,
    SundeeFundeeRN/src/repositories/LocalOnboardingProfileRepo.ts,
    SundeeFundeeRN/src/repositories/WorkoutRepo.ts,
    SundeeFundeeRN/src/repositories/FirestoreWorkoutRepo.ts,
    SundeeFundeeRN/src/repositories/LocalWorkoutRepo.ts,
    SundeeFundeeRN/src/repositories/SettingsRepo.ts,
    SundeeFundeeRN/src/repositories/FirestoreSettingsRepo.ts,
    SundeeFundeeRN/src/repositories/LocalSettingsRepo.ts,
    SundeeFundeeRN/src/repositories/ReadinessRepo.ts,
    SundeeFundeeRN/src/repositories/FirestoreReadinessRepo.ts,
    SundeeFundeeRN/src/repositories/LocalReadinessRepo.ts,
    SundeeFundeeRN/src/repositories/__tests__/FirestoreOnboardingProfileRepo.test.ts,
    SundeeFundeeRN/src/repositories/__tests__/LocalOnboardingProfileRepo.test.ts,
    SundeeFundeeRN/src/repositories/__tests__/FirestoreWorkoutRepo.test.ts,
    SundeeFundeeRN/src/repositories/__tests__/LocalWorkoutRepo.test.ts,
    SundeeFundeeRN/src/repositories/__tests__/FirestoreSettingsRepo.test.ts,
    SundeeFundeeRN/src/repositories/__tests__/LocalSettingsRepo.test.ts,
    SundeeFundeeRN/src/repositories/__tests__/FirestoreReadinessRepo.test.ts,
    SundeeFundeeRN/src/repositories/__tests__/LocalReadinessRepo.test.ts,
    SundeeFundeeRN/src/repositories/__tests__/repoFactory.test.ts
  </files>
  <behavior>
    - OnboardingProfileRepo: saveProfile writes all onboarding fields, getProfile returns them; Firestore uses /users/{uid} with merge:true; Local uses @sundee/onboarding_profile AsyncStorage key
    - WorkoutRepo: saveWorkout writes to Firestore subcollection /users/{uid}/workouts/{id}; getHistory returns records ordered by completedAt desc; deleteWorkout removes by id; Local uses @sundee/workouts key with JSON array
    - SettingsRepo: saveSettings writes weightUnit + notificationsEnabled; getSettings returns null when unset; Firestore merges into /users/{uid}; Local uses @sundee/settings key
    - ReadinessRepo: saveSurvey writes to Firestore subcollection /users/{uid}/readiness/{date}; getSurveyForDate returns record or null; getRecentSurveys returns up to limit records; Local uses @sundee/readiness_surveys key
    - Factory tests: getOnboardingProfileRepo(true) returns LocalOnboardingProfileRepo, getOnboardingProfileRepo(false) returns FirestoreOnboardingProfileRepo; same pattern for all 4 repos
  </behavior>
  <action>
    **Step 1: Expand UserProfile in UserRepository.ts**
    Add optional onboarding fields to the existing UserProfile interface (do NOT break existing code):
    - `name?: string`
    - `experienceLevel?: ExperienceLevel`
    - `primaryGoal?: PrimaryGoal`
    - `gender?: Gender` (import Gender from '../domain/types')
    - `cycleOptIn?: boolean`
    - `hasCompletedOnboarding?: boolean`

    Add new types above the interface:
    ```typescript
    export type ExperienceLevel = 'beginner' | 'intermediate' | 'advanced';
    export type PrimaryGoal = 'strength' | 'muscle' | 'endurance' | 'weightLoss' | 'general';
    ```

    **Step 2: Create OnboardingProfileRepo interface + implementations**
    - `OnboardingProfileRepo.ts`: Define `OnboardingProfile` interface (name, experienceLevel, primaryGoal, gender, cycleOptIn, hasCompletedOnboarding). Define `OnboardingProfileRepository` interface with `saveProfile(uid, profile)` and `getProfile(uid)`. Export `getOnboardingProfileRepo(isGuest)` factory.
    - `FirestoreOnboardingProfileRepo.ts`: Implements by writing to /users/{uid} with `set(profile, { merge: true })`. Follow FirestoreUserRepo pattern exactly.
    - `LocalOnboardingProfileRepo.ts`: Uses `@sundee/onboarding_profile` AsyncStorage key. Follow LocalUserRepo pattern.

    **Step 3: Create WorkoutRepo interface + implementations**
    - `WorkoutRepo.ts`: Define `WorkoutRecord` (id, uid, completedAt ISO string, durationSeconds, source: 'ai'|'program'|'custom', workout: GeneratedWorkout). Define `WorkoutRepository` with saveWorkout, getWorkout, getHistory(uid, limit?), deleteWorkout. Export `getWorkoutRepo(isGuest)` factory.
    - `FirestoreWorkoutRepo.ts`: Uses subcollection `/users/{uid}/workouts/{id}`. getHistory queries with orderBy('completedAt', 'desc') and limit. Import GeneratedWorkout type from domain.
    - `LocalWorkoutRepo.ts`: Uses `@sundee/workouts` AsyncStorage key storing JSON array. getHistory sorts by completedAt desc and applies limit. deleteWorkout filters array.

    **Step 4: Create SettingsRepo interface + implementations**
    - `SettingsRepo.ts`: Define `AppSettings` (weightUnit: 'lb'|'kg', notificationsEnabled: boolean). Export `DEFAULT_SETTINGS` constant (lb, true). Define `SettingsRepository` with saveSettings(uid, settings), getSettings(uid). Export `getSettingsRepo(isGuest)` factory.
    - `FirestoreSettingsRepo.ts`: Merges settings fields into /users/{uid} (same doc as profile, not subcollection). Per research: "Settings merged into /users/{uid} avoids an extra Firestore read on startup."
    - `LocalSettingsRepo.ts`: Uses `@sundee/settings` AsyncStorage key.

    **Step 5: Create ReadinessRepo interface + implementations**
    - `ReadinessRepo.ts`: Define `ReadinessSurveyRecord` (id, uid, date 'yyyy-MM-dd', sleepQuality, stressLevel, sorenessLevel, result: ReadinessResult). Define `ReadinessRepository` with saveSurvey, getSurveyForDate(uid, date), getRecentSurveys(uid, limit). Export `getReadinessRepo(isGuest)` factory.
    - `FirestoreReadinessRepo.ts`: Uses subcollection `/users/{uid}/readiness/{date}` (date as document ID). getRecentSurveys queries with orderBy('date', 'desc') and limit.
    - `LocalReadinessRepo.ts`: Uses `@sundee/readiness_surveys` AsyncStorage key storing JSON array.

    **Step 6: Write all test files**
    Follow the exact pattern from `FirestoreUserRepo.test.ts`:
    - Import mock firestore, create repo instance, verify collection/doc/set/get calls
    - For subcollection repos (Workout, Readiness): verify chained `.collection().doc().collection().doc()` calls
    - For Local repos: verify AsyncStorage.setItem/getItem calls with correct keys
    - For factory tests: verify each factory returns correct implementation class (use instanceof check)

    **IMPORTANT:**
    - All new types use string unions, NOT TypeScript enums (per project decision)
    - All Firestore repos import getFirestoreInstance from '../firebase/firestore' (never direct SDK import)
    - All AsyncStorage keys use `@sundee/` prefix
    - No real-time listeners (onSnapshot) — async/await only per locked decision
    - WorkoutRepo and ReadinessRepo are interface-complete implementations (not stubs) — the UI that uses them comes in Phase 4/5, but the repo layer is fully functional
  </action>
  <verify>
    <automated>cd SundeeFundeeRN && npx jest --testPathPattern="repositories" --passWithNoTests</automated>
  </verify>
  <done>
    All 4 repository interfaces defined with TypeScript types. 8 implementation classes (4 Firestore + 4 Local) pass all unit tests. Factory functions return correct implementation based on isGuest boolean. UserProfile expanded with onboarding fields. All existing tests still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create migration helper, barrel exports, and enable web Firestore persistence</name>
  <files>
    SundeeFundeeRN/src/repositories/migration.ts,
    SundeeFundeeRN/src/repositories/index.ts,
    SundeeFundeeRN/src/repositories/__tests__/migration.test.ts,
    SundeeFundeeRN/src/firebase/firestore.ts
  </files>
  <behavior>
    - migrateGuestDataToFirestore reads all @sundee/* keys from AsyncStorage, batch-writes to Firestore under new uid, then clears AsyncStorage
    - If batch.commit() throws, AsyncStorage is NOT cleared (data preserved for retry)
    - If an AsyncStorage key has null value, it is skipped (no JSON.parse(null) crash)
    - Web Firestore uses initializeFirestore with persistentLocalCache for offline writes
    - Barrel re-exports all interfaces, types, factories, and migration function
  </behavior>
  <action>
    **Step 1: Create migration.ts**
    - Export `async function migrateGuestDataToFirestore(uid: string): Promise<void>`
    - Read from AsyncStorage: `@sundee/onboarding_profile`, `@sundee/workouts`, `@sundee/settings`, `@sundee/readiness_surveys`
    - Use `AsyncStorage.multiGet()` with all 4 keys
    - Null-check each value before JSON.parse (per pitfall 6 from research: AsyncStorage.multiGet returns [key, null] for missing keys)
    - Create Firestore batch via `getFirestoreInstance().batch()`
    - For onboarding_profile: `batch.set(db.collection('users').doc(uid), data, { merge: true })`
    - For settings: `batch.set(db.collection('users').doc(uid), data, { merge: true })` (merges into same doc)
    - For workouts array: iterate and `batch.set(db.collection('users').doc(uid).collection('workouts').doc(w.id), w)` for each
    - For readiness array: iterate and `batch.set(db.collection('users').doc(uid).collection('readiness').doc(r.date), r)` for each
    - `await batch.commit()`
    - ONLY after commit succeeds: `await AsyncStorage.multiRemove(['@sundee/onboarding_profile', '@sundee/workouts', '@sundee/settings', '@sundee/readiness_surveys'])`
    - If commit throws, do NOT clear AsyncStorage — let the error propagate for caller to handle

    **Step 2: Create migration.test.ts**
    - Test: migrates profile and settings into /users/{uid} with merge
    - Test: migrates workouts into subcollection
    - Test: migrates readiness into subcollection
    - Test: skips null AsyncStorage values without crashing
    - Test: does NOT clear AsyncStorage if batch.commit() rejects
    - Test: clears all @sundee/* keys after successful commit

    **Step 3: Create repositories/index.ts barrel**
    - Re-export all types: UserProfile, ExperienceLevel, PrimaryGoal, OnboardingProfile, WorkoutRecord, AppSettings, DEFAULT_SETTINGS, ReadinessSurveyRecord
    - Re-export all interfaces: UserRepository, OnboardingProfileRepository, WorkoutRepository, SettingsRepository, ReadinessRepository
    - Re-export all factories: getOnboardingProfileRepo, getWorkoutRepo, getSettingsRepo, getReadinessRepo
    - Re-export all implementations: FirestoreUserRepo, LocalUserRepo, FirestoreOnboardingProfileRepo, etc.
    - Re-export: migrateGuestDataToFirestore
    - Mark with `/* istanbul ignore file */` (pure re-exports, per project convention)

    **Step 4: Update firebase/firestore.ts for web persistence**
    - In the web branch of `getFirestoreInstance()`, replace `getFirestore(app)` with `initializeFirestore(app, { localCache: persistentLocalCache({}) })` wrapped in a try-catch
    - Import `initializeFirestore` and `persistentLocalCache` from 'firebase/firestore'
    - If `initializeFirestore` throws (already initialized), fall back to `getFirestore(app)` — this handles hot-reload during development
    - Keep a module-level `let webInstance: WebFirestore | null = null` to cache the instance and avoid re-initialization
    - Add try-catch around the persistence setup: if it fails with 'failed-precondition' (multiple tabs), log warning and continue
    - Native branch is unchanged (offline persistence is automatic in @react-native-firebase/firestore)
  </action>
  <verify>
    <automated>cd SundeeFundeeRN && npx jest --testPathPattern="(migration|repositories)" --passWithNoTests && npx tsc --noEmit</automated>
  </verify>
  <done>
    Migration helper passes all tests including the critical "don't clear AsyncStorage on commit failure" case. Barrel exports compile without errors. Web Firestore persistence is configured. Full test suite passes. TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
1. `cd SundeeFundeeRN && npx jest --testPathPattern="repositories" --coverage` — all repository tests pass with high coverage
2. `cd SundeeFundeeRN && npx tsc --noEmit` — no type errors from expanded UserProfile or new interfaces
3. `cd SundeeFundeeRN && npx jest --coverage` — full suite green, no regressions from UserProfile expansion
</verification>

<success_criteria>
- All 4 repository interfaces defined with correct TypeScript types
- 8 implementation classes (4 Firestore + 4 Local) fully functional
- 4 factory functions return correct implementation based on isGuest
- Migration helper handles all edge cases (null values, commit failure)
- Web Firestore persistence enabled
- Barrel exports all public types and functions
- All tests pass, no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-layer-and-offline-architecture/03-01-SUMMARY.md`
</output>
