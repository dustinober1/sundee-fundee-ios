---
phase: 04-pwa-quality
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - pwa/src/hooks/useInstallPrompt.test.ts
autonomous: true
requirements: [PWA-04]

must_haves:
  truths:
    - "A failing test file exists for useInstallPrompt before the hook is implemented"
  artifacts:
    - path: "pwa/src/hooks/useInstallPrompt.test.ts"
      provides: "Failing test scaffold for install prompt hook (Nyquist compliance)"
  key_links: []
---

<objective>
Create a failing test scaffold for the useInstallPrompt hook before any implementation exists. This satisfies the Nyquist rule — tests must exist before the code they verify.

Purpose: Ensures test-first development for the install prompt feature. The test file will fail (RED) because the hook does not exist yet.
Output: A test file with meaningful test cases that import from a not-yet-created module.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pwa-quality/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create failing test scaffold for useInstallPrompt</name>
  <files>pwa/src/hooks/useInstallPrompt.test.ts</files>
  <action>
1. Create `pwa/src/hooks/` directory if it does not exist.

2. Create `pwa/src/hooks/useInstallPrompt.test.ts` with the following test cases that import from `./useInstallPrompt` (which does not exist yet):
   - Test: "returns isStandalone=true when display-mode: standalone matches" — mock `window.matchMedia` to return `{ matches: true }`, verify hook returns `isStandalone: true` and `showPrompt: false`
   - Test: "captures beforeinstallprompt event and stores androidPrompt" — dispatch a `beforeinstallprompt` event on window, verify `androidPrompt` is not null
   - Test: "detects iOS Safari via user agent when not standalone" — set `navigator.userAgent` to iPad string, verify `isIOS: true`
   - Test: "dismiss() sets sessionStorage flag and isDismissed=true" — call `dismiss()`, verify `sessionStorage.setItem` called with `'installPromptDismissed', '1'` and `isDismissed` is true
   - Test: "isDismissed initializes to true when sessionStorage flag already set" — pre-set sessionStorage, verify `isDismissed: true` on mount
   - Test: "triggerAndroid() calls prompt() on captured event" — capture event, call `triggerAndroid()`, verify `event.prompt()` was called

   Use `import { renderHook, act } from '@testing-library/react'` and `import { useInstallPrompt } from './useInstallPrompt'`.

3. Verify the test file exists and fails (because the module does not exist):
   `cd pwa && npx vitest run src/hooks/useInstallPrompt.test.ts 2>&1 | tail -3` — expect failure output.
  </action>
  <verify>
    <automated>test -f /Users/dustinober/Projects/Sundee-Fundee/pwa/src/hooks/useInstallPrompt.test.ts && echo "FILE EXISTS"</automated>
  </verify>
  <done>Test file exists at pwa/src/hooks/useInstallPrompt.test.ts with 6 test cases importing from ./useInstallPrompt. Tests fail because the hook module does not exist yet (RED phase of TDD).</done>
</task>

</tasks>

<verification>
- `test -f pwa/src/hooks/useInstallPrompt.test.ts` — file exists
- `grep -c "useInstallPrompt" pwa/src/hooks/useInstallPrompt.test.ts` — multiple references to the hook
- Tests fail when run (expected — hook not yet implemented)
</verification>

<success_criteria>
- Test scaffold file exists with 6 meaningful test cases
- Tests import from ./useInstallPrompt (not yet created)
- Running tests produces failures (RED state)
</success_criteria>

<output>
After completion, create `.planning/phases/04-pwa-quality/04-00-SUMMARY.md`
</output>
