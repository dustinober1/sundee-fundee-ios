---
phase: 07-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/deploy.yml
  - pwa/src/routes/Dashboard.tsx
  - pwa/src/routes/Dashboard.test.tsx
autonomous: true
requirements:
  - SEC-01
  - SEC-02
must_haves:
  truths:
    - "Firestore security rules are deployed to production on every production deploy"
    - "Manual deploy fallback includes Firestore rules deployment"
    - "Dashboard Start Workout button navigates to /workout-session"
    - "Dashboard AI Workout card navigates to /ai-workout/config"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "Firestore rules deploy step in CI/CD"
      contains: "firestore:rules"
    - path: "pwa/src/routes/Dashboard.tsx"
      provides: "Corrected route Link paths"
      contains: "/workout-session"
    - path: "pwa/src/routes/Dashboard.test.tsx"
      provides: "Route correctness regression tests"
      min_lines: 20
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "firestore.rules"
      via: "firebase-tools deploy --only firestore:rules"
      pattern: "firestore:rules"
    - from: "pwa/src/routes/Dashboard.tsx"
      to: "pwa/src/routes/router.tsx"
      via: "Link to= paths matching router path: entries"
      pattern: "/workout-session|/ai-workout/config"
---

<objective>
Close the two audit gaps blocking v1.0 milestone sign-off: (1) deploy Firestore security rules to production via CI/CD, and (2) fix broken Dashboard navigation routes.

Purpose: SEC-01 and SEC-02 rules exist and are tested but were never deployed — the Stripe paywall can be bypassed by writing premiumEntitlement directly. Dashboard route mismatches cause 404s on two primary user flows.
Output: Updated deploy.yml with Firestore rules step, corrected Dashboard.tsx links, new Dashboard.test.tsx for regression coverage.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gap-closure/07-RESEARCH.md

@.github/workflows/deploy.yml
@pwa/src/routes/Dashboard.tsx
@pwa/src/routes/router.tsx

<interfaces>
<!-- Key routes from router.tsx that Dashboard links must match -->
From pwa/src/routes/router.tsx:
```typescript
{ path: 'workout-session', element: <L><WorkoutSessionScreen /></L> }
{ path: 'ai-workout/config', element: <L><AIWorkoutConfig /></L> }
```

<!-- Existing Cloud Functions deploy pattern in deploy.yml (model for Firestore step) -->
From .github/workflows/deploy.yml:
```yaml
- name: Deploy Cloud Functions
  env:
    SA_KEY: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_SUNDEE_FUNDEE }}
  run: |
    echo "$SA_KEY" > /tmp/sa-key.json
    export GOOGLE_APPLICATION_CREDENTIALS=/tmp/sa-key.json
    npx firebase-tools deploy --only functions --project sundee-fundee --force
    rm /tmp/sa-key.json
```

<!-- firebase.json already has firestore block wired -->
From firebase.json:
```json
"firestore": {
  "rules": "firestore.rules"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Firestore rules deploy step to CI/CD pipeline</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
Add a "Deploy Firestore rules" step to `.github/workflows/deploy.yml` immediately AFTER the existing "Deploy Cloud Functions" step. Use the identical credential pattern already established for Cloud Functions:

```yaml
- name: Deploy Firestore rules
  env:
    SA_KEY: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_SUNDEE_FUNDEE }}
  run: |
    echo "$SA_KEY" > /tmp/sa-key.json
    export GOOGLE_APPLICATION_CREDENTIALS=/tmp/sa-key.json
    npx firebase-tools deploy --only firestore:rules --project sundee-fundee
    rm /tmp/sa-key.json
```

IMPORTANT: Use `--only firestore:rules` (NOT `--only firestore`) because `firestore.indexes.json` does not exist in the repo. Using `--only firestore` would attempt to deploy indexes and fail.

Also add a comment block at the bottom of the file documenting the manual deploy fallback that includes Firestore rules:

```yaml
# Manual deploy fallback (run from repo root with service account key):
#   export GOOGLE_APPLICATION_CREDENTIALS=/path/to/sa-key.json
#   cd pwa && npm ci && npm run build && cd ..
#   cd functions && npm ci && npm run build && cd ..
#   npx firebase-tools deploy --only hosting,functions,firestore:rules --project sundee-fundee
```
  </action>
  <verify>
    <automated>grep -q "firestore:rules" .github/workflows/deploy.yml && echo "PASS: Firestore rules deploy step found" || echo "FAIL"</automated>
  </verify>
  <done>deploy.yml contains a Firestore rules deploy step using firestore:rules target with the same SA_KEY credential pattern as Cloud Functions. Manual deploy fallback comment includes firestore:rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix Dashboard route links and add regression tests</name>
  <files>pwa/src/routes/Dashboard.tsx, pwa/src/routes/Dashboard.test.tsx</files>
  <behavior>
    - Test 1: "Start Workout" button's closest anchor has href="/workout-session"
    - Test 2: "AI Workout" card's closest anchor has href="/ai-workout/config"
  </behavior>
  <action>
**RED phase:** Create `pwa/src/routes/Dashboard.test.tsx` with two tests asserting correct route paths. Mock dependencies following the established pattern from SignIn.test.tsx (vi.mock factory pattern):
- Mock `../auth/AuthContext` with `useSession` returning `{ user: null, isGuest: true }`
- Mock `../entitlements/EntitlementContext` with `useEntitlementContext` returning `{ isPremium: false }`
- Mock `../repositories/WODRepo` with `getWODRepo` returning `{ getWODForDate: () => Promise.resolve(null) }`
- Mock `../repositories/OnboardingProfileRepo` with `getOnboardingProfileRepo` returning `{ getProfile: () => Promise.resolve(null) }`
- Wrap component in `<MemoryRouter>` for Link resolution
- Use `await screen.findByText('AI Workout')` (async) for AI Workout card because it is inside the isLoading conditional — the mocked promises must resolve first
- Use `screen.getByText('Start Workout')` (sync) because it renders outside the loading conditional

Run tests — they MUST fail (red) because Dashboard.tsx still has wrong paths.

**GREEN phase:** Fix the two broken Link paths in `pwa/src/routes/Dashboard.tsx`:
- Line 39: Change `<Link to="/workout"` to `<Link to="/workout-session"`
- Line 57: Change `<Link to="/ai-workout"` to `<Link to="/ai-workout/config"`

Run tests again — they MUST pass (green).
  </action>
  <verify>
    <automated>cd pwa && npx vitest run src/routes/Dashboard.test.tsx</automated>
  </verify>
  <done>Dashboard.test.tsx passes with 2 tests confirming correct route paths. Dashboard.tsx Link paths match router.tsx canonical paths: /workout-session and /ai-workout/config.</done>
</task>

</tasks>

<verification>
1. `grep -q "firestore:rules" .github/workflows/deploy.yml` — Firestore deploy step exists
2. `cd pwa && npx vitest run src/routes/Dashboard.test.tsx` — Route regression tests pass
3. `cd pwa && npx vitest run` — Full test suite still green (no regressions)
4. `grep -q "workout-session" pwa/src/routes/Dashboard.tsx` — Start Workout route corrected
5. `grep -q "ai-workout/config" pwa/src/routes/Dashboard.tsx` — AI Workout route corrected
</verification>

<success_criteria>
- deploy.yml contains Firestore rules deploy step using --only firestore:rules
- Manual deploy fallback documented with Firestore rules included
- Dashboard "Start Workout" links to /workout-session
- Dashboard "AI Workout" links to /ai-workout/config
- Dashboard.test.tsx provides regression coverage for both routes
- Full pwa test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/07-gap-closure/07-01-SUMMARY.md`
</output>
