// Stub — will be replaced in Task 5
import SwiftUI

struct ProgramEditorView: View {
    @State var program: Program
    let userID: String
    var onSave: (Program) -> Void = { _ in }

    var body: some View {
        Text("Editor — coming in Task 5")
    }
}
